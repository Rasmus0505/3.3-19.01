# Quick Task 260328-l5z: Fix desktop link import forcing Bottle 1.0 local generation after Bilibili download

**Date:** 2026-03-28
**Status:** Completed

## Goal

Fix the desktop client link-import handoff so a successfully downloaded Bilibili page link continues through the normal generation strategy instead of being hard-wired to Bottle 1.0 local generation.

## Tasks

1. Inspect the desktop link-import download polling path in `frontend/src/features/upload/UploadPanel.jsx`.
2. Remove the forced `submitDesktopLocalFast(...)` handoff after download success.
3. Route the imported file back through the normal `submit(...)` strategy selection with the resolved source duration.
4. Add a contract test proving link-import success now reuses the normal submit path.

## Verification

- Run focused desktop runtime contract tests for link import behavior.
- Build the frontend bundle to confirm the upload panel still compiles.
- Replay the real Bilibili link through the desktop helper download and local generation endpoints.
