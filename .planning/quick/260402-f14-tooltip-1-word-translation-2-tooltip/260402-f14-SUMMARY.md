# 260402-F14 摘要：修复生词本翻译保存和Tooltip问题

## 目标完成状态

| 目标 | 状态 |
|------|------|
| 新增 `word_translation` 字段存储单词翻译 | ✅ 完成 |
| 前端显示单词翻译 | ✅ 完成 |
| 修复沉浸式学习 Tooltip | ✅ 完成 |

## 卡片布局（已确认）

```
┌─────────────────────────┐
│ 📝 apple                │
│ 🔤 苹果                  │  ← 单词翻译（word_translation 字段）
│ ─────────────────────── │
│ 💬 I like apples.       │  ← 原句（latest_sentence_en）
│ 🀄 我喜欢苹果。           │  ← 原句翻译（latest_sentence_zh）
└─────────────────────────┘
```

## 变更记录

### 1. 数据库层 - 新增 word_translation 字段

**文件**: `app/models/lesson.py`

```python
word_translation: Mapped[str] = mapped_column(String(500), nullable=False, default="")
```

**提交**: `f3e44944` - feat(wordbook): add word_translation field

---

### 2. 后端服务 - 修改翻译逻辑

**文件**: `app/services/wordbook_service.py`

- `schedule_async_translation()`: 翻译结果存入 `word_translation` 而非 `latest_sentence_zh`
- `batch_translate_words()`: 翻译结果存入 `word_translation`
- `_entry_payload_to_dict()`: 返回 `"word_translation"` 字段

**提交**: `edb2c0f8` - feat(wordbook): store translation in word_translation field

---

### 3. 前端展示 - 更新卡片布局和修复 Tooltip

**文件**: `frontend/src/features/wordbook/WordbookPanel.jsx`

- 列表模式：显示 `word_translation` 字段（单词翻译）
- 复习模式：显示 `reviewItem.word_translation` 字段

**文件**: `frontend/src/features/immersive/ImmersiveLessonPage.jsx`

- 修复 TooltipProvider 层级问题：将 TooltipProvider 从 CardHeader 内部移至组件顶层
- 确保 Tooltip 在 CardContent 区域内也能正常显示

**提交**: `d58e38b9` - feat(wordbook): show word_translation and fix immersive Tooltip

---

## 字段设计说明

| 字段 | 用途 | 来源 |
|------|------|------|
| `latest_sentence_en` | 原句（英文语境） | 直接存储 |
| `latest_sentence_zh` | 原句翻译（复用已有翻译） | 无需 API |
| `word_translation` | 单词翻译（需要调用 API） | 调用翻译 API |

## 验证清单

- [x] 数据库字段 `word_translation` 已添加
- [x] 后端 API 返回 `word_translation` 字段
- [x] 前端列表模式显示单词翻译
- [x] 前端复习模式显示单词翻译
- [x] 沉浸式学习页 Tooltip 正常工作
- [x] 原子提交已完成

## 计划文件

`.planning/quick/260402-f14-tooltip-1-word-translation-2-tooltip/260402-f14-PLAN.md`
