# Quick Task 260327-2ca: Fix persist_lesson_workspace_summary signature mismatch in DashScope save stage

**Date:** 2026-03-27
**Status:** Completed

## Goal

Fix the `TypeError` raised during the DashScope save-complete stage when `LessonService.generate_from_dashscope_file_id` calls `persist_lesson_workspace_summary(...)` with an outdated argument contract.

## Tasks

1. Replace the stale DashScope workspace-summary call with the current helper signature.
2. Ensure the summary is persisted after `subtitle_cache_seed` is available and attach it to `lesson.workspace_summary`.
3. Update the regression test to exercise the real workspace-summary helper and assert the saved payload.

## Verification

- Run targeted integration regressions for DashScope direct-upload, 403 retry behavior, and saved-file translation billing.
