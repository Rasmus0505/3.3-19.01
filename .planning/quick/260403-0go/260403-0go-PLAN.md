# Quick Task 260403-0go: Fix Yellow Letter Color for Revealed Letters

## Task Description

沉浸式学习页面中，用户按提示键揭示的字母应该显示黄色（而非绿色）。代码中 `buildLetterSlots` 函数已经正确地将 `revealed` 状态的字母标记为 `state: "revealed"`，CSS 也定义了 `.immersive-letter-cell--revealed` 的黄色样式，但黄色没有实际显示出来。需要排查根因并修复。

## Codebase Analysis

### 1. Logic Layer — `buildLetterSlots`
- **File**: `frontend/src/features/immersive/ImmersiveLessonPage.jsx` (line 407-456)
- 当 `typedIndex < revealLength` 且字符正确时，赋 `state = "revealed"`
- 当 `typedIndex >= revealLength` 且字符正确时，赋 `state = "correct"`（绿色）
- **Status**: 逻辑正确，JS 侧没问题

### 2. Style Layer — CSS
- **File**: `frontend/src/features/immersive/immersive.css` (line 618-621)
- `.immersive-letter-cell--revealed` 定义了黄色（hue 85）
- `.immersive-letter-cell--correct` 定义了绿色（hue 145）
- **Status**: CSS 规则存在，但可能未生效

### 3. Rendering Layer
- **File**: `frontend/src/features/immersive/ImmersiveLessonPage.jsx` (line 3664-3666)
- JSX className: `immersive-letter-cell--${slot.state}`
- **Status**: 类名拼接正确

## Likely Root Cause

CSS 类名和 JS 完全对应，所以问题最可能出在：
1. **构建产物未更新** — `frontend/dist/` 中的 CSS 是旧的，没有包含新的 `.immersive-letter-cell--revealed` 规则
2. **构建时没有触发** — 由于 `main.jsx` 被删除，`npm run build` 之前失败过，构建缓存未更新
3. **浏览器缓存** — 浏览器加载了旧的 CSS 文件

## Task Plan

- [ ] **Task 1**: 验证 `buildLetterSlots` 中 `revealed` 状态确实被赋值（Read 行 407-456）
- [ ] **Task 2**: 验证 `immersive.css` 中 `.immersive-letter-cell--revealed` 规则存在（Read 行 618-621）
- [ ] **Task 3**: 检查是否有其他 CSS 规则优先级覆盖了 `.immersive-letter-cell--revealed`（搜索同文件中所有 `.immersive-letter-cell` 规则，验证 specificity）
- [ ] **Task 4**: 检查 `frontend/dist/` 中的 CSS 是否为最新（比对 timestamp 或内容）
- [ ] **Task 5**: 如果 CSS 旧了，重新构建并确认 dist 更新
- [ ] **Task 6**: 验证修复 — 触发 reveal 交互，确认字母显示黄色

## Files

- `frontend/src/features/immersive/ImmersiveLessonPage.jsx`
- `frontend/src/features/immersive/immersive.css`
- `frontend/dist/assets/index-*.css`

## Verification

- 重现步骤：在沉浸式学习页面输入字母后按提示键（如 `h` 键）揭示字母
- 预期结果：被揭示的字母显示黄色（不是绿色）
- 检查方法：DevTools Elements 面板查看字母 span 的 className 和 computed color
