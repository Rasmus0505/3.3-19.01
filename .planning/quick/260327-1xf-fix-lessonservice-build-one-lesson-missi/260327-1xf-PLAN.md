# Quick Task 260327-1xf: Fix LessonService._build_one_lesson missing in DashScope course generation path

**Date:** 2026-03-27
**Status:** Completed

## Goal

Fix the `AttributeError` raised during the `build_lesson` stage when the DashScope direct-upload generation flow calls `LessonService._build_one_lesson`.

## Tasks

1. Restore the missing helper in `app/services/lesson_service.py` so the DashScope path can persist the lesson row and related records.
2. Pass the required lesson metadata from `generate_from_dashscope_file_id` into the helper.
3. Add a regression test proving the built-in helper works without monkeypatching.

## Verification

- Run focused integration tests for the DashScope retry task flow and the direct service helper path.
