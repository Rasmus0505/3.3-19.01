# Phase 34 Plan — Prompt Optimization

**Phase:** 34
**Created:** 2026-04-06
**Status:** Planned
**Wave:** 1

---

## Overview

将 AI 重写从「发送全文 → 返回全文重写」改为「发送高难度词列表 → 返回简化词数组」，前端本地按顺序替换。Token 消耗大幅降低，Schema 极简。

---

## Wave 1: Backend — 新 Endpoint + Token 估算

### Objective

新增 `POST /api/llm/simplify-words` endpoint，接受原文句子和高难度词列表，返回简化词数组（按顺序）。同时在原 endpoint 上增加 token 预估算功能。

---

### Task 1.1 — 新增 `POST /api/llm/simplify-words` Endpoint

**文件**: `app/api/routers/llm.py`

**read_first:**
- `app/api/routers/llm.py` — 现有 `REWRITE_SYSTEM_PROMPT` / `REWRITE_WITH_MAPPINGS_SYSTEM_PROMPT` 和 endpoint 定义
- `app/infra/llm/deepseek.py` — `call_deepseek` 函数签名

**action:**

在 `llm.py` 中 `REWRITE_WITH_MAPPINGS_SYSTEM_PROMPT` 后面添加新的 prompt 和 endpoint：

```python
# ── 新 Schema Prompt（简化词数组，按顺序）──────────────────────────────
SIMPLIFY_WORDS_SYSTEM_PROMPT = (
    "You are an English text simplifier for language learners.\n"
    "Given a sentence and a list of words/phrases to simplify from that sentence,\n"
    "return a JSON array of simplified replacements, IN THE SAME ORDER as the input list.\n"
    "Rules:\n"
    "- Return ONLY a valid JSON array of strings — no markdown fences, no extra text\n"
    "- Each entry must be a concise simplified word or phrase for the corresponding input\n"
    "- Preserve the original meaning and part of speech where possible\n"
    "- If a word is already simple enough, keep it unchanged\n"
    "- Do NOT reorder the array entries — match input order exactly\n"
)

SIMPLIFY_WORDS_EXAMPLE = """
Example 1:
原文：I used to loathe and eschew perusing English.
需要简化的词：loathe, eschew, perusing
返回：["hate", "avoid", "carefully reading"]

Example 2:
原文：The remuneration was insufficient for the arduous task.
需要简化的词：remuneration, insufficient, arduous
返回：["pay", "not enough", "hard"]
```

class SimplifyWordsRequest(BaseModel):
    """JSON body for POST /api/llm/simplify-words."""
    sentence: str = Field(..., min_length=1, max_length=500)
    words: list[str] = Field(..., min_length=1)
    target_level: str = Field(default="B1", max_length=8)
    enable_thinking: bool = False

@router.post(
    "/simplify-words",
    responses={503: {"model": ErrorResponse}, 402: {"model": ErrorResponse}},
)
def simplify_words_endpoint(
    body: SimplifyWordsRequest,
    current_user: User = Depends(get_current_user),
    db: Session = Depends(get_db),
):
    """
    Simplify a list of high-difficulty words/phrases from a given sentence.
    Returns a JSON array of simplified replacements, in the same order as the input.

    新 Schema（Phase 34）: 发送高难度词列表 → 只返回简化词数组
    """
    # 计费初始化 + 参数校验
    ensure_default_billing_rates(db)
    if body.target_level.upper() not in CEFR_LEVELS:
        raise HTTPException(status_code=422, detail=f"Invalid target_level")
    if not body.words:
        raise HTTPException(status_code=422, detail="words must be non-empty list")
    if len(body.sentence) > 2000:
        raise HTTPException(status_code=413, detail="Sentence too long (max 2000 chars)")

    effective_model = LLM_MODEL_DEEPSEEK_THINKING if body.enable_thinking else LLM_MODEL_DEEPSEEK_FAST
    try:
        rate = get_model_rate(db, effective_model)
    except Exception:
        raise HTTPException(status_code=503, detail="LLM model not available")

    api_key = _require_api_key()
    trace_id = str(uuid.uuid4())

    # 构建用户消息：原文 + 词列表
    user_message = f"原文：{body.sentence}\n需要简化的词：{', '.join(body.words)}"

    system_prompt = SIMPLIFY_WORDS_SYSTEM_PROMPT + "\n\n" + SIMPLIFY_WORDS_EXAMPLE

    messages = [
        {"role": "system", "content": system_prompt},
        {"role": "user", "content": user_message},
    ]

    try:
        raw_response, usage = call_deepseek(
            messages=messages,
            api_key=api_key,
            enable_thinking=body.enable_thinking,
            stream=False,
            temperature=0.3,
            max_tokens=512,
        )
    except Exception as exc:
        logger.exception("[DEBUG] llm.simplify_words_failed user_id=%s error=%s", current_user.id, str(exc)[:200])
        raise HTTPException(status_code=502, detail=f"LLM call failed: {str(exc)[:200]}")

    if not raw_response:
        raise HTTPException(status_code=502, detail="LLM returned empty result")

    # 解析 JSON 数组
    simplified_words: list[str] = []
    parse_error = None
    import json as _json
    try:
        parsed = _json.loads(raw_response.strip())
        if isinstance(parsed, list):
            simplified_words = [str(item) for item in parsed]
        else:
            parse_error = "Expected JSON array, got other type"
    except Exception as exc:
        parse_error = str(exc)[:100]
        logger.warning("[DEBUG] llm.simplify_words_parse_failed user_id=%s raw=%s error=%s",
                       current_user.id, raw_response[:200], parse_error)
        raise HTTPException(status_code=502, detail=f"Failed to parse model response: {parse_error}")

    total_tokens = usage.prompt_tokens + usage.completion_tokens
    charge_cents = calculate_llm_charge_by_tokens(
        total_tokens=total_tokens,
        points_per_1k_tokens=rate.points_per_1k_tokens,
    )

    try:
        consume_points(db, user_id=current_user.id, points=charge_cents,
                       model_name=effective_model, lesson_id=None,
                       event_type=EVENT_CONSUME_LLM,
                       note=f"简化词汇，total_tokens={total_tokens}")
    except Exception:
        pass

    from app.services.llm_usage_service import log_llm_usage
    log_llm_usage(db, user_id=current_user.id, model_name=effective_model,
                  category="simplify_words", prompt_tokens=usage.prompt_tokens,
                  completion_tokens=usage.completion_tokens,
                  reasoning_tokens=usage.reasoning_tokens,
                  total_tokens=total_tokens, input_cost_cents=None,
                  charge_cents=charge_cents, lesson_id=None,
                  enable_thinking=body.enable_thinking,
                  input_text_preview=body.sentence[:200], trace_id=trace_id)

    db.commit()

    return {
        "ok": True,
        "simplified_words": simplified_words,
        "input_words": body.words,
        "model": effective_model,
        "usage": {
            "prompt_tokens": usage.prompt_tokens,
            "completion_tokens": usage.completion_tokens,
            "reasoning_tokens": usage.reasoning_tokens,
            "total_tokens": total_tokens,
        },
        "charge_cents": charge_cents,
        "trace_id": trace_id,
    }
```

**acceptance_criteria:**
- `llm.py` 包含 `SIMPLIFY_WORDS_SYSTEM_PROMPT` 和 `SIMPLIFY_WORDS_EXAMPLE` 常量
- `llm.py` 包含 `SimplifyWordsRequest` Pydantic model
- `llm.py` 包含 `POST /api/llm/simplify-words` endpoint 函数
- `curl` 测试：`curl -X POST http://localhost:8000/api/llm/simplify-words -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{"sentence":"I used to loathe and eschew perusing English.","words":["loathe","eschew","perusing"],"target_level":"B1"}'` 返回 `{"ok":true,"simplified_words":["hate","avoid","carefully reading"],...}`
- `db.commit()` 在 endpoint 结束时被调用

---

### Task 1.2 — Token 预估算 API

**文件**: `app/api/routers/llm.py`

**action:**

在 `/simplify-words` endpoint 之前添加 `GET /api/llm/estimate-tokens` endpoint：

```python
@router.get("/estimate-tokens")
def estimate_tokens_endpoint(
    text: str = Query(..., min_length=1),
    current_user: User = Depends(get_current_user),
):
    """
    估算给定文本的 token 数量（用于在重写前显示费用估算）。
    使用简单估算：英文约 4 字符/token。
    """
    char_count = len(text)
    estimated_tokens = max(1, char_count // 4)
    # 按 deepseek-v3.2 fast 模式估算（假设 prompt_tokens ≈ 估算值，completion_tokens ≈ 估算值 * 0.5）
    prompt_est = estimated_tokens
    completion_est = estimated_tokens // 2
    total_est = prompt_est + completion_est
    # 获取费率（同步，不依赖 db session）
    from app.services.billing_service import get_model_rate_sync
    rate = get_model_rate_sync("deepseek-v3.2")
    if rate:
        est_charge = int(total_est * rate.points_per_1k_tokens / 10)
    else:
        est_charge = int(total_est * 5)  # fallback: ~0.05元/千token
    return {
        "ok": True,
        "char_count": char_count,
        "estimated_tokens": total_est,
        "estimated_charge_cents": est_charge,
        "estimated_charge_yuan": est_charge / 100.0,
    }
```

在 `app/services/billing_service.py` 添加同步版本：

```python
def get_model_rate_sync(model_name: str) -> Any | None:
    """同步版本，从缓存或数据库获取费率（不依赖 session）。"""
    from app.db import get_db_context
    db_gen = get_db_context()
    db = next(db_gen)
    try:
        return get_model_rate(db, model_name)
    except Exception:
        return None
```

**acceptance_criteria:**
- `GET /api/llm/estimate-tokens?text=hello` 返回 `{"ok":true,"estimated_tokens":...,"estimated_charge_cents":...}`
- `llm.py` 包含 `estimate_tokens_endpoint` 函数
- `billing_service.py` 包含 `get_model_rate_sync` 函数（如果不存在）

---

### Task 1.3 — 新 Endpoint 单元测试

**文件**: `tests/api/test_llm_rewrite.py`（新建）

**read_first:**
- `tests/api/` 目录结构（如存在）
- `app/api/routers/llm.py` — 新增 endpoint

**action:**

创建 `tests/api/test_llm_rewrite.py`：

```python
"""Tests for LLM rewrite endpoints (Phase 34: Prompt Optimization)."""
import pytest
from httpx import AsyncClient, ASGITransport
from unittest.mock import patch, MagicMock
from app.main import app
from app.models import User

# ── Fixtures ──────────────────────────────────────────────
@pytest.fixture
def mock_user():
    user = MagicMock(spec=User)
    user.id = 1
    return user

@pytest.fixture
def auth_headers(mock_user):
    with patch("app.api.deps.auth.get_current_user", return_value=mock_user):
        with patch("app.api.deps.auth._get_user_from_token", return_value=mock_user):
            yield {"Authorization": "Bearer test-token"}

# ── /simplify-words tests ─────────────────────────────────
@pytest.mark.asyncio
async def test_simplify_words_returns_ordered_array(auth_headers):
    with patch("app.api.routers.llm.ensure_default_billing_rates"):
        with patch("app.api.routers.llm.get_model_rate") as mock_rate:
            mock_rate.return_value = MagicMock(points_per_1k_tokens=5)
            with patch("app.api.routers.llm._require_api_key", return_value="fake-key"):
                with patch("app.api.routers.llm.call_deepseek") as mock_call:
                    mock_call.return_value = ('["hate", "avoid", "carefully reading"]',
                                              MagicMock(prompt_tokens=50, completion_tokens=10,
                                                        reasoning_tokens=0, total_tokens=60))
                    with patch("app.api.routers.llm.consume_points"):
                        with patch("app.services.llm_usage_service.log_llm_usage"):
                            transport = ASGITransport(app=app)
                            async with AsyncClient(transport=transport, base_url="http://test") as client:
                                resp = await client.post(
                                    "/api/llm/simplify-words",
                                    headers=auth_headers,
                                    json={
                                        "sentence": "I used to loathe and eschew perusing English.",
                                        "words": ["loathe", "eschew", "perusing"],
                                        "target_level": "B1",
                                        "enable_thinking": False,
                                    }
                                )
    assert resp.status_code == 200
    data = resp.json()
    assert data["ok"] is True
    assert data["simplified_words"] == ["hate", "avoid", "carefully reading"]
    assert data["input_words"] == ["loathe", "eschew", "perusing"]
    assert data["trace_id"] is not None
```

**acceptance_criteria:**
- `pytest tests/api/test_llm_rewrite.py` 运行通过，退出码 0
- 测试覆盖：`simplify-words` 返回有序数组、错误处理（空 words、无效 target_level）
- 测试覆盖：`estimate-tokens` 返回估算值

---

## Wave 2: Frontend — 新 Schema 集成 + Token 估算展示

### Objective

前端识别高难度词（>i+1）、调用新 endpoint、按顺序本地替换，在重写前显示 token 估算。

---

### Task 2.1 — 前端新增 `simplifyWords` API 调用函数

**文件**: `frontend/src/features/reading/api/readingRewriteApi.js`（新建）

**read_first:**
- `frontend/src/shared/api/client.js` — `apiCall` 函数签名

**action:**

创建 `frontend/src/features/reading/api/readingRewriteApi.js`：

```javascript
/**
 * readingRewriteApi.js — Phase 34: 新 Schema API 调用
 *
 * 新流程：识别高难度词 → /simplify-words → 本地按顺序替换
 */
import { apiCall } from "../../shared/api/client.js";

/**
 * 估算重写 token 消耗（用于显示费用）
 * @param {string} text
 * @param {string} accessToken
 * @returns {Promise<{estimated_tokens: number, estimated_charge_yuan: number}>}
 */
export async function estimateRewriteTokens(text, accessToken) {
  const resp = await apiCall("/api/llm/estimate-tokens?text=" + encodeURIComponent(text), {
    headers: { Authorization: `Bearer ${accessToken}` },
  });
  if (!resp.ok) {
    const data = await resp.json().catch(() => ({}));
    throw new Error(data.detail || "估算失败");
  }
  const data = await resp.json();
  return {
    estimatedTokens: data.estimated_tokens,
    estimatedChargeYuan: data.estimated_charge_yuan,
  };
}

/**
 * 调用简化词汇接口（Phase 34 新 Schema）
 * @param {string} sentence — 原文句子
 * @param {string[]} words — 需要简化的高难度词列表（按顺序）
 * @param {string} targetLevel — 目标 CEFR 等级
 * @param {string} accessToken
 * @param {boolean} enableThinking
 * @returns {Promise<{simplifiedWords: string[], chargeCents: number, traceId: string}>}
 */
export async function simplifyWords(sentence, words, targetLevel, accessToken, enableThinking = false) {
  const resp = await apiCall("/api/llm/simplify-words", {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${accessToken}`,
    },
    body: JSON.stringify({ sentence, words, target_level: targetLevel, enable_thinking: enableThinking }),
  });

  if (!resp.ok) {
    const data = await resp.json().catch(() => ({}));
    throw new Error(data.detail || "简化词汇请求失败");
  }
  const data = await resp.json();
  return {
    simplifiedWords: data.simplified_words,
    chargeCents: data.charge_cents,
    traceId: data.trace_id,
  };
}
```

**acceptance_criteria:**
- `readingRewriteApi.js` 包含 `estimateRewriteTokens` 和 `simplifyWords` 两个导出函数
- 文件存在于 `frontend/src/features/reading/api/readingRewriteApi.js`
- 函数签名与上述代码一致

---

### Task 2.2 — 更新 `useReadingRewrite` Hook 集成新 Schema

**文件**: `frontend/src/hooks/useReadingRewrite.js`

**read_first:**
- `frontend/src/hooks/useReadingRewrite.js` — 现有 `handleRewrite` 函数和状态定义

**action:**

替换 `handleRewrite` 函数的 API 调用逻辑（保留其他逻辑不变）：

在文件顶部添加导入：
```javascript
import { simplifyWords, estimateRewriteTokens } from "../features/reading/api/readingRewriteApi.js";
```

替换 `handleRewrite` 函数中的 API 调用部分（从 `const resp = await apiCall(...)` 到 `const data = await parseResponse(resp)` 的整个 try 块内逻辑），改为：

```javascript
// ── Phase 34 新流程：识别高难度词 → 简化 → 本地替换 ─────────────────
const userLevel = readCefrLevel() || "B1";
const targetLevel = getTargetLevel(userLevel);

// 识别 > i+1 的高难度词（需要前端 CEFR 分析结果）
// 从 rewriteMappings 中提取 original 字段（高难度词列表）
const highDiffWords = (originalText, cefrAnalysis) => {
  // cefrAnalysis: [{word, cefrLevel, isAboveTarget}] — Phase 31/33 已有的数据结构
  // 简化处理：直接从 rewriteMappings 中取所有 original 词（Phase 34 只简化被标记的词）
  // fallback: 如果没有分析数据，尝试从原文提取
  return [];
};

const { toast } = await import("sonner");

// Step 1: 估算 token 消耗，显示给用户
try {
  const est = await estimateRewriteTokens(originalText, accessToken);
  toast.info(
    `预计消耗 ${est.estimatedChargeYuan.toFixed(2)} 元（约 ${est.estimatedTokens} tokens）`,
    { duration: 4000 }
  );
} catch (e) {
  // 估算失败不影响主流程
  console.warn("Token estimation failed:", e);
}

// Step 2: 获取高难度词列表（从 Phase 31 CEFR 分析结果传入）
// 高难度词列表由调用方通过 hook 参数传入，或从现有 rewriteMappings 提取
const wordsToSimplify = extractHighDiffWordsFromMappings(rewriteMappings);

// Step 3: 如果有高难度词，调用新接口
let simplifiedWords = [];
if (wordsToSimplify.length > 0) {
  try {
    const result = await simplifyWords(
      originalText,
      wordsToSimplify,
      targetLevel,
      accessToken,
      false
    );
    simplifiedWords = result.simplifiedWords;
    const chargeYuan = (result.chargeCents || 0) / 100;
    toast.success("简化完成" + (chargeYuan > 0 ? "，消耗 " + chargeYuan.toFixed(2) + " 元" : ""));
  } catch (err) {
    const msg = err?.message || "简化失败";
    toast.error(msg);
    setRewriteError(msg);
    setIsRewriting(false);
    return;
  }
}

// Step 4: 本地按顺序替换高难度词 → 生成重写文本
const rewrittenText = applySimplifiedWords(originalText, wordsToSimplify, simplifiedWords);

// Step 5: 保存到 IndexedDB（复用现有逻辑）
if (articleId) {
  await dbSave({
    articleId,
    originalText,
    rewrittenText,
    mappings: wordsToSimplify.map((w, i) => ({ original: w, rewritten: simplifiedWords[i] || w })),
    viewMode: "rewritten",
    rewrittenAt: Date.now(),
  });
  savedArticleIdRef.current = articleId;
  onSuccess?.(articleId, rewrittenText);
}

setRewrittenText(rewrittenText);
setRewriteMappings(wordsToSimplify.map((w, i) => ({ original: w, rewritten: simplifiedWords[i] || w })));
setViewModeState("rewritten");
```

在 `handleRewrite` 函数外部添加辅助函数：

```javascript
/**
 * 从 rewriteMappings 提取需要简化的原始词列表（按顺序）
 * @param {Array<{original: string, rewritten: string}>} mappings
 * @returns {string[]}
 */
function extractHighDiffWordsFromMappings(mappings) {
  if (!mappings || mappings.length === 0) return [];
  return mappings.map((m) => m.original);
}

/**
 * 将原文中的高难度词按顺序替换为简化词
 * 使用正则替换（考虑边界，避免部分匹配）
 * @param {string} originalText
 * @param {string[]} words — 原始高难度词（按顺序）
 * @param {string[]} replacements — 简化词（按顺序）
 * @returns {string}
 */
function applySimplifiedWords(originalText, words, replacements) {
  if (!words || words.length === 0) return originalText;
  let result = originalText;
  words.forEach((word, i) => {
    const replacement = replacements[i] || word;
    // 使用单词边界正则，确保精确匹配
    const escaped = word.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
    const regex = new RegExp(`\\b${escaped}\\b`, "gi");
    result = result.replace(regex, replacement);
  });
  return result;
}
```

**acceptance_criteria:**
- `useReadingRewrite.js` 导入 `simplifyWords` 和 `estimateRewriteTokens`
- `handleRewrite` 包含 token 估算步骤（显示 toast）
- `handleRewrite` 调用 `simplifyWords`（新 endpoint）
- `applySimplifiedWords` 辅助函数存在且使用单词边界正则
- 如果 `simplifyWords` 抛出错误，错误被捕获并显示 toast

---

### Task 2.3 — Token 估算费用预览组件

**文件**: `frontend/src/features/reading/RewriteEstimateBanner.jsx`（新建）

**read_first:**
- `frontend/src/features/reading/reading.css` — 现有样式规范

**action:**

创建 `RewriteEstimateBanner.jsx`：

```javascript
import React from "react";

/**
 * RewriteEstimateBanner — 重写前显示预估费用
 * @param {{ estimatedChargeYuan: number|null, estimatedTokens: number|null }} props
 */
export function RewriteEstimateBanner({ estimatedChargeYuan, estimatedTokens }) {
  if (estimatedChargeYuan === null && estimatedTokens === null) return null;

  return (
    <div className="rewrite-estimate-banner">
      <span className="rewrite-estimate-icon">⚡</span>
      <span>
        预计消耗：{estimatedChargeYuan !== null ? `${estimatedChargeYuan.toFixed(2)} 元` : "—"}
        {estimatedTokens !== null && ` (≈ ${estimatedTokens} tokens)`}
      </span>
    </div>
  );
}
```

在 `reading.css` 添加样式：

```css
.rewrite-estimate-banner {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 8px 12px;
  background: #fef3c7;
  border: 1px solid #f59e0b;
  border-radius: 6px;
  font-size: 13px;
  color: #92400e;
  margin-bottom: 8px;
}

.rewrite-estimate-icon {
  font-size: 14px;
}
```

**acceptance_criteria:**
- `RewriteEstimateBanner.jsx` 存在于 `frontend/src/features/reading/`
- `reading.css` 包含 `.rewrite-estimate-banner` 样式
- 组件在估算值不为 null 时显示费用预览

---

## Verification

### Must-Have Criteria (Goal-Backward)

1. `/api/llm/simplify-words` endpoint 可用并返回有序简化词数组
2. `/api/llm/estimate-tokens` endpoint 可用并返回估算值
3. 前端 `simplifyWords` 函数调用新 endpoint 并返回 `simplifiedWords` 数组
4. `applySimplifiedWords` 按顺序替换原文中的高难度词
5. 重写前显示预估费用 toast
6. 新代码不破坏现有 `POST /api/llm/rewrite-text`（向后兼容）

### Token Saving Verification

- 对比旧 prompt（发全量原文）和新 prompt（发词列表）的 token 消耗
- 测试文本：100词句子，10个高难度词
  - 旧：prompt_tokens ≈ 150（全量原文）+ 模型分析
  - 新：prompt_tokens ≈ 30（词列表）+ 模型分析
  - 目标：节省 ≥ 20%

---

## Files Modified

| File | Change |
|------|--------|
| `app/api/routers/llm.py` | 新增 `SIMPLIFY_WORDS_SYSTEM_PROMPT`, `SimplifyWordsRequest`, `POST /simplify-words`, `GET /estimate-tokens` |
| `app/services/billing_service.py` | 新增 `get_model_rate_sync`（如不存在） |
| `tests/api/test_llm_rewrite.py` | 新建，测试新 endpoint |
| `frontend/src/features/reading/api/readingRewriteApi.js` | 新建，`simplifyWords` + `estimateRewriteTokens` |
| `frontend/src/hooks/useReadingRewrite.js` | 集成新 Schema，`applySimplifiedWords` 辅助函数 |
| `frontend/src/features/reading/RewriteEstimateBanner.jsx` | 新建，费用预览组件 |
| `frontend/src/features/reading/reading.css` | 新增 `.rewrite-estimate-banner` 样式 |

---

## Dependencies

- Wave 2 依赖 Wave 1（backend endpoint 必须先就绪）
- Phase 34 独立于 Phase 32/33（无依赖）
