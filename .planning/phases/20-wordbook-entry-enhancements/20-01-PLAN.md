---
phase: 20-wordbook-entry-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/wordbook/WordbookPanel.jsx
autonomous: true
requirements:
  - WB-01
must_haves:
  truths:
    - 列表模式下每个词条卡片中翻译文字显示在单词下方，采用独立视觉区块背景色区分
    - 复习模式下每个词条卡片中翻译文字同样显示在单词下方，采用独立视觉区块背景色区分
    - 翻译区块有明确边界，与单词和其他内容清晰分隔
    - 卡片高度自适应，不因翻译文字长度导致截断或溢出
  artifacts:
    - path: frontend/src/features/wordbook/WordbookPanel.jsx
      provides: 翻译区块 UI 结构
      min_lines: 15
      contains: "bg-muted/20"
      exports: WordbookPanel
  key_links:
    - from: frontend/src/features/wordbook/WordbookPanel.jsx
      to: word_translation
      via: 列表模式词条卡片
      pattern: "bg-muted/20.*word_translation"
---

<objective>
Restructure wordbook entry cards to display translation in a dedicated visual block with independent background, below the word text. Both list mode and review mode cards must be updated.
</objective>

<context>
@frontend/src/features/wordbook/WordbookPanel.jsx

# Current List Mode Card Structure (lines 547-570):
```jsx
<div className="min-w-0 space-y-3">
  <div className="flex flex-wrap items-center gap-2">
    <div className="truncate text-lg font-semibold">{item.entry_text}</div>
    <Badge variant={isMastered ? "secondary" : "outline"}>
      {isMastered ? "已掌握" : `记忆率 ${formatMemoryScore(item.memory_score)}`}
    </Badge>
  </div>
  {item.word_translation ? (
    <p className="text-sm font-medium text-foreground">单词翻译：{item.word_translation}</p>
  ) : null}
  <div className="space-y-1 text-sm text-muted-foreground">
    ...
  </div>
</div>
```

# Current Review Mode Card Structure (lines 654-681):
```jsx
<div className="space-y-2 rounded-2xl border bg-background p-4">
  <div className="flex items-start justify-between gap-4">
    <div className="flex-1">
      <p className="text-lg font-semibold">{reviewItem.entry_text}</p>
      {reviewItem.word_translation ? (
        <p className="text-sm font-medium text-primary/80">单词翻译：{reviewItem.word_translation}</p>
      ) : null}
      <div className="flex items-center gap-2">
        <p className="text-sm text-muted-foreground flex-1">
          英文语境：{reviewItem.latest_sentence_en || "暂无英文语境"}
        </p>
        ...
      </div>
      <p className="text-sm text-muted-foreground">中文语境：{reviewItem.latest_sentence_zh || "暂无中文语境"}</p>
      ...
    </div>
    ...
  </div>
</div>
```

# Target Structure (per D-01, D-02, D-03):
```
┌───────────────────────────────────────┐
│  serendipity [Badge]                  │  ← 单词 + Badge (same row)
├───────────────────────────────────────┤
│  意外之喜；巧合                        │  ← 翻译区块（bg-muted/20 背景）
├───────────────────────────────────────┤
│  英文语境：It was pure serendipity... │
│  下次复习：明天 14:30                 │  ← 语境信息
└───────────────────────────────────────┘
```

# Locked Decisions (MUST honor):
- D-01: 卡片结构：上下堆叠，单词 → 翻译区块 → 语境（翻译在单词下方）
- D-02: 翻译区块样式：独立背景色（如 `bg-muted/20`）
- D-03: 卡片高度：自适应高度，内容超出时调整高度不截断
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure list mode card translation block</name>
  <files>frontend/src/features/wordbook/WordbookPanel.jsx</files>
  <read_first>
    - frontend/src/features/wordbook/WordbookPanel.jsx (lines 518-615 for list mode card structure)
  </read_first>
  <action>
    Modify the list mode wordbook card (inside items.map) to restructure translation display:

    1. Find the list mode card structure around lines 547-570
    2. Change the translation display from inline paragraph to dedicated block:
       - Replace `{item.word_translation ? ( <p className="text-sm font-medium text-foreground">单词翻译：{item.word_translation}</p> ) : null}`
       - With:
         ```jsx
         {item.word_translation ? (
           <div className="rounded-lg bg-muted/20 px-3 py-2">
             <p className="text-sm font-medium text-foreground">{item.word_translation}</p>
           </div>
         ) : null}
         ```
    3. The translation block must appear AFTER the word row (line 549-552) and BEFORE the context div (line 557)
    4. Remove "单词翻译：" prefix - translation block is visually distinct enough
    5. Keep the structure: word+badge row → translation block → context → metadata → actions

    <acceptance_criteria>
      - [ ] grep "bg-muted/20" returns the translation block in list mode
      - [ ] grep "word_translation.*text-sm.*font-medium.*text-foreground" shows translation without "单词翻译：" prefix
      - [ ] Translation block appears after Badge row, before context div
      - [ ] Translation block has rounded-lg background
    </acceptance_criteria>
  </action>
  <verify>
    <automated>grep -n "bg-muted/20" frontend/src/features/wordbook/WordbookPanel.jsx | head -5</automated>
  </verify>
  <done>List mode wordbook cards display translation in dedicated block with bg-muted/20 background</done>
</task>

<task type="auto">
  <name>Task 2: Restructure review mode card translation block</name>
  <files>frontend/src/features/wordbook/WordbookPanel.jsx</files>
  <read_first>
    - frontend/src/features/wordbook/WordbookPanel.jsx (lines 654-703 for review mode card structure)
  </read_first>
  <action>
    Modify the review mode wordbook card (inside the "review" panel) to restructure translation display:

    1. Find the review mode card structure around lines 654-681
    2. Change the translation display from inline paragraph to dedicated block:
       - Replace `{reviewItem.word_translation ? ( <p className="text-sm font-medium text-primary/80">单词翻译：{reviewItem.word_translation}</p> ) : null}`
       - With:
         ```jsx
         {reviewItem.word_translation ? (
           <div className="mt-2 rounded-lg bg-muted/20 px-3 py-2">
             <p className="text-sm font-medium text-foreground">{reviewItem.word_translation}</p>
           </div>
         ) : null}
         ```
    3. The translation block must appear AFTER the word line (line 657) and BEFORE the English context line (line 662-674)
    4. Add `mt-2` margin-top for spacing from word
    5. Remove "单词翻译：" prefix - translation block is visually distinct enough
    6. Update context line to use flex layout with items-start for proper alignment

    <acceptance_criteria>
      - [ ] grep "bg-muted/20" in review mode section (after line 650)
      - [ ] Review mode translation block has same styling as list mode
      - [ ] Translation block has mt-2 margin for spacing
      - [ ] No "单词翻译：" prefix in review mode
    </acceptance_criteria>
  </action>
  <verify>
    <automated>grep -n "bg-muted/20" frontend/src/features/wordbook/WordbookPanel.jsx | wc -l</automated>
  </verify>
  <done>Review mode wordbook cards display translation in dedicated block with bg-muted/20 background</done>
</task>

</tasks>

<verification>
- Both list mode and review mode cards show translation in dedicated block
- Translation blocks use bg-muted/20 background color
- Translation appears below word, above context
- No truncation or overflow due to translation length
</verification>

<success_criteria>
1. List mode: translation in `bg-muted/20` block below word
2. Review mode: translation in `bg-muted/20` block below word
3. Both modes use consistent translation block styling
4. Card height adapts to content without truncation
</success_criteria>

<output>
After completion, create `.planning/phases/20-wordbook-entry-enhancements/20-01-SUMMARY.md`
</output>
