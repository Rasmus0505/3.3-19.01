# Phase 28 Plan: 词交互与生词本集成

## 概述

Phase 28 基于 Phase 27 的阅读板块核心 UI，完成词点击选入、多选态、批量加入生词本、词汇翻译弹窗的完整交互链。

**前置依赖**：Phase 27（阅读板块核心 UI）必须已完成。

---

## 前置文件

实现前必须阅读：

- `frontend/src/features/reading/ReadingPage.jsx` — 现有词选状态管理
- `frontend/src/features/reading/ArticlePanel.jsx` — `ArticleWord` 组件、`computeCefrClassName`
- `frontend/src/features/reading/WordSidebar.jsx` — 现有边栏组件
- `frontend/src/features/wordbook/TranslationDialog.jsx` — 现有翻译弹窗（复用）
- `frontend/src/features/immersive/ImmersiveLessonPage.jsx` — `collectWordbookEntry()` API 调用模式（第 1907 行）
- `frontend/src/features/wordbook/WordbookPanel.jsx` — 批量操作 API 模式（`/api/wordbook/batch-*`）
- `frontend/src/features/immersive/immersive.css` — `.wordbook-token--success` 动画定义
- `frontend/src/app/authStorage.js` — `readCefrLevel()`
- `frontend/src/lib/utils.js` — `cn()` 工具
- `frontend/src/shared/api/client.js` — `parseResponse`, `toErrorText`

---

## Wave 1（并行）

### Plan 01 — 词选动画 + 选中态增强

**目标**：点击词时触发 scale 动画反馈，边栏 item 显示 check 徽章，选中词在文章中带视觉提示。

**修改文件**：
- `frontend/src/features/reading/ArticlePanel.jsx`
- `frontend/src/features/reading/reading.css`

**具体操作**：

1. 在 `ArticleWord` 中添加 `isSelected` prop，渲染选中态背景：

```jsx
function ArticleWord({ segment, userLevel, onWordClick, isSelected }) {
  const cefrClass = computeCefrClassName(segment.cefrLevel, userLevel);
  return (
    <span
      className={cn(
        "article-word",
        cefrClass,
        isSelected && "article-word--selected article-word--success"
      )}
      onClick={() => onWordClick?.(segment.text, segment)}
      title={`${segment.cefrLevel || "未知等级"} — ${segment.text}`}
    >
      {segment.text}
    </span>
  );
}
```

2. 在 `reading.css` 中添加 `.article-word--success` 动画（复用 Phase 25 `.wordbook-token--success` 逻辑）：

```css
/* 选中态 */
.article-word--selected {
  background-color: oklch(0.93 0.06 175 / 0.25);
  border-radius: 3px;
  padding-left: 2px;
  padding-right: 2px;
  margin-left: -2px;
}

/* 词选成功动画（复用 Phase 25 动画模式） */
@keyframes word-select-success {
  0%   { transform: scale(1); }
  40%  { transform: scale(1.08); }
  70%  { transform: scale(1.08); border-bottom-color: oklch(0.70 0.20 175) !important; }
  100% { transform: scale(1); }
}

.article-word--success {
  animation: word-select-success 400ms ease-out forwards;
}
```

3. 同时更新 `WordSidebar` item，添加 check 徽章（已选中的词边栏 item 带绿色勾）：

```jsx
function WordSidebarItem({ item, onRemove }) {
  return (
    <div className="word-sidebar-item word-sidebar-item--selected">
      {/* check icon */}
      <div className="word-sidebar-item__main">
        ...
```

4. 在 `reading.css` 中添加 `.word-sidebar-item--selected` 样式（左边框加粗 + 浅绿背景）：

```css
.word-sidebar-item--selected {
  border-left: 3px solid oklch(0.65 0.18 175);
  background: oklch(0.97 0.04 175 / 0.4);
}
```

**验收条件**：
- 点击词触发 scale(1→1.08→1) 动画，持续 400ms
- 边栏 item 带绿色左边框 + 浅绿背景
- 文章中选中词带浅蓝背景 + 动画

---

### Plan 02 — 词汇翻译弹窗集成

**目标**：点击边栏词条旁的「译」按钮，弹出翻译弹窗查看词义。

**修改文件**：
- `frontend/src/features/reading/WordSidebar.jsx`
- `frontend/src/features/reading/ReadingPage.jsx`

**具体操作**：

1. 在 `ReadingPage.jsx` 中导入并渲染 `TranslationDialog`：

```jsx
import { TranslationDialog } from "../wordbook/TranslationDialog";

export function ReadingPage({ accessToken, apiCall }) {
  // ... existing state
  const [translationDialog, setTranslationDialog] = useState({ open: false, text: "" });

  return (
    <>
      <div className="reading-layout">...</div>
      <TranslationDialog
        open={translationDialog.open}
        onClose={() => setTranslationDialog((s) => ({ ...s, open: false }))}
        text={translationDialog.text}
        apiCall={apiCall}
      />
    </>
  );
}
```

2. 在 `WordSidebar.jsx` 中添加「译」按钮（Lucide `Languages` icon）：

```jsx
import { Languages } from "lucide-react";

function WordSidebarItem({ item, onRemove, onTranslate }) {
  return (
    <div className="word-sidebar-item">
      <div className="word-sidebar-item__main">...</div>
      <div className="word-sidebar-item__actions">
        <button
          className="word-sidebar-item__action"
          onClick={() => onTranslate?.(item)}
          title="查看翻译"
        >
          <Languages className="size-3.5" />
        </button>
        <button ...>
          <X className="size-3" />
        </button>
      </div>
    </div>
  );
}
```

在 `WordSidebar` 的 props 中添加 `onTranslate: (item) => void`。

3. 在 `reading.css` 中添加 `.word-sidebar-item__actions` / `.word-sidebar-item__action` 样式：

```css
.word-sidebar-item__actions {
  display: flex;
  align-items: center;
  gap: 2px;
  flex-shrink: 0;
}

.word-sidebar-item__action {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 22px;
  height: 22px;
  border: none;
  background: transparent;
  color: oklch(0.55 0.02 85);
  cursor: pointer;
  border-radius: 4px;
  transition: background 0.15s, color 0.15s;
}

.word-sidebar-item__action:hover {
  background: oklch(0.93 0.02 85);
  color: oklch(0.35 0.10 175);
}
```

**验收条件**：
- 边栏每个词条旁有「译」按钮
- 点击「译」弹出 `TranslationDialog`
- 弹窗调用 `/api/wordbook/translate` API 显示翻译结果

---

## Wave 2

### Plan 03 — 批量加入生词本 API

**目标**：将边栏选中的词批量加入生词本，调用 `/api/wordbook/collect`（复用沉浸式学习 API）。

**修改文件**：
- `frontend/src/features/reading/ReadingPage.jsx`
- `frontend/src/features/reading/reading.css`

**具体操作**：

1. 实现 `handleAddAllToWordbook`（替换 Phase 27 的 console.warn）：

```jsx
import { parseResponse, toErrorText } from "../../shared/api/client";
import { toast } from "sonner";

async function handleAddAllToWordbook() {
  if (selectedWords.length === 0) return;
  if (!accessToken) {
    toast.error("请先登录");
    return;
  }

  let successCount = 0;
  let failCount = 0;

  for (const item of selectedWords) {
    try {
      const resp = await apiCall("/api/wordbook/collect", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          lesson_id: null, // 阅读板块无关联课程
          sentence_index: null,
          entry_text: item.word,
          entry_type: "word", // 单词
          start_token_index: null,
          end_token_index: null,
        }),
      });
      const data = await parseResponse(resp);
      if (resp.ok) {
        successCount++;
      } else {
        failCount++;
      }
    } catch (_) {
      failCount++;
    }
  }

  if (successCount > 0) {
    toast.success(`已加入 ${successCount} 个词到生词本`);
    setSelectedWords([]);
  } else if (failCount > 0) {
    toast.error(`加入失败 ${failCount} 个`);
  }
}
```

2. 添加「全部加入」按钮的 loading 状态：

在 `WordSidebar` 添加 `disabled` prop，`onAddAllToWordbook` 变为 `async`，期间 button 显示 spinner。

3. 加入成功后清空已选列表（`setSelectedWords([])`），同时触发 `onWordbookChanged` 回调（通知父组件刷新生词本计数）。

**验收条件**：
- 点击「加入生词本」，每个词调用一次 `/api/wordbook/collect`
- 成功提示「已加入 N 个词到生词本」
- 加入后清空边栏已选列表
- 未登录时提示「请先登录」

---

### Plan 04 — 边栏统计信息 + 选中词管理增强

**目标**：边栏显示文章难度分布（B1/B2/C1 徽章）、总词数、已选词数；完善多选管理（清空、全选反转）。

**修改文件**：
- `frontend/src/features/reading/WordSidebar.jsx`
- `frontend/src/features/reading/reading.css`

**具体操作**：

1. 在 `WordSidebar` 头部添加统计区：

```jsx
export function WordSidebar({
  selectedWords = [],
  wordStats = null,   // { total: 0, cefrCounts: { A1, A2, B1, B2, C1, C2, SUPER } }
  onRemove,
  onAddAllToWordbook,
  onClearAll,
  isAdding = false,
}) {
  return (
    <aside className="word-sidebar">
      {/* 统计区 */}
      {wordStats && <WordSidebarStats stats={wordStats} />}
      {/* ... */}
    </aside>
  );
}

function WordSidebarStats({ stats }) {
  const levels = [
    { key: "B1", label: "B1", className: "word-sidebar-stat--b1" },
    { key: "B2", label: "B2", className: "word-sidebar-stat--b2" },
    { key: "C1", label: "C1", className: "word-sidebar-stat--c1" },
    { key: "C2", label: "C2", className: "word-sidebar-stat--c2" },
    { key: "SUPER", label: "超纲", className: "word-sidebar-stat--super" },
  ];

  return (
    <div className="word-sidebar-stats">
      <div className="word-sidebar-stats__title">文章难度</div>
      <div className="word-sidebar-stats__bar">
        {levels.map((lvl) => {
          const count = stats.cefrCounts?.[lvl.key] || 0;
          const pct = stats.total > 0 ? (count / stats.total) * 100 : 0;
          return pct > 0 ? (
            <div
              key={lvl.key}
              className={cn("word-sidebar-stat", lvl.className)}
              style={{ width: `${pct}%` }}
              title={`${lvl.label}: ${count} 词`}
            />
          ) : null;
        })}
      </div>
      <div className="word-sidebar-stats__legend">
        {levels.map((lvl) => {
          const count = stats.cefrCounts?.[lvl.key] || 0;
          return count > 0 ? (
            <span key={lvl.key} className="word-sidebar-stat-badge">
              <span className={cn("word-sidebar-stat-dot", lvl.className)} />
              {lvl.label} {count}
            </span>
          ) : null;
        })}
      </div>
    </div>
  );
}
```

2. 在 `ReadingPage.jsx` 中计算 `wordStats`：

```jsx
import { useMemo } from "react";

function computeWordStats(selectedWords, lines) {
  // 从所有 lines 统计
  const cefrCounts = { A1: 0, A2: 0, B1: 0, B2: 0, C1: 0, C2: 0, SUPER: 0 };
  let total = 0;
  for (const line of lines) {
    for (const seg of line.segments) {
      if (!seg.cefrLevel) continue;
      if (cefrCounts[seg.cefrLevel] !== undefined) {
        cefrCounts[seg.cefrLevel]++;
      }
      total++;
    }
  }
  return { total, cefrCounts };
}

// 在 ReadingPage 组件内
const wordStats = useMemo(
  () => computeWordStats(selectedWords, articleLines),
  [selectedWords, articleLines]
);

// articleLines 从 ArticlePanel 通过 onLinesReady 回调传回
```

3. 更新 `ArticlePanel` 暴露 `onLinesReady: (lines: RichLine[]) => void` 回调，在布局完成后将 lines 传回 `ReadingPage`。

4. 完善边栏操作按钮：

```jsx
<div className="word-sidebar__header">
  <span className="word-sidebar__title">
    已选词汇
    {count > 0 && <span className="word-sidebar__count">({count})</span>}
  </span>
  {count > 0 && (
    <Button size="sm" variant="ghost" onClick={onClearAll} className="text-xs text-muted-foreground">
      清空
    </Button>
  )}
</div>
```

5. 在 `reading.css` 中添加统计区样式：

```css
.word-sidebar-stats {
  padding-bottom: 12px;
  border-bottom: 1px solid oklch(0.88 0.01 85);
  margin-bottom: 12px;
}

.word-sidebar-stats__title {
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: oklch(0.50 0.02 85);
  margin-bottom: 6px;
}

.word-sidebar-stats__bar {
  display: flex;
  height: 6px;
  border-radius: 3px;
  overflow: hidden;
  background: oklch(0.90 0.01 85);
  margin-bottom: 6px;
}

.word-sidebar-stat {
  height: 100%;
  min-width: 2px;
  transition: width 0.3s ease;
}

.word-sidebar-stat--b1 { background: oklch(0.70 0.10 175); }
.word-sidebar-stat--b2 { background: oklch(0.60 0.14 175); }
.word-sidebar-stat--c1 { background: oklch(0.60 0.16 25); }
.word-sidebar-stat--c2 { background: oklch(0.50 0.20 25); }
.word-sidebar-stat--super { background: oklch(0.45 0.02 85); }

.word-sidebar-stats__legend {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
}

.word-sidebar-stat-badge {
  display: flex;
  align-items: center;
  gap: 4px;
  font-size: 11px;
  color: oklch(0.45 0.02 85);
}

.word-sidebar-stat-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  flex-shrink: 0;
}
```

**验收条件**：
- 边栏顶部显示难度分布条（B1/B2/C1/C2/SUPER 分段）
- 分布条下方有图例（每个非零等级的徽章）
- 选中词数量在标题旁显示
- 「清空」按钮一键清空所有已选词

---

## Wave 3

### Plan 05 — Demo 数据完善 + ArticlePanel lines 回调 + 总结

**目标**：完善 `ArticlePanel` 的 `onLinesReady` 回调，计算 `wordStats`，整合所有组件，输出 Phase 28 SUMMARY。

**修改文件**：
- `frontend/src/features/reading/ArticlePanel.jsx`
- `frontend/src/features/reading/ReadingPage.jsx`
- `frontend/src/features/reading/WordSidebar.jsx`

**具体操作**：

1. 在 `ArticlePanel.jsx` 中暴露 `onLinesReady` prop：

```jsx
export function ArticlePanel({
  text,
  contentWidth,
  onWidthChange,
  onWordClick,
  onLinesReady,   // 新增
  selectedWords,
}) {
  // ... existing layout
  const { lines, isReady, error } = useRichLayout(text, measuredWidth, ARTICLE_FONT, ARTICLE_LINE_HEIGHT);

  // 布局完成后通知父组件
  useEffect(() => {
    if (isReady && lines.length > 0) {
      onLinesReady?.(lines);
    }
  }, [isReady, lines, onLinesReady]);
  // ...
}
```

2. 在 `ReadingPage.jsx` 中整合所有逻辑：

```jsx
export function ReadingPage({ accessToken, apiCall }) {
  const [contentWidth, setContentWidth] = useState(640);
  const [selectedWords, setSelectedWords] = useState([]);
  const [articleLines, setArticleLines] = useState([]);
  const [isAddingToWordbook, setIsAddingToWordbook] = useState(false);
  const [translationDialog, setTranslationDialog] = useState({ open: false, text: "" });

  const wordStats = useMemo(() => {
    const cefrCounts = { A1: 0, A2: 0, B1: 0, B2: 0, C1: 0, C2: 0, SUPER: 0 };
    let total = 0;
    for (const line of articleLines) {
      for (const seg of line.segments) {
        if (seg.cefrLevel && cefrCounts[seg.cefrLevel] !== undefined) {
          cefrCounts[seg.cefrLevel]++;
        }
        total++;
      }
    }
    return { total, cefrCounts };
  }, [articleLines]);

  const handleWordClick = useCallback((word, segment) => {
    setSelectedWords((prev) => {
      const exists = prev.some((w) => w.word === word);
      if (exists) return prev.filter((w) => w.word !== word);
      const userLevel = readCefrLevel() || "B1";
      const cefrClass = computeCefrClassName(segment.cefrLevel, userLevel);
      return [...prev, { word, cefrLevel: segment.cefrLevel, cefrClass }];
    });
  }, []);

  const handleAddAllToWordbook = useCallback(async () => {
    if (!accessToken) { toast.error("请先登录"); return; }
    setIsAddingToWordbook(true);
    let successCount = 0;
    for (const item of selectedWords) {
      try {
        const resp = await apiCall("/api/wordbook/collect", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            lesson_id: null, sentence_index: null,
            entry_text: item.word, entry_type: "word",
            start_token_index: null, end_token_index: null,
          }),
        });
        const data = await parseResponse(resp);
        if (resp.ok) successCount++;
      } catch (_) { /* fail silently per-item */ }
    }
    setIsAddingToWordbook(false);
    if (successCount > 0) {
      toast.success(`已加入 ${successCount} 个词到生词本`);
      setSelectedWords([]);
    }
  }, [accessToken, apiCall, selectedWords]);

  return (
    <>
      <div className="reading-layout">
        <ArticlePanel
          text={DEMO_ARTICLE}
          contentWidth={contentWidth}
          onWidthChange={setContentWidth}
          onWordClick={handleWordClick}
          onLinesReady={setArticleLines}
          selectedWords={selectedWords}
        />
        <WordSidebar
          selectedWords={selectedWords}
          wordStats={wordStats}
          onRemove={(item) => setSelectedWords((p) => p.filter((w) => w.word !== item.word))}
          onAddAllToWordbook={handleAddAllToWordbook}
          onClearAll={() => setSelectedWords([])}
          onTranslate={(item) => setTranslationDialog({ open: true, text: item.word })}
          isAdding={isAddingToWordbook}
        />
      </div>
      <TranslationDialog
        open={translationDialog.open}
        onClose={() => setTranslationDialog((s) => ({ ...s, open: false }))}
        text={translationDialog.text}
        apiCall={apiCall}
      />
    </>
  );
}
```

3. 完善 `WordSidebar` 的 `onTranslate` 按钮和 `wordStats` 统计区。

4. 运行 `npm run build` 验证通过。

5. 创建 `.planning/phases/28-word-interaction/28-01-SUMMARY.md`（内容见 Plan 内容）。

**验收条件**：
- `npm run build` 通过
- 点击词 → scale 动画 → 边栏出现该词（带 check/左边框）
- 点击边栏「译」 → 翻译弹窗
- 点击「加入生词本」 → 调用 `/api/wordbook/collect`，成功提示，清空列表
- 边栏顶部显示难度分布条和图例
- Phase 28 SUMMARY.md 生成

---

## 验收标准总览

| Plan | 验收 | 状态 |
|------|------|------|
| 01 | scale 动画、选中态背景、边栏 check 徽章 | ⬜ |
| 02 | 翻译弹窗集成、API 调用 | ⬜ |
| 03 | 批量加入生词本 API、loading 态、成功提示 | ⬜ |
| 04 | 难度分布条、图例、清空按钮 | ⬜ |
| 05 | 所有组件整合、build 通过、总结输出 | ⬜ |

## 依赖

- Phase 27：`ReadingPage`、`ArticlePanel`、`WordSidebar`、`reading.css`
- Phase 25：`.wordbook-token--success` 动画模式
- 现有 API：`/api/wordbook/collect`、`/api/wordbook/translate`

## 并行策略

- Wave 1（Plan 01, 02）：动画 + 翻译弹窗，并行开发
- Wave 2（Plan 03, 04）：API + 统计区，并行开发
- Wave 3（Plan 05）：整合、build、总结
