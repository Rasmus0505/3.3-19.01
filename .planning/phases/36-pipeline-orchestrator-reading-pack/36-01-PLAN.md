---
phase: 36-pipeline-orchestrator-reading-pack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/reading/readingPipelineMachine.js
  - frontend/src/features/reading/readingPack.js
  - frontend/src/features/reading/readingRewriteDB.js
  - frontend/src/hooks/useReadingRewrite.js
  - frontend/src/features/reading/readingPipelineMachine.test.js
  - frontend/src/features/reading/readingPack.test.js
  - frontend/src/features/reading/readingRewriteDB.pack.test.js
  - frontend/src/features/reading/useReadingRewrite.resume.test.js
autonomous: true
requirements: [PIPE-01, PIPE-02, PIPE-03, PACK-01]
user_setup: []
must_haves:
  truths:
    - The reading flow persists a five-stage pipeline ledger instead of only `idle | diagnosed | generated`.
    - Refresh or history reopen can restore either the last saved in-progress stage or a completed reading pack without recomputing state.
    - A single `reading_rewrites_v3` record evolves into the reading-pack asset and remains keyed by `articleId`.
    - Reading-pack assembly persists comparison-card data and diagnostic summary alongside original text, rewritten text, mappings, and target metadata.
  artifacts:
    - frontend/src/features/reading/readingPipelineMachine.js
    - frontend/src/features/reading/readingPack.js
    - frontend/src/features/reading/readingRewriteDB.js
    - frontend/src/hooks/useReadingRewrite.js
    - frontend/src/features/reading/readingPipelineMachine.test.js
    - frontend/src/features/reading/readingPack.test.js
    - frontend/src/features/reading/readingRewriteDB.pack.test.js
    - frontend/src/features/reading/useReadingRewrite.resume.test.js
  key_links:
    - `useReadingRewrite.js` must persist pipeline snapshots into `readingRewriteDB.js` after every stage transition.
    - `readingPack.js` must build pack payloads and comparison cards from the same saved original/rewrite/mapping data used by `ArticlePanel.jsx`.
    - `readingPipelineMachine.js` must encode the exact user-visible stages `parsing`, `difficulty judgment`, `simplification planning`, `text rewriting`, `reading-pack assembly`.
---

<objective>
Create the Phase 36 data contract, reducer-backed pipeline state, and validation foundation.

Purpose: give the reading feature a persisted pipeline ledger and a first-class reading-pack asset before any new stage UI or pack page is rendered.
Output: pure pipeline/pack helpers, upgraded IndexedDB schema usage inside the existing store, resume-aware hook behavior, and Wave 0 tests that lock the phase contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36-pipeline-orchestrator-reading-pack/36-CONTEXT.md
@.planning/phases/36-pipeline-orchestrator-reading-pack/36-RESEARCH.md
@.planning/phases/36-pipeline-orchestrator-reading-pack/36-VALIDATION.md
@.planning/phases/36-pipeline-orchestrator-reading-pack/36-UI-SPEC.md
@.planning/phases/35-material-intake-diagnostic-card/35-CONTEXT.md
@frontend/src/hooks/useReadingRewrite.js
@frontend/src/features/reading/readingRewriteDB.js
@frontend/src/features/reading/readingDiagnostics.js
@frontend/src/features/reading/ArticlePanel.jsx
@frontend/src/features/upload/uploadTaskViewModel.js
</context>

<threat_model>
## Threat Model

- **T-36-01 Information disclosure:** stage/failure UI must not expose raw backend or model-internal error text; use sanitized, user-facing copy only.
- **T-36-02 Tampering / stale recovery:** reopened packs and interrupted pipelines must derive from persisted article-scoped state, not ad hoc recomputation from partially cleared component state.
- **T-36-03 Integrity drift:** comparison cards must be assembled from the saved original text, rewritten text, and mappings so `ArticlePanel` and pack comparison never disagree.
- **Block threshold:** any plan change that reintroduces unsanitized error strings, a second persistence store, or render-time diff inference is a high-severity blocker.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add a pure pipeline reducer and comparison-pack assembly helpers</name>
  <files>frontend/src/features/reading/readingPipelineMachine.js, frontend/src/features/reading/readingPack.js, frontend/src/features/reading/readingPipelineMachine.test.js, frontend/src/features/reading/readingPack.test.js</files>
  <read_first>frontend/src/features/upload/uploadTaskViewModel.js, frontend/src/hooks/useReadingRewrite.js, frontend/src/features/reading/readingDiagnostics.js, frontend/src/features/reading/ArticlePanel.jsx, .planning/phases/36-pipeline-orchestrator-reading-pack/36-CONTEXT.md, .planning/phases/36-pipeline-orchestrator-reading-pack/36-RESEARCH.md, .planning/phases/36-pipeline-orchestrator-reading-pack/36-VALIDATION.md</read_first>
  <action>Create `frontend/src/features/reading/readingPipelineMachine.js` as a pure reducer/helper module that exports the exact five visible stage keys `parsing`, `difficulty_judgment`, `simplification_planning`, `text_rewriting`, and `reading_pack_assembly`, plus actions/helpers for stage start, stage completion, stage failure, resume restoration, and pack completion. Create `frontend/src/features/reading/readingPack.js` as a pure assembly layer that builds a persistent `readingPack` object containing `originalText`, `rewrittenText`, `targetLevel`, `diagnosticSummary`, `mappings`, `wordLevels`, and `comparisonCards`. Build `comparisonCards` once during assembly from the saved original/rewrite/mapping data; do not use DOM inspection or render-time diffing. Add `frontend/src/features/reading/readingPipelineMachine.test.js` to cover stage order, completion, failure, and resume transitions. Add `frontend/src/features/reading/readingPack.test.js` to cover pack assembly, diagnostic-summary extraction, and comparison-card output shape.</action>
  <verify>cd frontend; npm run test -- --run src/features/reading/readingPipelineMachine.test.js src/features/reading/readingPack.test.js</verify>
  <acceptance_criteria>
    - `frontend/src/features/reading/readingPipelineMachine.js` contains all five stage keys and exports reducer helpers for running/completed/failed/resume states.
    - `frontend/src/features/reading/readingPack.js` contains a `comparisonCards` builder and does not use `document`, `window.getSelection`, or DOM diff logic.
    - `cd frontend; npm run test -- --run src/features/reading/readingPipelineMachine.test.js src/features/reading/readingPack.test.js` exits 0.
  </acceptance_criteria>
  <done>Phase 36 has a deterministic state contract for pipeline orchestration and pack assembly.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade the existing rewrite record and hook to persist pipeline snapshots and pack assets</name>
  <files>frontend/src/features/reading/readingRewriteDB.js, frontend/src/hooks/useReadingRewrite.js, frontend/src/features/reading/readingRewriteDB.pack.test.js, frontend/src/features/reading/useReadingRewrite.resume.test.js</files>
  <read_first>frontend/src/features/reading/readingRewriteDB.js, frontend/src/hooks/useReadingRewrite.js, frontend/src/features/reading/readingPipelineMachine.js, frontend/src/features/reading/readingPack.js, frontend/src/features/reading/HistoryPanel.jsx, .planning/phases/36-pipeline-orchestrator-reading-pack/36-CONTEXT.md, .planning/phases/36-pipeline-orchestrator-reading-pack/36-RESEARCH.md</read_first>
  <action>Extend `frontend/src/features/reading/readingRewriteDB.js` so the existing `reading_rewrites_v3` record supports `pipeline`, `readingPack`, `packViewMode`, `packStatus`, `lastCompletedStage`, `resumeAvailable`, `failureStage`, and any compare-card payload required by `readingPack.js`, while keeping `articleId` as the only primary key and preserving the existing history relationship. Update `frontend/src/hooks/useReadingRewrite.js` to use the new reducer/helpers instead of only coarse `flowStatus`; expose pipeline state, resume metadata, pack payload, and pack-view mode setters. Persist after each stage start/completion/failure and when the final pack is assembled. Keep the recommendation from research: default interrupted runs to reopen at the last saved stage with an explicit `继续生成` path rather than silent auto-resume. Add `frontend/src/features/reading/readingRewriteDB.pack.test.js` and `frontend/src/features/reading/useReadingRewrite.resume.test.js` to verify persisted shape, pack reopen behavior, interrupted-stage restoration, and the absence of a second IndexedDB store.</action>
  <verify>cd frontend; npm run test -- --run src/features/reading/readingRewriteDB.pack.test.js src/features/reading/useReadingRewrite.resume.test.js</verify>
  <acceptance_criteria>
    - `frontend/src/features/reading/readingRewriteDB.js` contains `pipeline` and `readingPack` fields and still uses `articleId` as the store key.
    - `frontend/src/hooks/useReadingRewrite.js` contains explicit persistence calls for stage start/completion/failure, not only final success.
    - `frontend/src/hooks/useReadingRewrite.js` exposes resumed pack or pipeline state without creating a second database/store constant.
    - `cd frontend; npm run test -- --run src/features/reading/readingRewriteDB.pack.test.js src/features/reading/useReadingRewrite.resume.test.js` exits 0.
  </acceptance_criteria>
  <done>The reading hook can now restore interrupted generation or completed pack state from one persisted record.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run test -- --run src/features/reading/readingPipelineMachine.test.js src/features/reading/readingPack.test.js`
- [ ] `cd frontend && npm run test -- --run src/features/reading/readingRewriteDB.pack.test.js src/features/reading/useReadingRewrite.resume.test.js`
- [ ] The persisted record still uses one `articleId`-keyed store and contains both `pipeline` and `readingPack`
- [ ] Interrupted runs restore to a saved stage with an explicit continue path instead of silent auto-restart
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No second reading-pack store or backend orchestration endpoint introduced
- PIPE-03 and PACK-01 foundations are satisfied and ready for the UI layer
</success_criteria>

<output>
After completion, create `.planning/phases/36-pipeline-orchestrator-reading-pack/36-01-SUMMARY.md`
</output>
