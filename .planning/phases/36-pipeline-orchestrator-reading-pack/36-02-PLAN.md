---
phase: 36-pipeline-orchestrator-reading-pack
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/src/features/reading/ReadingPage.jsx
  - frontend/src/features/reading/ReadingPipelinePanel.jsx
  - frontend/src/features/reading/DiagnosticPanel.jsx
  - frontend/src/features/reading/HistoryPanel.jsx
  - frontend/src/features/reading/reading.css
  - frontend/src/features/reading/ReadingPage.pipeline.test.jsx
autonomous: true
requirements: [PIPE-01, PIPE-02, PIPE-03]
user_setup: []
must_haves:
  truths:
    - The reading experience shows a dedicated stage view with the exact five named stages before the user reaches the finished pack.
    - The current stage, last completed stage, and next stage are visible; a generic loading spinner is not the main focus.
    - Failure copy names the failed stage and keeps an explicit original-reading fallback plus resume CTA.
    - History reopen distinguishes completed packs from interrupted pipelines and routes each to the correct surface.
  artifacts:
    - frontend/src/features/reading/ReadingPage.jsx
    - frontend/src/features/reading/ReadingPipelinePanel.jsx
    - frontend/src/features/reading/DiagnosticPanel.jsx
    - frontend/src/features/reading/HistoryPanel.jsx
    - frontend/src/features/reading/reading.css
    - frontend/src/features/reading/ReadingPage.pipeline.test.jsx
  key_links:
    - `ReadingPage.jsx` must route `input -> diagnostic -> pipeline -> pack`.
    - `ReadingPipelinePanel.jsx` must consume the reducer-backed stage ledger from Plan 01 and the approved `36-UI-SPEC.md`.
    - `HistoryPanel.jsx` must reopen completed packs directly and reopen interrupted runs into the pipeline surface with resume metadata.
---

<objective>
Implement the visible pipeline orchestration surface and recovery flow.

Purpose: turn the Phase 35 diagnostic entry into the explicit five-stage generation experience with stage-specific copy, visible recovery, and history-aware reopening.
Output: a dedicated pipeline panel, updated page routing, history affordances, and UI tests covering staged execution and failure/recovery behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36-pipeline-orchestrator-reading-pack/36-CONTEXT.md
@.planning/phases/36-pipeline-orchestrator-reading-pack/36-RESEARCH.md
@.planning/phases/36-pipeline-orchestrator-reading-pack/36-UI-SPEC.md
@.planning/phases/36-pipeline-orchestrator-reading-pack/36-01-PLAN.md
@frontend/src/features/reading/ReadingPage.jsx
@frontend/src/features/reading/DiagnosticPanel.jsx
@frontend/src/features/reading/HistoryPanel.jsx
@frontend/src/features/reading/reading.css
@frontend/src/features/upload/uploadTaskViewModel.js
@frontend/src/components/ui/progress.jsx
</context>

<threat_model>
## Threat Model

- **T-36-04 Information disclosure:** stage cards and history badges must display sanitized user-facing messages, not raw backend/model error strings.
- **T-36-05 UX integrity:** interrupted runs must not silently resume into a mutated state without showing the restored checkpoint and explicit continue action.
- **T-36-06 Availability regression:** pipeline UI must preserve the original-reading fallback and keep the history surface usable even when generation fails.
- **Block threshold:** any implementation that hides the failed stage, removes the original fallback, or bypasses explicit recovery is a high-severity blocker.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add the dedicated pipeline stage panel and integrate it into ReadingPage</name>
  <files>frontend/src/features/reading/ReadingPage.jsx, frontend/src/features/reading/ReadingPipelinePanel.jsx, frontend/src/features/reading/DiagnosticPanel.jsx, frontend/src/features/reading/reading.css</files>
  <read_first>frontend/src/features/reading/ReadingPage.jsx, frontend/src/features/reading/DiagnosticPanel.jsx, frontend/src/features/reading/reading.css, frontend/src/features/reading/readingPipelineMachine.js, frontend/src/hooks/useReadingRewrite.js, frontend/src/features/upload/uploadTaskViewModel.js, frontend/src/components/ui/progress.jsx, .planning/phases/36-pipeline-orchestrator-reading-pack/36-UI-SPEC.md</read_first>
  <action>Create `frontend/src/features/reading/ReadingPipelinePanel.jsx` to render the approved Phase 36 stage surface: a dedicated stage rail plus stage card, exact stage names mapped to user-facing labels, running-stage progress bar, last-completed summary, next-stage preview, resume banner `已恢复到上次进度`, primary CTA `继续生成`, and fallback CTA `查看原文`. Refactor `frontend/src/features/reading/ReadingPage.jsx` so it supports four states `input`, `diagnostic`, `pipeline`, and `pack`; route confirm-generation into the pipeline sequence instead of jumping straight to normal reading. Update `DiagnosticPanel.jsx` copy/CTA usage only where needed so the diagnostic stage hands off cleanly into the pipeline surface. Update `reading.css` to match the approved UI-SPEC: desktop stage rail + stage card, mobile stacked version, accent/current-stage treatment, 48px control boxes for icon-only toggles, and no spinner-dominant loading pattern.</action>
  <verify>cd frontend; npm run test -- --run src/features/reading/ReadingPage.pipeline.test.jsx</verify>
  <acceptance_criteria>
    - `frontend/src/features/reading/ReadingPage.jsx` contains a distinct `pipeline` mode in addition to `diagnostic` and the finished pack mode.
    - `frontend/src/features/reading/ReadingPipelinePanel.jsx` contains the five stage names and both `继续生成` and `查看原文`.
    - `frontend/src/features/reading/reading.css` contains dedicated pipeline layout classes rather than only a loading overlay tweak.
    - `cd frontend; npm run test -- --run src/features/reading/ReadingPage.pipeline.test.jsx` exits 0.
  </acceptance_criteria>
  <done>The user can watch the explicit Phase 36 generation pipeline instead of falling into a generic reading/loading state.</done>
</task>

<task type="auto">
  <name>Task 2: Make recovery and history reopen pipeline-aware</name>
  <files>frontend/src/features/reading/ReadingPage.jsx, frontend/src/features/reading/HistoryPanel.jsx, frontend/src/features/reading/ReadingPage.pipeline.test.jsx</files>
  <read_first>frontend/src/features/reading/HistoryPanel.jsx, frontend/src/features/reading/ReadingPage.jsx, frontend/src/hooks/useReadingRewrite.js, frontend/src/features/reading/readingRewriteDB.js, .planning/phases/36-pipeline-orchestrator-reading-pack/36-CONTEXT.md, .planning/phases/36-pipeline-orchestrator-reading-pack/36-RESEARCH.md</read_first>
  <action>Update `frontend/src/features/reading/HistoryPanel.jsx` so each record distinguishes at least `生成中断`, `已生成阅读包`, and pre-pack diagnostic states using the upgraded pack/pipeline metadata from Plan 01. Refine `ReadingPage.jsx` reopen logic so selecting history opens a completed pack directly, opens interrupted work into the pipeline stage with its saved last-completed stage, and never replays the full generation ceremony for finished packs. Extend `frontend/src/features/reading/ReadingPage.pipeline.test.jsx` to verify interrupted-state reopen, completed-pack reopen, stage-specific failure copy, and the preserved original-view fallback.</action>
  <verify>cd frontend; npm run test -- --run src/features/reading/ReadingPage.pipeline.test.jsx</verify>
  <acceptance_criteria>
    - `frontend/src/features/reading/HistoryPanel.jsx` contains stage-aware labels or badges for interrupted and completed pack states.
    - `frontend/src/features/reading/ReadingPage.jsx` routes completed records directly into the finished pack mode instead of the pipeline mode.
    - `frontend/src/features/reading/ReadingPage.pipeline.test.jsx` asserts reopen behavior for both interrupted and completed records.
    - `cd frontend; npm run test -- --run src/features/reading/ReadingPage.pipeline.test.jsx` exits 0.
  </acceptance_criteria>
  <done>Recovery and history now satisfy PIPE-02 and PIPE-03 at the page-flow level.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run test -- --run src/features/reading/ReadingPage.pipeline.test.jsx`
- [ ] The page flow contains `input`, `diagnostic`, `pipeline`, and finished pack routing
- [ ] Interrupted runs display restored progress and explicit continue/original fallback actions
- [ ] Completed history records reopen directly into the pack surface
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The Phase 36 pipeline experience is explicit, named, recoverable, and history-aware
- PIPE-01, PIPE-02, and PIPE-03 are satisfied in the visible UI flow
</success_criteria>

<output>
After completion, create `.planning/phases/36-pipeline-orchestrator-reading-pack/36-02-SUMMARY.md`
</output>
