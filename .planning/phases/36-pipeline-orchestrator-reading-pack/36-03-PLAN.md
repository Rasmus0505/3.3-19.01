---
phase: 36-pipeline-orchestrator-reading-pack
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - frontend/src/features/reading/ReadingPage.jsx
  - frontend/src/features/reading/ReadingPackPanel.jsx
  - frontend/src/features/reading/ArticlePanel.jsx
  - frontend/src/features/reading/AnalysisPanel.jsx
  - frontend/src/features/reading/reading.css
  - frontend/src/features/reading/ReadingPackPanel.test.jsx
  - app/static
autonomous: true
requirements: [PACK-01, PACK-02]
user_setup: []
must_haves:
  truths:
    - A successful Phase 36 run lands on a dedicated reading-pack asset surface with pack header metadata and diagnostic summary.
    - Pack mode switching offers exactly `原文`, `i+1`, and `逐句对照`.
    - `original` and `i+1` continue to use the existing reading renderer/highlight contract, while `逐句对照` uses persisted sentence cards.
    - The web-served static bundle is updated after the frontend work lands.
  artifacts:
    - frontend/src/features/reading/ReadingPackPanel.jsx
    - frontend/src/features/reading/ReadingPage.jsx
    - frontend/src/features/reading/ArticlePanel.jsx
    - frontend/src/features/reading/AnalysisPanel.jsx
    - frontend/src/features/reading/reading.css
    - frontend/src/features/reading/ReadingPackPanel.test.jsx
    - app/static
  key_links:
    - `ReadingPackPanel.jsx` must read the persisted `readingPack` and shared `Tabs` primitives.
    - `ReadingPage.jsx` must route successful completion into the pack page and preserve last-selected pack mode on reopen.
    - `ArticlePanel.jsx` must remain the renderer for `original` and `i+1` modes so highlight/tooltip behavior stays intact.
---

<objective>
Deliver the finished reading-pack asset page, comparison mode, and web-static sync.

Purpose: turn successful pipeline completion into a reusable pack surface that exposes original, i+1, and sentence-card comparison modes without regressing the existing reading renderer.
Output: a dedicated pack panel, pack-mode persistence, targeted pack-view tests, and updated `app/static` assets.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36-pipeline-orchestrator-reading-pack/36-CONTEXT.md
@.planning/phases/36-pipeline-orchestrator-reading-pack/36-RESEARCH.md
@.planning/phases/36-pipeline-orchestrator-reading-pack/36-UI-SPEC.md
@.planning/phases/36-pipeline-orchestrator-reading-pack/36-VALIDATION.md
@.planning/phases/36-pipeline-orchestrator-reading-pack/36-01-PLAN.md
@.planning/phases/36-pipeline-orchestrator-reading-pack/36-02-PLAN.md
@frontend/src/features/reading/ReadingPage.jsx
@frontend/src/features/reading/ArticlePanel.jsx
@frontend/src/features/reading/AnalysisPanel.jsx
@frontend/src/features/reading/reading.css
@frontend/src/components/ui/tabs.jsx
@AGENT.md
</context>

<threat_model>
## Threat Model

- **T-36-07 Information disclosure:** pack and comparison views must render plain text, not HTML injection or raw server messages.
- **T-36-08 Integrity regression:** `original` and `i+1` views must keep the existing tooltip/highlight contract; comparison mode must not redefine mappings with a second algorithm.
- **T-36-09 Delivery drift:** web-facing completion is incomplete until `app/static` is rebuilt from the new frontend bundle.
- **Block threshold:** any implementation that bypasses the existing `ArticlePanel` rendering contract for `original`/`i+1`, invents a second comparison inference path, or skips `app/static` sync is a high-severity blocker.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add the dedicated reading-pack asset page with three pack modes</name>
  <files>frontend/src/features/reading/ReadingPage.jsx, frontend/src/features/reading/ReadingPackPanel.jsx, frontend/src/features/reading/ArticlePanel.jsx, frontend/src/features/reading/AnalysisPanel.jsx, frontend/src/features/reading/reading.css, frontend/src/features/reading/ReadingPackPanel.test.jsx</files>
  <read_first>frontend/src/features/reading/ReadingPage.jsx, frontend/src/features/reading/ArticlePanel.jsx, frontend/src/features/reading/AnalysisPanel.jsx, frontend/src/components/ui/tabs.jsx, frontend/src/features/reading/readingPack.js, frontend/src/hooks/useReadingRewrite.js, .planning/phases/36-pipeline-orchestrator-reading-pack/36-UI-SPEC.md, .planning/phases/36-pipeline-orchestrator-reading-pack/36-CONTEXT.md</read_first>
  <action>Create `frontend/src/features/reading/ReadingPackPanel.jsx` as the dedicated result surface that renders the pack header card, status badge, target-level badge, last-generated timestamp, and exactly three diagnostic-summary metrics: material difficulty, preserved i+1 count, above-i+1 count. Use shared `Tabs` so the mode switch exposes exactly `原文`, `i+1`, and `逐句对照`. Keep `ArticlePanel.jsx` as the renderer for `原文` and `i+1` using the persisted pack/original/mapping data; do not replace it with a new renderer. Render `逐句对照` as a vertical card list from the persisted `comparisonCards`. Update `ReadingPage.jsx` to mount `ReadingPackPanel.jsx` as the success/result surface, preserve the last selected pack mode on reopen, and keep the existing right-column analysis shell only where it still matches the approved pack layout. Update `AnalysisPanel.jsx` and `reading.css` only as needed to support the approved pack header + result-page layout without introducing Phase 37 explanation-panel scope. Add `frontend/src/features/reading/ReadingPackPanel.test.jsx` to verify pack-header fields, three-tab switching, comparison-card rendering, and preserved original/i+1 renderer behavior.</action>
  <verify>cd frontend; npm run test -- --run src/features/reading/ReadingPackPanel.test.jsx src/features/reading/ReadingPage.pipeline.test.jsx</verify>
  <acceptance_criteria>
    - `frontend/src/features/reading/ReadingPackPanel.jsx` contains `原文`, `i+1`, and `逐句对照`.
    - `frontend/src/features/reading/ReadingPackPanel.jsx` renders the three required diagnostic summary metrics and a pack status badge.
    - `frontend/src/features/reading/ArticlePanel.jsx` remains referenced for `original` and `i+1` rendering paths.
    - `frontend/src/features/reading/ReadingPackPanel.test.jsx` exists and exits 0 together with the targeted reading-page pack test command.
  </acceptance_criteria>
  <done>The finished Phase 36 output is a reusable pack asset rather than a generic rewrite result.</done>
</task>

<task type="auto">
  <name>Task 2: Sync the static web bundle after the pack UI lands</name>
  <files>app/static</files>
  <read_first>AGENT.md, frontend/package.json, frontend/src/features/reading/ReadingPage.jsx, frontend/src/features/reading/ReadingPackPanel.jsx</read_first>
  <action>After the frontend tests and local build pass, rebuild and sync the web-served bundle with `cd frontend && npm run build:app-static`. Do not treat the phase as complete until `app/static` reflects the new pipeline and reading-pack surfaces, because the project delivery contract says web-facing frontend changes are incomplete without static sync.</action>
  <verify>cd frontend; npm run build:app-static</verify>
  <acceptance_criteria>
    - `cd frontend; npm run build:app-static` exits 0.
    - `app/static/assets` contains freshly updated build artifacts after the sync command completes.
  </acceptance_criteria>
  <done>The FastAPI-served web app matches the new reading-pack implementation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run test -- --run src/features/reading/ReadingPackPanel.test.jsx src/features/reading/ReadingPage.pipeline.test.jsx`
- [ ] `cd frontend && npm run build`
- [ ] `cd frontend && npm run build:app-static`
- [ ] Successful generation lands on a dedicated pack page with `原文`, `i+1`, and `逐句对照`
- [ ] Comparison mode renders persisted sentence cards while word-level hover remains intact in `original` / `i+1`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- PACK-01 and PACK-02 are satisfied without pulling Phase 37 features into scope
- The web-served bundle is synced in `app/static`
</success_criteria>

<output>
After completion, create `.planning/phases/36-pipeline-orchestrator-reading-pack/36-03-SUMMARY.md`
</output>
