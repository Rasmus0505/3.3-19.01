# Phase 24: CEFR 基础设施与 i 水平设置

---
phase: 24-cefr-infra
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/authStorage.js
  - frontend/src/store/slices/authSlice.ts
autonomous: true
requirements:
  - CEFR-15

must_haves:
  truths:
    - CEFR level persists locally in localStorage under BOTTLE_CEFR_LEVEL key
    - Zustand authSlice has cefrLevel state and setCefrLevel action
    - authSlice initializes cefrLevel from localStorage on app load
  artifacts:
    - path: frontend/src/app/authStorage.js
      contains: USER_CEFR_LEVEL_KEY, writeCefrLevel, readCefrLevel
    - path: frontend/src/store/slices/authSlice.ts
      contains: cefrLevel state, setCefrLevel action
  key_links:
    - from: frontend/src/store/slices/authSlice.ts
      to: frontend/src/app/authStorage.js
      via: readCefrLevel() called in buildAuthInitialState
    - from: frontend/src/app/authStorage.js
      to: localStorage
      via: window.localStorage.setItem/getItem
---

<objective>
Add localStorage persistence for CEFR level: authStorage.js helpers + authSlice state management.
</objective>

<context>
@frontend/src/app/authStorage.js — existing writeStoredUser/readStoredUser pattern to follow
@frontend/src/store/slices/authSlice.ts — existing auth slice with setCurrentUser pattern
@.planning/phases/24-cefr-infra/24-CONTEXT.md — D-05: use USER_CEFR_LEVEL_KEY constant, follow writeStoredUser pattern
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CEFR level helpers to authStorage.js</name>
  <files>frontend/src/app/authStorage.js</files>
  <action>
    1. Add constant: `export const USER_CEFR_LEVEL_KEY = "BOTTLE_CEFR_LEVEL";` (at top with other KEY constants)
    2. Add `writeCefrLevel(cefrLevel: string | null): void` function:
       - Get localStorage via getStorage()
       - If cefrLevel is provided and valid (one of A1/A2/B1/B2/C1/C2), setItem(USER_CEFR_LEVEL_KEY, cefrLevel)
       - Otherwise removeItem(USER_CEFR_LEVEL_KEY)
    3. Add `readCefrLevel(): string | null` function:
       - Get localStorage via getStorage()
       - GetItem(USER_CEFR_LEVEL_KEY), validate it matches A1/A2/B1/B2/C1/C2, return value or null
    Follow existing writeStoredUser / readStoredUser pattern exactly.
  </action>
  <verify>
    <automated>grep -n "USER_CEFR_LEVEL_KEY\|writeCefrLevel\|readCefrLevel" frontend/src/app/authStorage.js</automated>
  </verify>
  <done>authStorage.js exports USER_CEFR_LEVEL_KEY, writeCefrLevel, readCefrLevel</done>
</task>

<task type="auto">
  <name>Task 2: Add cefrLevel state and setCefrLevel action to authSlice</name>
  <files>frontend/src/store/slices/authSlice.ts</files>
  <action>
    1. Add to imports: `import { USER_CEFR_LEVEL_KEY, writeCefrLevel, readCefrLevel } from "../../app/authStorage";`
    2. In buildAuthInitialState(): add `cefrLevel: readCefrLevel() || "B1"` — read from localStorage or default to B1
    3. Add state field: `cefrLevel: string` (initialized via buildAuthInitialState)
    4. Add `setCefrLevel: (cefrLevel: string) => void` action:
       - Validate cefrLevel is one of A1/A2/B1/B2/C1/C2
       - set({ cefrLevel }) — update Zustand state
       - writeCefrLevel(cefrLevel) — sync to localStorage
    Place setCefrLevel action in the slice object returned by createAuthSlice.
  </action>
  <verify>
    <automated>grep -n "cefrLevel\|setCefrLevel" frontend/src/store/slices/authSlice.ts</automated>
  </verify>
  <done>authSlice has cefrLevel state (string) and setCefrLevel action</done>
</task>

</tasks>

<verification>
Frontend changes verified by:
- `grep -n "USER_CEFR_LEVEL_KEY" frontend/src/app/authStorage.js` — constant defined
- `grep -n "writeCefrLevel\|readCefrLevel" frontend/src/app/authStorage.js` — functions defined
- `grep -n "cefrLevel" frontend/src/store/slices/authSlice.ts` — state exists
- `grep -n "setCefrLevel" frontend/src/store/slices/authSlice.ts` — action exists
</verification>

<success_criteria>
- Calling writeCefrLevel("B2") persists "B2" under key "BOTTLE_CEFR_LEVEL" in localStorage
- Calling readCefrLevel() after writeCefrLevel returns the persisted value
- authSlice initializes with cefrLevel from localStorage (or "B1" if none)
- setCefrLevel("C1") updates both Zustand state and localStorage
</success_criteria>

<output>
After completion, create `.planning/phases/24-cefr-infra/24-02-SUMMARY.md`
</output>
