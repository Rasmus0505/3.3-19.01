# Phase 24: CEFR 基础设施与 i 水平设置

---
phase: 24-cefr-infra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/user.py
  - app/schemas/auth.py
  - app/api/serializers.py
  - app/repositories/user.py
  - app/api/routers/auth/router.py
autonomous: true
requirements:
  - CEFR-14
  - CEFR-15
user_setup: []

must_haves:
  truths:
    - User's CEFR level is persisted to database and survives login/logout
    - PATCH /api/auth/profile accepts and stores cefr_level field
    - UserResponse includes cefr_level field
  artifacts:
    - path: app/models/user.py
      contains: cefr_level column definition
    - path: app/schemas/auth.py
      contains: ProfileUpdateRequest with cefr_level, UserResponse with cefr_level
    - path: app/api/serializers.py
      contains: to_user_response maps cefr_level
    - path: app/repositories/user.py
      contains: update_cefr_level method
    - path: app/api/routers/auth/router.py
      contains: PATCH /profile handler updates cefr_level
  key_links:
    - from: app/api/routers/auth/router.py
      to: app/repositories/user.py
      via: update_cefr_level method call
    - from: app/api/serializers.py
      to: app/models/user.py
      via: to_user_response reads user.cefr_level
---

<objective>
Add cefr_level field to user profile: database column, PATCH API, and response serialization.
</objective>

<context>
@app/models/user.py — User model, add cefr_level column
@app/schemas/auth.py — ProfileUpdateRequest + UserResponse schemas
@app/api/serializers.py — to_user_response function
@app/repositories/user.py — UserRepository with update_username pattern
@app/api/routers/auth/router.py — PATCH /profile endpoint
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cefr_level column to User model</name>
  <files>app/models/user.py</files>
  <action>
    Add `cefr_level: Mapped[str | None] = mapped_column(String(2), nullable=True, default="B1")` to the User model class.
    Place it after `is_admin` column (line ~21).
    Default value "B1" per D-07 decision (B1 is the default CEFR level).
  </action>
  <verify>
    <automated>grep -n "cefr_level" app/models/user.py</automated>
  </verify>
  <done>User model has cefr_level column with default "B1"</done>
</task>

<task type="auto">
  <name>Task 2: Add cefr_level to Pydantic schemas</name>
  <files>app/schemas/auth.py</files>
  <action>
    In ProfileUpdateRequest (line ~17): add `cefr_level: str | None = Field(default=None, pattern="^(A1|A2|B1|B2|C1|C2)$")` — optional field, validated enum.
    In UserResponse (line ~33): add `cefr_level: str | None = None` — include cefr_level in API response.
  </action>
  <verify>
    <automated>grep -n "cefr_level" app/schemas/auth.py</automated>
  </verify>
  <done>Both schemas include cefr_level with correct types</done>
</task>

<task type="auto">
  <name>Task 3: Add update_cefr_level to UserRepository</name>
  <files>app/repositories/user.py</files>
  <action>
    Add `update_cefr_level(self, user_id: int, cefr_level: str | None) -> Optional[User]` method to UserRepository class.
    Method pattern: fetch user by id, update cefr_level field, flush, return user.
    Add at end of class (after update_username method, line ~95).
  </action>
  <verify>
    <automated>grep -n "update_cefr_level" app/repositories/user.py</automated>
  </verify>
  <done>UserRepository has update_cefr_level method</done>
</task>

<task type="auto">
  <name>Task 4: Update to_user_response serializer</name>
  <files>app/api/serializers.py</files>
  <action>
    Update to_user_response function (line ~46): add `cefr_level=str(getattr(user, "cefr_level", None) or None)` to UserResponse construction.
    The cefr_level should be included in the returned UserResponse so client receives the updated value after PATCH.
  </action>
  <verify>
    <automated>grep -n "cefr_level" app/api/serializers.py</automated>
  </verify>
  <done>to_user_response includes cefr_level in returned UserResponse</done>
</task>

<task type="auto">
  <name>Task 5: Update PATCH /profile endpoint to handle cefr_level</name>
  <files>app/api/routers/auth/router.py</files>
  <action>
    In update_profile function (lines ~79-98):
    1. After username validation and update (line ~93), check if payload.cefr_level is provided (not None).
    2. If provided: call `user_repo.update_cefr_level(current_user.id, payload.cefr_level)`.
    3. Re-fetch updated user from db after any update: `updated_user = db.get(User, current_user.id)`.
    4. Keep db.commit() before db.refresh().
    Per D-04: keep cefr_level in same PATCH endpoint as username.
  </action>
  <verify>
    <automated>grep -n "cefr_level" app/api/routers/auth/router.py</automated>
  </verify>
  <done>PATCH /api/auth/profile updates cefr_level when provided</done>
</task>

</tasks>

<verification>
Backend changes verified by:
- `grep -n "cefr_level" app/models/user.py` — column defined
- `grep -n "cefr_level" app/schemas/auth.py` — schemas updated
- `grep -n "cefr_level" app/api/routers/auth/router.py` — endpoint updated
- Database migration needed: `alembic revision --autogenerate -m "add cefr_level to users"` (handled by postgresql-update skill)
</verification>

<success_criteria>
- PATCH /api/auth/profile with {username: "test", cefr_level: "B2"} returns 200 with user.cefr_level = "B2"
- GET /api/auth/me returns user with cefr_level field
- Database users table has cefr_level column (nullable, default "B1")
</success_criteria>

<output>
After completion, create `.planning/phases/24-cefr-infra/24-01-SUMMARY.md`
</output>
