---
phase: 01-shared-cloud-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/routers/dashscope_upload.py
  - app/api/routers/lessons/router.py
  - app/services/lesson_command_service.py
  - app/services/lesson_service.py
  - tests/unit/test_dashscope_upload_router.py
  - tests/integration/test_regression_api.py
autonomous: true
requirements: [WEB-01, WEB-03, DESK-02]
user_setup: []
must_haves:
  truths:
    - Bottle 2.0 direct upload returns a canonical cloud object identifier for audio and video uploads.
    - `/api/lessons/tasks` accepts the Bottle 2.0 `dashscope_file_id` path and starts a lesson-generation task without local server-side media conversion.
    - Lesson generation from `dashscope_file_id` obtains a signed cloud URL and skips `extract_audio_for_asr` on the Bottle server.
  artifacts:
    - app/api/routers/dashscope_upload.py
    - app/api/routers/lessons/router.py
    - app/services/lesson_command_service.py
    - app/services/lesson_service.py
    - tests/unit/test_dashscope_upload_router.py
  key_links:
    - `dashscope_upload.py` response contract matches the fields consumed by `UploadPanel.jsx` direct upload flow.
    - `create_lesson_task_from_dashscope_file()` artifacts are consumed by `LessonService.generate_from_dashscope_file_id()`.
---

<objective>
Stabilize the canonical Bottle 2.0 backend contract around direct cloud upload and `dashscope_file_id` task creation.

Purpose: Make the direct cloud-file path trustworthy enough to be the shared web + desktop Bottle 2.0 foundation.
Output: Hardened upload-policy response, task-creation flow, and direct-cloud lesson-generation path with regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-cloud-generation/01-CONTEXT.md
@.planning/phases/01-shared-cloud-generation/01-RESEARCH.md
@app/api/routers/dashscope_upload.py
@app/api/routers/lessons/router.py
@app/services/lesson_command_service.py
@app/services/lesson_service.py
@tests/unit/test_dashscope_upload_router.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize the Bottle 2.0 upload-policy contract</name>
  <files>app/api/routers/dashscope_upload.py, tests/unit/test_dashscope_upload_router.py</files>
  <read_first>app/api/routers/dashscope_upload.py, tests/unit/test_dashscope_upload_router.py, frontend/src/features/upload/UploadPanel.jsx</read_first>
  <action>Update `app/api/routers/dashscope_upload.py` so the Bottle 2.0 upload-policy endpoint always returns a canonical contract for the direct-upload flow: `upload_host`, `upload_dir`, `upload_url`, `file_id`, and `oss_fields`. Ensure `file_id` resolves to the exact object key that the frontend will submit later, including the original filename suffix for audio/video uploads. When upstream policy data is flat rather than nested, normalize it into `oss_fields` and include `key`, `x-oss-content-type`, and `success_action_status=200` so the frontend multipart POST can succeed without guessing missing values. Keep video MIME types such as `video/mp4` intact rather than coercing them to audio defaults. Extend `tests/unit/test_dashscope_upload_router.py` to cover both nested and flat policy payloads and to assert a canonical `file_id`/`key` result for video uploads.</action>
  <verify>pytest tests/unit/test_dashscope_upload_router.py -q</verify>
  <acceptance_criteria>
    - `app/api/routers/dashscope_upload.py` contains `upload_host`, `upload_dir`, `upload_url`, `file_id`, and `oss_fields` in the success response.
    - `app/api/routers/dashscope_upload.py` contains `success_action_status` and preserves the incoming content type in normalized OSS fields.
    - `tests/unit/test_dashscope_upload_router.py` exits 0.
  </acceptance_criteria>
  <done>Upload-policy responses are canonical for Bottle 2.0 direct upload and covered by unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Harden `dashscope_file_id` task creation and signed-URL generation</name>
  <files>app/api/routers/lessons/router.py, app/services/lesson_command_service.py, app/services/lesson_service.py, tests/integration/test_regression_api.py</files>
  <read_first>app/api/routers/lessons/router.py, app/services/lesson_command_service.py, app/services/lesson_service.py, tests/integration/test_regression_api.py, .planning/phases/01-shared-cloud-generation/01-CONTEXT.md</read_first>
  <action>Keep `/api/lessons/tasks` as the canonical Bottle 2.0 task-creation endpoint. Ensure the router rejects empty `dashscope_file_id` values, passes the exact field through unchanged, and returns task metadata that remains compatible with the shared upload flow. In `app/services/lesson_command_service.py`, preserve the direct-upload artifact by writing `dashscope_file_id.txt` and patching task artifacts with the exact cloud object key. In `app/services/lesson_service.py`, keep `generate_from_dashscope_file_id()` on the cloud-file path: obtain a signed URL from the stored object key, skip any local `extract_audio_for_asr` stage, and continue ASR/lesson generation from the signed URL. Add or update focused regression coverage in `tests/integration/test_regression_api.py` so the direct-upload task path proves that `dashscope_file_id` is consumed, persisted, and routed into lesson generation without local server media conversion.</action>
  <verify>pytest tests/integration/test_regression_api.py -k "dashscope_file_id or request_url or qwen3" -q</verify>
  <acceptance_criteria>
    - `app/api/routers/lessons/router.py` contains `dashscope_file_id: str = Form(...)` and rejects empty values with `DASHSCOPE_FILE_ID_REQUIRED`.
    - `app/services/lesson_command_service.py` contains `dashscope_file_id.txt` and patches task artifacts with `dashscope_file_id`.
    - `app/services/lesson_service.py` contains `get_file_signed_url(dashscope_file_id)` and the comment or progress path that skips local conversion for cloud-hosted files.
  </acceptance_criteria>
  <done>The direct-upload backend path is stable end-to-end and regression-covered.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_dashscope_upload_router.py -q`
- [ ] `pytest tests/integration/test_regression_api.py -k "dashscope_file_id or request_url or qwen3" -q`
- [ ] Direct-upload backend contract matches the fields consumed by the shared upload flow
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Bottle 2.0 direct upload and task creation are stable enough to act as the canonical cloud backend path
</success_criteria>

<output>
After completion, create `.planning/phases/01-shared-cloud-generation/01-01-SUMMARY.md`
</output>
