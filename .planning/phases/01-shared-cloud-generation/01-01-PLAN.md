---
phase: 01-shared-cloud-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/routers/dashscope_upload.py
  - app/api/routers/lessons/router.py
  - app/services/lesson_command_service.py
  - app/services/lesson_service.py
  - tests/unit/test_dashscope_upload_router.py
  - tests/integration/test_regression_api.py
autonomous: true
requirements: [WEB-01, WEB-03, DESK-02]
user_setup: []
must_haves:
  truths:
    - Bottle 2.0 直传上传策略会返回音频和视频上传都可用的规范云对象标识。
    - `/api/lessons/tasks` 可以接收 Bottle 2.0 的 `dashscope_file_id`，并在不做本地服务器媒体转换的前提下启动生成任务。
    - `dashscope_file_id` 生成路径会取得签名云 URL，并跳过 Bottle 服务器上的 `extract_audio_for_asr`。
  artifacts:
    - app/api/routers/dashscope_upload.py
    - app/api/routers/lessons/router.py
    - app/services/lesson_command_service.py
    - app/services/lesson_service.py
    - tests/unit/test_dashscope_upload_router.py
  key_links:
    - `dashscope_upload.py` 的响应契约与 `UploadPanel.jsx` 的直传消费字段一致。
    - `create_lesson_task_from_dashscope_file()` 产生的任务产物被 `LessonService.generate_from_dashscope_file_id()` 消费。
---

<objective>
稳定 Bottle 2.0 直传后端契约，以及基于 `dashscope_file_id` 的任务创建路径。

Purpose: 让直连云端文件路径足够稳定，成为 Web + Desktop 共享的 Bottle 2.0 云端基础。
Output: 加固后的上传策略响应、任务创建链路和基于云端文件的生成回归覆盖。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-cloud-generation/01-CONTEXT.md
@.planning/phases/01-shared-cloud-generation/01-RESEARCH.md
@app/api/routers/dashscope_upload.py
@app/api/routers/lessons/router.py
@app/services/lesson_command_service.py
@app/services/lesson_service.py
@tests/unit/test_dashscope_upload_router.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 规范化 Bottle 2.0 上传策略契约</name>
  <files>app/api/routers/dashscope_upload.py, tests/unit/test_dashscope_upload_router.py</files>
  <read_first>app/api/routers/dashscope_upload.py, tests/unit/test_dashscope_upload_router.py, frontend/src/features/upload/UploadPanel.jsx</read_first>
  <action>更新 `app/api/routers/dashscope_upload.py`，让 Bottle 2.0 上传策略接口始终返回一套规范直传契约：`upload_host`、`upload_dir`、`upload_url`、`file_id` 和 `oss_fields`。确保 `file_id` 最终落到前端后就是后续提交任务时要使用的真实云对象 key，并且要包含原始文件名后缀，以支持音频和视频上传。当上游策略字段是扁平结构而不是嵌套 `oss_fields` 时，将其规范化进 `oss_fields`，并补齐 `key`、`x-oss-content-type`、`success_action_status=200`，让前端 multipart POST 无需猜字段。保留视频 MIME，例如 `video/mp4`，不要粗暴改写成音频默认值。扩展 `tests/unit/test_dashscope_upload_router.py`，覆盖嵌套和扁平两种策略响应，并断言视频上传时 `file_id` / `key` 的最终结果是规范的。</action>
  <verify>pytest tests/unit/test_dashscope_upload_router.py -q</verify>
  <acceptance_criteria>
    - `app/api/routers/dashscope_upload.py` 成功响应中包含 `upload_host`、`upload_dir`、`upload_url`、`file_id` 和 `oss_fields`。
    - `app/api/routers/dashscope_upload.py` 规范化后的 OSS 字段中包含 `success_action_status`，并保留传入内容类型。
    - `tests/unit/test_dashscope_upload_router.py` exits 0.
  </acceptance_criteria>
  <done>上传策略响应对 Bottle 2.0 直传来说是规范、稳定且有测试覆盖的。</done>
</task>

<task type="auto">
  <name>Task 2: 加固 `dashscope_file_id` 任务创建与签名 URL 生成链路</name>
  <files>app/api/routers/lessons/router.py, app/services/lesson_command_service.py, app/services/lesson_service.py, tests/integration/test_regression_api.py</files>
  <read_first>app/api/routers/lessons/router.py, app/services/lesson_command_service.py, app/services/lesson_service.py, tests/integration/test_regression_api.py, .planning/phases/01-shared-cloud-generation/01-CONTEXT.md</read_first>
  <action>保持 `/api/lessons/tasks` 作为 Bottle 2.0 的唯一规范任务创建入口。确保 router 对空的 `dashscope_file_id` 进行拒绝、原样透传该字段，并返回与共享上传流兼容的任务元数据。在 `app/services/lesson_command_service.py` 中，继续通过 `dashscope_file_id.txt` 持久化直传产物，并在任务 artifacts 中补丁写入准确的云对象 key。在 `app/services/lesson_service.py` 中，坚持 `generate_from_dashscope_file_id()` 走云端文件路径：从保存的对象 key 取得签名 URL，跳过任何本地 `extract_audio_for_asr` 阶段，后续直接走 ASR / 课程生成。为 `tests/integration/test_regression_api.py` 增加或更新定向回归，证明 `dashscope_file_id` 被接收、持久化并进入 lesson generation，而不是回退到本地服务器媒体转换。</action>
  <verify>pytest tests/integration/test_regression_api.py -k "dashscope_file_id or request_url or qwen3" -q</verify>
  <acceptance_criteria>
    - `app/api/routers/lessons/router.py` 中存在 `dashscope_file_id: str = Form(...)`，并对空值返回 `DASHSCOPE_FILE_ID_REQUIRED`。
    - `app/services/lesson_command_service.py` 中存在 `dashscope_file_id.txt` 写入和 `dashscope_file_id` artifacts patch。
    - `app/services/lesson_service.py` 中存在 `get_file_signed_url(dashscope_file_id)`，并明确跳过本地转换路径。
  </acceptance_criteria>
  <done>直传后端链路端到端稳定，并具备回归覆盖。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/test_dashscope_upload_router.py -q`
- [ ] `pytest tests/integration/test_regression_api.py -k "dashscope_file_id or request_url or qwen3" -q`
- [ ] 直传后端契约与共享上传前端使用字段一致
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Bottle 2.0 直传与任务创建链路足够稳定，可作为共享云端主路径
</success_criteria>

<output>
After completion, create `.planning/phases/01-shared-cloud-generation/01-01-SUMMARY.md`
</output>