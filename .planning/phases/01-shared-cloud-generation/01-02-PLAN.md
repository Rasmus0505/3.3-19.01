---
phase: 01-shared-cloud-generation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - frontend/src/features/upload/UploadPanel.jsx
  - frontend/src/features/upload/asrStrategy.js
  - frontend/src/shared/lib/asrModels.js
  - frontend/src/shared/api/client.js
  - tests/contracts/test_desktop_runtime_contract.py
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03, DESK-02]
user_setup: []
must_haves:
  truths:
    - Web and desktop Bottle 2.0 use the same canonical stage model in the shared upload flow.
    - The primary Bottle 2.0 UX uses direct upload plus `/api/lessons/tasks`, not the legacy server-passthrough route.
    - Desktop-only scenarios show a clear popup with a bottom-right CTA instead of silently falling back to server-side processing.
  artifacts:
    - frontend/src/features/upload/UploadPanel.jsx
    - frontend/src/features/upload/asrStrategy.js
    - frontend/src/shared/api/client.js
    - tests/contracts/test_desktop_runtime_contract.py
  key_links:
    - `UploadPanel.jsx` stage/status text aligns with the `01-CONTEXT.md` stage model.
    - Desktop and web still share the same request path through `shared/api/client.js` and `requestCloudApi`.
---

<objective>
Unify the shared Bottle 2.0 user experience across web and desktop.

Purpose: Make Bottle 2.0 feel like one product flow while preserving the runtime boundary decisions captured in context.
Output: Canonical Bottle 2.0 UI state flow, direct-upload-first behavior, and desktop-only guidance with a visible CTA.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-cloud-generation/01-CONTEXT.md
@.planning/phases/01-shared-cloud-generation/01-RESEARCH.md
@.planning/phases/01-shared-cloud-generation/01-01-SUMMARY.md
@frontend/src/features/upload/UploadPanel.jsx
@frontend/src/features/upload/asrStrategy.js
@frontend/src/shared/api/client.js
@tests/contracts/test_desktop_runtime_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize the Bottle 2.0 stage model in the shared upload flow</name>
  <files>frontend/src/features/upload/UploadPanel.jsx, frontend/src/features/upload/asrStrategy.js, tests/contracts/test_desktop_runtime_contract.py</files>
  <read_first>frontend/src/features/upload/UploadPanel.jsx, frontend/src/features/upload/asrStrategy.js, frontend/src/shared/api/client.js, .planning/phases/01-shared-cloud-generation/01-CONTEXT.md, tests/contracts/test_desktop_runtime_contract.py</read_first>
  <action>Update `frontend/src/features/upload/UploadPanel.jsx` so Bottle 2.0 uses the direct-upload path as the canonical shared flow: request upload policy from `/api/dashscope-upload/request-url`, upload to cloud storage, then create a task through `/api/lessons/tasks` with `dashscope_file_id`. Standardize the user-facing Bottle 2.0 stage model around the context-approved sequence: `upload`, `submit cloud task`, `transcribing`, `generating lesson`, `completed`, `failed`. Ensure the same vocabulary is used when the shared renderer runs inside desktop and when it runs in the browser. Update `frontend/src/features/upload/asrStrategy.js` or adjacent helpers so cloud failure messages still map cleanly into that stage model without reintroducing the legacy server-passthrough route as the preferred path. Extend contract coverage in `tests/contracts/test_desktop_runtime_contract.py` only if necessary to lock the canonical endpoint usage and cloud-bridge assumptions.</action>
  <verify>pytest tests/contracts/test_desktop_runtime_contract.py -k "requestCloudApi or uploadWithProgress" -q</verify>
  <acceptance_criteria>
    - `frontend/src/features/upload/UploadPanel.jsx` contains `/api/dashscope-upload/request-url` and `/api/lessons/tasks` as the Bottle 2.0 primary flow.
    - `frontend/src/features/upload/UploadPanel.jsx` contains user-facing Bottle 2.0 stages aligned with `upload`, `submit cloud task`, `transcribing`, `generating lesson`, `completed`, or `failed`.
    - `tests/contracts/test_desktop_runtime_contract.py` exits 0 for the targeted contract checks.
  </acceptance_criteria>
  <done>The shared Bottle 2.0 flow exposes one canonical stage model across web and desktop.</done>
</task>

<task type="auto">
  <name>Task 2: Add desktop-only guidance with a bottom-right CTA and soft large-file guidance</name>
  <files>frontend/src/features/upload/UploadPanel.jsx, frontend/src/shared/lib/asrModels.js</files>
  <read_first>frontend/src/features/upload/UploadPanel.jsx, frontend/src/shared/lib/asrModels.js, .planning/phases/01-shared-cloud-generation/01-CONTEXT.md</read_first>
  <action>Implement explicit desktop-only guidance in `frontend/src/features/upload/UploadPanel.jsx` for scenarios that Phase 1 should not solve with server fallback. Show a popup/modal rather than a passive inline note. The popup must place the desktop CTA in the bottom-right action area. Support a temporary distribution fallback if no final installer URL exists yet by reading from a dedicated config/env source or constant and rendering either a direct download action or an alternate instruction string (for example a group number / distribution note). Also add soft guidance for large or reliability-risky files: do not hard-block below the provider's documented 2 GB / 12 hour ceiling, but surface copy that recommends desktop for very large or unreliable cases instead of server-side fallback. Preserve support for both audio and video file selection in the Bottle 2.0 path.</action>
  <verify>rg -n "desktop|download|群|2 GB|12 hour|12 小时|video/|audio/" frontend/src/features/upload/UploadPanel.jsx frontend/src/shared/lib/asrModels.js</verify>
  <acceptance_criteria>
    - `frontend/src/features/upload/UploadPanel.jsx` contains a desktop-only popup/modal path with a bottom-right CTA action.
    - `frontend/src/features/upload/UploadPanel.jsx` contains user-facing copy that recommends desktop instead of server fallback for unsupported or oversized/reliability-risky cases.
    - `frontend/src/features/upload/UploadPanel.jsx` still supports both audio and video file selection for Bottle 2.0.
  </acceptance_criteria>
  <done>Users get explicit desktop guidance and soft file-size messaging without server fallback creep.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/contracts/test_desktop_runtime_contract.py -k "requestCloudApi or uploadWithProgress" -q`
- [ ] `rg -n "request-url|dashscope_file_id|desktop|2 GB|12 hour|video/|audio/" frontend/src/features/upload/UploadPanel.jsx`
- [ ] Bottle 2.0 UI behavior is direct-upload-first and desktop guidance is explicit
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Bottle 2.0 feels like one shared cloud workflow and clearly directs users to desktop when browser limits are hit
</success_criteria>

<output>
After completion, create `.planning/phases/01-shared-cloud-generation/01-02-SUMMARY.md`
</output>
