---
phase: 01-shared-cloud-generation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - frontend/src/features/upload/UploadPanel.jsx
  - frontend/src/features/upload/asrStrategy.js
  - frontend/src/shared/lib/asrModels.js
  - frontend/src/shared/api/client.js
  - tests/contracts/test_desktop_runtime_contract.py
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03, DESK-02]
user_setup: []
must_haves:
  truths:
    - Web 和 Desktop 的 Bottle 2.0 在共享上传流中使用同一套规范状态模型。
    - 主 Bottle 2.0 UX 使用直传 + `/api/lessons/tasks`，而不是旧的服务器中转路径。
    - 桌面专属场景会弹出清晰提示，并在右下角提供 CTA，而不是静默回退到服务器处理。
  artifacts:
    - frontend/src/features/upload/UploadPanel.jsx
    - frontend/src/features/upload/asrStrategy.js
    - frontend/src/shared/api/client.js
    - tests/contracts/test_desktop_runtime_contract.py
  key_links:
    - `UploadPanel.jsx` 的阶段/状态文本与 `01-CONTEXT.md` 的状态模型一致。
    - Desktop 和 Web 仍通过 `shared/api/client.js` 与 `requestCloudApi` 共享同一路径。
---

<objective>
统一 Web 与 Desktop 下 Bottle 2.0 的共享用户体验。

Purpose: 在不打破运行时边界决策的前提下，让 Bottle 2.0 看起来像同一条云端产品流程。
Output: 规范化 Bottle 2.0 阶段状态、直传优先的前端路径，以及带 CTA 的桌面专属提示。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-cloud-generation/01-CONTEXT.md
@.planning/phases/01-shared-cloud-generation/01-RESEARCH.md
@.planning/phases/01-shared-cloud-generation/01-01-SUMMARY.md
@frontend/src/features/upload/UploadPanel.jsx
@frontend/src/features/upload/asrStrategy.js
@frontend/src/shared/api/client.js
@tests/contracts/test_desktop_runtime_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在共享上传流中规范化 Bottle 2.0 阶段模型</name>
  <files>frontend/src/features/upload/UploadPanel.jsx, frontend/src/features/upload/asrStrategy.js, tests/contracts/test_desktop_runtime_contract.py</files>
  <read_first>frontend/src/features/upload/UploadPanel.jsx, frontend/src/features/upload/asrStrategy.js, frontend/src/shared/api/client.js, .planning/phases/01-shared-cloud-generation/01-CONTEXT.md, tests/contracts/test_desktop_runtime_contract.py</read_first>
  <action>更新 `frontend/src/features/upload/UploadPanel.jsx`，让 Bottle 2.0 只把直传路径作为规范共享流程：先向 `/api/dashscope-upload/request-url` 获取上传策略，再直传到云端存储，然后通过 `/api/lessons/tasks` + `dashscope_file_id` 创建任务。把用户可见的 Bottle 2.0 阶段标准化为上下文中锁定的顺序：`upload`、`submit cloud task`、`transcribing`、`generating lesson`、`completed`、`failed`。确保这套词汇在同一共享 renderer 下运行于浏览器和桌面端时都成立。同步更新 `frontend/src/features/upload/asrStrategy.js` 或相邻 helper，让云端错误映射到这套阶段模型，而不是把旧的服务器中转路径继续作为优先路径。必要时补充 `tests/contracts/test_desktop_runtime_contract.py`，锁定关键端点和 cloud bridge 假设。</action>
  <verify>pytest tests/contracts/test_desktop_runtime_contract.py -k "requestCloudApi or uploadWithProgress" -q</verify>
  <acceptance_criteria>
    - `frontend/src/features/upload/UploadPanel.jsx` 中 Bottle 2.0 主路径使用 `/api/dashscope-upload/request-url` 和 `/api/lessons/tasks`。
    - `frontend/src/features/upload/UploadPanel.jsx` 中存在符合 `upload`、`submit cloud task`、`transcribing`、`generating lesson`、`completed`、`failed` 的用户可见阶段表达。
    - `tests/contracts/test_desktop_runtime_contract.py` 的目标检查 exits 0。
  </acceptance_criteria>
  <done>共享 Bottle 2.0 流程在 Web / Desktop 下暴露同一套规范阶段模型。</done>
</task>

<task type="auto">
  <name>Task 2: 加入桌面专属引导弹窗与软性大文件提示</name>
  <files>frontend/src/features/upload/UploadPanel.jsx, frontend/src/shared/lib/asrModels.js</files>
  <read_first>frontend/src/features/upload/UploadPanel.jsx, frontend/src/shared/lib/asrModels.js, .planning/phases/01-shared-cloud-generation/01-CONTEXT.md</read_first>
  <action>在 `frontend/src/features/upload/UploadPanel.jsx` 中实现桌面专属场景提示，覆盖当前阶段不能靠服务器 fallback 解决的问题。必须使用弹窗/modal，而不是仅做弱提示。弹窗动作区右下角必须放置桌面端 CTA。若正式安装包 URL 还未确定，支持从配置、常量或 env 中读取临时分发信息，并在 UI 中展示为直链下载动作或替代文本（例如群号 / 分发说明）。同时加入软性大文件/高风险文件提示：在提供方文档给出的 2 GB / 12 小时上限以下，不要直接硬阻断，但要在超大或高风险场景中优先提示用户使用桌面端，而不是回退到服务器转码。Bottle 2.0 下必须继续支持音频和视频文件选择。</action>
  <verify>rg -n "desktop|download|群|2 GB|12 hour|12 小时|video/|audio/" frontend/src/features/upload/UploadPanel.jsx frontend/src/shared/lib/asrModels.js</verify>
  <acceptance_criteria>
    - `frontend/src/features/upload/UploadPanel.jsx` 中存在桌面专属 modal/popup 分支和右下角 CTA。
    - `frontend/src/features/upload/UploadPanel.jsx` 中存在“推荐桌面端而不是服务器兜底”的用户文案。
    - `frontend/src/features/upload/UploadPanel.jsx` 仍支持 Bottle 2.0 的音频和视频选择。
  </acceptance_criteria>
  <done>用户在浏览器边界场景下能明确知道该转向桌面端，而不是让服务器默默兜底。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/contracts/test_desktop_runtime_contract.py -k "requestCloudApi or uploadWithProgress" -q`
- [ ] `rg -n "request-url|dashscope_file_id|desktop|2 GB|12 hour|video/|audio/" frontend/src/features/upload/UploadPanel.jsx`
- [ ] Bottle 2.0 前端行为以直传为主，并明确表达桌面端推荐路径
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Bottle 2.0 在用户视角上是一条统一云端工作流，并能在浏览器受限时清晰引导用户去桌面端
</success_criteria>

<output>
After completion, create `.planning/phases/01-shared-cloud-generation/01-02-SUMMARY.md`
</output>