---
phase: 01-shared-cloud-generation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - frontend/src/shared/api/client.js
  - frontend/src/app/authStorage.js
  - frontend/src/features/upload/UploadPanel.jsx
  - frontend/src/features/upload/asrStrategy.js
  - tests/e2e/test_e2e_key_flows.py
  - tests/integration/test_regression_api.py
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, BILL-01]
user_setup: []
must_haves:
  truths:
    - Bottle 2.0 入口在鉴权缺失或失效时会明确失败，不会静默发出坏请求。
    - Desktop 与 Web 会话处理仍然兼容共享云端任务流。
    - 余额不足和任务恢复状态在 Bottle 2.0 路径中被一致表达。
  artifacts:
    - frontend/src/shared/api/client.js
    - frontend/src/app/authStorage.js
    - frontend/src/features/upload/UploadPanel.jsx
    - frontend/src/features/upload/asrStrategy.js
    - tests/e2e/test_e2e_key_flows.py
  key_links:
    - `shared/api/client.js` 的 auth/error 处理和 `authStorage.js` 的 restore/clear 行为协同工作。
    - `UploadPanel.jsx` 在 Bottle 2.0 中使用共享 auth/balance 失败文案，而不是临时分叉。
---

<objective>
加固共享 Bottle 2.0 生成流程周围的鉴权、余额和任务恢复护栏。

Purpose: 避免共享云端生成因为可预防的 auth/session/billing 边界问题显得不可靠。
Output: 清晰的重新登录行为、一致的余额不足提示，以及稳定的 Bottle 2.0 任务恢复路径。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-cloud-generation/01-CONTEXT.md
@.planning/phases/01-shared-cloud-generation/01-RESEARCH.md
@.planning/phases/01-shared-cloud-generation/01-02-SUMMARY.md
@frontend/src/shared/api/client.js
@frontend/src/app/authStorage.js
@frontend/src/features/upload/UploadPanel.jsx
@frontend/src/features/upload/asrStrategy.js
@tests/e2e/test_e2e_key_flows.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 让 Bottle 2.0 在 Web 与 Desktop 下都显式处理 auth/session 失败</name>
  <files>frontend/src/shared/api/client.js, frontend/src/app/authStorage.js, frontend/src/features/upload/UploadPanel.jsx</files>
  <read_first>frontend/src/shared/api/client.js, frontend/src/app/authStorage.js, frontend/src/features/upload/UploadPanel.jsx, .planning/phases/01-shared-cloud-generation/01-CONTEXT.md</read_first>
  <action>更新共享请求栈，确保 Bottle 2.0 生成入口在鉴权缺失或 session 失效时不会静默失败。在 `frontend/src/shared/api/client.js` 中保留 401/403 传播能力，并确保生成请求能触发显式 auth 失败处理。在 `frontend/src/app/authStorage.js` 中保持桌面端 cached-session restore/clear 行为与共享云端流程兼容。在 `frontend/src/features/upload/UploadPanel.jsx` 中，针对 Bottle 2.0 路径增加或调整 auth 失败分支：缺少 token、session 过期、被拒绝时，应展示清晰的重新登录/恢复提示，而不是泛化成网络错误。不要给桌面端单独造一套 auth UX，仍以共享 renderer 逻辑为准。</action>
  <verify>pytest tests/e2e/test_e2e_key_flows.py -q</verify>
  <acceptance_criteria>
    - `frontend/src/shared/api/client.js` 中存在 401/403 的 auth 错误处理路径。
    - `frontend/src/app/authStorage.js` 中存在桌面端 session restore 与 clear 路径，并被共享 auth 流使用。
    - `frontend/src/features/upload/UploadPanel.jsx` 中存在 Bottle 2.0 的专门 auth 失败分支，而不是统一落到 generic network error。
  </acceptance_criteria>
  <done>Web 和 Desktop 下的 Bottle 2.0 用户都能收到明确的 auth/session 恢复反馈。</done>
</task>

<task type="auto">
  <name>Task 2: 对齐 Bottle 2.0 的余额提示与任务恢复行为</name>
  <files>frontend/src/features/upload/UploadPanel.jsx, frontend/src/features/upload/asrStrategy.js, tests/integration/test_regression_api.py</files>
  <read_first>frontend/src/features/upload/UploadPanel.jsx, frontend/src/features/upload/asrStrategy.js, tests/integration/test_regression_api.py, .planning/REQUIREMENTS.md</read_first>
  <action>细化 Bottle 2.0 的失败映射，使余额不足和任务恢复状态被一致表达。在 `frontend/src/features/upload/asrStrategy.js` 中保留并统一 `INSUFFICIENT_BALANCE`、云端不可用、超时、无效模型等情况的 Bottle 2.0 用户文案，使其符合共享运行时语言。在 `frontend/src/features/upload/UploadPanel.jsx` 中，确保任务创建、轮询、重试以及 resume/terminate 路径都能保留 task ID，并在瞬时失败后继续维持同一套用户可见状态模型。更新 `tests/integration/test_regression_api.py` 中与 Bottle 2.0 任务创建/恢复/计费相关的回归断言，保证共享云端任务流稳定。</action>
  <verify>pytest tests/integration/test_regression_api.py -k "resume or terminate or task or qwen3" -q</verify>
  <acceptance_criteria>
    - `frontend/src/features/upload/asrStrategy.js` 中存在显式 `INSUFFICIENT_BALANCE` Bottle 2.0 映射。
    - `frontend/src/features/upload/UploadPanel.jsx` 中存在 Bottle 2.0 的 task resume/retry 处理，并保留 task ID 与共享状态流。
    - `tests/integration/test_regression_api.py` 中针对 Bottle 2.0 任务的目标断言 exits 0。
  </acceptance_criteria>
  <done>auth / 余额 / 恢复边界不再让共享 Bottle 2.0 流程显得不一致。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/e2e/test_e2e_key_flows.py -q`
- [ ] `pytest tests/integration/test_regression_api.py -k "resume or terminate or task or qwen3" -q`
- [ ] Bottle 2.0 的 auth / balance / recovery 失败在 Web 与 Desktop 下都被清晰表达
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 共享 Bottle 2.0 生成在鉴权、余额和恢复边界上具有清晰、一致的表现
</success_criteria>

<output>
After completion, create `.planning/phases/01-shared-cloud-generation/01-03-SUMMARY.md`
</output>