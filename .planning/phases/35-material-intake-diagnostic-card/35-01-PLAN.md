---
phase: 35-material-intake-diagnostic-card
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/reading/ReadingPage.jsx
  - frontend/src/features/reading/LeftPanel.jsx
  - frontend/src/features/reading/HistoryPanel.jsx
  - frontend/src/features/reading/readingRewriteDB.js
  - frontend/src/features/reading/reading.css
  - frontend/src/features/reading/DiagnosticPanel.jsx
  - frontend/src/features/reading/readingDiagnostics.js
  - frontend/src/features/reading/readingDiagnostics.test.js
  - frontend/src/hooks/useReadingRewrite.js
autonomous: true
requirements: [DIAG-01, DIAG-02, DIAG-03, DIAG-04]
user_setup: []
must_haves:
  truths:
    - Reading submit no longer auto-starts rewrite; it enters a diagnostic stage first.
    - The diagnostic stage shows material difficulty, user CEFR level, recommended target level, impact statistics, and explicit generation confirmation.
    - Users can change the target level from A1 to C2 before generation and the selected target is the one used when generation starts.
    - Diagnosed but not yet generated drafts reopen directly into the diagnostic stage from history.
  artifacts:
    - frontend/src/features/reading/ReadingPage.jsx
    - frontend/src/features/reading/DiagnosticPanel.jsx
    - frontend/src/features/reading/readingDiagnostics.js
    - frontend/src/hooks/useReadingRewrite.js
    - frontend/src/features/reading/readingRewriteDB.js
    - frontend/src/features/reading/HistoryPanel.jsx
  key_links:
    - `ReadingPage.jsx` must route `input -> diagnostic -> reading` instead of `input -> reading`.
    - `DiagnosticPanel.jsx` consumes the persisted diagnostic snapshot generated by `readingDiagnostics.js`.
    - `useReadingRewrite.js` must honor a selected target override when `continue generation` is confirmed.
---

<objective>
Implement the full Phase 35 diagnostic gate inside the reading workspace.

Purpose: Turn the current one-click rewrite entry into a competition-style pre-generation diagnostic stage with local persistence and resume behavior.
Output: A resumable diagnostic dashboard, target-level override flow, and explicit confirm-to-generate contract wired into the existing reading module.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/35-material-intake-diagnostic-card/35-CONTEXT.md
@.planning/phases/35-material-intake-diagnostic-card/35-RESEARCH.md
@.planning/phases/35-material-intake-diagnostic-card/35-UI-SPEC.md
@frontend/src/features/reading/ReadingPage.jsx
@frontend/src/features/reading/LeftPanel.jsx
@frontend/src/features/reading/HistoryPanel.jsx
@frontend/src/features/reading/readingRewriteDB.js
@frontend/src/hooks/useReadingRewrite.js
@frontend/src/utils/vocabAnalyzer.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a diagnostic data contract and persistable draft state</name>
  <files>frontend/src/features/reading/readingDiagnostics.js, frontend/src/features/reading/readingDiagnostics.test.js, frontend/src/features/reading/readingRewriteDB.js, frontend/src/hooks/useReadingRewrite.js</files>
  <read_first>frontend/src/utils/vocabAnalyzer.js, frontend/src/features/reading/readingRewriteDB.js, frontend/src/hooks/useReadingRewrite.js, .planning/phases/35-material-intake-diagnostic-card/35-CONTEXT.md, .planning/phases/35-material-intake-diagnostic-card/35-RESEARCH.md</read_first>
  <action>Create `frontend/src/features/reading/readingDiagnostics.js` as the pure data layer for Phase 35. It must export CEFR-level helpers plus deterministic functions that derive a diagnostic snapshot from analyzer report data: material difficulty, recommended target level, selected-target metrics, segmented-bar level counts, simplification impact percentage, and human-readable estimate text. Create `frontend/src/features/reading/readingDiagnostics.test.js` with Vitest coverage for recommended target calculation and selected-target impact derivation. Extend `frontend/src/features/reading/readingRewriteDB.js` so rewrite records can safely store and retrieve `flowStatus`, `diagnosticSnapshot`, `selectedTargetLevel`, `recommendedTargetLevel`, and `diagnosedAt`, and add a helper to clear or enumerate all rewrite records from the current `reading_rewrites_v3` database. Update `frontend/src/hooks/useReadingRewrite.js` so it loads those new draft fields on article change, exposes a way to persist a diagnostic snapshot before generation, and lets `handleRewrite()` accept a concrete `targetLevel` override instead of always deriving `userLevel + 1` internally.</action>
  <verify>cd frontend; npm run test -- readingDiagnostics.test.js --runInBand</verify>
  <acceptance_criteria>
    - `frontend/src/features/reading/readingDiagnostics.js` exports helper functions for recommended target and selected-target metrics.
    - `frontend/src/features/reading/readingDiagnostics.test.js` exits 0.
    - `frontend/src/features/reading/readingRewriteDB.js` contains `flowStatus` and `diagnosticSnapshot`.
    - `frontend/src/hooks/useReadingRewrite.js` contains `targetLevelOverride ??` or equivalent explicit target override usage instead of only `getTargetLevel(userLevel)`.
  </acceptance_criteria>
  <done>Diagnostic computation and persistence are stable enough to drive the UI stage and resume behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Replace submit-time auto-rewrite with a resumable diagnostic stage</name>
  <files>frontend/src/features/reading/ReadingPage.jsx, frontend/src/features/reading/LeftPanel.jsx, frontend/src/features/reading/HistoryPanel.jsx, frontend/src/features/reading/DiagnosticPanel.jsx, frontend/src/features/reading/reading.css</files>
  <read_first>frontend/src/features/reading/ReadingPage.jsx, frontend/src/features/reading/LeftPanel.jsx, frontend/src/features/reading/HistoryPanel.jsx, frontend/src/features/reading/AnalysisPanel.jsx, frontend/src/features/reading/reading.css, .planning/phases/35-material-intake-diagnostic-card/35-UI-SPEC.md</read_first>
  <action>Add a dedicated `DiagnosticPanel.jsx` that renders the Phase 35 right-column dashboard defined in `35-UI-SPEC.md`: difficulty triad, A1-C2 target selector, impact scoreboard, segmented CEFR bar, estimate card, and primary CTA `继续生成`. Refactor `ReadingPage.jsx` so submit now saves a history record, runs diagnosis, persists the diagnostic snapshot, and sets `mode` to a diagnostic stage instead of calling `handleRewrite()` immediately. Ensure the page supports three states: `input`, `diagnostic`, and `reading`. In the diagnostic stage, reuse the left reading preview surface for original text only, keep the history rail visible, and route the CTA to `handleRewrite()` only after explicit confirmation. Update `HistoryPanel.jsx` to read rewrite-record metadata from `reading_rewrites_v3`, display pending/generated state badges, and reopen diagnosed-but-not-generated drafts directly into the diagnostic stage. Fix the stale clear-all implementation so it clears the current v3 rewrite DB instead of the old v2 store. Update `reading.css` so the diagnostic stage matches the approved UI-SPEC: left preview + sticky right dashboard on desktop, dashboard-first stacking on mobile, scoreboard-style cards, and visible status badges in history.</action>
  <verify>cd frontend; npm run build</verify>
  <acceptance_criteria>
    - `frontend/src/features/reading/ReadingPage.jsx` contains a `diagnostic` mode and no longer calls `handleRewrite(` inside initial submit before diagnosis completes.
    - `frontend/src/features/reading/DiagnosticPanel.jsx` contains `继续生成` and `重新诊断`.
    - `frontend/src/features/reading/HistoryPanel.jsx` contains a status badge or label for diagnosed-but-not-generated drafts.
    - `frontend/src/features/reading/HistoryPanel.jsx` no longer opens `reading_rewrites_v2`.
    - `cd frontend; npm run build` exits 0.
  </acceptance_criteria>
  <done>The reading module now gates generation behind a persistent diagnostic dashboard and supports resume-before-generation behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Sync static web output and verify the web delivery contract</name>
  <files>app/static</files>
  <read_first>AGENTS.md, frontend/package.json, frontend/src/features/reading/ReadingPage.jsx</read_first>
  <action>After the frontend implementation is complete and local build passes, run the frontend static-sync pipeline so the FastAPI-served web app includes the new diagnostic-stage behavior. Use `cd frontend && npm run build:app-static` to rebuild and sync into `app/static`. Do not skip this step: the project delivery contract explicitly says web-facing route changes are not complete until `app/static` is updated from the new frontend build.</action>
  <verify>cd frontend; npm run build:app-static</verify>
  <acceptance_criteria>
    - `cd frontend; npm run build:app-static` exits 0.
    - `app/static/assets` contains freshly updated build artifacts after the command completes.
  </acceptance_criteria>
  <done>The web-served static bundle matches the new frontend reading diagnostic flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run test -- readingDiagnostics.test.js --runInBand`
- [ ] `cd frontend && npm run build`
- [ ] `cd frontend && npm run build:app-static`
- [ ] Reading submit no longer auto-generates before diagnostic confirmation
- [ ] Diagnosed drafts reopen into the diagnostic stage from history
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 35 requirements DIAG-01 through DIAG-04 are satisfied within the reading workspace
</success_criteria>

<output>
After completion, create `.planning/phases/35-material-intake-diagnostic-card/35-01-SUMMARY.md`
</output>
