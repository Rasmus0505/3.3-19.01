---
phase: 01.1-fix-asr-subtitle-recognition-403-file-access-failures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/lesson_service.py
  - tests/unit/test_lesson_service_dashscope_url.py
  - tests/integration/test_regression_api.py
autonomous: true
requirements: [WEB-01, WEB-03, DESK-02]
must_haves:
  truths:
    - A Bottle 2.0 direct-upload task that first fails with `ASR_TASK_FAILED` plus `FILE_403_FORBIDDEN` retries once on the server with a freshly resolved signed URL and can still succeed without exposing the first failure to the user.
    - The retry uses the canonical `dashscope_file_id` as the recovery key and does not switch primary ownership to a client-supplied `dashscope_file_url`.
    - If the second attempt still fails, the backend returns a dedicated cloud file-access failure instead of collapsing the condition into a generic Bottle 2.0 outage.
  artifacts:
    - path: app/services/lesson_service.py
      provides: One-time `FILE_403_FORBIDDEN` self-heal for `generate_from_dashscope_file_id()`
    - path: tests/unit/test_lesson_service_dashscope_url.py
      provides: Unit coverage for retry success and retry exhaustion
    - path: tests/integration/test_regression_api.py
      provides: Regression coverage for transparent direct-upload retry behavior
  key_links:
    - from: app/services/lesson_service.py
      to: app/infra/dashscope_storage.py
      via: `_resolve_dashscope_asr_source_url()` calling `get_file_signed_url()` twice on the same `dashscope_file_id`
      pattern: get_file_signed_url
    - from: app/services/lesson_service.py
      to: app/services/asr_dashscope.py
      via: `transcribe_signed_url()` retry branch guarded by `ASR_TASK_FAILED` + `FILE_403_FORBIDDEN`
      pattern: transcribe_signed_url
---

<objective>
Add the hidden one-time backend self-heal for DashScope direct-upload subtitle recognition when ASR task polling returns `FILE_403_FORBIDDEN`.

Purpose: Repair the existing Bottle 2.0 cloud-file path itself per D-01, D-02, D-03, and D-04 instead of sending users toward fallback routes or re-upload as the primary fix.
Output: A one-retry `dashscope_file_id`-driven recovery path in `LessonService.generate_from_dashscope_file_id()` with targeted unit and integration regression coverage.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/STATE.md
@.planning/workstreams/milestone/REQUIREMENTS.md
@.planning/workstreams/milestone/phases/01.1-fix-asr-subtitle-recognition-403-file-access-failures/01.1-CONTEXT.md
@.planning/workstreams/milestone/phases/01-shared-cloud-generation/01-CONTEXT.md
@.planning/workstreams/milestone/phases/01-shared-cloud-generation/01-01-SUMMARY.md
@app/services/lesson_service.py
@app/services/asr_dashscope.py
@app/infra/dashscope_storage.py
@tests/unit/test_lesson_service_dashscope_url.py
@tests/integration/test_regression_api.py

<interfaces>
From `app/services/lesson_service.py`:
```python
def _resolve_dashscope_asr_source_url(*, dashscope_file_id: str, dashscope_file_url: str | None = None) -> str

@staticmethod
def generate_from_dashscope_file_id(
    *,
    dashscope_file_id: str,
    dashscope_file_url: str | None = None,
    source_filename: str,
    req_dir: Path,
    owner_id: int,
    asr_model: str,
    db: Session,
    progress_callback: ProgressCallback | None = None,
    task_id: str | None = None,
    semantic_split_enabled: bool | None = None,
) -> Lesson
```

From `app/services/asr_dashscope.py`:
```python
class AsrError(Exception):
    code: str
    message: str
    detail: str

def transcribe_signed_url(
    signed_url: str,
    *,
    model: str = DEFAULT_MODEL,
    requests_timeout: int = 120,
    audio_path_for_cancel: str | None = None,
    progress_callback=None,
) -> dict[str, Any]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add retry-contract coverage for DashScope `FILE_403_FORBIDDEN` recovery</name>
  <files>tests/unit/test_lesson_service_dashscope_url.py, tests/integration/test_regression_api.py</files>
  <read_first>tests/unit/test_lesson_service_dashscope_url.py, tests/integration/test_regression_api.py, app/services/lesson_service.py, app/services/asr_dashscope.py, app/infra/dashscope_storage.py, .planning/workstreams/milestone/phases/01.1-fix-asr-subtitle-recognition-403-file-access-failures/01.1-CONTEXT.md</read_first>
  <behavior>
    - Test 1: `generate_from_dashscope_file_id()` re-resolves a signed URL from the same `dashscope_file_id` and retries exactly once when the first `transcribe_signed_url()` call raises `AsrError("ASR_TASK_FAILED", ..., {"subtask_code":"FILE_403_FORBIDDEN"})`.
    - Test 2: The retry-success path does not surface a failed task to the polling API and preserves `dashscope_file_id` as the canonical source artifact.
    - Test 3: When both attempts fail with `FILE_403_FORBIDDEN`, the raised backend error code is `DASHSCOPE_FILE_ACCESS_FORBIDDEN` and its JSON detail contains `dashscope_file_id`, `first_failure_stage`, `first_failure_code`, `first_failure_message`, `retry_attempted`, `retry_outcome`, and `final_outcome`.
  </behavior>
  <action>Extend `tests/unit/test_lesson_service_dashscope_url.py` with targeted service-level cases that monkeypatch `get_file_signed_url()` and `transcribe_signed_url()` so the first call returns an `ASR_TASK_FAILED` detail payload containing `task_status="FAILED"`, `subtask_code="FILE_403_FORBIDDEN"`, and a concrete provider message, while the second call either succeeds or fails again. Assert `get_file_signed_url()` is called twice with the same `uploads/...` file ID, `transcribe_signed_url()` is called at most twice, and the exhausted path expects backend code `DASHSCOPE_FILE_ACCESS_FORBIDDEN`. Extend `tests/integration/test_regression_api.py` with a direct-upload task regression that exercises the real `LessonService.generate_from_dashscope_file_id()` path through `/api/lessons/tasks`, proves the first 403 is hidden when retry succeeds, and proves the task path still never routes into local conversion or a server-side fallback flow. Use exact selector-friendly test names containing `dashscope_403` or `file_access_retry`.</action>
  <verify>
    <automated>pytest tests/unit/test_lesson_service_dashscope_url.py tests/integration/test_regression_api.py -k "dashscope_403 or file_access_retry" -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/test_lesson_service_dashscope_url.py` contains `FILE_403_FORBIDDEN` and `DASHSCOPE_FILE_ACCESS_FORBIDDEN`.
    - `tests/integration/test_regression_api.py` contains a test name matching `dashscope_403` or `file_access_retry`.
    - `pytest tests/unit/test_lesson_service_dashscope_url.py tests/integration/test_regression_api.py -k "dashscope_403 or file_access_retry" -q` exits 0.
  </acceptance_criteria>
  <done>Regression coverage exists for one-time retry success and retry exhaustion on the direct-upload ASR path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement one-time signed-URL self-heal in `LessonService.generate_from_dashscope_file_id()`</name>
  <files>app/services/lesson_service.py, tests/unit/test_lesson_service_dashscope_url.py, tests/integration/test_regression_api.py</files>
  <read_first>app/services/lesson_service.py, app/services/asr_dashscope.py, app/infra/dashscope_storage.py, tests/unit/test_lesson_service_dashscope_url.py, tests/integration/test_regression_api.py, .planning/workstreams/milestone/phases/01.1-fix-asr-subtitle-recognition-403-file-access-failures/01.1-CONTEXT.md</read_first>
  <behavior>
    - Test 1: On first `ASR_TASK_FAILED` plus `FILE_403_FORBIDDEN`, the backend refreshes the signed URL from canonical `dashscope_file_id` and retries once immediately.
    - Test 2: On retry success, the returned `Lesson` carries recovery metadata showing `retry_attempted=true`, `retry_outcome="succeeded"`, and `final_outcome="recovered"`.
    - Test 3: On retry exhaustion, the backend raises `AsrError("DASHSCOPE_FILE_ACCESS_FORBIDDEN", ...)` with `retry_outcome="failed"` and `final_outcome="cloud_file_access_failed"`.
  </behavior>
  <action>Modify `app/services/lesson_service.py` around the current `signed_url = _resolve_dashscope_asr_source_url(...)` and `asr_raw = transcribe_signed_url(...)` block. Add a small internal helper that parses `AsrError.detail` JSON and identifies the exact combination `code == "ASR_TASK_FAILED"` plus `subtask_code == "FILE_403_FORBIDDEN"` per D-01 and D-04. On that exact combination only, build a `dashscope_recovery` object with these exact keys: `dashscope_file_id`, `first_failure_stage`, `first_failure_code`, `first_failure_message`, `retry_attempted`, `retry_outcome`, `final_outcome`. Re-resolve the signed URL by calling `_resolve_dashscope_asr_source_url(dashscope_file_id=..., dashscope_file_url=...)` again, then call `transcribe_signed_url()` a second and final time. Do not add product fallback routes, do not ask for re-upload, and do not treat `dashscope_file_url` as the primary recovery key per D-03. When retry succeeds, attach the `dashscope_recovery` object to `lesson.task_result_meta["dashscope_recovery"]`. When retry fails again with the same file-access shape, raise `AsrError("DASHSCOPE_FILE_ACCESS_FORBIDDEN", "DashScope 云端文件访问失败", json.dumps(dashscope_recovery, ensure_ascii=False))` where `first_failure_stage` is `"asr_transcribe"` and the final object uses `retry_attempted=true`, `retry_outcome="failed"`, and `final_outcome="cloud_file_access_failed"`.</action>
  <verify>
    <automated>pytest tests/unit/test_lesson_service_dashscope_url.py tests/integration/test_regression_api.py -k "dashscope_403 or file_access_retry" -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/lesson_service.py` contains `FILE_403_FORBIDDEN`, `DASHSCOPE_FILE_ACCESS_FORBIDDEN`, and `dashscope_recovery`.
    - `app/services/lesson_service.py` contains a second call to `_resolve_dashscope_asr_source_url(` inside the retry branch.
    - `app/services/lesson_service.py` contains `lesson.task_result_meta["dashscope_recovery"]`.
    - `pytest tests/unit/test_lesson_service_dashscope_url.py tests/integration/test_regression_api.py -k "dashscope_403 or file_access_retry" -q` exits 0.
  </acceptance_criteria>
  <done>The direct-upload lesson path self-heals once on DashScope file-access 403 failures and raises a first-class file-access error when retry is exhausted.</done>
</task>

</tasks>

<verification>
- `pytest tests/unit/test_lesson_service_dashscope_url.py tests/integration/test_regression_api.py -k "dashscope_403 or file_access_retry" -q`
- `rg -n "FILE_403_FORBIDDEN|DASHSCOPE_FILE_ACCESS_FORBIDDEN|dashscope_recovery" app/services/lesson_service.py tests/unit/test_lesson_service_dashscope_url.py tests/integration/test_regression_api.py`
- Confirm no task action introduces a new server-side fallback route or a user-visible first-attempt retry prompt.
</verification>

<success_criteria>
- Direct-upload ASR retries exactly once when the first failure is `ASR_TASK_FAILED` + `FILE_403_FORBIDDEN`.
- `dashscope_file_id` remains the canonical recovery key throughout the retry path.
- Retry exhaustion produces `DASHSCOPE_FILE_ACCESS_FORBIDDEN` with structured recovery detail instead of a generic cloud outage.
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/01.1-fix-asr-subtitle-recognition-403-file-access-failures/01.1-01-SUMMARY.md`
</output>
