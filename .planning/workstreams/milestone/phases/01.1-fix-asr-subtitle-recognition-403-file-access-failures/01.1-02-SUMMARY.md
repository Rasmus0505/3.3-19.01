---
phase: 01.1-fix-asr-subtitle-recognition-403-file-access-failures
plan: 02
subsystem: upload
tags: [dashscope, task-diagnostics, frontend, regression]
requires:
  - phase: 01.1-fix-asr-subtitle-recognition-403-file-access-failures
    provides: One-time FILE_403_FORBIDDEN self-heal and dedicated backend file-access error contract
provides:
  - Structured `dashscope_recovery` persistence for succeeded and failed lesson tasks
  - Dedicated frontend cloud-file-access classification and user-facing Bottle 2.0 copy
  - Regression and contract coverage for the recovery metadata and message mapping
affects: [lesson-tasks, upload-panel, cloud-generation]
tech-stack:
  added: []
  patterns: [task artifact persistence, cloud error classification, stale-session cleanup guard]
key-files:
  created: []
  modified:
    - app/services/lesson_command_service.py
    - app/services/lesson_service.py
    - app/services/lesson_task_manager.py
    - frontend/src/features/upload/UploadPanel.jsx
    - frontend/src/features/upload/asrStrategy.js
    - tests/integration/test_regression_api.py
    - tests/contracts/test_desktop_runtime_contract.py
key-decisions:
  - "Successful DashScope 403 recovery is persisted into task artifacts before any cleanup path can touch the row."
  - "Successful task artifacts are retained long enough for polling/debug instead of being cleaned immediately on the first read."
  - "Frontend cloud error mapping consumes structured backend error payloads instead of flattening them to strings first."
patterns-established:
  - "Task cleanup must refresh stale sessions before mutating JSON artifacts."
  - "Cloud file-access failures are a first-class Bottle 2.0 condition, distinct from generic service unavailability."
requirements-completed: [WEB-01, WEB-03, DESK-02]
duration: 68min
completed: 2026-03-27
---

# Phase 01.1 Plan 02: DashScope Recovery Diagnostics and Frontend Classification Summary

**DashScope 403 recovery now survives task persistence and exhausted retries surface a dedicated cloud file-access message in the upload flow**

## Performance

- **Duration:** 68 min
- **Started:** 2026-03-26T22:56:00+08:00
- **Completed:** 2026-03-27T00:07:16+08:00
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments

- Persisted `dashscope_recovery` into task success/failure artifacts and failure debug payloads, including the recovered-success path that was previously being lost through stale-session cleanup.
- Fixed stale-session cleanup behavior so task artifact cleanup refreshes current database state before mutating JSON payloads.
- Stopped immediate successful-task artifact cleanup by giving succeeded tasks a retention window, keeping structured diagnostics available to polling/debug consumers.
- Updated frontend Bottle 2.0 cloud failure mapping to classify `DASHSCOPE_FILE_ACCESS_FORBIDDEN` and nested `FILE_403_FORBIDDEN` payloads as a dedicated cloud-file-access failure.
- Updated upload flow error handling to pass structured backend payloads into `asrStrategy` instead of collapsing them to plain strings first.
- Added regression and contract coverage for persisted recovery artifacts and the new frontend message mapping.

## Task Commits

Each task was committed atomically:

1. **Task 1: Persist `dashscope_recovery` into task success and failure artifacts** - `1d070c42` (fix)
2. **Task 2: Classify exhausted retry as a cloud file-access failure in frontend error mapping** - `770772be` (fix)

## Files Created/Modified

- `app/services/lesson_command_service.py` - Forwarded and force-persisted success/failure recovery metadata into task artifacts.
- `app/services/lesson_service.py` - Seeded successful recovery metadata into task artifacts while still inside the DashScope generation path.
- `app/services/lesson_task_manager.py` - Added `dashscope_recovery` normalization/persistence hooks, refreshed stale sessions before cleanup, and delayed success artifact expiry.
- `frontend/src/features/upload/UploadPanel.jsx` - Preserved structured backend error payloads when mapping Bottle 2.0 failures.
- `frontend/src/features/upload/asrStrategy.js` - Added `CLOUD_FILE_ACCESS_FORBIDDEN` classification and dedicated user-facing copy.
- `tests/integration/test_regression_api.py` - Added recovery artifact assertions for succeeded and exhausted retry paths.
- `tests/contracts/test_desktop_runtime_contract.py` - Added frontend contract coverage for the file-access failure mapping.

## Verification

- `pytest tests/integration/test_regression_api.py -k "dashscope_403 or file_access_retry or dashscope_recovery" -q`
- `pytest tests/contracts/test_desktop_runtime_contract.py -k "asr_strategy and file_access" -q`

Both commands passed.

## Issues Encountered

- The original Wave 2 executor stalled and left partial backend changes without a summary or commits.
- The main defect turned out to be stale-session cleanup during task polling: a reused route session was overwriting newly persisted success artifacts with an older JSON snapshot and immediately cleaning successful work dirs.

## User Setup Required

None.

## Next Phase Readiness

- Phase `01.1` is ready for phase-level verification and completion routing.
- The upload path now distinguishes cloud file-access exhaustion from generic Bottle 2.0 outages while keeping successful hidden retries invisible to the user.

## Post-Completion Follow-up

- **2026-03-27:** A live regression showed some direct-upload objects no longer exposed a usable HTTPS download URL through `Files.get()`, causing `DASHSCOPE_STORAGE_SIGNED_URL_MISSING` before the retry logic could run.
- **Adjustment:** The upload-policy response now returns `file_url=oss://<file_id>`, the lesson-generation path prefers that client-provided `oss://` resource URL when present, and DashScope ASR task creation enables `X-DashScope-OssResourceResolve` for `oss://` inputs.
- **Why this matters:** Phase `01.1` originally hardened the retry-and-diagnostics path after ASR task creation. This follow-up closes an upstream access variant where the direct-upload object is valid but the metadata lookup no longer provides a separate HTTPS signed URL.
- **Verification:** `pytest tests/unit/test_dashscope_upload_router.py tests/unit/test_lesson_service_dashscope_url.py tests/unit/test_asr_dashscope.py -q` and focused direct-upload integration regressions in `tests/integration/test_regression_api.py` passed after the adjustment.

## Self-Check

PASSED

- FOUND: `.planning/workstreams/milestone/phases/01.1-fix-asr-subtitle-recognition-403-file-access-failures/01.1-02-SUMMARY.md`
- FOUND: `1d070c42`
- FOUND: `770772be`

---
*Phase: 01.1-fix-asr-subtitle-recognition-403-file-access-failures*
*Completed: 2026-03-27*
