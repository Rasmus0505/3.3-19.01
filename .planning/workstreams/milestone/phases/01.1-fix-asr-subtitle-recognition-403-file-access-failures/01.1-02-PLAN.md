---
phase: 01.1-fix-asr-subtitle-recognition-403-file-access-failures
plan: 02
type: execute
wave: 2
depends_on: ["01.1-01"]
files_modified:
  - app/services/lesson_command_service.py
  - app/services/lesson_task_manager.py
  - frontend/src/features/upload/asrStrategy.js
  - tests/integration/test_regression_api.py
  - tests/contracts/test_desktop_runtime_contract.py
autonomous: true
requirements: [WEB-01, WEB-03, DESK-02]
must_haves:
  truths:
    - Task and debug artifacts record structured recovery data for the 403 self-heal path, including the canonical `dashscope_file_id`, first failure facts, retry attempt, retry outcome, and final outcome.
    - When the second attempt fails, the task is classified as a cloud file-access failure instead of a generic Bottle 2.0 outage.
    - The frontend maps the exhausted file-access failure to a specific Bottle 2.0 message while leaving successful hidden recovery invisible to the user per D-02.
  artifacts:
    - path: app/services/lesson_command_service.py
      provides: Extraction and forwarding of `dashscope_recovery` debug metadata
    - path: app/services/lesson_task_manager.py
      provides: Persistence of `dashscope_recovery` into task artifacts and failure debug payloads
    - path: frontend/src/features/upload/asrStrategy.js
      provides: First-class cloud file-access classification and user-facing copy
    - path: tests/integration/test_regression_api.py
      provides: Regression coverage for persisted recovery artifacts
    - path: tests/contracts/test_desktop_runtime_contract.py
      provides: Contract coverage for frontend cloud-file failure mapping
  key_links:
    - from: app/services/lesson_command_service.py
      to: app/services/lesson_task_manager.py
      via: `mark_task_failed()` and `mark_task_succeeded()` forwarding `dashscope_recovery`
      pattern: dashscope_recovery
    - from: frontend/src/features/upload/asrStrategy.js
      to: backend error payloads
      via: parsing `errorCode`, `detail`, and nested JSON fields for `DASHSCOPE_FILE_ACCESS_FORBIDDEN` and `FILE_403_FORBIDDEN`
      pattern: DASHSCOPE_FILE_ACCESS_FORBIDDEN
---

<objective>
Persist structured DashScope 403 recovery diagnostics and surface exhausted retries as a cloud file-access failure across task/debug artifacts and frontend error classification.

Purpose: Complete D-04 and D-05 so operations and users can distinguish a true cloud file-access problem from generic service unavailability after the backend self-heal has already been attempted.
Output: Persisted `dashscope_recovery` task metadata plus specific frontend classification for `DASHSCOPE_FILE_ACCESS_FORBIDDEN`.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/STATE.md
@.planning/workstreams/milestone/REQUIREMENTS.md
@.planning/workstreams/milestone/phases/01.1-fix-asr-subtitle-recognition-403-file-access-failures/01.1-CONTEXT.md
@.planning/workstreams/milestone/phases/01.1-fix-asr-subtitle-recognition-403-file-access-failures/01.1-01-PLAN.md
@app/services/lesson_command_service.py
@app/services/lesson_task_manager.py
@frontend/src/features/upload/asrStrategy.js
@tests/integration/test_regression_api.py
@tests/contracts/test_desktop_runtime_contract.py

<interfaces>
From `app/services/lesson_task_manager.py`:
```python
def mark_task_failed(
    task_id: str,
    *,
    error_code: str,
    message: str,
    exception_type: str = "",
    detail_excerpt: str = "",
    traceback_excerpt: str = "",
    failed_stage: str | None = None,
    translation_debug: dict | None = None,
    resume_available: bool | None = None,
    db: Session | None = None,
    session_factory: SessionFactory | None = None,
) -> None

def mark_task_succeeded(
    task_id: str,
    *,
    lesson_id: int,
    subtitle_cache_seed: dict | None = None,
    translation_debug: dict | None = None,
    result_kind: str = TASK_RESULT_FULL_SUCCESS,
    result_message: str = "",
    partial_failure_stage: str = "",
    partial_failure_code: str = "",
    partial_failure_message: str = "",
    db: Session | None = None,
    session_factory: SessionFactory | None = None,
) -> None
```

From `frontend/src/features/upload/asrStrategy.js`:
```javascript
function inferCloudErrorCode({ errorCode = "", message = "", detail = "", browserOnline = true, serverStatus = {} } = {})
export function mapCloudAsrFailureToMessage({ errorCode = "", message = "", detail = "", browserOnline = true, serverStatus = {} } = {})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist `dashscope_recovery` into task success and failure artifacts</name>
  <files>app/services/lesson_command_service.py, app/services/lesson_task_manager.py, tests/integration/test_regression_api.py</files>
  <read_first>app/services/lesson_command_service.py, app/services/lesson_task_manager.py, app/services/lesson_service.py, tests/integration/test_regression_api.py, .planning/workstreams/milestone/phases/01.1-fix-asr-subtitle-recognition-403-file-access-failures/01.1-CONTEXT.md</read_first>
  <behavior>
    - Test 1: A task that recovers on the hidden retry persists `artifacts_json["dashscope_recovery"]` with `retry_attempted=true`, `retry_outcome="succeeded"`, and `final_outcome="recovered"`.
    - Test 2: A task that still fails after retry persists the same `dashscope_recovery` object into `failure_debug_json["dashscope_recovery"]` and leaves `error_code == "DASHSCOPE_FILE_ACCESS_FORBIDDEN"`.
    - Test 3: The persisted object always contains `dashscope_file_id`, `first_failure_stage`, `first_failure_code`, `first_failure_message`, `retry_attempted`, `retry_outcome`, and `final_outcome`.
  </behavior>
  <action>Extend `app/services/lesson_command_service.py` so after `LessonService.generate_from_dashscope_file_id()` returns it reads `task_result_meta["dashscope_recovery"]` and forwards it into task persistence. Extend `app/services/lesson_task_manager.py` with an explicit optional parameter named `dashscope_recovery: dict | None = None` on both `mark_task_failed()` and `mark_task_succeeded()`. On success, merge that object into `task.artifacts_json["dashscope_recovery"]` without changing user-visible task status. On failure, store the same object in both `failure_debug_json["dashscope_recovery"]` and `task.artifacts_json["dashscope_recovery"]`, alongside the existing failure fields. In the `except AsrError as exc:` block in `lesson_command_service.py`, parse `exc.detail` JSON when `exc.code == "DASHSCOPE_FILE_ACCESS_FORBIDDEN"` and pass the concrete `dashscope_recovery` object to `mark_task_failed()`. Extend `tests/integration/test_regression_api.py` with task-level assertions against persisted database rows and task polling payloads for both recovered and exhausted paths.</action>
  <verify>
    <automated>pytest tests/integration/test_regression_api.py -k "dashscope_403 or file_access_retry or dashscope_recovery" -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/lesson_task_manager.py` contains `dashscope_recovery` in both `mark_task_failed(` and `mark_task_succeeded(` signatures or bodies.
    - `app/services/lesson_command_service.py` contains `task_result_meta.get("dashscope_recovery")`.
    - `tests/integration/test_regression_api.py` contains assertions on `artifacts_json["dashscope_recovery"]` or `failure_debug["dashscope_recovery"]`.
    - `pytest tests/integration/test_regression_api.py -k "dashscope_403 or file_access_retry or dashscope_recovery" -q` exits 0.
  </acceptance_criteria>
  <done>Structured recovery diagnostics are persisted in task/debug artifacts for both recovered and exhausted 403 paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify exhausted retry as a cloud file-access failure in frontend error mapping</name>
  <files>frontend/src/features/upload/asrStrategy.js, tests/contracts/test_desktop_runtime_contract.py</files>
  <read_first>frontend/src/features/upload/asrStrategy.js, frontend/src/features/upload/UploadPanel.jsx, tests/contracts/test_desktop_runtime_contract.py, .planning/workstreams/milestone/phases/01.1-fix-asr-subtitle-recognition-403-file-access-failures/01.1-CONTEXT.md</read_first>
  <behavior>
    - Test 1: `inferCloudErrorCode()` returns `CLOUD_FILE_ACCESS_FORBIDDEN` when `errorCode` is `DASHSCOPE_FILE_ACCESS_FORBIDDEN`.
    - Test 2: `inferCloudErrorCode()` also returns `CLOUD_FILE_ACCESS_FORBIDDEN` when `detail` JSON still contains `first_failure_code="FILE_403_FORBIDDEN"` or `subtask_code="FILE_403_FORBIDDEN"`.
    - Test 3: `mapCloudAsrFailureToMessage()` returns a dedicated Bottle 2.0 cloud-file-access message instead of the generic service-unavailable copy for this case.
  </behavior>
  <action>Update `frontend/src/features/upload/asrStrategy.js` so the cloud classifier recognizes the new backend error contract from Plan 01. Add a first-class normalized frontend code named `CLOUD_FILE_ACCESS_FORBIDDEN`. Match it when `errorCode === "DASHSCOPE_FILE_ACCESS_FORBIDDEN"` or when parsed `detail` JSON contains `first_failure_code === "FILE_403_FORBIDDEN"` or `subtask_code === "FILE_403_FORBIDDEN"`. Add a dedicated user-facing message: `Bottle 2.0 暂时无法访问已上传的云端文件，请稍后重试；若再次失败，再重新上传当前素材。` Do not surface this message on hidden retry success. Extend `tests/contracts/test_desktop_runtime_contract.py` with a Node-based source-module test that imports `mapCloudAsrFailureToMessage()` from `asrStrategy.js`, passes both the direct backend code and the nested detail payload, and asserts the returned code/message are the new cloud-file-access values.</action>
  <verify>
    <automated>pytest tests/contracts/test_desktop_runtime_contract.py -k "asr_strategy and file_access" -q</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/features/upload/asrStrategy.js` contains `CLOUD_FILE_ACCESS_FORBIDDEN`, `DASHSCOPE_FILE_ACCESS_FORBIDDEN`, and `FILE_403_FORBIDDEN`.
    - `frontend/src/features/upload/asrStrategy.js` contains the exact message `Bottle 2.0 暂时无法访问已上传的云端文件，请稍后重试；若再次失败，再重新上传当前素材。`
    - `tests/contracts/test_desktop_runtime_contract.py` contains a contract test name matching both `asr_strategy` and `file_access`.
    - `pytest tests/contracts/test_desktop_runtime_contract.py -k "asr_strategy and file_access" -q` exits 0.
  </acceptance_criteria>
  <done>Exhausted direct-upload 403 retries are surfaced as a specific cloud file-access failure in frontend messaging.</done>
</task>

</tasks>

<verification>
- `pytest tests/integration/test_regression_api.py -k "dashscope_403 or file_access_retry or dashscope_recovery" -q`
- `pytest tests/contracts/test_desktop_runtime_contract.py -k "asr_strategy and file_access" -q`
- `rg -n "dashscope_recovery|DASHSCOPE_FILE_ACCESS_FORBIDDEN|CLOUD_FILE_ACCESS_FORBIDDEN|FILE_403_FORBIDDEN" app/services/lesson_command_service.py app/services/lesson_task_manager.py frontend/src/features/upload/asrStrategy.js tests/integration/test_regression_api.py tests/contracts/test_desktop_runtime_contract.py`
</verification>

<success_criteria>
- Task/debug artifacts store the full structured recovery object required by D-05.
- Failed second attempts remain classified as cloud file-access failures rather than generic cloud outages.
- Frontend Bottle 2.0 messaging distinguishes exhausted cloud-file access failures from connectivity or rate-limit failures.
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/01.1-fix-asr-subtitle-recognition-403-file-access-failures/01.1-02-SUMMARY.md`
</output>
