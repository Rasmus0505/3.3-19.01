# Phase 01.1: fix-asr-subtitle-recognition-403-file-access-failures - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md; this log preserves the alternatives considered.

**Date:** 2026-03-26
**Phase:** 01.1-fix-asr-subtitle-recognition-403-file-access-failures
**Areas discussed:** 403 recovery path, failure observability

---

## 403 recovery path

| Option | Description | Selected |
|--------|-------------|----------|
| Backend transparent self-heal once | On `FILE_403_FORBIDDEN`, refresh a fresh signed URL from canonical `dashscope_file_id` and retry the ASR task one time automatically | ✓ |
| Pre-check before retry | Refresh signed URL, do an additional file-access/meta validation step, then retry | |
| Better error only | Do not auto-retry; only expose the failure more accurately | |

**User's choice:** Backend transparent self-heal once.
**Notes:** The user explicitly chose the simplest fix that actually repairs the path and rejected spending this phase on fallback-oriented handling.

---

## Failure observability

| Option | Description | Selected |
|--------|-------------|----------|
| Structured debug fields | Record `dashscope_file_id`, first failure stage/code/message, retry attempted flag, retry outcome, and final failure reason | ✓ |
| Keep generic failure logging | Preserve current task failure handling without extra structured recovery fields | |

**User's choice:** Structured debug fields.
**Notes:** The user selected area `4` together with the recovery discussion and framed the goal as “solve the problem rather than consider fallback paths,” so observability is treated as supporting instrumentation for the fix rather than a separate product behavior change.

---

## the agent's Discretion

- Exact retry timing/backoff
- Exact artifact/debug field names and placement
- Exact post-retry failure copy

## Deferred Ideas

- Heavier preflight object validation before retry
- Broader fallback-path decisions beyond repairing the direct-upload chain itself
