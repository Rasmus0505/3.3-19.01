---
phase: 01.1-fix-asr-subtitle-recognition-403-file-access-failures
verified: 2026-03-26T16:22:49.4913750Z
status: passed
score: 6/6 must-haves verified
re_verification:
  previous_status: gaps_found
  previous_score: 6/6
  gaps_closed:
    - "Shared task failure recording remains functional after adding dashscope_recovery persistence"
  gaps_remaining: []
  regressions: []
---

# Phase 01.1: Fix ASR subtitle recognition 403 file access failures Verification Report

**Phase Goal:** Stabilize the existing Bottle 2.0 DashScope direct-upload lesson pipeline when subtitle recognition fails with cloud file-access 403 errors. Repair the current cloud-file transcription path itself with a one-time backend self-heal and structured recovery diagnostics.
**Verified:** 2026-03-26T16:22:49.4913750Z
**Status:** passed
**Re-verification:** Yes — after gap closure

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | A Bottle 2.0 direct-upload task that first fails with `ASR_TASK_FAILED` plus `FILE_403_FORBIDDEN` retries once on the server with a freshly resolved signed URL and can still succeed without exposing the first failure to the user. | ✓ VERIFIED | `app/services/lesson_service.py:2741-2787`; `tests/unit/test_lesson_service_dashscope_url.py` targeted retry tests passed; `tests/integration/test_regression_api.py:4800-5007` verifies polling stays succeeded with no surfaced first failure. |
| 2 | The retry uses the canonical `dashscope_file_id` as the recovery key and does not switch primary ownership to a client-supplied `dashscope_file_url`. | ✓ VERIFIED | `app/services/lesson_service.py:2754-2766` retries by re-calling `_resolve_dashscope_asr_source_url()` with the same `dashscope_file_id`; `tests/integration/test_regression_api.py:4976-4979` asserts the same file ID is resolved twice. |
| 3 | If the second attempt still fails, the backend returns a dedicated cloud file-access failure instead of collapsing the condition into a generic Bottle 2.0 outage. | ✓ VERIFIED | `app/services/lesson_service.py:2775-2783` raises `DASHSCOPE_FILE_ACCESS_FORBIDDEN`; `tests/integration/test_regression_api.py:5012-5086` verifies task error code and persisted debug payload. |
| 4 | Task and debug artifacts record structured recovery data for the 403 self-heal path, including the canonical `dashscope_file_id`, first failure facts, retry attempt, retry outcome, and final outcome. | ✓ VERIFIED | `app/services/lesson_command_service.py:525-574`, `app/services/lesson_task_manager.py:1242-1279`, and `app/services/lesson_task_manager.py:1310-1335`; `tests/integration/test_regression_api.py:4999-5007` and `tests/integration/test_regression_api.py:5072-5086` assert persisted recovery data on both success and failure paths. |
| 5 | When the second attempt fails, the task is classified as a cloud file-access failure instead of a generic Bottle 2.0 outage. | ✓ VERIFIED | `app/services/lesson_command_service.py:612-626` forwards `DASHSCOPE_FILE_ACCESS_FORBIDDEN`; `frontend/src/features/upload/asrStrategy.js:232-237` normalizes it to `CLOUD_FILE_ACCESS_FORBIDDEN`; `tests/contracts/test_desktop_runtime_contract.py:584-624` covers direct and nested payloads. |
| 6 | The frontend maps the exhausted file-access failure to a specific Bottle 2.0 message while leaving successful hidden recovery invisible to the user. | ✓ VERIFIED | `frontend/src/features/upload/asrStrategy.js:290-294` returns the dedicated copy; `frontend/src/features/upload/UploadPanel.jsx:382-398` and `frontend/src/features/upload/UploadPanel.jsx:4538-4547` preserve structured backend payloads when building the message; the success-path integration test at `tests/integration/test_regression_api.py:4965-4975` confirms no failed user state leaks through. |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `app/services/lesson_service.py` | One-time `FILE_403_FORBIDDEN` self-heal for `generate_from_dashscope_file_id()` | ✓ VERIFIED | Retries once with a fresh signed URL, preserves `dashscope_file_id`, and emits `DASHSCOPE_FILE_ACCESS_FORBIDDEN` on exhaustion. |
| `tests/unit/test_lesson_service_dashscope_url.py` | Unit coverage for retry success and retry exhaustion | ✓ VERIFIED | Focused retry tests passed: `pytest tests/unit/test_lesson_service_dashscope_url.py -k "dashscope_403 or file_access_retry" -q` -> `2 passed`. |
| `tests/integration/test_regression_api.py` | Regression coverage for transparent direct-upload retry behavior and persisted recovery artifacts | ✓ VERIFIED | Success and exhaustion task regressions passed: `pytest tests/integration/test_regression_api.py -k "dashscope_403 or file_access_retry or dashscope_recovery" -q` -> `2 passed`. |
| `app/services/lesson_command_service.py` | Extraction and forwarding of `dashscope_recovery` debug metadata | ✓ VERIFIED | Pulls recovery metadata from lesson/task checkpoint and forwards it into `mark_task_succeeded()` / `mark_task_failed()`. |
| `app/services/lesson_task_manager.py` | Persistence of `dashscope_recovery` into task artifacts and failure debug payloads without breaking shared task failure handling | ✓ VERIFIED | `mark_task_failed()` now builds failure payloads without the prior undefined variable regression and stores recovery metadata only when present. |
| `frontend/src/features/upload/asrStrategy.js` | First-class cloud file-access classification and user-facing copy | ✓ VERIFIED | Recognizes backend error code and nested `FILE_403_FORBIDDEN` detail, returns dedicated message. |
| `frontend/src/features/upload/UploadPanel.jsx` | Structured backend error payload preserved into frontend mapper | ✓ VERIFIED | `getCloudFailureMessage()` passes `error_code`, `message`, and `detail` through to `mapCloudAsrFailureToMessage()`. |
| `tests/contracts/test_desktop_runtime_contract.py` | Contract coverage for frontend cloud-file failure mapping | ✓ VERIFIED | Contract test passed: `pytest tests/contracts/test_desktop_runtime_contract.py -k "asr_strategy and file_access" -q` -> `1 passed`. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `app/services/lesson_service.py` | `app/infra/dashscope_storage.py` | `_resolve_dashscope_asr_source_url()` calling `get_file_signed_url()` twice on the same `dashscope_file_id` | ✓ VERIFIED | Retry branch at `app/services/lesson_service.py:2763-2766`; integration test asserts two signed URL resolutions for the same file ID. |
| `app/services/lesson_service.py` | `app/services/asr_dashscope.py` | `transcribe_signed_url()` retry branch guarded by `ASR_TASK_FAILED` + `FILE_403_FORBIDDEN` | ✓ VERIFIED | Guard at `app/services/lesson_service.py:2750-2753`; retry and dedicated exhaustion mapping at `app/services/lesson_service.py:2767-2783`. |
| `app/services/lesson_command_service.py` | `app/services/lesson_task_manager.py` | `mark_task_failed()` and `mark_task_succeeded()` forwarding `dashscope_recovery` | ✓ VERIFIED | Success forwarding at `app/services/lesson_command_service.py:525-550`; failure forwarding at `app/services/lesson_command_service.py:612-626`. |
| `frontend/src/features/upload/asrStrategy.js` | Backend error payloads | Parsing `errorCode`, `detail`, and nested JSON fields for `DASHSCOPE_FILE_ACCESS_FORBIDDEN` and `FILE_403_FORBIDDEN` | ✓ VERIFIED | Parsing logic at `frontend/src/features/upload/asrStrategy.js:194-237`, invoked from `frontend/src/features/upload/UploadPanel.jsx:382-398`. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `app/services/lesson_service.py` | `dashscope_recovery` | `AsrError.detail` from `transcribe_signed_url()` plus fresh signed URL lookup from `get_file_signed_url()` | Yes | ✓ FLOWING |
| `app/services/lesson_command_service.py` | `dashscope_recovery` | `lesson.task_result_meta["dashscope_recovery"]`, `subtitle_cache_seed["dashscope_recovery"]`, or parsed `AsrError.detail` | Yes | ✓ FLOWING |
| `app/services/lesson_task_manager.py` | `failure_debug_json["dashscope_recovery"]` / `artifacts_json["dashscope_recovery"]` | `mark_task_failed()` and `mark_task_succeeded()` persistence | Yes | ✓ FLOWING |
| `frontend/src/features/upload/asrStrategy.js` | Normalized cloud error code and message | Structured backend `error_code`, `message`, and `detail` from `UploadPanel` | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Shared task failure recording still works after the recovery-persistence changes | `pytest tests/integration/test_lesson_task_recovery.py -k "mark_task_failed or recovery" -q` | `5 passed` | ✓ PASS |
| Backend retry contract still covers service-level 403 retry success and exhaustion | `pytest tests/unit/test_lesson_service_dashscope_url.py -k "dashscope_403 or file_access_retry" -q` | `2 passed, 3 deselected` | ✓ PASS |
| Task API still hides the first 403, persists recovery data, and reports exhausted retries correctly | `pytest tests/integration/test_regression_api.py -k "dashscope_403 or file_access_retry or dashscope_recovery" -q` | `2 passed, 110 deselected` | ✓ PASS |
| Frontend error mapping still classifies exhausted file access failures correctly | `pytest tests/contracts/test_desktop_runtime_contract.py -k "asr_strategy and file_access" -q` | `1 passed, 22 deselected` | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| `WEB-01` | `01.1-01`, `01.1-02` | Web users can upload local media and generate lessons through Bottle 2.0 when the browser/runtime path supports it | ✓ SATISFIED | Direct-upload retry success remains on the Bottle 2.0 task flow and returns a succeeded lesson/task state in `tests/integration/test_regression_api.py:4800-5007`. |
| `WEB-03` | `01.1-01`, `01.1-02` | Web generation avoids turning the central server into the default heavy media-processing bottleneck | ✓ SATISFIED | Integration regression explicitly asserts no saved-file fallback or local-ASR fallback route is invoked in `tests/integration/test_regression_api.py:4825-4834` and `tests/integration/test_regression_api.py:4984`. |
| `DESK-02` | `01.1-01`, `01.1-02` | Desktop users can generate lessons with Bottle 2.0 cloud ASR from the same product surface | ✓ SATISFIED | Runtime-neutral frontend mapping for Bottle 2.0 cloud-file failures is covered by `frontend/src/features/upload/asrStrategy.js:232-294` and `tests/contracts/test_desktop_runtime_contract.py:584-624`. |

No orphaned requirements were identified for Phase `01.1`; the phase plans and requested requirement IDs align on `WEB-01`, `WEB-03`, and `DESK-02`.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| None | - | No blocking anti-patterns found in the scanned phase files | - | No blocker or warning-level stub patterns were found in the verified phase artifacts. |

### Human Verification Required

None.

### Gaps Summary

The previous blocker is closed. `mark_task_failed()` in `app/services/lesson_task_manager.py` no longer references an undefined local when `dashscope_recovery` is absent, and the shared task recovery/failure regression suite now passes.

The original Phase `01.1` goal is achieved in the codebase: the direct-upload DashScope path self-heals once on `FILE_403_FORBIDDEN`, exhausted retries are persisted and classified as a first-class cloud file-access failure, and the frontend maps that condition to the intended Bottle 2.0 message without surfacing the hidden first failure.

---

_Verified: 2026-03-26T16:22:49.4913750Z_  
_Verifier: Claude (gsd-verifier)_
