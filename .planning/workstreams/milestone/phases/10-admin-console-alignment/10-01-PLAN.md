---
phase: 10-admin-console-alignment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/AdminApp.jsx
  - frontend/src/shared/lib/adminSearchParams.js
  - frontend/src/features/admin-workspaces/AdminMonitoringWorkspace.jsx
  - frontend/src/features/admin-pages/AdminSecurityPage.jsx
  - tests/integration/test_admin_console_api.py
autonomous: true
requirements:
  - ADM-03
must_haves:
  truths:
    - "管理台一级入口收口为 `用户运营 / 活动兑换 / 排障中心` 三条主线"
    - "旧 `/admin/security` 深链继续可访问，但语义上并入排障中心而不是维持独立一级导航"
    - "页面可见表面保持中文优先，URL 和 query 深链兼容契约不强行重写"
  artifacts:
    - path: frontend/src/shared/lib/adminSearchParams.js
      provides: Canonical admin nav definition and deep-link resolution
      contains: "排障中心"
    - path: frontend/src/AdminApp.jsx
      provides: Top-level routing and compatibility redirects
      contains: "troubleshooting"
    - path: frontend/src/features/admin-workspaces/AdminMonitoringWorkspace.jsx
      provides: Troubleshooting workspace that can absorb security semantics
      contains: "排障中心"
  key_links:
    - "Phase 10 context already locked the rule that visible Chinese structure changes can happen without replacing `/admin/...` routes"
    - "`/admin/security` must remain a compatible entry even if the primary nav no longer lists 安全中心 as a separate top-level item"
---

<objective>
Collapse the admin console’s top-level information architecture into three Chinese-first operator entry points while preserving legacy deep links and routing compatibility.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/REQUIREMENTS.md
@.planning/workstreams/milestone/STATE.md
@.planning/workstreams/milestone/phases/10-admin-console-alignment/10-CONTEXT.md
@frontend/src/AdminApp.jsx
@frontend/src/shared/lib/adminSearchParams.js
@frontend/src/features/admin-workspaces/AdminMonitoringWorkspace.jsx
@frontend/src/features/admin-pages/AdminSecurityPage.jsx
@tests/integration/test_admin_console_api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 收口一级导航并保留旧深链兼容</name>
  <files>
    - frontend/src/shared/lib/adminSearchParams.js
    - frontend/src/AdminApp.jsx
  </files>
  <read_first>
    - frontend/src/shared/lib/adminSearchParams.js
    - frontend/src/AdminApp.jsx
    - .planning/workstreams/milestone/phases/10-admin-console-alignment/10-CONTEXT.md
    - .planning/workstreams/milestone/ROADMAP.md
  </read_first>
  <action>
    Update the canonical admin navigation definition and route entry logic so the visible top-level structure becomes exactly three items:
    - `用户运营`
    - `活动兑换`
    - `排障中心`

    Keep these compatibility rules:
    - `/admin/users` still resolves to `用户运营`
    - `/admin/redeem` still resolves to `活动兑换`
    - `/admin/troubleshooting`, `/admin/health`, and `/admin/security` all resolve to `排障中心`
    - `/admin/security` remains a valid route target, but it must no longer produce a standalone top-level navigation item

    Keep the Chinese-first visible label and description text in sync with the Phase 10 context, but do not force a URL rename or query-parameter rewrite.
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/shared/lib/adminSearchParams.js` contains `用户运营`
    - `frontend/src/shared/lib/adminSearchParams.js` contains `活动兑换`
    - `frontend/src/shared/lib/adminSearchParams.js` contains `排障中心`
    - `frontend/src/shared/lib/adminSearchParams.js` contains no `安全中心`
    - `frontend/src/shared/lib/adminSearchParams.js` contains `pathname.startsWith("/admin/security")`
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>一级导航已经收口成三条主线，同时旧安全页深链仍然能落到排障语义下。</done>
</task>

<task type="auto">
  <name>Task 2: 把安全能力并入排障中心表面语义</name>
  <files>
    - frontend/src/features/admin-workspaces/AdminMonitoringWorkspace.jsx
    - frontend/src/features/admin-pages/AdminSecurityPage.jsx
    - tests/integration/test_admin_console_api.py
  </files>
  <read_first>
    - frontend/src/features/admin-workspaces/AdminMonitoringWorkspace.jsx
    - frontend/src/features/admin-pages/AdminSecurityPage.jsx
    - tests/integration/test_admin_console_api.py
    - .planning/workstreams/milestone/phases/10-admin-console-alignment/10-CONTEXT.md
  </read_first>
  <action>
    Adjust the troubleshooting workspace and security page wording so the product surface reads as one merged operator concern rather than two unrelated top-level systems.

    Implement the visible-surface changes with these outcomes:
    - troubleshooting copy explicitly covers health, task failures, operations, and security maintenance together
    - the security page header and helper text describe itself as part of `排障中心`
    - legacy `/admin/security` remains available, but its own copy should stop implying it is a separate first-level area

    Add or update an integration assertion in `tests/integration/test_admin_console_api.py` only where it helps lock the compatibility contract; do not invent a brittle frontend snapshot test in Python.
  </action>
  <verify>
    <automated>python -m pytest tests/integration/test_admin_console_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/features/admin-workspaces/AdminMonitoringWorkspace.jsx` contains `排障中心`
    - `frontend/src/features/admin-workspaces/AdminMonitoringWorkspace.jsx` contains `安全`
    - `frontend/src/features/admin-pages/AdminSecurityPage.jsx` contains `排障中心`
    - `frontend/src/features/admin-pages/AdminSecurityPage.jsx` contains no `Security Center`
    - `python -m pytest tests/integration/test_admin_console_api.py -q` exits 0
  </acceptance_criteria>
  <done>安全页和排障中心已经在产品语义上合并，兼容入口仍然可用。</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm --prefix frontend run build` exits 0
2. `python -m pytest tests/integration/test_admin_console_api.py -q` exits 0
3. Admin top-level navigation visibly exposes only three operator entry points
</verification>

<success_criteria>
- ADM-03 is met through a Chinese-first three-entry structure
- `/admin/security` remains compatible without staying as a separate top-level nav item
- Troubleshooting and security now read as one operator workflow
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/10-admin-console-alignment/10-01-SUMMARY.md`
</output>
