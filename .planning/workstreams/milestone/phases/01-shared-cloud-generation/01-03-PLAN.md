---
phase: 01-shared-cloud-generation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - frontend/src/shared/api/client.js
  - frontend/src/app/authStorage.js
  - frontend/src/features/upload/UploadPanel.jsx
  - frontend/src/features/upload/asrStrategy.js
  - tests/e2e/test_e2e_key_flows.py
  - tests/integration/test_regression_api.py
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, BILL-01]
user_setup: []
must_haves:
  truths:
    - Bottle 2.0 generation entrypoints fail clearly when auth is missing or expired instead of silently submitting broken requests.
    - Desktop and web session handling remain compatible with the shared cloud task flow.
    - Insufficient balance and task-recovery states are surfaced consistently in the Bottle 2.0 path.
  artifacts:
    - frontend/src/shared/api/client.js
    - frontend/src/app/authStorage.js
    - frontend/src/features/upload/UploadPanel.jsx
    - frontend/src/features/upload/asrStrategy.js
    - tests/e2e/test_e2e_key_flows.py
  key_links:
    - `shared/api/client.js` auth/error handling integrates with `authStorage.js` restore/clear behavior.
    - `UploadPanel.jsx` uses the shared auth/balance failure messages instead of ad hoc Bottle 2.0 branches.
---

<objective>
Harden auth, balance, and task-recovery guardrails around the shared Bottle 2.0 generation flow.

Purpose: Prevent shared cloud generation from feeling unreliable because of avoidable auth/session/billing edge cases.
Output: Clear re-login behavior, consistent insufficient-balance messaging, and stable task recovery for Bottle 2.0 entrypoints.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-cloud-generation/01-CONTEXT.md
@.planning/phases/01-shared-cloud-generation/01-RESEARCH.md
@.planning/phases/01-shared-cloud-generation/01-02-SUMMARY.md
@frontend/src/shared/api/client.js
@frontend/src/app/authStorage.js
@frontend/src/features/upload/UploadPanel.jsx
@frontend/src/features/upload/asrStrategy.js
@tests/e2e/test_e2e_key_flows.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make Bottle 2.0 auth/session failures explicit across web and desktop</name>
  <files>frontend/src/shared/api/client.js, frontend/src/app/authStorage.js, frontend/src/features/upload/UploadPanel.jsx</files>
  <read_first>frontend/src/shared/api/client.js, frontend/src/app/authStorage.js, frontend/src/features/upload/UploadPanel.jsx, .planning/phases/01-shared-cloud-generation/01-CONTEXT.md</read_first>
  <action>Update the shared request stack so Bottle 2.0 generation entrypoints cannot fail silently on missing or expired auth. In `frontend/src/shared/api/client.js`, preserve 401/403 propagation and ensure generation requests can trigger explicit auth failure handling. In `frontend/src/app/authStorage.js`, keep desktop cached-session restore/clear behavior aligned with the shared cloud flow. In `frontend/src/features/upload/UploadPanel.jsx`, add or refine the Bottle 2.0 path so a missing token, expired session, or rejected auth state leads to a clear re-login or auth-recovery message instead of a generic network failure. Do not create a separate desktop-only auth flow; keep the shared renderer logic authoritative.</action>
  <verify>pytest tests/e2e/test_e2e_key_flows.py -q</verify>
  <acceptance_criteria>
    - `frontend/src/shared/api/client.js` contains auth-error handling for 401/403 responses.
    - `frontend/src/app/authStorage.js` contains desktop cache restore and clear paths used by the shared auth flow.
    - `frontend/src/features/upload/UploadPanel.jsx` contains a Bottle 2.0 auth-failure branch that is not just a generic network-error message.
  </acceptance_criteria>
  <done>Bottle 2.0 users get explicit auth/session recovery behavior in both web and desktop runs.</done>
</task>

<task type="auto">
  <name>Task 2: Align balance messaging and task recovery for Bottle 2.0</name>
  <files>frontend/src/features/upload/UploadPanel.jsx, frontend/src/features/upload/asrStrategy.js, tests/integration/test_regression_api.py</files>
  <read_first>frontend/src/features/upload/UploadPanel.jsx, frontend/src/features/upload/asrStrategy.js, tests/integration/test_regression_api.py, .planning/REQUIREMENTS.md</read_first>
  <action>Refine Bottle 2.0 failure mapping so insufficient balance and recoverable task states are communicated consistently. In `frontend/src/features/upload/asrStrategy.js`, keep `INSUFFICIENT_BALANCE`, cloud-unavailable, timeout, and invalid-model cases mapped to user-facing Bottle 2.0 copy that matches the shared runtime language. In `frontend/src/features/upload/UploadPanel.jsx`, ensure task creation, poll/retry, and resume/terminate paths preserve task IDs and show the same user-facing state model after transient failures. Update focused regression coverage in `tests/integration/test_regression_api.py` for Bottle 2.0 task creation/resume flows or related billing/task branches so the shared cloud task path remains stable.</action>
  <verify>pytest tests/integration/test_regression_api.py -k "resume or terminate or task or qwen3" -q</verify>
  <acceptance_criteria>
    - `frontend/src/features/upload/asrStrategy.js` contains explicit `INSUFFICIENT_BALANCE` Bottle 2.0 mapping.
    - `frontend/src/features/upload/UploadPanel.jsx` contains Bottle 2.0 task resume/retry handling that preserves task IDs and shared status flow.
    - `tests/integration/test_regression_api.py` targeted Bottle 2.0 task assertions exit 0.
  </acceptance_criteria>
  <done>Auth/balance/recovery edge cases no longer make the shared Bottle 2.0 flow feel inconsistent.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/e2e/test_e2e_key_flows.py -q`
- [ ] `pytest tests/integration/test_regression_api.py -k "resume or terminate or task or qwen3" -q`
- [ ] Bottle 2.0 auth/balance/recovery failures are explicit and aligned across web and desktop
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Shared Bottle 2.0 generation is guarded by clear auth, balance, and recovery behavior
</success_criteria>

<output>
After completion, create `.planning/phases/01-shared-cloud-generation/01-03-SUMMARY.md`
</output>
