---
phase: 04-desktop-link-import
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/upload/UploadPanel.jsx
  - desktop-client/electron/main.mjs
  - app/api/routers/desktop_asr.py
  - app/infra/runtime_tools.py
  - tests/unit/test_desktop_local_asr.py
  - tests/contracts/test_desktop_runtime_contract.py
autonomous: true
requirements:
  - DESK-04
must_haves:
  truths:
    - "Upload area exposes always-visible `本地文件` / `链接导入` source tabs"
    - "Link import sanitizes noisy pasted text, keeps the first valid URL, and starts the whole pipeline on one click"
    - "Desktop helper URL import supports common video page links or fails with explicit local-first fallback messaging"
    - "Failure preserves the cleaned link input and exposes consistent clickable `SnapAny` behavior"
  artifacts:
    - path: frontend/src/features/upload/UploadPanel.jsx
      provides: Link import source tabs and primary CTA flow
      contains: '导入并生成课程'
    - path: app/api/routers/desktop_asr.py
      provides: Desktop URL import task API
      contains: '/url-import/tasks'
    - path: tests/unit/test_desktop_local_asr.py
      provides: URL import runtime coverage
      contains: 'url-import/tasks'
---

<objective>
Harden the desktop link-import runtime and upload entry UX so one-link common video page imports work through the bundled desktop toolchain with the exact sanitation, fallback, and interruption behavior agreed in discuss-phase.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@frontend/src/features/upload/UploadPanel.jsx
@desktop-client/electron/main.mjs
@app/api/routers/desktop_asr.py
@app/infra/runtime_tools.py
@tests/unit/test_desktop_local_asr.py
@tests/contracts/test_desktop_runtime_contract.py
@tests/contracts/test_desktop_installer_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encode the agreed link-import UX in the upload surface</name>
  <files>
    - frontend/src/features/upload/UploadPanel.jsx
  </files>
  <read_first>
    - frontend/src/features/upload/UploadPanel.jsx
    - .planning/workstreams/milestone/phases/04-desktop-link-import/04-CONTEXT.md
  </read_first>
  <action>
    In `frontend/src/features/upload/UploadPanel.jsx`, implement the Phase 04 link-import UI behavior exactly as decided:

    - always-visible source tabs for `本地文件` and `链接导入`
    - the `链接导入` primary CTA text must be `导入并生成课程`
    - clicking the CTA immediately runs the full chain with no extra confirmation step
    - sanitize pasted share text automatically, keeping the first valid URL
    - keep the cleaned link in the input after failure for retry
    - keep `SnapAny` visible as a secondary fallback on both web and desktop
    - every visible `SnapAny` word must copy the fallback URL and then attempt to open the site
    - invalid-link wording must include `未识别到可导入链接。`
    - restriction/login failures must show a dedicated message and recommend SnapAny
    - switching to `本地文件` during active import must show a confirmation with `继续后台执行` and `取消当前链接任务`
    - title edits during link import must remain available and update the current import flow state
  </action>
  <acceptance_criteria>
    - `frontend/src/features/upload/UploadPanel.jsx` contains `本地文件` and `链接导入`
    - `frontend/src/features/upload/UploadPanel.jsx` contains the exact text `导入并生成课程`
    - `frontend/src/features/upload/UploadPanel.jsx` contains `未识别到可导入链接`
    - `frontend/src/features/upload/UploadPanel.jsx` contains `继续后台执行` and `取消当前链接任务`
    - `frontend/src/features/upload/UploadPanel.jsx` contains `SnapAny`
  </acceptance_criteria>
  <verify>
    <automated>rg -n '本地文件|链接导入|导入并生成课程|未识别到可导入链接|继续后台执行|取消当前链接任务|SnapAny' frontend/src/features/upload/UploadPanel.jsx</automated>
  </verify>
  <done>The upload surface now matches the agreed source-tab, sanitation, fallback, and interruption behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade the helper/runtime path for real-world page-link import</name>
  <files>
    - app/api/routers/desktop_asr.py
    - app/infra/runtime_tools.py
    - desktop-client/electron/main.mjs
  </files>
  <read_first>
    - app/api/routers/desktop_asr.py
    - app/infra/runtime_tools.py
    - desktop-client/electron/main.mjs
    - tests/unit/test_desktop_local_asr.py
    - tests/contracts/test_desktop_runtime_contract.py
    - tests/contracts/test_desktop_installer_contract.py
  </read_first>
  <action>
    In `app/api/routers/desktop_asr.py`, evolve the URL import path so it can support common public video page links through the bundled desktop-local toolchain rather than assuming direct HTTP media URLs only.

    Keep the current task model:
    - `POST /api/desktop-asr/url-import/tasks`
    - `GET /api/desktop-asr/url-import/tasks/{task_id}`
    - `POST /api/desktop-asr/url-import/tasks/{task_id}/cancel`
    - `GET /api/desktop-asr/url-import/tasks/{task_id}/file`

    Concrete behavior:
    - accept exactly one `source_url`
    - use runtime-resolved `yt-dlp` support where available for page-link resolution/download
    - classify invalid URL, unsupported link, restriction/login cases, and cancellation distinctly
    - keep the downloaded file handoff through `/file`

    In `app/infra/runtime_tools.py`, keep `yt-dlp` resolution as the canonical runtime helper source of truth.

    In `desktop-client/electron/main.mjs`, keep the renderer-to-helper bridge aligned with the URL import task endpoints and ensure the main process remains the single bridge for this flow.
  </action>
  <acceptance_criteria>
    - `app/api/routers/desktop_asr.py` still contains `/url-import/tasks`, `/cancel`, and `/file`
    - the URL import path no longer assumes only direct HTTP file URLs
    - `app/infra/runtime_tools.py` still resolves `yt-dlp`
    - `desktop-client/electron/main.mjs` still references `"/api/desktop-asr/url-import"`
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/unit/test_desktop_local_asr.py tests/contracts/test_desktop_runtime_contract.py tests/contracts/test_desktop_installer_contract.py -q</automated>
  </verify>
  <done>The helper/runtime path can now support real-world desktop page-link import without server fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Lock sanitation and failure rules with tests</name>
  <files>
    - tests/unit/test_desktop_local_asr.py
    - tests/contracts/test_desktop_runtime_contract.py
  </files>
  <read_first>
    - tests/unit/test_desktop_local_asr.py
    - tests/contracts/test_desktop_runtime_contract.py
    - .planning/workstreams/milestone/phases/04-desktop-link-import/04-CONTEXT.md
  </read_first>
  <action>
    Extend tests to lock the agreed product rules:

    In `tests/unit/test_desktop_local_asr.py`, add or update coverage for:
    - sanitation of noisy pasted input
    - first-valid-URL extraction
    - invalid-link wording `未识别到可导入链接。`
    - login/platform restriction messaging
    - preserved retry input behavior

    In `tests/contracts/test_desktop_runtime_contract.py`, add or update assertions covering:
    - desktop link-import bridge behavior
    - continued presence of packaged/runtime URL import support
    - upload-surface references to the agreed desktop link import flow
  </action>
  <acceptance_criteria>
    - `tests/unit/test_desktop_local_asr.py` contains sanitation and invalid-link coverage
    - `tests/unit/test_desktop_local_asr.py` contains login/platform restriction coverage
    - `tests/contracts/test_desktop_runtime_contract.py` contains desktop URL import bridge assertions
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/unit/test_desktop_local_asr.py tests/contracts/test_desktop_runtime_contract.py -q</automated>
  </verify>
  <done>Tests now lock the Phase 04 runtime and UX rules before canonical handoff work begins.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pytest tests/unit/test_desktop_local_asr.py -q` exits 0
2. `pytest tests/contracts/test_desktop_runtime_contract.py tests/contracts/test_desktop_installer_contract.py -q` exits 0
3. `npm --prefix frontend run build` exits 0
4. Desktop upload UI shows always-visible source tabs and the `导入并生成课程` CTA
</verification>

<success_criteria>
- Desktop link import can handle real user-pasted page links through the local runtime
- Upload UI matches the exact agreed source-tab, sanitation, fallback, and interruption rules
- Failure and retry behavior are locked in tests
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/04-desktop-link-import/04-01-SUMMARY.md`
</output>
