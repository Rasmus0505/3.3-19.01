---
phase: 04-desktop-link-import
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - frontend/src/features/upload/UploadPanel.jsx
  - frontend/src/store/slices/lessonSlice.ts
  - frontend/src/app/learning-shell/LearningShellContainer.jsx
  - frontend/src/features/lessons/LessonList.jsx
  - tests/integration/test_regression_api.py
  - tests/e2e/test_e2e_key_flows.py
autonomous: true
requirements:
  - DESK-04
must_haves:
  truths:
    - "Imported link media enters the same canonical lesson/history/learning flow as file uploads"
    - "Parsed titles and in-progress title edits become the final generated lesson title"
    - "Successful link import routes directly into learning"
    - "Learner flow still does not expose source-specific identity after lesson creation"
  artifacts:
    - path: frontend/src/features/upload/UploadPanel.jsx
      provides: Successful link-import handoff into lesson flow
      contains: 'onNavigateToLesson'
    - path: frontend/src/features/lessons/LessonList.jsx
      provides: Canonical post-generation history flow
      not_contains: '本地课程'
    - path: tests/e2e/test_e2e_key_flows.py
      provides: End-to-end learner flow coverage
      contains: 'progress'
---

<objective>
Feed imported desktop link media into the same canonical lesson, history, and learning pipeline with title propagation, direct-to-learning success navigation, and verification that Phase 03’s learner-flow cleanup remains intact.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@frontend/src/features/upload/UploadPanel.jsx
@frontend/src/store/slices/lessonSlice.ts
@frontend/src/app/learning-shell/LearningShellContainer.jsx
@frontend/src/features/lessons/LessonList.jsx
@app/api/routers/lessons/router.py
@app/services/lesson_query_service.py
@tests/integration/test_regression_api.py
@tests/e2e/test_e2e_key_flows.py
@.planning/workstreams/milestone/phases/03-lesson-output-consistency/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Connect link-import success to the canonical lesson and direct-learning flow</name>
  <files>
    - frontend/src/features/upload/UploadPanel.jsx
    - frontend/src/store/slices/lessonSlice.ts
    - frontend/src/app/learning-shell/LearningShellContainer.jsx
  </files>
  <read_first>
    - frontend/src/features/upload/UploadPanel.jsx
    - frontend/src/store/slices/lessonSlice.ts
    - frontend/src/app/learning-shell/LearningShellContainer.jsx
    - .planning/workstreams/milestone/phases/03-lesson-output-consistency/03-CONTEXT.md
  </read_first>
  <action>
    Extend successful link import so it behaves like the agreed Phase 04 product flow:

    - imported media lands in the same canonical lesson/task path used by file uploads
    - parsed link title seeds the lesson title when available
    - title edits made during import/generation propagate into the final lesson title
    - on success, route directly into learning
    - preserve the canonical history/detail hydration and no-source-exposure rules from Phase 03
  </action>
  <acceptance_criteria>
    - successful link import routes through existing canonical lesson creation and hydration logic
    - parsed link title can appear as the resulting lesson title
    - in-progress title edits propagate into the final lesson title
    - successful completion triggers direct learning navigation
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'onNavigateToLesson|title|desktopLink|lesson.title|loadLessonDetail|setCurrentLesson' frontend/src/features/upload/UploadPanel.jsx frontend/src/store/slices/lessonSlice.ts frontend/src/app/learning-shell/LearningShellContainer.jsx</automated>
  </verify>
  <done>Imported links now hand off into the same canonical lesson and direct-to-learning flow as other media sources.</done>
</task>

<task type="auto">
  <name>Task 2: Keep post-import history and learner flow source-agnostic</name>
  <files>
    - frontend/src/features/lessons/LessonList.jsx
    - frontend/src/app/learning-shell/LearningShellContainer.jsx
  </files>
  <read_first>
    - frontend/src/features/lessons/LessonList.jsx
    - frontend/src/app/learning-shell/LearningShellContainer.jsx
    - .planning/workstreams/milestone/phases/03-lesson-output-consistency/03-CONTEXT.md
  </read_first>
  <action>
    Ensure imported lessons do not reintroduce source-specific learner UX after lesson creation:

    - history cards stay canonical and source-agnostic
    - no special imported-link learner flow appears once the lesson exists
    - title changes and completion state remain driven by canonical lesson/progress data
    - imported link metadata, if stored, remains supplemental
  </action>
  <acceptance_criteria>
    - `frontend/src/features/lessons/LessonList.jsx` contains no imported-link-specific learner badge or alternate history flow
    - `frontend/src/app/learning-shell/LearningShellContainer.jsx` still routes imported lessons through the same learning shell entry as any other lesson
    - canonical lesson/progress data remains the source of truth for history state
  </acceptance_criteria>
  <verify>
    <automated>rg -n '本地课程|云端课程|imported|link import|desktop link' frontend/src/features/lessons/LessonList.jsx frontend/src/app/learning-shell/LearningShellContainer.jsx</automated>
  </verify>
  <done>Post-import learner flow remains canonical and source-agnostic.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for title propagation, direct learning navigation, and canonical handoff</name>
  <files>
    - tests/integration/test_regression_api.py
    - tests/e2e/test_e2e_key_flows.py
  </files>
  <read_first>
    - tests/integration/test_regression_api.py
    - tests/e2e/test_e2e_key_flows.py
    - frontend/src/features/upload/UploadPanel.jsx
  </read_first>
  <action>
    Extend integration/e2e coverage so Phase 04 locks the handoff behavior:

    In `tests/integration/test_regression_api.py`, add or extend assertions for:
    - imported media workspace/lesson handoff
    - title propagation into the final canonical lesson
    - preserved canonical history fields after import

    In `tests/e2e/test_e2e_key_flows.py`, add or extend an end-to-end learner flow that verifies:
    - a generated lesson can still be opened and progressed after import-style creation
    - the learner lands directly in learning on success
    - progress updates continue to work through the canonical lesson/progress APIs
  </action>
  <acceptance_criteria>
    - `tests/integration/test_regression_api.py` contains assertions for title propagation or workspace-to-lesson handoff
    - `tests/e2e/test_e2e_key_flows.py` contains direct learning / progress continuity assertions
    - imported lessons are validated through the same canonical learner-flow assertions as other lesson sources
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/integration/test_regression_api.py tests/e2e/test_e2e_key_flows.py -k "lesson or workspace or progress" -q</automated>
  </verify>
  <done>Regression coverage now locks imported-link handoff into the canonical lesson and learning flow.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pytest tests/integration/test_regression_api.py -k "desktop or workspace or lesson" -q` exits 0
2. `pytest tests/e2e/test_e2e_key_flows.py -k "lesson or progress" -q` exits 0
3. Successful link import enters learning directly
4. Imported lessons still appear in canonical history/learning flow with no source-specific learner identity
</verification>

<success_criteria>
- Imported desktop links feed into the same canonical lesson flow as file uploads
- Parsed and edited titles survive into the final lesson
- Success navigation goes directly into learning
- Canonical learner flow remains clean after import
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/04-desktop-link-import/04-02-SUMMARY.md`
</output>
