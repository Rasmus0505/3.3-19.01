---
phase: 08-immersive-learning-refactor
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - frontend/src/features/immersive/ImmersiveLessonPage.jsx
  - frontend/src/features/immersive/useImmersiveSessionController.js
  - frontend/src/features/immersive/useSentencePlayback.js
  - frontend/src/features/immersive/immersive.css
autonomous: true
requirements:
  - IMM-04
  - IMM-05
must_haves:
  truths:
    - "Entering/exiting fullscreen, toggling previous-sentence visibility, and toggling/dragging the translation mask no longer reset sentence index, typed input, reveal progress, loop state, or session playback rate"
    - "The previous-sentence area exposes a single right-side speaker button that plays the previous sentence exactly once"
    - "Previous/next sentence navigation hard-interrupts current playback/loop/replay state and auto-plays the target sentence"
    - "Playing the previous sentence hard-interrupts the current sentence and returns to the current sentence idle state when finished"
  artifacts:
    - path: frontend/src/features/immersive/ImmersiveLessonPage.jsx
      provides: Display-only fullscreen/mask interactions and previous-sentence speaker button
      contains: 'aria-label="播放上一句"'
    - path: frontend/src/features/immersive/useImmersiveSessionController.js
      provides: Hard-interrupt navigation / previous-sentence playback orchestration
      contains: "interruptCurrentSentencePlayback"
    - path: frontend/src/features/immersive/immersive.css
      provides: Previous-sentence speaker button styling inside the existing block
      contains: "immersive-previous-sentence"
  key_links:
    - "Phase 8 must remove the current fullscreen-side reset `setShowFullscreenPreviousSentence(false)` from the user flow"
    - "Display-layer actions may mutate UI preferences but may not call sentence-reset logic such as `resetWordTyping()` or force a new sentence index"
---

<objective>
Stabilize fullscreen, previous-sentence, and translation-mask interactions so they become pure display-layer controls, and add the locked previous-sentence speaker behavior without reintroducing state conflicts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/08-immersive-learning-refactor/08-CONTEXT.md
@.planning/workstreams/milestone/phases/08-immersive-learning-refactor/08-RESEARCH.md
@frontend/src/features/immersive/ImmersiveLessonPage.jsx
@frontend/src/features/immersive/useImmersiveSessionController.js
@frontend/src/features/immersive/useSentencePlayback.js
@frontend/src/features/immersive/immersive.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make fullscreen and translation-mask interactions display-only</name>
  <files>
    - frontend/src/features/immersive/ImmersiveLessonPage.jsx
    - frontend/src/features/immersive/useImmersiveSessionController.js
  </files>
  <read_first>
    - frontend/src/features/immersive/ImmersiveLessonPage.jsx
    - frontend/src/features/immersive/useImmersiveSessionController.js
    - .planning/workstreams/milestone/phases/08-immersive-learning-refactor/08-CONTEXT.md
  </read_first>
  <action>
    Refactor fullscreen, previous-sentence visibility, and translation-mask interactions so they only mutate display-layer preferences and never reset immersive learning state.

    Concrete fixes:
    - remove the current fullscreen-entry reset `setShowFullscreenPreviousSentence(false)`
    - ensure fullscreen enter/exit, translation-mask toggle, and translation-mask drag do not call sentence-reset logic or overwrite the active sentence/session state
    - keep `showFullscreenPreviousSentence`, `translationMask.enabled`, and translation-mask geometry persisted through the existing learning-settings path
    - keep the current session playback rate and loop state intact across all of these display-layer interactions
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/features/immersive/ImmersiveLessonPage.jsx` no longer contains `setShowFullscreenPreviousSentence(false)`
    - `frontend/src/features/immersive/ImmersiveLessonPage.jsx` still calls `persistFullscreenPreviousSentencePreference` and `persistTranslationMaskPreference`
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>Fullscreen and translation-mask controls are now display-only and preserve learning state.</done>
</task>

<task type="auto">
  <name>Task 2: Add the previous-sentence speaker button and hard-interrupt playback rules</name>
  <files>
    - frontend/src/features/immersive/ImmersiveLessonPage.jsx
    - frontend/src/features/immersive/useImmersiveSessionController.js
    - frontend/src/features/immersive/useSentencePlayback.js
    - frontend/src/features/immersive/immersive.css
  </files>
  <read_first>
    - frontend/src/features/immersive/ImmersiveLessonPage.jsx
    - frontend/src/features/immersive/useImmersiveSessionController.js
    - frontend/src/features/immersive/useSentencePlayback.js
    - .planning/workstreams/milestone/phases/08-immersive-learning-refactor/08-CONTEXT.md
  </read_first>
  <action>
    Rework previous-sentence and sentence-navigation behavior to match the locked contract.

    Concrete behavior:
    - add a single right-side speaker button in the previous-sentence block with `aria-label="播放上一句"`
    - clicking that button must hard-interrupt the current sentence playback / pause / loop / answer-completed replay state
    - play the previous sentence exactly once at the currently selected fixed playback rate
    - when previous-sentence playback finishes, return to the current sentence idle state without auto-resuming current playback or loop
    - previous/next sentence navigation must use the same hard-interrupt helper and auto-play the target sentence immediately

    Implement shared helpers named `interruptCurrentSentencePlayback` and `requestPlayPreviousSentence` so the behavior is not duplicated across speaker-button and previous/next paths.
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/features/immersive/ImmersiveLessonPage.jsx` contains `aria-label="播放上一句"`
    - `frontend/src/features/immersive/useImmersiveSessionController.js` contains `interruptCurrentSentencePlayback`
    - `frontend/src/features/immersive/useImmersiveSessionController.js` contains `requestPlayPreviousSentence`
    - `frontend/src/features/immersive/ImmersiveLessonPage.jsx` contains `requestPlayPreviousSentence(`
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>The previous-sentence block now exposes the required speaker button and shared hard-interrupt playback rules.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm --prefix frontend run build` exits 0
2. `frontend/src/features/immersive/ImmersiveLessonPage.jsx` contains `aria-label="播放上一句"`
3. Fullscreen / previous-sentence / translation-mask interactions preserve current sentence and input state
</verification>

<success_criteria>
- Display-layer interactions no longer reset immersive learning state
- Previous-sentence listening follows the exact hard-interrupt / idle-return contract
- Navigation and previous-sentence playback share one interruption model
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/08-immersive-learning-refactor/08-03-SUMMARY.md`
</output>
