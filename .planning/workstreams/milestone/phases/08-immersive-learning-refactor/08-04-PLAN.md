---
phase: 08-immersive-learning-refactor
plan: "04"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - tests/contracts/test_learning_immersive_contract.py
  - tests/e2e/test_e2e_key_flows.py
  - app/static/index.html
  - app/static/assets/*
autonomous: true
requirements:
  - IMM-01
  - IMM-02
  - IMM-03
  - IMM-04
  - IMM-05
must_haves:
  truths:
    - "Phase 08 has dedicated contract coverage for the extracted state-machine module, loop preference, fixed-speed controls, previous-sentence speaker button, and fullscreen reset guardrail"
    - "Practice-flow smoke coverage still proves lesson progress APIs remain compatible with immersive learning"
    - "Frontend changes are synchronized into `app/static` and the built web bundle still contains the new immersive controls"
  artifacts:
    - path: tests/contracts/test_learning_immersive_contract.py
      provides: Phase 08 immersive source contract coverage
      contains: "singleSentenceLoopEnabled"
    - path: tests/e2e/test_e2e_key_flows.py
      provides: Practice/progress smoke coverage
      contains: "practice_progress"
    - path: app/static/assets/*
      provides: Synced web bundle containing the Phase 08 immersive UI strings
      contains: "单句循环"
  key_links:
    - "The project-level web delivery contract requires `app/static` sync for frontend behavior changes"
    - "Phase 08 verification must cover both source contracts and built static output, not just `frontend/src`"
---

<objective>
Lock Phase 08 behind automated contract coverage and the required `app/static` sync so the new immersive interaction model cannot silently regress in source or in the shipped web bundle.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/phases/08-immersive-learning-refactor/08-CONTEXT.md
@.planning/workstreams/milestone/phases/08-immersive-learning-refactor/08-RESEARCH.md
@.planning/workstreams/milestone/phases/08-immersive-learning-refactor/08-VALIDATION.md
@frontend/src/features/immersive/ImmersiveLessonPage.jsx
@frontend/src/features/immersive/immersiveSessionMachine.js
@frontend/src/features/immersive/learningSettings.js
@tests/contracts/test_desktop_runtime_contract.py
@tests/e2e/test_e2e_key_flows.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dedicated immersive contract regression tests</name>
  <files>
    - tests/contracts/test_learning_immersive_contract.py
  </files>
  <read_first>
    - frontend/src/features/immersive/ImmersiveLessonPage.jsx
    - frontend/src/features/immersive/immersiveSessionMachine.js
    - frontend/src/features/immersive/learningSettings.js
    - tests/contracts/test_desktop_runtime_contract.py
    - .planning/workstreams/milestone/phases/08-immersive-learning-refactor/08-CONTEXT.md
  </read_first>
  <action>
    Create `tests/contracts/test_learning_immersive_contract.py` as the Phase 08 source-level regression suite.

    The contract file must assert all of the following concrete rules:
    - `frontend/src/features/immersive/immersiveSessionMachine.js` exists and exports the new session-state contract
    - `frontend/src/features/immersive/learningSettings.js` contains `singleSentenceLoopEnabled`
    - `frontend/src/features/immersive/ImmersiveLessonPage.jsx` contains the exact strings `单句循环`, `0.75x`, `0.90x`, and `1.00x`
    - `frontend/src/features/immersive/ImmersiveLessonPage.jsx` contains `aria-label="播放上一句"`
    - `frontend/src/features/immersive/ImmersiveLessonPage.jsx` does not contain the fullscreen reset `setShowFullscreenPreviousSentence(false)`

    Reuse the existing contract-test style from `tests/contracts/test_desktop_runtime_contract.py`: read source files and assert exact string-level contracts.
  </action>
  <verify>
    <automated>pytest tests/contracts/test_learning_immersive_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/contracts/test_learning_immersive_contract.py` exists
    - `tests/contracts/test_learning_immersive_contract.py` contains `singleSentenceLoopEnabled`
    - `pytest tests/contracts/test_learning_immersive_contract.py -q` exits 0
  </acceptance_criteria>
  <done>Phase 08 now has dedicated immersive contract tests that lock the key user-facing and source-level decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run practice smoke coverage and sync the web bundle to app/static</name>
  <files>
    - tests/e2e/test_e2e_key_flows.py
    - app/static/index.html
    - app/static/assets/*
  </files>
  <read_first>
    - tests/e2e/test_e2e_key_flows.py
    - .planning/PROJECT.md
    - frontend/package.json
  </read_first>
  <action>
    Keep Phase 08 compliant with the project’s web-delivery rule.

    Concrete work:
    - add or update one focused assertion block in `tests/e2e/test_e2e_key_flows.py` so `test_e2e_login_create_lesson_practice_progress` still covers canonical lesson progress after the immersive refactor
    - run `npm --prefix frontend run build:app-static` so the new immersive UI ships into `app/static`
    - verify the built bundle in `app/static/assets` still contains the new immersive strings `单句循环`, `0.75x`, `0.90x`, and `播放上一句`

    Do not stop at a successful `frontend/src` build; this task is incomplete unless `app/static` is synchronized.
  </action>
  <verify>
    <automated>npm --prefix frontend run build:app-static && pytest tests/e2e/test_e2e_key_flows.py -k "practice_progress or lesson" -q</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix frontend run build:app-static` exits 0
    - `pytest tests/e2e/test_e2e_key_flows.py -k "practice_progress or lesson" -q` exits 0
    - `rg -n '单句循环|0\\.75x|0\\.90x|播放上一句' app/static/assets` returns at least one match
  </acceptance_criteria>
  <done>Phase 08 is regression-covered and the shipped web bundle in `app/static` contains the new immersive controls.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pytest tests/contracts/test_learning_immersive_contract.py -q` exits 0
2. `npm --prefix frontend run build:app-static` exits 0
3. `pytest tests/e2e/test_e2e_key_flows.py -k "practice_progress or lesson" -q` exits 0
4. `rg -n '单句循环|0\.75x|0\.90x|播放上一句' app/static/assets` returns matches
</verification>

<success_criteria>
- Phase 08 user decisions are locked by contract tests
- Practice/progress smoke coverage still passes
- Web static output is synchronized and verifiably contains the new immersive UI
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/08-immersive-learning-refactor/08-04-SUMMARY.md`
</output>
