---
phase: 08-immersive-learning-refactor
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - frontend/src/features/immersive/ImmersiveLessonPage.jsx
  - frontend/src/features/immersive/learningSettings.js
  - frontend/src/features/immersive/useSentencePlayback.js
  - frontend/src/features/immersive/immersive.css
autonomous: true
requirements:
  - IMM-01
  - IMM-02
  - IMM-03
  - IMM-04
must_haves:
  truths:
    - "Single-sentence loop is a persistent user preference and keeps the learner on the current sentence until manually changed"
    - "Only `0.75x`, `0.90x`, and `1.00x` are exposed as playback-rate choices during immersive study"
    - "The selected playback rate is session-scoped: it survives sentence/fullscreen/mask changes in one immersive session, then resets to `1.00x` on exit"
    - "Manual replay, answer-completed replay, and loop playback all use the selected fixed playback rate instead of the old hidden tail-rate ladder"
  artifacts:
    - path: frontend/src/features/immersive/learningSettings.js
      provides: Persistent single-sentence loop preference
      contains: "singleSentenceLoopEnabled"
    - path: frontend/src/features/immersive/ImmersiveLessonPage.jsx
      provides: Cinema answer-board loop and rate controls
      contains: "0.90x"
    - path: frontend/src/features/immersive/useSentencePlayback.js
      provides: Fixed-rate playback execution path
      contains: "initialRate"
  key_links:
    - "`resolveReplayAssistance()` may still drive reveal-letter / reveal-word timing, but not hidden audible playback rates"
    - "Loop/rate controls stay inside the fullscreen answer board and do not create a second non-fullscreen control surface"
---

<objective>
Implement the user-locked single-sentence loop and fixed-speed playback contract on top of the new state-machine foundation, replacing the old hidden replay slowdown model with explicit learner-controlled speeds.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/phases/08-immersive-learning-refactor/08-CONTEXT.md
@.planning/workstreams/milestone/phases/08-immersive-learning-refactor/08-RESEARCH.md
@frontend/src/features/immersive/ImmersiveLessonPage.jsx
@frontend/src/features/immersive/learningSettings.js
@frontend/src/features/immersive/useSentencePlayback.js
@frontend/src/features/immersive/immersiveSessionMachine.js
@frontend/src/features/immersive/useImmersiveSessionController.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist single-sentence loop and formalize session-scoped rate state</name>
  <files>
    - frontend/src/features/immersive/learningSettings.js
    - frontend/src/features/immersive/ImmersiveLessonPage.jsx
    - frontend/src/features/immersive/immersiveSessionMachine.js
  </files>
  <read_first>
    - frontend/src/features/immersive/learningSettings.js
    - frontend/src/features/immersive/ImmersiveLessonPage.jsx
    - .planning/workstreams/milestone/phases/08-immersive-learning-refactor/08-CONTEXT.md
  </read_first>
  <action>
    In `frontend/src/features/immersive/learningSettings.js`, add `playbackPreferences.singleSentenceLoopEnabled` with a default of `false`, and sanitize/read/write it through the existing localStorage settings path.

    In the immersive session layer, add explicit state for:
    - persistent loop-enabled preference sourced from `learningSettings`
    - session-only selected playback rate with exact allowed values `1`, `0.9`, and `0.75`

    The selected playback rate must reset to `1.00x` whenever immersive mode exits, but must survive sentence changes, fullscreen changes, previous-sentence show/hide, and translation-mask interactions inside the current immersive session.
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/features/immersive/learningSettings.js` contains `singleSentenceLoopEnabled`
    - `frontend/src/features/immersive/learningSettings.js` still contains `autoReplayAnsweredSentence`
    - `frontend/src/features/immersive/ImmersiveLessonPage.jsx` contains exact rate values `0.75`, `0.9`, and `1`
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>Loop preference is persisted globally, and rate selection is formalized as session-scoped state.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hidden replay slowdown with explicit fixed-rate playback</name>
  <files>
    - frontend/src/features/immersive/useSentencePlayback.js
    - frontend/src/features/immersive/learningSettings.js
    - frontend/src/features/immersive/useImmersiveSessionController.js
  </files>
  <read_first>
    - frontend/src/features/immersive/useSentencePlayback.js
    - frontend/src/features/immersive/learningSettings.js
    - frontend/src/features/immersive/useImmersiveSessionController.js
    - .planning/workstreams/milestone/phases/08-immersive-learning-refactor/08-RESEARCH.md
  </read_first>
  <action>
    Update playback planning so the audible playback speed always comes from the selected fixed-rate preset.

    Concrete rules:
    - the only learner-visible playback speeds are `0.75x`, `0.90x`, and `1.00x`
    - manual replay, answer-completed replay, single-sentence loop playback, and future previous-sentence speaker playback must all use the selected fixed rate as `initialRate`
    - `resolveReplayAssistance()` may continue to compute reveal-letter / reveal-word counts, but it must stop acting as the primary source of audible `tailRate` values such as `0.95` or `0.85`

    Adjust `useSentencePlayback.js` only as needed to keep `playSentence()` compatible with fixed-rate playback plans.
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/features/immersive/learningSettings.js` contains no `firstReplay: 0.95`
    - `frontend/src/features/immersive/learningSettings.js` contains no `secondReplay: 0.85`
    - `frontend/src/features/immersive/useImmersiveSessionController.js` contains `selectedPlaybackRate`
    - `frontend/src/features/immersive/useSentencePlayback.js` still exports `currentPlaybackRate`
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>Fixed-rate playback now governs immersive listening instead of the old hidden replay slowdown ladder.</done>
</task>

<task type="auto">
  <name>Task 3: Expose loop and rate controls in the fullscreen answer board only</name>
  <files>
    - frontend/src/features/immersive/ImmersiveLessonPage.jsx
    - frontend/src/features/immersive/immersive.css
  </files>
  <read_first>
    - frontend/src/features/immersive/ImmersiveLessonPage.jsx
    - frontend/src/features/immersive/immersive.css
    - .planning/workstreams/milestone/phases/08-immersive-learning-refactor/08-CONTEXT.md
  </read_first>
  <action>
    In `frontend/src/features/immersive/ImmersiveLessonPage.jsx`, add always-visible fullscreen answer-board controls for:
    - a `单句循环` toggle
    - rate buttons labeled exactly `0.75x`, `0.90x`, and `1.00x`

    Place these controls inside the cinema/fullscreen answer-board area rather than a settings page or a separate non-fullscreen control surface. Update `immersive.css` so the controls remain compact and readable inside the fullscreen answer board.

    The loop contract must follow the locked decisions exactly:
    - loop off: answer completion triggers one full current-sentence replay, then auto-advance
    - loop on: stay on the current sentence and keep replaying until the user manually turns loop off or jumps to another sentence
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/features/immersive/ImmersiveLessonPage.jsx` contains the exact text `单句循环`
    - `frontend/src/features/immersive/ImmersiveLessonPage.jsx` contains `0.75x`, `0.90x`, and `1.00x`
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>The fullscreen answer board now exposes the locked loop and fixed-speed controls directly.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm --prefix frontend run build` exits 0
2. Fullscreen answer-board UI exposes `单句循环`, `0.75x`, `0.90x`, and `1.00x`
3. Loop-off and loop-on progression rules match the locked context decisions
</verification>

<success_criteria>
- Single-sentence loop behaves exactly as specified
- Playback-rate choices are fixed, explicit, and session-scoped
- Old hidden replay slowdown behavior no longer overrides learner-selected speed
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/08-immersive-learning-refactor/08-02-SUMMARY.md`
</output>
