---
phase: 08-immersive-learning-refactor
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/immersive/ImmersiveLessonPage.jsx
  - frontend/src/features/immersive/immersiveSessionMachine.js
  - frontend/src/features/immersive/useImmersiveSessionController.js
  - frontend/src/features/immersive/useSentencePlayback.js
autonomous: true
requirements:
  - IMM-04
must_haves:
  truths:
    - "Immersive session state transitions no longer live only as scattered `useState` / `setState` chains inside `ImmersiveLessonPage.jsx`"
    - "A dedicated local state-machine module defines explicit events for lesson load, playback start/finish, answer completion, sentence navigation, and immersive exit"
    - "Keyboard, button, and automatic sentence-advance flows dispatch through shared controller helpers instead of duplicating transition logic"
  artifacts:
    - path: frontend/src/features/immersive/immersiveSessionMachine.js
      provides: Canonical immersive session reducer and event constants
      contains: "PLAYBACK_FINISHED"
    - path: frontend/src/features/immersive/useImmersiveSessionController.js
      provides: Shared session action orchestration
      contains: "requestNavigateSentence"
    - path: frontend/src/features/immersive/ImmersiveLessonPage.jsx
      provides: Reducer-driven immersive page wiring
      contains: "useImmersiveSessionController"
  key_links:
    - "`useSentencePlayback.js` remains the playback execution layer; the new session machine only orchestrates when to call it"
    - "`LearningShellContainer.jsx` stays the shell-level immersive entry point and does not absorb page-internal state"
---

<objective>
Extract the monolithic immersive page into an explicit local session-state model so sentence progression, answer completion, replay, pause/continue, and navigation all share one event contract before new loop/speed/fullscreen rules are added.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/STATE.md
@.planning/workstreams/milestone/phases/08-immersive-learning-refactor/08-CONTEXT.md
@.planning/workstreams/milestone/phases/08-immersive-learning-refactor/08-RESEARCH.md
@frontend/src/features/immersive/ImmersiveLessonPage.jsx
@frontend/src/features/immersive/useSentencePlayback.js
@frontend/src/features/immersive/learningSettings.js
@frontend/src/app/learning-shell/LearningShellPanelContent.jsx
@frontend/src/app/learning-shell/LearningShellContainer.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a canonical immersive session machine module</name>
  <files>
    - frontend/src/features/immersive/immersiveSessionMachine.js
    - frontend/src/features/immersive/ImmersiveLessonPage.jsx
  </files>
  <read_first>
    - frontend/src/features/immersive/ImmersiveLessonPage.jsx
    - .planning/workstreams/milestone/phases/08-immersive-learning-refactor/08-CONTEXT.md
    - .planning/workstreams/milestone/phases/08-immersive-learning-refactor/08-RESEARCH.md
  </read_first>
  <action>
    Create `frontend/src/features/immersive/immersiveSessionMachine.js` as the single source of truth for immersive session state transitions.

    The new module must export:
    - a state factory for the current lesson session
    - explicit event constants / types for `LESSON_LOADED`, `PLAYBACK_STARTED`, `PLAYBACK_FINISHED`, `ANSWER_COMPLETED`, `NAVIGATE_TO_SENTENCE`, `POST_ANSWER_REPLAY_STARTED`, `POST_ANSWER_REPLAY_COMPLETED`, `SET_MEDIA_BINDING_REQUIRED`, and `EXIT_IMMERSIVE`
    - a reducer that owns the current equivalents of `phase`, `currentSentenceIndex`, `completedIndexes`, `sentenceTypingDone`, `sentencePlaybackDone`, `sentencePlaybackRequired`, `postAnswerReplayState`, `translationDisplayMode`, and `sentenceJumpValue`

    Update `frontend/src/features/immersive/ImmersiveLessonPage.jsx` to consume this reducer state instead of keeping those fields as independent `useState` chains.
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/features/immersive/immersiveSessionMachine.js` exists
    - `frontend/src/features/immersive/immersiveSessionMachine.js` contains `LESSON_LOADED`, `PLAYBACK_FINISHED`, and `ANSWER_COMPLETED`
    - `frontend/src/features/immersive/ImmersiveLessonPage.jsx` imports from `./immersiveSessionMachine`
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>A canonical local reducer module now owns immersive session state transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Route navigation, replay, and completion through one controller layer</name>
  <files>
    - frontend/src/features/immersive/useImmersiveSessionController.js
    - frontend/src/features/immersive/ImmersiveLessonPage.jsx
    - frontend/src/features/immersive/useSentencePlayback.js
  </files>
  <read_first>
    - frontend/src/features/immersive/ImmersiveLessonPage.jsx
    - frontend/src/features/immersive/useSentencePlayback.js
    - frontend/src/features/immersive/immersiveSessionMachine.js
  </read_first>
  <action>
    Create `frontend/src/features/immersive/useImmersiveSessionController.js` and move shared transition orchestration into it.

    The controller must provide shared helpers that both keyboard and button flows use:
    - `requestReplayCurrentSentence`
    - `requestTogglePausePlayback`
    - `requestNavigateSentence`
    - `requestRevealLetter`
    - `requestRevealWord`
    - `requestHandleSentencePassed`

    Refactor `frontend/src/features/immersive/ImmersiveLessonPage.jsx` so both the global `window` shortcut listener and the local handler path call the same controller helpers instead of duplicating transition logic.

    Keep `frontend/src/features/immersive/useSentencePlayback.js` as the playback execution layer; only adjust its API surface if the controller needs a cleaner interrupt/start contract.
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/features/immersive/useImmersiveSessionController.js` exists
    - `frontend/src/features/immersive/useImmersiveSessionController.js` contains `requestNavigateSentence` and `requestReplayCurrentSentence`
    - `frontend/src/features/immersive/ImmersiveLessonPage.jsx` contains `useImmersiveSessionController`
    - `frontend/src/features/immersive/ImmersiveLessonPage.jsx` contains `requestReplayCurrentSentence(` and `requestNavigateSentence(`
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>Navigation, replay, reveal, and completion transitions now flow through one shared controller layer.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm --prefix frontend run build` exits 0
2. `frontend/src/features/immersive/immersiveSessionMachine.js` exists and exports reducer events
3. `frontend/src/features/immersive/useImmersiveSessionController.js` exists and is imported by `ImmersiveLessonPage.jsx`
</verification>

<success_criteria>
- Immersive page state transitions are centralized into an explicit local state-machine layer
- Keyboard, button, and auto-advance flows share one controller contract
- The phase foundation is ready for loop / rate / fullscreen rules without adding more ad-hoc state
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/08-immersive-learning-refactor/08-01-SUMMARY.md`
</output>
