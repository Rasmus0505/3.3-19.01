# Phase 14: 桌面程序与模型增量更新产品化 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-01
**Phase:** 14-桌面程序与模型增量更新产品化
**Areas discussed:** 更新入口与可见性, 程序更新健康路径, 模型/资源增量更新策略, 失败恢复与资产边界说明

---

## 更新入口与可见性

| Question | Options presented | Selected |
|--------|-------------|----------|
| 用户主要从哪里看到“有新版本可更新”？ | A. 只在诊断里看 / B. 生成工作台放更新卡片 / C. 启动后非阻塞横幅，诊断里保留详情 | C |
| 更新信息最少显示哪些字段？ | A. 当前版本 + 是否有新版本 / B. 当前版本 + 最新版本 + 发布时间 / C. 当前版本 + 最新版本 + 发布名称或一句更新说明 | C |
| 什么时候检查程序更新？ | A. 启动自动检查，同时保留手动刷新 / B. 只手动检查 / C. 只启动时自动检查 | A |
| 发现新版本后默认怎么提示？ | A. 只显示小红点 / B. 非阻塞横幅，带“立即更新/稍后再说” / C. 强提醒模态框 | B |

**User's choice:** `1C 2C 3A 4B`
**Notes:** 用户额外补充“并且有小红点”，因此横幅之外还要有红点提醒。

---

## 程序更新健康路径

| Question | Options presented | Selected |
|--------|-------------|----------|
| 健康路径下“立即更新”点下去后理想行为是什么？ | A. 客户端内下载更新包，下载完成后提示“重启并安装” / B. 只检查，下载仍跳浏览器 / C. 下载到本地后让用户手动去文件夹运行安装包 | A |
| 如果用户正在执行任务，更新应该怎么处理？ | A. 允许先继续工作，只提示任务结束后更新 / B. 允许用户选“现在更新”或“稍后更新”，默认不打断当前任务 / C. 发现更新就阻止继续使用 | B |
| 更新包下载完成后应该怎么收口？ | A. 立刻强制退出并安装 / B. 提示“重启并安装”或“稍后”，由用户决定 / C. 下次启动自动安装，不再提示 | B |
| 健康路径失败时主恢复出口是什么？ | A. 显示失败原因 + 重试按钮 + 打开日志目录 + 官方下载入口 / B. 只提示去官网下载最新版 / C. 静默失败，下次启动再试 | A |

**User's choice:** `A B B A`
**Notes:** 用户接受程序更新在健康路径下完全留在客户端内处理，但失败时必须保留官方下载安装兜底。

---

## 模型/资源增量更新策略

| Question | Options presented | Selected |
|--------|-------------|----------|
| 模型/资源更新默认什么时候检查？ | A. 启动时自动检查 / B. 进入 Bottle 1.0 相关界面时检查 / C. 只手动检查 | A |
| 如果发现有模型增量更新，默认怎么触发？ | A. 自动后台下载 / B. 提示可更新，由用户点“更新模型” / C. 强制先更新再允许使用 Bottle 1.0 | B |
| 增量更新应该落到哪里？ | A. 始终更新到用户目录；打包内置模型只当只读基线 / B. 直接改安装目录里的 bundled 模型 / C. 两者都允许 | A |
| 模型更新进度希望看到什么粒度？ | A. 只显示“正在更新/已完成” / B. 文件数进度 / C. 文件数进度 + 当前文件名 + 失败后可重试 | C |

**User's choice:** `A B A C`
**Notes:** 用户选择启动时就检查模型更新，但仍然要求下载动作由用户显式触发。

---

## 失败恢复与资产边界说明

| Question | Options presented | Selected |
|--------|-------------|----------|
| 程序更新失败时，主文案应该偏哪种？ | A. 面向普通用户，只说可理解原因和下一步动作 / B. 普通文案 + 可展开详细错误 / C. 直接显示原始错误文本 | A |
| 模型更新失败后，恢复动作默认提供哪些？ | A. 重试更新 + 打开日志目录 / B. 重试更新 + 回退到当前可用版本 + 打开日志目录 / C. 重试更新 + 重新完整下载模型 + 打开日志目录 | 自定义 |
| 资产边界对用户侧怎么表达？ | A. 不对用户解释，只内部文档说明 / B. 在诊断或帮助里说明“程序核心运行时随正式包更新，Bottle 1.0 模型资源可单独增量更新” / C. 在主界面长期展示这段说明 | B |
| 资产边界对团队侧要落成什么？ | A. 只写一段说明文字 / B. 一份明确清单，区分“打包保护资产”与“允许增量更新资产”，并纳入发布检查 / C. 只靠代码约定 | B |

**User's choice:** `1A 2 自定义 3B 4B`
**Notes:** 第 2 题用户自由回答为“重试更新 + 暂不更新”，不希望失败后默认强推完整重下。

---

## the agent's Discretion

- 小红点的精确位置、清除规则和样式层级
- 横幅与诊断入口的 copy 组合
- 程序更新下载/安装的底层实现方式
- 失败状态与日志字段的内部分类

## Deferred Ideas

- preview/internal 分发面恢复
- staged rollout / forced update 策略
- 更完整的技术诊断展开面板
