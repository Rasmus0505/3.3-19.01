---
phase: 05-billing-and-admin-alignment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/AdminApp.jsx
  - frontend/src/shared/lib/adminSearchParams.js
  - frontend/src/features/admin-workspaces/AdminUsersWorkspace.jsx
  - frontend/src/features/admin-pages/AdminRedeemPage.jsx
autonomous: true
requirements:
  - ADMIN-03
must_haves:
  truths:
    - "The default admin landing route opens the user-centered workflow instead of the old health-first shell"
    - "Top-level admin navigation no longer contains the `模型配置` concept"
    - "Billing stays reachable from the user workflow, while redeem tooling remains secondary"
    - "A separate troubleshooting route exists, but routine operators still start from users"
  artifacts:
    - path: frontend/src/AdminApp.jsx
      provides: Top-level admin route defaults and legacy redirects
      contains: 'users?tab=list'
    - path: frontend/src/shared/lib/adminSearchParams.js
      provides: Canonical admin nav labels and active-key resolution
      contains: 'troubleshooting'
    - path: frontend/src/features/admin-workspaces/AdminUsersWorkspace.jsx
      provides: Primary business-facing user workflow shell
      contains: '用户运营工作台'
    - path: frontend/src/features/admin-pages/AdminRedeemPage.jsx
      provides: Secondary redeem workflow with back-links into user operations
      contains: '返回用户工作台'
---

<objective>
Replace the current health-first, model-config-labeled admin entry with a lighter user-first shell where routine operators land in the users workspace, billing remains nested under that workflow, redeem tooling stays secondary, and troubleshooting has its own separate route.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@frontend/src/AdminApp.jsx
@frontend/src/shared/lib/adminSearchParams.js
@frontend/src/features/admin-workspaces/AdminUsersWorkspace.jsx
@frontend/src/features/admin-pages/AdminRedeemPage.jsx
@frontend/src/features/admin-workspaces/AdminMonitoringWorkspace.jsx
@frontend/src/features/admin-overview/AdminOverviewTab.jsx
@.planning/workstreams/milestone/phases/05-billing-and-admin-alignment/05-CONTEXT.md
@.planning/workstreams/milestone/phases/05-billing-and-admin-alignment/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire the top-level admin shell and nav metadata around users first</name>
  <files>
    - frontend/src/AdminApp.jsx
    - frontend/src/shared/lib/adminSearchParams.js
  </files>
  <read_first>
    - frontend/src/AdminApp.jsx
    - frontend/src/shared/lib/adminSearchParams.js
    - frontend/src/features/admin-workspaces/AdminMonitoringWorkspace.jsx
    - .planning/workstreams/milestone/phases/05-billing-and-admin-alignment/05-CONTEXT.md
  </read_first>
  <action>
    In `frontend/src/shared/lib/adminSearchParams.js`, replace the current top-level nav definition so the canonical keys and labels are exactly:

    - `users` -> label `用户运营`, description `查用户、钱包流水和计费`
    - `redeem` -> label `活动兑换`, description `批次、兑换码与审计`
    - `troubleshooting` -> label `排障中心`, description `健康、失败、翻译与操作日志`
    - `security` -> label `安全中心`, description `数据库、权限与导出保护`

    Remove the `models` nav key entirely. `resolveAdminNavKey()` must map:
    - `/admin/users` to `users`
    - `/admin/redeem` to `redeem`
    - `/admin/troubleshooting` and legacy `/admin/health` to `troubleshooting`
    - `/admin/security` to `security`

    In `frontend/src/AdminApp.jsx`:
    - make the index route redirect to `users?tab=list`
    - add a first-class `troubleshooting` route that renders the existing `AdminMonitoringWorkspace`
    - keep `/admin/health` only as a redirect to `/admin/troubleshooting?tab=health&panel=overview`
    - keep `/admin/rates` only as a redirect to `/admin/users?tab=rates`
    - keep `/admin/logs` only as a redirect to `/admin/users?tab=wallet`
    - make the wildcard route redirect to `users?tab=list`

    Do not reintroduce any top-level route or label named `models` or `模型配置`.
  </action>
  <acceptance_criteria>
    - `frontend/src/AdminApp.jsx` contains `users?tab=list`
    - `frontend/src/AdminApp.jsx` contains `path="troubleshooting"`
    - `frontend/src/shared/lib/adminSearchParams.js` contains `key: "troubleshooting"`
    - `frontend/src/shared/lib/adminSearchParams.js` contains `label: "用户运营"` and `label: "排障中心"`
    - `frontend/src/shared/lib/adminSearchParams.js` does not contain `key: "models"`
    - `frontend/src/shared/lib/adminSearchParams.js` does not contain `模型配置`
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'users\\?tab=list|path="troubleshooting"|key: "troubleshooting"|label: "用户运营"|label: "排障中心"' frontend/src/AdminApp.jsx frontend/src/shared/lib/adminSearchParams.js</automated>
  </verify>
  <done>The admin shell now lands in the user workflow and no longer advertises model configuration as a top-level concept.</done>
</task>

<task type="auto">
  <name>Task 2: Promote the users workspace into the primary business-facing workflow</name>
  <files>
    - frontend/src/features/admin-workspaces/AdminUsersWorkspace.jsx
  </files>
  <read_first>
    - frontend/src/features/admin-workspaces/AdminUsersWorkspace.jsx
    - frontend/src/features/admin-pages/AdminRedeemPage.jsx
    - .planning/workstreams/milestone/phases/05-billing-and-admin-alignment/05-CONTEXT.md
  </read_first>
  <action>
    In `frontend/src/features/admin-workspaces/AdminUsersWorkspace.jsx`, make the page copy and quick actions match the locked Phase 5 business workflow:

    - card title must be `用户运营工作台`
    - badge text must stay business-facing, for example `用户 + 钱包 + 计费`
    - card description must explicitly state that operators should start from users, then drill into wallet, billing, or redeem investigation from there
    - quick actions must include exact buttons for:
      - `查用户` -> `/admin/users?tab=list`
      - `看余额流水` -> `/admin/users?tab=wallet`
      - `改计费价格` -> `/admin/users?tab=rates`
      - `查看兑换审计` -> `/admin/redeem?panel=audit`

    Keep the existing tabs `list`, `wallet`, and `rates`, but make the copy describe billing as pricing-only rather than model configuration. The workspace should remain the primary business-facing landing page.
  </action>
  <acceptance_criteria>
    - `frontend/src/features/admin-workspaces/AdminUsersWorkspace.jsx` contains `用户运营工作台`
    - `frontend/src/features/admin-workspaces/AdminUsersWorkspace.jsx` contains `查用户`, `看余额流水`, `改计费价格`, and `查看兑换审计`
    - `frontend/src/features/admin-workspaces/AdminUsersWorkspace.jsx` contains `/admin/redeem?panel=audit`
    - `frontend/src/features/admin-workspaces/AdminUsersWorkspace.jsx` does not contain `模型配置`
  </acceptance_criteria>
  <verify>
    <automated>rg -n '用户运营工作台|查用户|看余额流水|改计费价格|查看兑换审计|/admin/redeem\\?panel=audit' frontend/src/features/admin-workspaces/AdminUsersWorkspace.jsx</automated>
  </verify>
  <done>The users workspace is now the clear default business workflow and its copy matches the user-centered operational model.</done>
</task>

<task type="auto">
  <name>Task 3: Keep redeem tooling secondary but adjacent to user operations</name>
  <files>
    - frontend/src/features/admin-pages/AdminRedeemPage.jsx
  </files>
  <read_first>
    - frontend/src/features/admin-pages/AdminRedeemPage.jsx
    - frontend/src/features/admin-workspaces/AdminUsersWorkspace.jsx
    - .planning/workstreams/milestone/phases/05-billing-and-admin-alignment/05-CONTEXT.md
  </read_first>
  <action>
    In `frontend/src/features/admin-pages/AdminRedeemPage.jsx`, preserve the three redeem sections (`批次`, `兑换码`, `审计`) but make the page explicitly secondary to the user workflow:

    - add a visible button labeled `返回用户工作台` that links to `/admin/users?tab=list`
    - add a visible button labeled `按用户回查` that links to `/admin/users?tab=wallet`
    - keep the overview cards and section IDs `admin-redeem-batches`, `admin-redeem-codes`, and `admin-redeem-audit`
    - do not add billing or troubleshooting controls into this page

    The resulting page should behave as campaign/redeem tooling that operators reach from the main user workflow, not the other way around.
  </action>
  <acceptance_criteria>
    - `frontend/src/features/admin-pages/AdminRedeemPage.jsx` contains `返回用户工作台`
    - `frontend/src/features/admin-pages/AdminRedeemPage.jsx` contains `按用户回查`
    - `frontend/src/features/admin-pages/AdminRedeemPage.jsx` contains `/admin/users?tab=list`
    - `frontend/src/features/admin-pages/AdminRedeemPage.jsx` contains `/admin/users?tab=wallet`
    - `frontend/src/features/admin-pages/AdminRedeemPage.jsx` still contains `admin-redeem-batches`, `admin-redeem-codes`, and `admin-redeem-audit`
  </acceptance_criteria>
  <verify>
    <automated>rg -n '返回用户工作台|按用户回查|/admin/users\\?tab=list|/admin/users\\?tab=wallet|admin-redeem-batches|admin-redeem-codes|admin-redeem-audit' frontend/src/features/admin-pages/AdminRedeemPage.jsx</automated>
  </verify>
  <done>Redeem tooling remains fully reachable, but the page now reads as secondary operational tooling rather than the main admin home.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm --prefix frontend run build` exits 0
2. `rg -n 'users\\?tab=list|path="troubleshooting"|key: "troubleshooting"|用户运营工作台|返回用户工作台' frontend/src/AdminApp.jsx frontend/src/shared/lib/adminSearchParams.js frontend/src/features/admin-workspaces/AdminUsersWorkspace.jsx frontend/src/features/admin-pages/AdminRedeemPage.jsx` exits 0
3. Manual browser verification confirms `/admin` lands in the users workspace, the nav contains no `模型配置`, and redeem remains reachable as a secondary route
</verification>

<success_criteria>
- Default admin flow starts from users rather than health
- Billing is nested under the user workflow instead of being presented as model configuration
- Redeem remains available but secondary
- Troubleshooting has its own route without becoming the default business-facing home
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/05-billing-and-admin-alignment/05-01-SUMMARY.md`
</output>
