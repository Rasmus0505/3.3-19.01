---
phase: 05-billing-and-admin-alignment
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/admin-rates/AdminRatesTab.jsx
  - app/schemas/admin.py
  - app/schemas/billing.py
  - app/api/serializers.py
  - app/api/routers/admin.py
  - app/services/billing.py
  - tests/unit/test_billing_cleanup.py
  - tests/integration/test_regression_api.py
  - tests/e2e/test_e2e_key_flows.py
autonomous: true
requirements:
  - BILL-02
  - ADMIN-01
must_haves:
  truths:
    - "The billing editor only exposes price, cost reference, billing unit, and active/inactive state"
    - "Bottle 1.0 stays on canonical `faster-whisper-medium` billing identity"
    - "Bottle 2.0 stays on canonical `qwen3-asr-flash-filetrans` billing identity"
    - "Admin and public billing APIs remain aligned after removing runtime-edit fields from the admin surface"
  artifacts:
    - path: frontend/src/features/admin-rates/AdminRatesTab.jsx
      provides: Pricing-only billing editor UI
      contains: '保存'
    - path: app/schemas/admin.py
      provides: Admin billing update request contract without runtime-edit fields
      contains: 'class AdminBillingRateUpdateRequest'
    - path: app/schemas/billing.py
      provides: Billing response contract for admin/public APIs
      contains: 'class BillingRateItem'
    - path: tests/unit/test_billing_cleanup.py
      provides: Guardrails for Bottle 1.0 billing continuity and removed runtime-edit fields
      contains: 'faster-whisper-medium'
---

<objective>
Turn the current mixed billing-plus-runtime-control surface into a pricing-only admin billing flow that preserves Bottle 1.0 and Bottle 2.0 canonical `model_name` continuity, keeps admin/public billing aligned, and removes routine runtime tuning from both the UI and the admin update contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@frontend/src/features/admin-rates/AdminRatesTab.jsx
@app/schemas/admin.py
@app/schemas/billing.py
@app/api/serializers.py
@app/api/routers/admin.py
@app/services/billing.py
@tests/unit/test_billing_cleanup.py
@tests/integration/test_regression_api.py
@tests/e2e/test_e2e_key_flows.py
@.planning/workstreams/milestone/phases/05-billing-and-admin-alignment/05-CONTEXT.md
@.planning/workstreams/milestone/phases/05-billing-and-admin-alignment/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove runtime-edit controls from the billing UI and save payload</name>
  <files>
    - frontend/src/features/admin-rates/AdminRatesTab.jsx
  </files>
  <read_first>
    - frontend/src/features/admin-rates/AdminRatesTab.jsx
    - app/schemas/admin.py
    - app/schemas/billing.py
    - .planning/workstreams/milestone/phases/05-billing-and-admin-alignment/05-CONTEXT.md
  </read_first>
  <action>
    In `frontend/src/features/admin-rates/AdminRatesTab.jsx`, narrow the draft state, change detection, row summary, and save payload so the UI only edits these fields:

    - `price_per_minute_yuan`
    - `points_per_1k_tokens`
    - `cost_per_minute_yuan`
    - `billing_unit`
    - `is_active`

    Remove all draft state, form controls, validation, summary text, and save payload references to:

    - `parallel_enabled`
    - `parallel_threshold_seconds`
    - `segment_seconds`
    - `max_concurrency`

    The visible billing UI should present billing as pricing-only. Keep row save behavior per `model_name`, keep Bottle 1.0 and Bottle 2.0 rows visible, and keep the token-billing path for `qwen-mt-flash` intact.
  </action>
  <acceptance_criteria>
    - `frontend/src/features/admin-rates/AdminRatesTab.jsx` contains `price_per_minute_yuan`, `points_per_1k_tokens`, `cost_per_minute_yuan`, `billing_unit`, and `is_active`
    - `frontend/src/features/admin-rates/AdminRatesTab.jsx` does not contain `parallel_threshold_seconds`
    - `frontend/src/features/admin-rates/AdminRatesTab.jsx` does not contain `segment_seconds`
    - `frontend/src/features/admin-rates/AdminRatesTab.jsx` does not contain `max_concurrency`
    - `frontend/src/features/admin-rates/AdminRatesTab.jsx` does not contain `parallel_enabled`
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/unit/test_billing_cleanup.py -q</automated>
  </verify>
  <done>The admin billing UI now edits pricing only and no longer exposes runtime tuning controls.</done>
</task>

<task type="auto">
  <name>Task 2: Narrow the admin billing contract while preserving internal runtime defaults</name>
  <files>
    - app/schemas/admin.py
    - app/schemas/billing.py
    - app/api/serializers.py
    - app/api/routers/admin.py
    - app/services/billing.py
  </files>
  <read_first>
    - app/schemas/admin.py
    - app/schemas/billing.py
    - app/api/serializers.py
    - app/api/routers/admin.py
    - app/services/billing.py
    - tests/integration/test_regression_api.py
  </read_first>
  <action>
    Update the backend contract so routine admin billing edits are pricing-only:

    In `app/schemas/admin.py`, remove these fields from `AdminBillingRateUpdateRequest`:
    - `parallel_enabled`
    - `parallel_threshold_seconds`
    - `segment_seconds`
    - `max_concurrency`

    In `app/schemas/billing.py`, remove those same runtime-edit fields from `BillingRateItem`.

    In `app/api/serializers.py`, stop serializing those runtime-edit fields from `to_rate_item()`. Keep:
    - `model_name`
    - `display_name`
    - `price_per_minute_yuan`
    - `cost_per_minute_yuan`
    - `gross_profit_per_minute_yuan`
    - `price_per_minute_cents`
    - `points_per_minute`
    - `points_per_1k_tokens`
    - `cost_per_minute_cents`
    - `gross_profit_per_minute_cents`
    - `billing_unit`
    - `is_active`
    - `runtime_kind`
    - `updated_at`

    In `app/api/routers/admin.py`, make `PUT /api/admin/billing-rates/{model_name}` accept only the pricing payload above. Do not require runtime fields in the request.

    In `app/services/billing.py`, keep existing internal seed/default/runtime-healing logic so old rows still self-heal and execution defaults remain intact, but make the admin update flow preserve existing runtime fields rather than expecting the client to send them.

    Preserve canonical identities:
    - Bottle 1.0 -> `faster-whisper-medium`
    - Bottle 2.0 -> `qwen3-asr-flash-filetrans`
  </action>
  <acceptance_criteria>
    - `app/schemas/admin.py` does not contain `parallel_threshold_seconds`, `segment_seconds`, `max_concurrency`, or `parallel_enabled` inside `AdminBillingRateUpdateRequest`
    - `app/schemas/billing.py` does not contain `parallel_threshold_seconds`, `segment_seconds`, `max_concurrency`, or `parallel_enabled` inside `BillingRateItem`
    - `app/api/serializers.py` does not serialize `parallel_threshold_seconds`, `segment_seconds`, `max_concurrency`, or `parallel_enabled` in `to_rate_item`
    - `app/api/routers/admin.py` still contains `/billing-rates/{model_name}`
    - `app/services/billing.py` still contains `faster-whisper-medium` and `qwen3-asr-flash-filetrans`
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/integration/test_regression_api.py -k "billing_rates or faster_whisper_medium or qwen3_asr" -q</automated>
  </verify>
  <done>The admin billing API contract is now pricing-only, while internal runtime defaults remain intact behind the scenes.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite billing regressions around the pricing-only contract and canonical model identities</name>
  <files>
    - tests/unit/test_billing_cleanup.py
    - tests/integration/test_regression_api.py
    - tests/e2e/test_e2e_key_flows.py
  </files>
  <read_first>
    - tests/unit/test_billing_cleanup.py
    - tests/integration/test_regression_api.py
    - tests/e2e/test_e2e_key_flows.py
    - app/api/routers/admin.py
    - frontend/src/features/admin-rates/AdminRatesTab.jsx
  </read_first>
  <action>
    Update automated coverage so it locks the new pricing-only contract:

    In `tests/unit/test_billing_cleanup.py`, assert that Bottle 1.0 (`faster-whisper-medium`) remains present in admin billing output and that returned rate items do not require runtime-edit fields.

    In `tests/integration/test_regression_api.py`, update the admin billing update flows so PUT payloads for:
    - `faster-whisper-medium`
    - `qwen3-asr-flash-filetrans`
    - `qwen-mt-flash`

    only send pricing-related fields (`price_per_minute_yuan`, `points_per_1k_tokens`, `cost_per_minute_yuan`, `billing_unit`, `is_active`, and legacy cents companions if the endpoint still accepts them). The assertions must confirm admin billing responses and public `/api/billing/rates` stay aligned on `model_name` and updated prices.

    In `tests/e2e/test_e2e_key_flows.py`, extend the existing admin billing smoke flow so it proves the pricing-only payload still updates a canonical model row and the public billing API reflects the change.
  </action>
  <acceptance_criteria>
    - `tests/unit/test_billing_cleanup.py` contains `faster-whisper-medium`
    - `tests/integration/test_regression_api.py` contains `/api/admin/billing-rates/faster-whisper-medium`
    - `tests/integration/test_regression_api.py` contains `/api/admin/billing-rates/qwen3-asr-flash-filetrans`
    - `tests/e2e/test_e2e_key_flows.py` contains `/api/admin/billing-rates/`
    - updated billing tests no longer send `parallel_threshold_seconds`, `segment_seconds`, `max_concurrency`, or `parallel_enabled`
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/unit/test_billing_cleanup.py tests/integration/test_regression_api.py tests/e2e/test_e2e_key_flows.py -k "billing or faster_whisper_medium or admin_update_rate_visible_in_public_api" -q</automated>
  </verify>
  <done>The test suite now proves billing is still editable and visible without exposing runtime tuning as routine admin behavior.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pytest tests/unit/test_billing_cleanup.py -q` exits 0
2. `pytest tests/integration/test_regression_api.py -k "billing_rates or faster_whisper_medium or qwen3_asr" -q` exits 0
3. `pytest tests/e2e/test_e2e_key_flows.py -k "admin_update_rate_visible_in_public_api" -q` exits 0
4. `npm --prefix frontend run build` exits 0
5. Manual/API verification confirms Bottle 1.0 and Bottle 2.0 still appear as canonical billing rows, but no runtime tuning controls are visible in the admin billing UI
</verification>

<success_criteria>
- Billing editing is pricing-only
- Bottle 1.0 and Bottle 2.0 remain on canonical `model_name` identities
- Admin/public billing APIs remain aligned after the contract cleanup
- Runtime defaults remain internal implementation details instead of routine admin controls
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/05-billing-and-admin-alignment/05-02-SUMMARY.md`
</output>
