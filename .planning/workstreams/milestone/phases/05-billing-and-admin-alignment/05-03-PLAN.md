---
phase: 05-billing-and-admin-alignment
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - frontend/src/features/admin-workspaces/AdminMonitoringWorkspace.jsx
  - frontend/src/features/admin-overview/AdminOverviewTab.jsx
  - frontend/src/features/admin-system/AdminSystemTab.jsx
  - app/schemas/admin.py
  - app/api/routers/admin.py
  - tests/integration/test_admin_console_api.py
  - tests/e2e/test_e2e_key_flows.py
autonomous: true
requirements:
  - ADMIN-02
must_haves:
  truths:
    - "Troubleshooting is exposed as a separate route/workspace from the business-facing admin shell"
    - "Troubleshooting covers system health, lesson-task failures, translation failures, operation audit, and Bottle 1.0 / Bottle 2.0 runtime readiness"
    - "Overview and quick-link buttons route to live troubleshooting panels instead of dead or stale paths"
    - "Runtime readiness is diagnostic-only and does not reintroduce runtime tuning controls"
  artifacts:
    - path: frontend/src/features/admin-workspaces/AdminMonitoringWorkspace.jsx
      provides: Canonical troubleshooting tab/panel structure
      contains: 'health'
    - path: frontend/src/features/admin-system/AdminSystemTab.jsx
      provides: Runtime readiness cards for Bottle 1.0 and Bottle 2.0
      contains: 'Bottle 1.0'
    - path: app/api/routers/admin.py
      provides: Diagnostic runtime readiness endpoint
      contains: '/runtime-readiness'
    - path: tests/integration/test_admin_console_api.py
      provides: Integration coverage for troubleshooting APIs
      contains: 'runtime-readiness'
---

<objective>
Expose a real troubleshooting center for developers and advanced operators by wiring the existing monitoring pieces into a live route, cleaning up broken copy and deep links, and adding explicit Bottle 1.0 / Bottle 2.0 runtime readiness visibility without turning troubleshooting into a runtime-tuning control plane.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@frontend/src/features/admin-workspaces/AdminMonitoringWorkspace.jsx
@frontend/src/features/admin-overview/AdminOverviewTab.jsx
@frontend/src/features/admin-system/AdminSystemTab.jsx
@frontend/src/features/admin-logs/AdminLessonTaskLogsTab.jsx
@frontend/src/features/admin-logs/AdminTranslationLogsTab.jsx
@frontend/src/features/admin-operation-logs/AdminOperationLogsTab.jsx
@app/schemas/admin.py
@app/api/routers/admin.py
@app/services/asr_model_registry.py
@tests/integration/test_admin_console_api.py
@tests/e2e/test_e2e_key_flows.py
@.planning/workstreams/milestone/phases/05-billing-and-admin-alignment/05-CONTEXT.md
@.planning/workstreams/milestone/phases/05-billing-and-admin-alignment/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the troubleshooting workspace readable, stable, and route-correct</name>
  <files>
    - frontend/src/features/admin-workspaces/AdminMonitoringWorkspace.jsx
    - frontend/src/features/admin-overview/AdminOverviewTab.jsx
  </files>
  <read_first>
    - frontend/src/features/admin-workspaces/AdminMonitoringWorkspace.jsx
    - frontend/src/features/admin-overview/AdminOverviewTab.jsx
    - frontend/src/features/admin-logs/AdminLessonTaskLogsTab.jsx
    - frontend/src/features/admin-logs/AdminTranslationLogsTab.jsx
    - frontend/src/features/admin-operation-logs/AdminOperationLogsTab.jsx
  </read_first>
  <action>
    In `frontend/src/features/admin-workspaces/AdminMonitoringWorkspace.jsx`, keep the existing structural tab groups but normalize them into a readable troubleshooting surface:

    - tab values remain `health`, `tasks`, and `operations`
    - panel values remain `overview`, `system`, `task-failures`, `translations`, `operations`, and `sql-console`
    - replace the mojibake/corrupted Chinese labels and descriptions with clean readable copy
    - keep scroll-to-panel behavior and query-string normalization intact

    In `frontend/src/features/admin-overview/AdminOverviewTab.jsx`, update every quick link so it points at live routes:
    - system check -> `/admin/troubleshooting?tab=health&panel=system`
    - task failures -> `/admin/troubleshooting?tab=tasks&panel=task-failures`
    - operation logs -> `/admin/troubleshooting?tab=operations&panel=operations`
    - redeem batches -> `/admin/redeem?panel=batches`

    Remove any remaining links to `/admin/monitoring` or `/admin/business`.
  </action>
  <acceptance_criteria>
    - `frontend/src/features/admin-workspaces/AdminMonitoringWorkspace.jsx` contains clean readable labels for `health`, `tasks`, and `operations`
    - `frontend/src/features/admin-workspaces/AdminMonitoringWorkspace.jsx` does not contain mojibake text fragments such as `绯荤粺` or `缈昏瘧`
    - `frontend/src/features/admin-overview/AdminOverviewTab.jsx` contains `/admin/troubleshooting?tab=health&panel=system`
    - `frontend/src/features/admin-overview/AdminOverviewTab.jsx` contains `/admin/troubleshooting?tab=tasks&panel=task-failures`
    - `frontend/src/features/admin-overview/AdminOverviewTab.jsx` contains `/admin/troubleshooting?tab=operations&panel=operations`
    - `frontend/src/features/admin-overview/AdminOverviewTab.jsx` does not contain `/admin/monitoring` or `/admin/business`
  </acceptance_criteria>
  <verify>
    <automated>rg -n '/admin/troubleshooting\\?tab=health&panel=system|/admin/troubleshooting\\?tab=tasks&panel=task-failures|/admin/troubleshooting\\?tab=operations&panel=operations' frontend/src/features/admin-overview/AdminOverviewTab.jsx</automated>
  </verify>
  <done>The troubleshooting workspace and overview links now form a readable, live diagnostic entry point instead of a partially hidden or broken one.</done>
</task>

<task type="auto">
  <name>Task 2: Add Bottle 1.0 and Bottle 2.0 runtime readiness visibility as diagnostics</name>
  <files>
    - app/schemas/admin.py
    - app/api/routers/admin.py
    - frontend/src/features/admin-system/AdminSystemTab.jsx
  </files>
  <read_first>
    - app/schemas/admin.py
    - app/api/routers/admin.py
    - app/services/asr_model_registry.py
    - frontend/src/features/admin-system/AdminSystemTab.jsx
  </read_first>
  <action>
    Add a diagnostic runtime-readiness path without exposing runtime tuning:

    In `app/schemas/admin.py`, add response models for a runtime readiness payload with these fields per item:
    - `model_key`
    - `display_name`
    - `runtime_kind`
    - `status`
    - `available`
    - `message`
    - `actions`

    In `app/api/routers/admin.py`, add `GET /api/admin/runtime-readiness` that returns the descriptors from `app.services.asr_model_registry.list_asr_models_with_status()`, filtered to the canonical admin-facing ASR rows:
    - `qwen3-asr-flash-filetrans` -> Bottle 2.0
    - `faster-whisper-medium` -> Bottle 1.0

    In `frontend/src/features/admin-system/AdminSystemTab.jsx`, fetch that endpoint and render a new diagnostic section titled `Bottle 运行就绪度` that shows at least:
    - `Bottle 1.0`
    - `Bottle 2.0`
    - readiness state/status
    - human-readable message

    This section must be read-only. Do not add runtime knobs or editable configuration controls.
  </action>
  <acceptance_criteria>
    - `app/api/routers/admin.py` contains `/runtime-readiness`
    - `app/api/routers/admin.py` contains `qwen3-asr-flash-filetrans` and `faster-whisper-medium`
    - `frontend/src/features/admin-system/AdminSystemTab.jsx` contains `Bottle 运行就绪度`
    - `frontend/src/features/admin-system/AdminSystemTab.jsx` contains `Bottle 1.0` and `Bottle 2.0`
    - `frontend/src/features/admin-system/AdminSystemTab.jsx` does not contain editable runtime fields such as `parallel_threshold_seconds`, `segment_seconds`, or `max_concurrency`
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/integration/test_admin_console_api.py -k "runtime or overview" -q</automated>
  </verify>
  <done>Operators can now see Bottle runtime readiness in troubleshooting without turning diagnostics into another settings surface.</done>
</task>

<task type="auto">
  <name>Task 3: Lock troubleshooting coverage in integration and end-to-end smoke tests</name>
  <files>
    - tests/integration/test_admin_console_api.py
    - tests/e2e/test_e2e_key_flows.py
  </files>
  <read_first>
    - tests/integration/test_admin_console_api.py
    - tests/e2e/test_e2e_key_flows.py
    - app/api/routers/admin.py
    - frontend/src/features/admin-system/AdminSystemTab.jsx
    - frontend/src/features/admin-overview/AdminOverviewTab.jsx
  </read_first>
  <action>
    Extend automated coverage so troubleshooting stays complete and separate:

    In `tests/integration/test_admin_console_api.py`, add or extend assertions for:
    - `GET /api/admin/overview`
    - `GET /api/admin/operation-logs`
    - `GET /api/admin/lesson-task-logs`
    - `GET /api/admin/runtime-readiness`

    The runtime-readiness assertions must confirm the response includes Bottle 1.0 and Bottle 2.0 descriptors and returns diagnostic status data without any runtime-edit payload requirement.

    In `tests/e2e/test_e2e_key_flows.py`, extend the admin smoke flow so it proves a privileged operator can:
    - adjust wallet state
    - update billing
    - read troubleshooting/runtime readiness endpoints
    - keep redeem operations reachable

    The e2e assertions do not need browser rendering, but they must prove the API surfaces that back the troubleshooting center remain reachable after the Phase 5 routing cleanup.
  </action>
  <acceptance_criteria>
    - `tests/integration/test_admin_console_api.py` contains `/api/admin/runtime-readiness`
    - `tests/integration/test_admin_console_api.py` contains `faster-whisper-medium` and `qwen3-asr-flash-filetrans`
    - `tests/e2e/test_e2e_key_flows.py` contains `/api/admin/runtime-readiness`
    - `tests/e2e/test_e2e_key_flows.py` contains wallet, billing, and redeem admin smoke assertions in the same phase surface
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/integration/test_admin_console_api.py tests/e2e/test_e2e_key_flows.py -k "runtime or wallet or billing or redeem" -q</automated>
  </verify>
  <done>The test suite now proves the troubleshooting center is complete, separate, and still integrated with the rest of admin operations.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pytest tests/integration/test_admin_console_api.py -k "overview or operation_logs or lesson_task_logs or runtime" -q` exits 0
2. `pytest tests/e2e/test_e2e_key_flows.py -k "wallet or billing or redeem" -q` exits 0
3. `npm --prefix frontend run build` exits 0
4. Manual browser verification confirms the troubleshooting route exposes overview, system, task failures, translation logs, operation logs, and Bottle runtime readiness as a separate diagnostic area
</verification>

<success_criteria>
- Troubleshooting is a real, separate operator/developer route
- Overview links and panel routes are live and readable
- Bottle 1.0 and Bottle 2.0 runtime readiness is visible without reintroducing tuning controls
- Integration and e2e coverage protect the full diagnostic surface
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/05-billing-and-admin-alignment/05-03-SUMMARY.md`
</output>
