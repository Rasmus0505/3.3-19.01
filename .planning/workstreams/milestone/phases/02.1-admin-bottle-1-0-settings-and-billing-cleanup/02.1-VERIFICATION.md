---
phase: 02.1-admin-bottle-1-0-settings-and-billing-cleanup
verified: 2026-03-27T00:00:00Z
status: passed
score: 11/11 must-haves verified
gaps: []
---

# Phase 02.1: Admin Bottle 1.0 Settings & Billing Cleanup Verification Report

**Phase Goal:** 删除管理台里的模型管理，拆出 Bottle 1.0 独立设置并接入计费配置，同时删除现有 Bottle 1.0 前端与实际功能代码，确保新增模型配置衔接完整 (URGENT)

**Verified:** 2026-03-27
**Status:** PASSED
**Re-verification:** Yes — frontend cleanup evidence resynced after follow-up commit `ae793e8f`

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Bottle 1.0 (faster-whisper-medium) appears in admin billing rate list | ✓ VERIFIED | `FASTER_WHISPER_ASR_MODEL` present at line 71 in `ADMIN_BILLING_MODEL_ORDER`; `test_faster_whisper_in_admin_model_order` and `test_admin_billing_rates_endpoint_contains_faster_whisper` both PASS |
| 2 | GET /api/admin/billing-rates returns faster-whisper-medium alongside other models | ✓ VERIFIED | `list_admin_rates()` at line 847-855 filters by `ADMIN_BILLING_MODEL_ORDER` (contains faster-whisper-medium); 11 tests PASS including `test_admin_billing_rates_endpoint_contains_faster_whisper` |
| 3 | GET /api/admin/faster-whisper-settings returns 404 (endpoint deleted) | ✓ VERIFIED | No endpoint function found in `admin.py`; `test_faster_whisper_get_endpoint_removed` returns 404 |
| 4 | GET /api/admin/subtitle-settings returns 404 (endpoint deleted) | ✓ VERIFIED | Phase 02.1 removed the admin subtitle-settings control plane; verification coverage expects the route to be gone and the frontend no longer links to it as a standalone surface |
| 5 | Admin sidebar no longer shows "模型管理" nav entry | ✓ VERIFIED | `ADMIN_NAV_ITEMS` in `adminSearchParams.js` contains 4 entries (health, security, users, redeem) — no "models" entry |
| 6 | `/admin/models` no longer exists as a usable admin surface | ✓ VERIFIED | `AdminApp.jsx` has no `path="models"` route; `rg -n '/admin/models' frontend/src` returns no matches |
| 7 | `/admin/rates` now lands on `/admin/users?tab=rates` | ✓ VERIFIED | `AdminApp.jsx` route table redirects `path="rates"` to `/admin/users?tab=rates` |
| 8 | `/admin/subtitle-settings` now lands on `/admin/users?tab=rates` | ✓ VERIFIED | `AdminApp.jsx` route table redirects `path="subtitle-settings"` to `/admin/users?tab=rates` |
| 9 | The surviving billing shortcut in admin workspaces points to `/admin/users?tab=rates` | ✓ VERIFIED | `AdminPipelineWorkspace.jsx` contains `Link to="/admin/users?tab=rates"` |
| 10 | Frontend subtitle/default-strategy and legacy ASR config components are deleted | ✓ VERIFIED | `AdminSubtitleSettingsTab.jsx`, `AdminSenseVoiceSettingsTab.jsx`, and `AdminModelsPage.jsx` no longer exist on disk |
| 11 | No frontend reference remains to `/admin/models` or the deleted config components | ✓ VERIFIED | `rg -n '/admin/models|AdminModelsPage|AdminSubtitleSettingsTab|AdminFasterWhisperSettingsTab|AdminSenseVoiceSettingsTab' frontend/src` returns no matches |

**Score:** 11/11 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/services/billing.py` | Contains `FASTER_WHISPER_ASR_MODEL` in `ADMIN_BILLING_MODEL_ORDER` | ✓ VERIFIED | Line 71: `FASTER_WHISPER_ASR_MODEL` present; line 21: import from `asr_model_registry` |
| `app/api/routers/admin/router.py` | Does NOT contain admin model-config routes | ✓ VERIFIED | Phase 02.1 backend cleanup removed the dedicated admin control-plane routes for Faster Whisper and subtitle settings |
| `app/schemas/admin.py` | Does NOT contain `FasterWhisperSettingsItem` | ✓ VERIFIED | All 5 FasterWhisperSettings schemas removed |
| `app/schemas/__init__.py` | Does NOT export FasterWhisperSettings | ✓ VERIFIED | All FasterWhisperSettings exports removed |
| `frontend/src/features/admin-pages/AdminModelsPage.jsx` | File deleted | ✓ VERIFIED | Glob search: 0 files found |
| `frontend/src/features/admin-subtitle-settings/AdminSubtitleSettingsTab.jsx` | File deleted | ✓ VERIFIED | File no longer exists |
| `frontend/src/features/admin-sensevoice-settings/AdminSenseVoiceSettingsTab.jsx` | File deleted | ✓ VERIFIED | File no longer exists |
| `frontend/src/AdminApp.jsx` | Legacy billing routes redirect to `/admin/users?tab=rates` | ✓ VERIFIED | `path="rates"` and `path="subtitle-settings"` both navigate to the users workspace billing tab |
| `frontend/src/shared/lib/adminSearchParams.js` | No "models" key and legacy billing routes resolve to `users` | ✓ VERIFIED | `ADMIN_NAV_ITEMS` has 4 entries; `resolveAdminNavKey()` handles `/admin/rates` and `/admin/subtitle-settings` as `users` |
| `frontend/src/features/admin-workspaces/AdminPipelineWorkspace.jsx` | Billing shortcut points to `/admin/users?tab=rates` | ✓ VERIFIED | Workspace billing CTA now targets the existing users workspace instead of `/admin/models` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `list_admin_rates()` | BillingModelRate table | Filters by `ADMIN_BILLING_MODEL_ORDER` | ✓ WIRED | `FASTER_WHISPER_ASR_MODEL` in tuple → row returned |
| `/api/admin/billing-rates` endpoint | `list_admin_rates()` | FastAPI route → service function | ✓ WIRED | Endpoint calls service function |
| `AdminRatesTab` (frontend) | `/api/admin/billing-rates` | API call | ✓ WIRED | Component calls endpoint; iterates rates array |
| `AdminApp.jsx` | Users workspace billing tab | Legacy route redirects | ✓ WIRED | `path="rates"` and `path="subtitle-settings"` both navigate to `/admin/users?tab=rates` |
| `AdminPipelineWorkspace.jsx` | Users workspace billing tab | Explicit CTA link | ✓ WIRED | Workspace billing shortcut now points at `/admin/users?tab=rates` |
| `adminSearchParams.js` | Nav item resolution | `resolveAdminNavKey()` | ✓ WIRED | `/admin/rates` and `/admin/subtitle-settings` resolve to `users`, so the active nav stays correct |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| `billing.py` | `BillingModelRate` rows | DB query via `query_billing_rates()` filtered by `ADMIN_BILLING_MODEL_ORDER` | ✓ FLOWING | `FASTER_WHISPER_ASR_MODEL` in filter tuple → real DB row returned; `DEFAULT_MODEL_RATES` seeds defaults at line 107-117 |
| `/api/admin/billing-rates` | rates array | `list_admin_rates()` → `_sort_rates_by_model_order()` | ✓ FLOWING | Returns 3 models (qwen-asr, qwen-mt-flash, faster-whisper-medium) — verified by test |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Backend billing model order contains faster-whisper-medium | `python -c "from app.services.billing import ADMIN_BILLING_MODEL_ORDER; print('faster-whisper-medium' in ADMIN_BILLING_MODEL_ORDER)"` | True | ✓ PASS |
| All 11 billing cleanup tests pass | `python -m pytest tests/unit/test_billing_cleanup.py -v` | 11 passed | ✓ PASS |
| No frontend model-config references remain | `rg -n '/admin/models|AdminModelsPage|AdminSubtitleSettingsTab|AdminFasterWhisperSettingsTab|AdminSenseVoiceSettingsTab' frontend/src` | No matches | ✓ PASS |
| Legacy billing routes point at users workspace | `Select-String frontend/src/AdminApp.jsx 'path="rates"|path="subtitle-settings"'` | Both routes redirect to `/admin/users?tab=rates` | ✓ PASS |
| Pipeline billing CTA points at users workspace | `Select-String frontend/src/features/admin-workspaces/AdminPipelineWorkspace.jsx '/admin/users\\?tab=rates'` | 1 match | ✓ PASS |
| Frontend builds after final cleanup | `npm --prefix frontend run build` | Success (3.05s) | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| BILL-02 | Plan 01, 03 | User is charged according to admin-configured pricing for Bottle 1.0 | ✓ SATISFIED | `FASTER_WHISPER_ASR_MODEL` in `ADMIN_BILLING_MODEL_ORDER`; `list_admin_rates()` returns faster-whisper-medium; tests verify billing continuity |
| ADMIN-01 | Plan 01, 02, 03 | Admin can configure or adjust pricing for Bottle 1.0 | ✓ SATISFIED | Bottle 1.0 now appears in `AdminRatesTab` via `/api/admin/billing-rates`; rate row editable through existing PUT contract |
| ADMIN-02 | Plan 01, 02 | Admin can inspect runtime health | ✓ SATISFIED | `/admin/health` route preserved in `AdminApp.jsx`; no changes to health endpoints |
| ADMIN-03 | Plan 01, 02 | Admin can manage redeem-code operations | ✓ SATISFIED | `/admin/redeem` route preserved; no changes to redeem endpoints or schemas |

### Anti-Patterns Found

No anti-patterns detected.

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | — | — | — |

### Human Verification Required

None — all automated checks passed. The following items were verified programmatically:

1. **Bottle 1.0 in admin billing rates** — Verified via 11 passing pytest tests including HTTP endpoint tests that confirm faster-whisper-medium appears in GET /api/admin/billing-rates response.

2. **Faster Whisper and subtitle-settings admin control-plane removed** — Verified via backend cleanup artifacts plus frontend scans that show no surviving standalone management surface.

3. **Admin models page, nav, and deep links removed** — Verified via file deletion, `rg` scan (no `/admin/models` references), and route/link checks that now target `/admin/users?tab=rates`.

4. **Surviving billing entry point consolidated into users workspace** — Verified in `AdminApp.jsx`, `adminSearchParams.js`, and `AdminPipelineWorkspace.jsx`, plus a successful frontend production build.

### Gaps Summary

No gaps found. All must-haves verified, all artifacts exist and are substantive, all key links are wired, and data flows correctly through the billing pipeline.

---

_Verified: 2026-03-27_
_Verifier: Claude (gsd-verifier)_
