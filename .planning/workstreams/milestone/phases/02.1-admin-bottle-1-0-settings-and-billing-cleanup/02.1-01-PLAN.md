---
phase: 02.1-admin-bottle-1-0-settings-and-billing-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/AdminApp.jsx
  - frontend/src/shared/lib/adminSearchParams.js
  - frontend/src/features/admin-pages/AdminModelsPage.jsx
  - frontend/src/features/admin-subtitle-settings/AdminSubtitleSettingsTab.jsx
  - frontend/src/features/admin-faster-whisper-settings/AdminFasterWhisperSettingsTab.jsx
  - frontend/src/features/admin-sensevoice-settings/AdminSenseVoiceSettingsTab.jsx
  - frontend/src/features/admin-workspaces/AdminPipelineWorkspace.jsx
autonomous: true
requirements:
  - BILL-02
  - ADMIN-01
must_haves:
  truths:
    - "Admin no longer exposes /admin/models or a '模型配置' navigation entry"
    - "No frontend model-parameter configuration UI remains for subtitle/default strategy, legacy ASR, or Bottle 1.0 runtime settings"
    - "Canonical billing access lands in an existing admin workspace at /admin/users?tab=rates"
    - "No frontend deep link still points to /admin/models"
  artifacts:
    - path: frontend/src/AdminApp.jsx
      provides: Admin route table
      not_contains: 'path="models"'
    - path: frontend/src/shared/lib/adminSearchParams.js
      provides: Admin navigation registry
      not_contains: 'label: "模型配置"'
    - path: frontend/src/features/admin-workspaces/AdminPipelineWorkspace.jsx
      provides: Billing deep link inside existing workspace
      contains: '/admin/users?tab=rates'
---

<objective>
Delete the standalone model-configuration frontend surface and move every surviving billing entry point onto an existing admin workspace. This plan only handles route/nav/deep-link/UI cleanup; backend control-plane deletion and billing continuity are handled in Plan 02.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@frontend/src/AdminApp.jsx
@frontend/src/shared/lib/adminSearchParams.js
@frontend/src/features/admin-pages/AdminModelsPage.jsx
@frontend/src/features/admin-subtitle-settings/AdminSubtitleSettingsTab.jsx
@frontend/src/features/admin-faster-whisper-settings/AdminFasterWhisperSettingsTab.jsx
@frontend/src/features/admin-sensevoice-settings/AdminSenseVoiceSettingsTab.jsx
@frontend/src/features/admin-workspaces/AdminUsersWorkspace.jsx
@frontend/src/features/admin-workspaces/AdminPipelineWorkspace.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the standalone model-config route and nav entry</name>
  <files>
    - frontend/src/AdminApp.jsx
    - frontend/src/shared/lib/adminSearchParams.js
  </files>
  <read_first>
    - frontend/src/AdminApp.jsx
    - frontend/src/shared/lib/adminSearchParams.js
    - frontend/src/features/admin-workspaces/AdminUsersWorkspace.jsx
  </read_first>
  <action>
    In `frontend/src/AdminApp.jsx`, delete the `AdminModelsPage` import and remove the `<Route path="models" ... />` entry entirely.

    In the same file, change the legacy redirects so the surviving billing entry point is the existing users workspace billing tab:
    - `<Route path="rates" ... />` must redirect to `/admin/users?tab=rates`
    - `<Route path="subtitle-settings" ... />` must also redirect to `/admin/users?tab=rates`

    In `frontend/src/shared/lib/adminSearchParams.js`, delete the `ADMIN_NAV_ITEMS` object with `key: "models"`, `label: "模型配置"`, and `href: "/admin/models"`.

    In `resolveAdminNavKey`, remove the `/admin/models` branch and make `/admin/rates` resolve to `"users"` so the active nav remains correct when the redirect target is the users workspace.
  </action>
  <acceptance_criteria>
    - `frontend/src/AdminApp.jsx` contains no `AdminModelsPage` import
    - `frontend/src/AdminApp.jsx` contains no `path="models"` route
    - `frontend/src/AdminApp.jsx` contains `/admin/users?tab=rates` for both `rates` and `subtitle-settings` redirects
    - `frontend/src/shared/lib/adminSearchParams.js` contains no `label: "模型配置"`
    - `frontend/src/shared/lib/adminSearchParams.js` contains no `href: "/admin/models"`
    - `frontend/src/shared/lib/adminSearchParams.js` resolves `/admin/rates` to `"users"`
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'AdminModelsPage|path="models"|/admin/models|label: "模型配置"|href: "/admin/models"' frontend/src/AdminApp.jsx frontend/src/shared/lib/adminSearchParams.js</automated>
  </verify>
  <done>`/admin/models` and the "模型配置" nav entry are gone, and legacy billing links now land on `/admin/users?tab=rates`.</done>
</task>

<task type="auto">
  <name>Task 2: Delete all frontend model-parameter configuration UI components</name>
  <files>
    - frontend/src/features/admin-pages/AdminModelsPage.jsx
    - frontend/src/features/admin-subtitle-settings/AdminSubtitleSettingsTab.jsx
    - frontend/src/features/admin-faster-whisper-settings/AdminFasterWhisperSettingsTab.jsx
    - frontend/src/features/admin-sensevoice-settings/AdminSenseVoiceSettingsTab.jsx
  </files>
  <read_first>
    - frontend/src/features/admin-pages/AdminModelsPage.jsx
    - frontend/src/features/admin-subtitle-settings/AdminSubtitleSettingsTab.jsx
    - frontend/src/features/admin-faster-whisper-settings/AdminFasterWhisperSettingsTab.jsx
    - frontend/src/features/admin-sensevoice-settings/AdminSenseVoiceSettingsTab.jsx
  </read_first>
  <action>
    Delete these files because they are model-parameter configuration surfaces that must not survive this phase:
    - `frontend/src/features/admin-pages/AdminModelsPage.jsx`
    - `frontend/src/features/admin-subtitle-settings/AdminSubtitleSettingsTab.jsx`
    - `frontend/src/features/admin-faster-whisper-settings/AdminFasterWhisperSettingsTab.jsx`
    - `frontend/src/features/admin-sensevoice-settings/AdminSenseVoiceSettingsTab.jsx`

    If `frontend/src/features/admin-subtitle-settings/`, `frontend/src/features/admin-faster-whisper-settings/`, or `frontend/src/features/admin-sensevoice-settings/` become empty after deletion, remove the empty directories as part of the same task.
  </action>
  <acceptance_criteria>
    - None of the four files above exist after the task
    - `rg -n 'AdminSubtitleSettingsTab|AdminFasterWhisperSettingsTab|AdminSenseVoiceSettingsTab|AdminModelsPage' frontend/src` returns no matches
    - `frontend/src/features/admin-rates/AdminRatesTab.jsx` still exists
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'AdminSubtitleSettingsTab|AdminFasterWhisperSettingsTab|AdminSenseVoiceSettingsTab|AdminModelsPage' frontend/src</automated>
  </verify>
  <done>All frontend model-parameter configuration components have been removed.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite surviving deep links to the existing billing workspace and prove no /admin/models references remain</name>
  <files>
    - frontend/src/features/admin-workspaces/AdminPipelineWorkspace.jsx
  </files>
  <read_first>
    - frontend/src/features/admin-workspaces/AdminPipelineWorkspace.jsx
    - frontend/src/features/admin-workspaces/AdminUsersWorkspace.jsx
    - frontend/src/AdminApp.jsx
  </read_first>
  <action>
    In `frontend/src/features/admin-workspaces/AdminPipelineWorkspace.jsx`, replace the `Link to="/admin/models?tab=billing"` billing shortcut with `Link to="/admin/users?tab=rates"`.

    After updating the explicit deep link, scan `frontend/src` for every remaining `/admin/models` reference and remove or rewrite it so the only surviving canonical billing surface is `/admin/users?tab=rates`.

    Do not create a new billing page. Reuse the existing `AdminUsersWorkspace` billing tab as the canonical destination.
  </action>
  <acceptance_criteria>
    - `frontend/src/features/admin-workspaces/AdminPipelineWorkspace.jsx` contains `/admin/users?tab=rates`
    - `rg -n '/admin/models' frontend/src` returns no matches
    - `rg -n 'subtitle-settings' frontend/src` returns matches only for the legacy redirect route in `frontend/src/AdminApp.jsx` if it still exists and now points to `/admin/users?tab=rates`
  </acceptance_criteria>
  <verify>
    <automated>rg -n '/admin/models|/admin/users\\?tab=rates' frontend/src</automated>
  </verify>
  <done>All surviving frontend billing entry points now route into the existing users workspace billing tab.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `rg -n '/admin/models|AdminModelsPage|AdminSubtitleSettingsTab|AdminFasterWhisperSettingsTab|AdminSenseVoiceSettingsTab' frontend/src` returns no matches
2. `npm --prefix frontend run build` exits 0
3. Opening `/admin/rates` in the app lands on `/admin/users?tab=rates`
4. The admin sidebar no longer contains "模型配置"
</verification>

<success_criteria>
- No standalone `/admin/models` route exists
- No "模型配置" nav entry exists
- No frontend model-parameter configuration component survives
- Billing entry points all use an existing admin workspace
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/02.1-admin-bottle-1-0-settings-and-billing-cleanup/02.1-01-SUMMARY.md`
</output>
