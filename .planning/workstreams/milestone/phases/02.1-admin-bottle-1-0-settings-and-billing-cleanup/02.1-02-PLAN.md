---
phase: 02.1-admin-bottle-1-0-settings-and-billing-cleanup
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/routers/admin/router.py
  - app/api/serializers.py
  - app/schemas/admin.py
  - app/schemas/__init__.py
  - app/services/faster_whisper_asr.py
  - app/services/billing.py
  - app/api/routers/billing/wallet.py
  - app/models/billing.py
  - app/models/__init__.py
  - app/db/base.py
  - migrations/versions/20260327_0030_drop_faster_whisper_settings.py
autonomous: true
requirements:
  - BILL-02
  - ADMIN-01
must_haves:
  truths:
    - "No admin API remains for /api/admin/faster-whisper-settings* or /api/admin/subtitle-settings*"
    - "Bottle 1.0 appears in the admin billing list via FASTER_WHISPER_ASR_MODEL in ADMIN_BILLING_MODEL_ORDER"
    - "Runtime Faster Whisper execution no longer depends on the FasterWhisperSetting ORM model or the faster_whisper_settings table"
    - "An Alembic migration drops faster_whisper_settings on upgrade"
    - "Bottle 1.0 runtime billing still resolves through the same canonical model_name used by admin/public billing surfaces"
  artifacts:
    - path: app/api/routers/admin/router.py
      provides: Admin route surface
      not_contains: '/subtitle-settings'
    - path: app/services/billing.py
      provides: Canonical admin/public billing model ordering
      contains: 'FASTER_WHISPER_ASR_MODEL'
    - path: app/services/faster_whisper_asr.py
      provides: Runtime Faster Whisper settings snapshot
      not_contains: 'FasterWhisperSetting'
    - path: migrations/versions/20260327_0030_drop_faster_whisper_settings.py
      provides: Schema cleanup for faster_whisper_settings
      contains: 'drop_table'
---

<objective>
Delete the backend model-configuration control plane, remove the dedicated Faster Whisper settings table from the runtime path, and make Bottle 1.0 billing use the same canonical model key across admin editing, public rates, and actual charge lookup.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@app/api/routers/__init__.py
@app/main.py
@app/api/routers/admin/router.py
@app/api/serializers.py
@app/api/routers/billing/router.py
@app/api/routers/billing/wallet.py
@app/services/billing.py
@app/services/faster_whisper_asr.py
@app/services/lesson_service.py
@app/services/lesson_command_service.py
@app/models/billing.py
@app/models/__init__.py
@app/db/base.py
@migrations/versions/20260327_0029_seed_faster_whisper_billing_rate.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the admin-side model-parameter API surface from the real router package</name>
  <files>
    - app/api/routers/admin/router.py
    - app/api/serializers.py
    - app/schemas/admin.py
    - app/schemas/__init__.py
  </files>
  <read_first>
    - app/api/routers/__init__.py
    - app/main.py
    - app/api/routers/admin/router.py
    - app/api/serializers.py
    - app/schemas/admin.py
    - app/schemas/__init__.py
  </read_first>
  <action>
    Treat `app/api/routers/admin/router.py` as the source-of-truth admin router because `app/api/routers/__init__.py` imports `from app.api.routers.admin.router import router as admin`.

    In `app/api/routers/admin/router.py`, delete every admin model-parameter configuration endpoint and helper for both surfaces:
    - `GET /api/admin/faster-whisper-settings`
    - `GET /api/admin/faster-whisper-settings/history`
    - `PUT /api/admin/faster-whisper-settings`
    - `POST /api/admin/faster-whisper-settings/rollback-last` or rollback equivalent
    - `GET /api/admin/subtitle-settings`
    - `GET /api/admin/subtitle-settings/history`
    - `PUT /api/admin/subtitle-settings`
    - `POST /api/admin/subtitle-settings/rollback-last`

    Remove the helper functions used only by those endpoints, including:
    - `_subtitle_settings_item_with_meta`
    - `_subtitle_settings_item_from_dict`
    - `_load_subtitle_settings_rollback_candidate`
    - any faster-whisper settings helper/rollback functions in the same file

    In `app/api/serializers.py`, delete `to_admin_subtitle_settings_item()` if no remaining import uses it.

    In `app/schemas/admin.py` and `app/schemas/__init__.py`, delete the admin-only schema classes and exports for:
    - `AdminSubtitleSettingsItem`
    - `AdminSubtitleSettingsUpdateRequest`
    - `AdminSubtitleSettingsResponse`
    - `AdminSubtitleSettingsHistoryItem`
    - `AdminSubtitleSettingsHistoryResponse`
    - any `FasterWhisperSettings*` schemas that still exist

    Do not remove `to_public_subtitle_settings()` or the public `subtitle_settings` payload in billing APIs. This task removes only the admin control plane.
  </action>
  <acceptance_criteria>
    - `rg -n '/subtitle-settings|/faster-whisper-settings' app/api/routers/admin/router.py` returns no matches
    - `rg -n 'AdminSubtitleSettings|FasterWhisperSettings' app/schemas/admin.py app/schemas/__init__.py` returns no matches
    - `rg -n 'to_admin_subtitle_settings_item' app/api/serializers.py app/api/routers/admin/router.py` returns no matches
    - `app/api/routers/billing/router.py` still contains `subtitle_settings = get_subtitle_settings(db)`
  </acceptance_criteria>
  <verify>
    <automated>rg -n '/subtitle-settings|/faster-whisper-settings|AdminSubtitleSettings|FasterWhisperSettings|to_admin_subtitle_settings_item' app/api/routers/admin/router.py app/api/serializers.py app/schemas/admin.py app/schemas/__init__.py</automated>
  </verify>
  <done>The admin API no longer exposes any model-parameter configuration workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Remove FasterWhisperSetting from the runtime path and drop the faster_whisper_settings table</name>
  <files>
    - app/services/faster_whisper_asr.py
    - app/models/billing.py
    - app/models/__init__.py
    - app/db/base.py
    - migrations/versions/20260327_0030_drop_faster_whisper_settings.py
  </files>
  <read_first>
    - app/services/faster_whisper_asr.py
    - app/models/billing.py
    - app/models/__init__.py
    - app/db/base.py
    - migrations/versions/20260327_0029_seed_faster_whisper_billing_rate.py
  </read_first>
  <action>
    In `app/services/faster_whisper_asr.py`, remove the database-backed runtime settings path entirely:
    - delete the imports of `SessionLocal` and `FasterWhisperSetting`
    - delete `_use_database_backed_settings()`
    - delete `_ensure_faster_whisper_settings_schema()`
    - delete `get_faster_whisper_settings()`
    - delete `_resolve_runtime_settings_row()`
    - rewrite `_runtime_settings_snapshot()` so it builds `FasterWhisperSettingsSnapshot` directly from `_DEFAULT_SETTINGS` plus `_resolve_device_config()`, with no DB session or ORM access

    In `app/models/billing.py`, delete the `FasterWhisperSetting` ORM model class.
    In `app/models/__init__.py`, remove `FasterWhisperSetting` from the imports and `__all__`.
    In `app/db/base.py`, remove `"faster_whisper_settings"` from `BUSINESS_TABLES`.

    Create `migrations/versions/20260327_0030_drop_faster_whisper_settings.py` with an Alembic upgrade that drops the `faster_whisper_settings` table if it exists and a downgrade that recreates it with these columns:
    - `id`
    - `device`
    - `compute_type`
    - `cpu_threads`
    - `num_workers`
    - `beam_size`
    - `vad_filter`
    - `condition_on_previous_text`
    - `updated_at`
    - `updated_by_user_id`
  </action>
  <acceptance_criteria>
    - `rg -n 'FasterWhisperSetting|SessionLocal' app/services/faster_whisper_asr.py app/models/billing.py app/models/__init__.py` returns no matches
    - `app/services/faster_whisper_asr.py` still defines `_DEFAULT_SETTINGS`, `_resolve_device_config()`, and `_runtime_settings_snapshot()`
    - `app/db/base.py` contains no `faster_whisper_settings` entry in `BUSINESS_TABLES`
    - `migrations/versions/20260327_0030_drop_faster_whisper_settings.py` exists
    - the new migration contains `drop_table("faster_whisper_settings"` or equivalent conditional drop logic
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'FasterWhisperSetting|SessionLocal|faster_whisper_settings' app/services/faster_whisper_asr.py app/models/billing.py app/models/__init__.py app/db/base.py && rg -n 'drop_table|faster_whisper_settings' migrations/versions/20260327_0030_drop_faster_whisper_settings.py</automated>
  </verify>
  <done>Faster Whisper runtime no longer depends on a dedicated settings table, and Alembic knows how to drop that table.</done>
</task>

<task type="auto">
  <name>Task 3: Align Bottle 1.0 billing on one canonical model_name across admin, public, and charge entry points</name>
  <files>
    - app/services/billing.py
    - app/api/routers/billing/wallet.py
  </files>
  <read_first>
    - app/services/billing.py
    - app/api/routers/billing/wallet.py
    - app/services/lesson_service.py
    - app/services/lesson_command_service.py
    - app/services/asr_model_registry.py
  </read_first>
  <action>
    In `app/services/billing.py`, make `ADMIN_BILLING_MODEL_ORDER` exactly:
    - `FAST_CLOUD_MODEL`
    - `MT_FLASH_MODEL`
    - `FASTER_WHISPER_ASR_MODEL`

    Keep `PUBLIC_BILLING_MODEL_ORDER` containing `FAST_CLOUD_MODEL` and `FASTER_WHISPER_ASR_MODEL`, and do not rename the canonical Bottle 1.0 model key away from `FASTER_WHISPER_ASR_MODEL`.

    In `app/api/routers/billing/wallet.py`, import `FASTER_WHISPER_ASR_MODEL` and replace any literal `"faster-whisper-medium"` fallback with that constant so the wallet entry point shares the same canonical identifier.

    While reading `app/services/lesson_service.py` and `app/services/lesson_command_service.py`, confirm the runtime charge path already calls `get_model_rate(db, asr_model)` and writes ledger events with `model_name=asr_model`. If any literal Bottle 1.0 model key remains in those billing call paths, normalize it to `FASTER_WHISPER_ASR_MODEL` instead of a hard-coded string.
  </action>
  <acceptance_criteria>
    - `app/services/billing.py` contains `FASTER_WHISPER_ASR_MODEL` inside `ADMIN_BILLING_MODEL_ORDER`
    - `app/api/routers/billing/wallet.py` contains `FASTER_WHISPER_ASR_MODEL` and no hard-coded `"faster-whisper-medium"` fallback literal
    - `rg -n 'get_model_rate\\(db, asr_model\\)|model_name=asr_model' app/services/lesson_service.py app/services/lesson_command_service.py` returns matches
    - `app/services/asr_model_registry.py` still defines `FASTER_WHISPER_ASR_MODEL = "faster-whisper-medium"`
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'ADMIN_BILLING_MODEL_ORDER|FASTER_WHISPER_ASR_MODEL|faster-whisper-medium|get_model_rate\\(db, asr_model\\)|model_name=asr_model' app/services/billing.py app/api/routers/billing/wallet.py app/services/lesson_service.py app/services/lesson_command_service.py app/services/asr_model_registry.py</automated>
  </verify>
  <done>Admin billing, public billing, wallet fallback, and lesson charge paths all point at the same Bottle 1.0 model key.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pytest tests/unit/test_billing_cleanup.py -q` passes
2. `pytest tests/unit/test_faster_whisper_asr.py -q` passes
3. `pytest tests/integration/test_regression_api.py -k "billing or subtitle_settings or faster_whisper" -q` passes
4. `alembic upgrade head` applies the new migration without leaving `faster_whisper_settings` behind
5. `GET /api/admin/billing-rates` includes `faster-whisper-medium`, while `GET /api/admin/faster-whisper-settings` and `GET /api/admin/subtitle-settings` return 404
</verification>

<success_criteria>
- No admin model-parameter endpoints remain
- FasterWhisperSetting ORM model and table are removed from the runtime path
- Bottle 1.0 billing appears in admin rates and uses the canonical `FASTER_WHISPER_ASR_MODEL`
- Public billing still exposes Bottle 1.0 and subtitle settings without an admin control plane
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/02.1-admin-bottle-1-0-settings-and-billing-cleanup/02.1-02-SUMMARY.md`
</output>
