---
phase: 02.1-admin-bottle-1-0-settings-and-billing-cleanup
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - tests/unit/test_billing_cleanup.py
  - tests/unit/test_faster_whisper_asr.py
  - tests/integration/test_regression_api.py
  - tests/integration/test_admin_console_api.py
  - tests/e2e/test_e2e_key_flows.py
autonomous: true
requirements:
  - BILL-02
  - ADMIN-01
must_haves:
  truths:
    - "Automated tests prove Bottle 1.0 is present in admin/public billing while admin model-config routes return 404"
    - "Automated tests prove Faster Whisper runtime no longer depends on the FasterWhisperSetting ORM/table path"
    - "Automated tests no longer expect subtitle-settings or faster-whisper-settings happy paths in admin"
    - "Manual verification can prove /admin/models is gone and /admin/users?tab=rates is the surviving billing entry"
  artifacts:
    - path: tests/unit/test_billing_cleanup.py
      provides: Unit coverage for removed admin config routes and Bottle 1.0 billing visibility
    - path: tests/unit/test_faster_whisper_asr.py
      provides: Runtime-default coverage for Faster Whisper after DB settings removal
    - path: tests/e2e/test_e2e_key_flows.py
      provides: API-level admin/public billing continuity smoke coverage
---

<objective>
Replace stale verification with tests that match the new 2.1 contract: no admin model-config control plane, Bottle 1.0 visible in real billing surfaces, and runtime Faster Whisper no longer coupled to the deleted settings table.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@tests/unit/test_billing_cleanup.py
@tests/unit/test_faster_whisper_asr.py
@tests/integration/test_regression_api.py
@tests/integration/test_admin_console_api.py
@tests/e2e/test_e2e_key_flows.py
@app/services/billing.py
@app/services/faster_whisper_asr.py
@app/api/routers/admin/router.py
@frontend/src/AdminApp.jsx
@frontend/src/shared/lib/adminSearchParams.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite unit tests to match the deleted admin control plane and default-only runtime</name>
  <files>
    - tests/unit/test_billing_cleanup.py
    - tests/unit/test_faster_whisper_asr.py
  </files>
  <read_first>
    - tests/unit/test_billing_cleanup.py
    - tests/unit/test_faster_whisper_asr.py
    - app/services/billing.py
    - app/services/faster_whisper_asr.py
    - app/api/routers/admin/router.py
  </read_first>
  <action>
    In `tests/unit/test_billing_cleanup.py`, remove stale assumptions that admin subtitle/faster-whisper routes still partially exist. Update the file so it asserts:
    - `GET /api/admin/faster-whisper-settings` returns 404
    - `GET /api/admin/faster-whisper-settings/history` returns 404
    - `PUT /api/admin/faster-whisper-settings` returns 404
    - `POST /api/admin/faster-whisper-settings/rollback-last` returns 404
    - `GET /api/admin/subtitle-settings` returns 404
    - `GET /api/admin/subtitle-settings/history` returns 404
    - `PUT /api/admin/subtitle-settings` returns 404
    - `POST /api/admin/subtitle-settings/rollback-last` returns 404
    - `/api/admin/billing-rates` and `/api/billing/rates` still include `FASTER_WHISPER_ASR_MODEL`

    In `tests/unit/test_faster_whisper_asr.py`, add coverage that `_runtime_settings_snapshot()` can build from `_DEFAULT_SETTINGS` without importing or querying `FasterWhisperSetting` / `SessionLocal`, and preserve the existing runtime transcription behavior under monkeypatched model/cache helpers.
  </action>
  <acceptance_criteria>
    - `tests/unit/test_billing_cleanup.py` contains assertions for both removed `faster-whisper-settings` and removed `subtitle-settings` admin routes
    - `tests/unit/test_billing_cleanup.py` contains assertions that admin/public billing still include `FASTER_WHISPER_ASR_MODEL`
    - `tests/unit/test_faster_whisper_asr.py` contains a test covering `_runtime_settings_snapshot()` with no DB-backed settings path
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/unit/test_billing_cleanup.py tests/unit/test_faster_whisper_asr.py -q</automated>
  </verify>
  <done>Unit tests now assert the new contract instead of the removed admin configuration flows.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite integration regressions away from removed admin config workflows and toward billing continuity</name>
  <files>
    - tests/integration/test_regression_api.py
    - tests/integration/test_admin_console_api.py
  </files>
  <read_first>
    - tests/integration/test_regression_api.py
    - tests/integration/test_admin_console_api.py
    - app/api/routers/admin/router.py
    - app/services/billing.py
  </read_first>
  <action>
    In `tests/integration/test_regression_api.py`, delete or rewrite the existing happy-path tests that currently exercise `/api/admin/subtitle-settings*` and `/api/admin/faster-whisper-settings*`.

    Replace them with regression coverage that asserts:
    - the removed admin config endpoints return 404
    - `/api/admin/billing-rates` includes a `model_name` of `faster-whisper-medium`
    - updating `/api/admin/billing-rates/faster-whisper-medium` works as the admin editing path for Bottle 1.0
    - `/api/billing/rates` still returns Bottle 1.0 plus `subtitle_settings`

    In `tests/integration/test_admin_console_api.py`, remove any assumptions that admin subtitle settings are a supported control surface and replace them with a focused admin-console regression that the removed endpoints return 404 for an authenticated admin.
  </action>
  <acceptance_criteria>
    - `tests/integration/test_regression_api.py` contains no passing-happy-path test for `/api/admin/subtitle-settings`
    - `tests/integration/test_regression_api.py` contains no passing-happy-path test for `/api/admin/faster-whisper-settings`
    - at least one integration test asserts `/api/admin/billing-rates/faster-whisper-medium` is editable
    - at least one integration test asserts removed admin config endpoints return 404
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/integration/test_regression_api.py tests/integration/test_admin_console_api.py -k "billing or subtitle_settings or faster_whisper" -q</automated>
  </verify>
  <done>Integration regressions now cover the real Bottle 1.0 billing path and the deleted control-plane routes.</done>
</task>

<task type="auto">
  <name>Task 3: Extend API-level smoke coverage for the surviving admin billing surface and final manual verification</name>
  <files>
    - tests/e2e/test_e2e_key_flows.py
  </files>
  <read_first>
    - tests/e2e/test_e2e_key_flows.py
    - frontend/src/AdminApp.jsx
    - frontend/src/shared/lib/adminSearchParams.js
    - app/api/routers/admin/router.py
  </read_first>
  <action>
    Extend `tests/e2e/test_e2e_key_flows.py` so the existing admin billing smoke flow also verifies:
    - admin `GET /api/admin/billing-rates` includes `faster-whisper-medium`
    - public `GET /api/billing/rates` includes `faster-whisper-medium`
    - removed admin config endpoints such as `GET /api/admin/faster-whisper-settings` and `GET /api/admin/subtitle-settings` return 404

    Add a manual verification checklist in the summary/verification notes for the executor to confirm in a real browser:
    - `/admin/models` no longer exists as a usable page
    - the sidebar contains no "模型配置"
    - `/admin/users?tab=rates` is the surviving billing entry
    - Bottle 1.0 appears there as a normal row with enable/disable and price editing
  </action>
  <acceptance_criteria>
    - `tests/e2e/test_e2e_key_flows.py` contains assertions for admin/public Bottle 1.0 billing visibility
    - `tests/e2e/test_e2e_key_flows.py` contains 404 assertions for removed admin config endpoints
    - the manual verification checklist mentions `/admin/models`, sidebar nav, `/admin/users?tab=rates`, and Bottle 1.0 row behavior
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/e2e/test_e2e_key_flows.py -k "admin_update_rate_visible_in_public_api" -q</automated>
  </verify>
  <done>Smoke coverage and manual verification both target the surviving billing surface instead of the deleted model-config surface.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pytest tests/unit/test_billing_cleanup.py tests/unit/test_faster_whisper_asr.py -q` exits 0
2. `pytest tests/integration/test_regression_api.py tests/integration/test_admin_console_api.py -k "billing or subtitle_settings or faster_whisper" -q` exits 0
3. `pytest tests/e2e/test_e2e_key_flows.py -k "admin_update_rate_visible_in_public_api" -q` exits 0
4. Manual browser/API verification confirms:
   - `/admin/models` is gone
   - the sidebar has no "模型配置"
   - `/admin/users?tab=rates` is the surviving billing entry
   - Bottle 1.0 is present there as a normal model row
</verification>

<success_criteria>
- Verification no longer expects removed admin config flows to exist
- Automated tests prove Bottle 1.0 billing continuity through admin/public surfaces
- Automated tests prove removed admin config endpoints return 404
- Manual verification proves the frontend information architecture matches the new context
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/02.1-admin-bottle-1-0-settings-and-billing-cleanup/02.1-03-SUMMARY.md`
</output>
