# Phase 2.1: Admin Bottle 1.0 Settings & Billing Cleanup - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-27
**Phase:** 02.1-admin-bottle-1-0-settings-and-billing-cleanup
**Areas discussed:** 管理台信息架构, Bottle 1.0 计费验收, 旧模型配置删除范围

---

## 管理台信息架构

| Option | Description | Selected |
|--------|-------------|----------|
| 取消独立模型配置页 | 整个取消 `/admin/models` 和“模型配置”导航，把计费放到现有工作台里 | ✓ |
| 保留模型配置页 | 保留页面，但只剩计费，不再有默认策略或 Legacy ASR | |
| Other | 用户自由描述其他 IA 方案 | |

**User's choice:** 整个取消 `/admin/models` 和“模型配置”导航；计费放到现有的管理工作台里。
**Notes:** 不接受用别名或换壳方式保留独立模型配置页面。

---

## Bottle 1.0 计费验收

| Option | Description | Selected |
|--------|-------------|----------|
| 真正接通同一模型键 | Bottle 1.0 作为普通模型行出现，可启停、可改价，而且实际扣费走同一个 `model_name` | ✓ |
| 仅展示模型行 | 只要页面上能看到 Bottle 1.0 即可 | |
| Other | 用户自由描述其他验收标准 | |

**User's choice:** 必须作为普通模型行出现，可启停、可改价，而且实际 Bottle 1.0 扣费就走这同一个 `model_name`。
**Notes:** UI 展示不是验收终点，必须证明运行时账单路径也接到同一行。

---

## 旧模型配置删除范围

| Option | Description | Selected |
|--------|-------------|----------|
| 彻底删除 | 前端入口、后端接口、相关表都删除，不保留任何专用模型参数配置后台 | ✓ |
| 只删 UI | 前端移除，后端接口或表暂时保留 | |
| Other | 用户自由描述其他保留策略 | |

**User's choice:** 删掉，不需要管理台里面对任何模型进行参数配置；除了前端，实际接口和表都要删去。
**Notes:** 用户把范围从“旧 Bottle 1.0 专用配置”进一步收紧为“管理台不承担任何模型参数配置职责”。

---

## the agent's Discretion

- 选择哪个现有 admin workspace 承接计费入口
- 删除旧表时采用删除还是安全迁移的具体技术路径
- 移除管理台默认策略后，运行时默认值由哪个非 admin 配置源承接

## Deferred Ideas

- Phase 3 lesson output consistency
- Phase 4 desktop link import
- Later admin/billing polish beyond the urgent 2.1 repair
