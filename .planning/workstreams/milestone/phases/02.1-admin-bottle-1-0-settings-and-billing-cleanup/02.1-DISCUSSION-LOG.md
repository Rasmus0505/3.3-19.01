# Phase 2.1: Admin Bottle 1.0 Settings & Billing Cleanup - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-27
**Phase:** 02.1-admin-bottle-1-0-settings-and-billing-cleanup
**Areas discussed:** admin surface restructuring, Bottle 1.0 settings removal, billing-model continuity

---

## Admin surface restructuring

| Option | Description | Selected |
|--------|-------------|----------|
| Keep "模型管理" and only change tab order | Retain the existing page shell and shuffle billing/Bottle 1.0 placement | |
| Remove "模型管理" and fold surviving work into existing admin surfaces | Delete the page-level concept and keep only the billing-oriented flow | ✓ |
| Create a new standalone Bottle 1.0 settings page | Replace the current tab with another dedicated admin page | |

**User's choice:** Remove the admin "模型管理" surface entirely.
**Notes:** User explicitly said the issue must be solved now, not deferred to a later admin phase, and corrected the phase placement to immediately follow Phase 2.

---

## Bottle 1.0 dedicated settings

| Option | Description | Selected |
|--------|-------------|----------|
| Preserve the dedicated Bottle 1.0 settings flow | Keep the standalone Bottle 1.0 tab and backend settings contract | |
| Move Bottle 1.0 settings elsewhere as another standalone feature | Keep the concept but relocate it | |
| Delete the dedicated Bottle 1.0 settings frontend and functional code | Remove the separate settings path end-to-end | ✓ |

**User's choice:** Delete the separate Bottle 1.0 settings UI and actual function code.
**Notes:** The cleanup is intended to remove the special-case admin path, not merely hide it.

---

## Billing-model continuity

| Option | Description | Selected |
|--------|-------------|----------|
| Leave billing unchanged and only remove UI | No Bottle 1.0 billing row added during this cleanup | |
| Add Bottle 1.0 as a normal billing model entry and wire the contracts through | Admin billing config, public billing rates, and runtime billing all share the same model identity | ✓ |
| Add Bottle 1.0 as a display-only billing row | Show it in billing UI without guaranteeing backend/runtime continuity | |

**User's choice:** Add Bottle 1.0 into billing configuration and pay attention to end-to-end model integration.
**Notes:** The user explicitly called out "要注意这个模型新增的衔接", so planner/executor must verify model-name continuity rather than stopping at UI changes.

---

## the agent's Discretion

- Exact post-removal admin navigation wording
- Exact implementation path for retiring the obsolete Faster Whisper settings persistence if safe to leave unused temporarily

## Deferred Ideas

- None — discussion stayed within this urgent cleanup scope
