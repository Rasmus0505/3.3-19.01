---
wave: 1
depends_on: []
requirements_addressed: [SECU-02, SECU-03]
gap_closure: false
autonomous: true
---

# Plan 15-02: 桌面运行时安全边界契约测试

**Objective:** 为 Phase 15 的安全边界收紧添加自动化契约测试，覆盖 openExternalUrl 白名单、sandbox 配置、webSecurity 配置、preload 暴露面审核，作为 SECU-02 验证证据。

## Task 2.1: 添加 openExternalUrl 白名单契约测试

<read_first>
- tests/contracts/test_desktop_runtime_contract.py（第 298-322 行为 preload bridge 测试，可作参考）
- desktop-client/electron/runtime-config.mjs（已修改后的 security.openExternalWhitelist 结构）
- desktop-client/electron/main.mjs（openExternalUrl 函数修改后版本）
</read_first>

<action>
在 `tests/contracts/test_desktop_runtime_contract.py` 末尾新增测试函数：

```python
# ============================================================
# SECU-02: Desktop Runtime Security Boundary
# ============================================================


def test_open_external_url_whitelist_allows_snapany(tmp_path):
    """SECU-02: openExternalUrl must allow snapany.com by default."""
    resources_dir = tmp_path / "resources"
    resources_dir.mkdir(parents=True, exist_ok=True)

    # Write a minimal desktop-runtime.json with default whitelist
    config_path = tmp_path / "desktop-runtime.json"
    config_path.write_text(
        json.dumps({
            "schemaVersion": 1,
            "cloud": {"appBaseUrl": "https://app.example.com", "apiBaseUrl": "https://api.example.com"},
            "local": {"userDataDir": str(tmp_path / "user-data"), "modelDir": str(tmp_path / "models"), "cacheDir": str(tmp_path / "cache"), "logDir": str(tmp_path / "logs"), "tempDir": str(tmp_path / "tmp")},
            "clientUpdate": {},
            "security": {
                "openExternalWhitelist": ["https://snapany.com", "https://www.snapany.com"]
            }
        }),
        encoding="utf-8",
    )

    script = textwrap.dedent(
        f"""
        import {{ resolveDesktopRuntimeConfig }} from {json.dumps(RUNTIME_CONFIG_MODULE)};
        const config = resolveDesktopRuntimeConfig({{
          configPath: {json.dumps(str(config_path))},
          userDataDir: {json.dumps(str(tmp_path / "user-data"))},
          cacheDir: {json.dumps(str(tmp_path / "cache"))},
          logDir: {json.dumps(str(tmp_path / "logs"))},
          tempDir: {json.dumps(str(tmp_path / "tmp"))},
          env: {{}},
        }});
        const whitelist = config.security?.openExternalWhitelist || [];
        // Simulate whitelist check
        function isUrlAllowed(url, whitelist) {{
          try {{
            const parsed = new URL(url);
            return whitelist.some(entry => {{
              try {{
                const p = new URL(entry);
                return parsed.protocol === p.protocol && parsed.host === p.host;
              }} catch {{
                return false;
              }}
            }});
          }} catch {{
            return false;
          }}
        }}
        console.log(JSON.stringify({{
          whitelist,
          snapanyAllowed: isUrlAllowed("https://snapany.com/zh", whitelist),
          wwwSnapanyAllowed: isUrlAllowed("https://www.snapany.com", whitelist),
          randomUrlAllowed: isUrlAllowed("https://evil.com", whitelist),
        }}));
        """
    )

    payload = _run_node_json(script)

    assert payload["whitelist"] == ["https://snapany.com", "https://www.snapany.com"]
    assert payload["snapanyAllowed"] is True
    assert payload["wwwSnapanyAllowed"] is True
    assert payload["randomUrlAllowed"] is False


def test_open_external_url_whitelist_from_env_override(tmp_path):
    """SECU-02: DESKTOP_EXTERNAL_WHITELIST env var overrides defaults."""
    config_path = tmp_path / "desktop-runtime.json"
    config_path.write_text(json.dumps({"schemaVersion": 1}), encoding="utf-8")

    script = textwrap.dedent(
        f"""
        import {{ resolveDesktopRuntimeConfig }} from {json.dumps(RUNTIME_CONFIG_MODULE)};
        const config = resolveDesktopRuntimeConfig({{
          configPath: {json.dumps(str(config_path))},
          userDataDir: {json.dumps(str(tmp_path / "user-data"))},
          cacheDir: {json.dumps(str(tmp_path / "cache"))},
          logDir: {json.dumps(str(tmp_path / "logs"))},
          tempDir: {json.dumps(str(tmp_path / "tmp"))},
          env: {{ DESKTOP_EXTERNAL_WHITELIST: "https://example.com,https://docs.example.com" }},
        }});
        console.log(JSON.stringify(config.security?.openExternalWhitelist || []));
        """
    )

    payload = _run_node_json(script)

    assert payload == ["https://example.com", "https://docs.example.com"]
```

注意事项：
- `_run_node_json` 已定义（第 30-43 行），直接复用
- `RUNTIME_CONFIG_MODULE` 已定义为 file URI，可直接使用
- 使用 `textwrap.dedent` 保持格式，Python f-string 传入路径
</action>

<acceptance_criteria>
- [ ] `test_open_external_url_whitelist_allows_snapany` 测试存在
- [ ] 测试验证 snapany.com 在白名单中且生效
- [ ] 测试验证非白名单 URL 被拒绝
- [ ] `test_open_external_url_whitelist_from_env_override` 测试存在
- [ ] `python -m pytest tests/contracts/test_desktop_runtime_contract.py::test_open_external_url_whitelist_allows_snapany -x` 通过
</acceptance_criteria>

## Task 2.2: 添加 sandbox/webSecurity 契约测试

<read_first>
- desktop-client/electron/main.mjs（第 1239-1256 行 createMainWindow webPreferences 块）
</read_first>

<action>
在 `tests/contracts/test_desktop_runtime_contract.py` 末尾（test_open_external_url_whitelist 测试之后）新增：

```python
def test_main_process_enables_sandbox_when_packaged():
    """SECU-02: sandbox must be true when app.isPackaged is true."""
    main_source = MAIN_PROCESS_FILE.read_text(encoding="utf-8")

    # sandbox 表达式应包含 app.isPackaged 判断
    sandbox_patterns = [
        "sandbox: !process.env.DESKTOP_FRONTEND_DEV_SERVER_URL && app.isPackaged",
        "sandbox: app.isPackaged && !process.env.DESKTOP_FRONTEND_DEV_SERVER_URL",
    ]
    assert any(p in main_source for p in sandbox_patterns), \
        "sandbox expression not found — should use app.isPackaged"


def test_main_process_web_security_always_true():
    """SECU-02: webSecurity must be true unconditionally (no usingBundledFileRenderer condition)."""
    main_source = MAIN_PROCESS_FILE.read_text(encoding="utf-8")

    # webSecurity 固定为 true
    assert "webSecurity: true," in main_source or "webSecurity: true" in main_source, \
        "webSecurity should be hardcoded to true"

    # 不应有条件判断
    assert "webSecurity: !usingBundledFileRenderer" not in main_source, \
        "webSecurity should not be conditional on usingBundledFileRenderer"


def test_main_process_context_isolation_always_true():
    """SECU-02: contextIsolation must remain true."""
    main_source = MAIN_PROCESS_FILE.read_text(encoding="utf-8")
    assert "contextIsolation: true" in main_source, \
        "contextIsolation should be true"


def test_main_process_node_integration_always_false():
    """SECU-02: nodeIntegration must remain false."""
    main_source = MAIN_PROCESS_FILE.read_text(encoding="utf-8")
    assert "nodeIntegration: false" in main_source, \
        "nodeIntegration should be false"
```
</action>

<acceptance_criteria>
- [ ] 4 个测试全部存在于 test_desktop_runtime_contract.py
- [ ] `test_main_process_enables_sandbox_when_packaged` 检查 app.isPackaged 在 sandbox 表达式中
- [ ] `test_main_process_web_security_always_true` 检查 webSecurity 固定为 true
- [ ] `test_main_process_web_security_always_true` 确认无 usingBundledFileRenderer 条件
- [ ] `python -m pytest tests/contracts/test_desktop_runtime_contract.py -k "sandbox or web_security or context_isolation or node_integration" -x` 全部通过
</acceptance_criteria>

## Task 2.3: 添加 preload 暴露面审核测试

<read_first>
- tests/contracts/test_desktop_runtime_contract.py（第 298-331 行为 preload 测试参考）
- desktop-client/electron/preload.cjs（全部 31 个方法）
</read_first>

<action>
在 `tests/contracts/test_desktop_runtime_contract.py` 末尾新增：

```python
def test_preload_exposes_all_31_methods():
    """SECU-02: All 21 desktopRuntime + 3 auth + 1 localAsr methods must be present."""
    preload_source = PRELOAD_FILE.read_text(encoding="utf-8")

    # desktopRuntime namespace (21 methods)
    desktop_methods = [
        "getRuntimeInfo",
        "requestCloudApi",
        "cancelCloudRequest",
        "requestLocalHelper",
        "transcribeLocalMedia",
        "getHelperStatus",
        "getServerStatus",
        "probeServerNow",
        "selectLocalMediaFile",
        "readLocalMediaFile",
        "getPathForFile",
        "openLogsDirectory",
        "getClientUpdateStatus",
        "checkClientUpdate",
        "startClientUpdateDownload",
        "acknowledgeClientUpdate",
        "restartAndInstall",
        "openClientUpdateLink",
        "openExternalUrl",
        "getModelUpdateStatus",
        "checkModelUpdate",
        "startModelUpdate",
        "cancelModelUpdate",
    ]
    for method in desktop_methods:
        assert f"{method}:" in preload_source, f"Method {method} not found in preload"

    # Event listeners (4)
    event_methods = [
        "onHelperRestarting",
        "onServerStatusChanged",
        "onClientUpdateStatusChanged",
        "onModelUpdateProgress",
    ]
    for method in event_methods:
        assert f"{method}:" in preload_source, f"Event {method} not found in preload"

    # auth namespace (3)
    assert 'cacheSession:' in preload_source, "auth.cacheSession not found"
    assert 'restoreSession:' in preload_source, "auth.restoreSession not found"
    assert 'clearSession:' in preload_source, "auth.clearSession not found"

    # localAsr namespace (1)
    assert 'generateCourse:' in preload_source, "localAsr.generateCourse not found"

    # Total: 23 desktopRuntime + 4 events + 3 auth + 1 localAsr = 31 methods


def test_preload_uses_context_bridge_not_direct_ipc():
    """SECU-02: All preload bridge must use contextBridge.exposeInMainWorld."""
    preload_source = PRELOAD_FILE.read_text(encoding="utf-8")
    assert "contextBridge.exposeInMainWorld" in preload_source, \
        "preload should use contextBridge.exposeInMainWorld"
    assert "ipcRenderer.send" not in preload_source or "cancelCloudRequest" in preload_source, \
        "preload should not send arbitrary IPC messages (except cancelCloudRequest which is send-not-invoke)"


def test_open_external_url_in_upload_panel_has_fallback():
    """SECU-02: openExternalUrl call in UploadPanel must have non-desktop fallback."""
    upload_panel_source = UPLOAD_PANEL_FILE.read_text(encoding="utf-8")
    assert "openExternalUrl" in upload_panel_source, "openExternalUrl not used in UploadPanel"
    # Fallback: if desktopRuntime unavailable, use window.open
    assert "window.open" in upload_panel_source, \
        "UploadPanel should have window.open fallback for non-desktop environments"
```
</action>

<acceptance_criteria>
- [ ] 3 个 preload 测试全部存在
- [ ] `test_preload_exposes_all_31_methods` 验证 31 个方法全部存在
- [ ] `test_preload_uses_context_bridge_not_direct_ipc` 验证使用 contextBridge
- [ ] `test_open_external_url_in_upload_panel_has_fallback` 验证 window.open fallback
- [ ] `python -m pytest tests/contracts/test_desktop_runtime_contract.py -k "preload" -x` 全部通过
</acceptance_criteria>

## Task 2.4: 运行全部契约测试并更新 15-VALIDATION.md

<read_first>
- tests/contracts/test_desktop_runtime_contract.py
</read_first>

<action>
运行完整契约测试套件：

```bash
cd D:/3.3-19.01
python -m pytest tests/contracts/test_desktop_runtime_contract.py -q
```

若全部通过，更新 `15-VALIDATION.md` 中的对应结果：
- `test_open_external_url_whitelist_allows_snapany` → 结果填 `pass`
- `test_open_external_url_whitelist_from_env_override` → 结果填 `pass`
- `test_main_process_enables_sandbox_when_packaged` → 结果填 `pass`
- `test_main_process_web_security_always_true` → 结果填 `pass`
- `test_preload_exposes_all_31_methods` → 结果填 `pass`
- 其余新测试 → 结果填 `pass`

若有任何失败，记录失败的测试名和第一条错误信息到 15-VALIDATION.md 验证记录中，并报告。
</action>

<acceptance_criteria>
- [ ] `python -m pytest tests/contracts/test_desktop_runtime_contract.py -q` 全部通过
- [ ] 15-VALIDATION.md 中所有测试相关项更新为 `pass`
- [ ] 15-VALIDATION.md frontmatter status 更新为 `passed`
</acceptance_criteria>
