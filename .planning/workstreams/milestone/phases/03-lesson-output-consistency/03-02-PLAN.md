---
phase: 03-lesson-output-consistency
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - frontend/src/store/slices/lessonSlice.ts
  - frontend/src/app/learning-shell/LearningShellContainer.jsx
  - frontend/src/app/learning-shell/LearningShellPanelContent.jsx
  - frontend/src/features/lessons/LessonList.jsx
  - tests/integration/test_regression_api.py
  - tests/e2e/test_e2e_key_flows.py
autonomous: true
requirements:
  - LESS-02
  - LEARN-02
must_haves:
  truths:
    - "History and lesson-open flow remain source-agnostic once a lesson exists"
    - "Sentence count, progress summary, and subtitle recovery all stay keyed to canonical lesson IDs"
    - "Users can open lessons from history and continue learning regardless of whether the lesson originated from desktop-local or cloud generation"
  artifacts:
    - path: frontend/src/store/slices/lessonSlice.ts
      provides: Canonical history/detail hydration
      contains: 'buildCatalogProgressSnapshot'
    - path: frontend/src/app/learning-shell/LearningShellContainer.jsx
      provides: History/detail/start-learning orchestration
      contains: 'requestOriginalSubtitleVariant'
    - path: frontend/src/features/lessons/LessonList.jsx
      provides: Source-agnostic history card surface
      contains: 'startLessonFromHistory'
---

<objective>
Normalize the history and lesson-opening flow so generated lessons expose the same sentence review and start-learning behavior regardless of source runtime, with local subtitle recovery remaining supplemental to the canonical lesson record.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@frontend/src/store/slices/lessonSlice.ts
@frontend/src/app/learning-shell/LearningShellContainer.jsx
@frontend/src/app/learning-shell/LearningShellPanelContent.jsx
@frontend/src/features/lessons/LessonList.jsx
@frontend/src/shared/media/localSubtitleStore.js
@app/services/lesson_query_service.py
@app/repositories/lessons.py
@tests/integration/test_regression_api.py
@tests/e2e/test_e2e_key_flows.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize catalog/detail hydration around canonical lesson and progress metadata</name>
  <files>
    - frontend/src/store/slices/lessonSlice.ts
    - frontend/src/app/learning-shell/LearningShellPanelContent.jsx
  </files>
  <read_first>
    - frontend/src/store/slices/lessonSlice.ts
    - frontend/src/app/learning-shell/LearningShellPanelContent.jsx
    - app/services/lesson_query_service.py
    - app/repositories/lessons.py
  </read_first>
  <action>
    In `frontend/src/store/slices/lessonSlice.ts`, keep catalog and detail hydration keyed to canonical lesson IDs and persisted catalog/detail fields only.

    Preserve and normalize these exact history/detail fields when hydrating state:
    - `sentence_count`
    - `progress_summary.current_sentence_index`
    - `progress_summary.completed_sentence_count`
    - `progress_summary.last_played_at_ms`
    - `subtitle_cache_seed`
    - `currentLesson`
    - `lessonCardMetaMap`

    In `frontend/src/app/learning-shell/LearningShellPanelContent.jsx`, keep the history panel and immersive panel selection driven by `currentLesson`, `lessonCardMetaMap`, and the shared learner shell props rather than by source-specific branches.
  </action>
  <acceptance_criteria>
    - `frontend/src/store/slices/lessonSlice.ts` contains `sentence_count`, `progress_summary`, and `subtitle_cache_seed` handling
    - `frontend/src/store/slices/lessonSlice.ts` updates `lessonCardMetaMap` with canonical `sentenceCount` and `progress`
    - `frontend/src/app/learning-shell/LearningShellPanelContent.jsx` still routes history through `LessonList` and immersive mode through `ImmersiveLessonPage`
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'sentence_count|progress_summary|subtitle_cache_seed|lessonCardMetaMap|currentLesson|LessonList|ImmersiveLessonPage' frontend/src/store/slices/lessonSlice.ts frontend/src/app/learning-shell/LearningShellPanelContent.jsx</automated>
  </verify>
  <done>History/detail hydration now stays anchored on canonical lesson and progress metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Keep subtitle recovery and history entry supplemental to the canonical lesson record</name>
  <files>
    - frontend/src/app/learning-shell/LearningShellContainer.jsx
    - frontend/src/features/lessons/LessonList.jsx
  </files>
  <read_first>
    - frontend/src/app/learning-shell/LearningShellContainer.jsx
    - frontend/src/features/lessons/LessonList.jsx
    - frontend/src/shared/media/localSubtitleStore.js
    - frontend/src/store/slices/lessonSlice.ts
  </read_first>
  <action>
    In `frontend/src/app/learning-shell/LearningShellContainer.jsx`, keep original subtitle recovery keyed to canonical `lesson.id` and ensure recovered subtitle variants enrich `currentLesson` instead of creating an alternate lesson identity.

    Implement the user-decided lazy recovery rule explicitly:
    - do not block normal history rendering on subtitle/translation recovery
    - do not precompute recovery for every lesson card
    - only run recovery when the user opens a lesson or explicitly triggers a history-menu repair action such as "补翻译"

    In `frontend/src/features/lessons/LessonList.jsx`, keep history cards source-agnostic:
    - start-learning uses canonical lesson ID through `startLessonFromHistory`
    - sentence count and progress labels come from shared `lessonCardMetaMap`
    - local-only/export/binding helpers must not create a second learner-facing namespace for the same canonical lesson

    Do not introduce different start-learning flows for local vs cloud generated lessons once they are in history.
  </action>
  <acceptance_criteria>
    - `frontend/src/app/learning-shell/LearningShellContainer.jsx` still calls `requestOriginalSubtitleVariant(accessToken, lessonId, asrPayload)`
    - `frontend/src/app/learning-shell/LearningShellContainer.jsx` applies recovered variants through `applyLessonSubtitleVariantToStore(lessonId, activeVariant)`
    - `frontend/src/features/lessons/LessonList.jsx` does not require eager subtitle/translation recovery before rendering the history list
    - `frontend/src/features/lessons/LessonList.jsx` contains `startLessonFromHistory(lessonId)`
    - `frontend/src/features/lessons/LessonList.jsx` uses `lessonCardMetaMap` to render sentence/progress state
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'requestOriginalSubtitleVariant|applyLessonSubtitleVariantToStore|lessonId|startLessonFromHistory|lessonCardMetaMap|progressState' frontend/src/app/learning-shell/LearningShellContainer.jsx frontend/src/features/lessons/LessonList.jsx</automated>
  </verify>
  <done>Subtitle recovery and history entry remain supplemental to the canonical lesson record rather than replacing it.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for source-agnostic history and lesson-open flow</name>
  <files>
    - tests/integration/test_regression_api.py
    - tests/e2e/test_e2e_key_flows.py
  </files>
  <read_first>
    - tests/integration/test_regression_api.py
    - tests/e2e/test_e2e_key_flows.py
    - frontend/src/store/slices/lessonSlice.ts
    - app/repositories/lessons.py
  </read_first>
  <action>
    Extend `tests/integration/test_regression_api.py` so the catalog/detail path asserts canonical history metadata is present for generated lessons:
    - `sentence_count`
    - `progress_summary.current_sentence_index`
    - `progress_summary.completed_sentence_count`
    - `last_played_at_ms`
    - learner-facing lesson detail that still opens through the same lesson ID

    Extend `tests/e2e/test_e2e_key_flows.py` so the learner flow verifies:
    - create lesson
    - update progress
    - fetch lesson progress/history again
    - continue learning via the same lesson ID without source-specific branching

    Prefer extending the existing lesson-flow test instead of adding a separate alternate local/cloud-only harness.
  </action>
  <acceptance_criteria>
    - `tests/integration/test_regression_api.py` contains assertions for `sentence_count` and `progress_summary`
    - `tests/e2e/test_e2e_key_flows.py` contains assertions for create lesson -> update progress -> fetch progress continuity
    - the e2e learner flow still uses canonical lesson IDs rather than runtime-specific IDs
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/integration/test_regression_api.py tests/e2e/test_e2e_key_flows.py -k "progress or lesson" -q</automated>
  </verify>
  <done>Automated regressions now lock a source-agnostic history and lesson-opening flow.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pytest tests/integration/test_regression_api.py -k "catalog or progress_summary or lesson" -q` exits 0
2. `pytest tests/e2e/test_e2e_key_flows.py -k "login_create_lesson_practice_progress" -q` exits 0
3. History cards render sentence count and progress summary from canonical lesson metadata
4. Opening a lesson from history still routes through the same lesson ID and shared learner shell
</verification>

<success_criteria>
- History and lesson-open flow stay source-agnostic once a lesson exists
- Canonical lesson ID remains the only learner-facing lesson identity
- Subtitle recovery enriches the canonical lesson rather than replacing it
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/03-lesson-output-consistency/03-02-SUMMARY.md`
</output>
