---
phase: 03-lesson-output-consistency
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/routers/lessons/router.py
  - app/api/serializers.py
  - app/schemas/lesson.py
  - app/services/lesson_query_service.py
  - app/services/lesson_command_service.py
  - app/services/lesson_task_manager.py
  - tests/contracts/test_lessons_contract.py
  - tests/integration/test_regression_api.py
autonomous: true
requirements:
  - LESS-01
  - LESS-03
must_haves:
  truths:
    - "Bottle 1.0 local generation and Bottle 2.0 cloud generation both emit the same canonical learner-facing lesson contract"
    - "Lesson detail, catalog, and task responses preserve subtitle cache seed, result-kind, and partial-failure metadata without runtime-specific response shapes"
    - "Degraded-success outputs remain canonical lesson artifacts rather than task-only blobs"
  artifacts:
    - path: app/schemas/lesson.py
      provides: Canonical learner-facing lesson/task response schemas
      contains: 'completion_kind'
    - path: app/api/routers/lessons/router.py
      provides: Shared lesson/task response assembly for cloud and local generation
      contains: '_to_task_response'
    - path: app/api/serializers.py
      provides: Shared catalog/detail serializer choke points
      contains: 'to_lesson_detail_response'
---

<objective>
Align the backend lesson/task/detail contracts so Bottle 1.0 local generation and Bottle 2.0 cloud generation both land in the same canonical lesson artifact with the same learner-facing response fields, including degraded-success metadata.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@app/models/lesson.py
@app/schemas/lesson.py
@app/api/routers/lessons/router.py
@app/api/serializers.py
@app/services/lesson_query_service.py
@app/services/lesson_command_service.py
@app/services/lesson_task_manager.py
@tests/contracts/test_lessons_contract.py
@tests/integration/test_regression_api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize task and completed-lesson response payloads across cloud and local generation</name>
  <files>
    - app/api/routers/lessons/router.py
    - app/schemas/lesson.py
    - app/api/serializers.py
  </files>
  <read_first>
    - app/api/routers/lessons/router.py
    - app/schemas/lesson.py
    - app/api/serializers.py
    - app/models/lesson.py
  </read_first>
  <action>
    In `app/api/routers/lessons/router.py`, make the cloud task path (`_to_task_response`) and the local completed-generation path (`create_local_generated_lesson`) return the same canonical learner-facing fields instead of hand-assembled variants that can drift.

    Preserve and normalize these exact fields whenever a lesson or task response can carry them:
    - `completion_kind`
    - `result_kind`
    - `result_label`
    - `result_message`
    - `partial_failure_stage`
    - `partial_failure_code`
    - `partial_failure_message`
    - `subtitle_cache_seed`
    - `lesson`

    In `app/schemas/lesson.py`, remove duplicate or conflicting lesson task schema declarations so there is one authoritative `LessonTaskResponse` and one authoritative `LessonTaskDebugReportResponse`.

    In `app/api/serializers.py`, keep `to_lesson_detail_response()` as the single serializer that turns persisted lessons plus `subtitle_cache_seed` into learner-facing lesson detail payloads, and use that serializer consistently instead of rebuilding lesson detail fields ad hoc.
  </action>
  <acceptance_criteria>
    - `app/schemas/lesson.py` contains only one `class LessonTaskResponse`
    - `app/schemas/lesson.py` contains only one `class LessonTaskDebugReportResponse`
    - `app/api/routers/lessons/router.py` contains `completion_kind`, `result_kind`, `result_label`, `result_message`, `partial_failure_stage`, `partial_failure_code`, and `partial_failure_message` on learner-facing task/lesson responses
    - `app/api/serializers.py` still contains `to_lesson_detail_response` and `SubtitleCacheSeedResponse`
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'class LessonTaskResponse|class LessonTaskDebugReportResponse|completion_kind|result_kind|partial_failure_stage|SubtitleCacheSeedResponse|to_lesson_detail_response' app/schemas/lesson.py app/api/routers/lessons/router.py app/api/serializers.py</automated>
  </verify>
  <done>Cloud and local generation now share one canonical lesson/task response contract.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize canonical persistence handoff for lesson detail and task recovery metadata</name>
  <files>
    - app/services/lesson_query_service.py
    - app/services/lesson_command_service.py
    - app/services/lesson_task_manager.py
  </files>
  <read_first>
    - app/services/lesson_query_service.py
    - app/services/lesson_command_service.py
    - app/services/lesson_task_manager.py
    - app/repositories/lessons.py
    - app/models/lesson.py
  </read_first>
  <action>
    In `app/services/lesson_query_service.py`, ensure canonical lesson detail and catalog reads continue to load persisted `Lesson`, `LessonSentence`, and progress-derived metadata only.

    In `app/services/lesson_command_service.py` and `app/services/lesson_task_manager.py`, normalize task result metadata and workspace summary writes so both local and cloud generation preserve the same canonical identifiers and recovery fields:
    - persisted `lesson_id`
    - `result_kind`
    - `result_message`
    - `partial_failure_stage`
    - `partial_failure_code`
    - `partial_failure_message`
    - `subtitle_cache_seed`
    - canonical workspace/restore pointer keyed to the same lesson/task identity

    Do not introduce a runtime-specific lesson table, task subtype, or alternate history record. All recovery data must remain supplemental to the canonical lesson/task records.
  </action>
  <acceptance_criteria>
    - `app/services/lesson_query_service.py` continues to read from `get_lesson_for_user`, `get_lesson_sentences`, and `list_lesson_catalog_for_user`
    - `app/services/lesson_command_service.py` contains `result_kind` handling for both queued task and local generation paths
    - `app/services/lesson_task_manager.py` writes `result_kind`, `result_message`, and `partial_failure_*` into task artifacts or task state
    - `app/services/lesson_task_manager.py` keeps workspace summary synchronized through canonical `lesson_id` or `task_id` pointers rather than creating a second lesson identity
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'get_lesson_for_user|get_lesson_sentences|list_lesson_catalog_for_user|result_kind|result_message|partial_failure_stage|partial_failure_code|partial_failure_message|lesson_id|workspace_id|restore_pointer' app/services/lesson_query_service.py app/services/lesson_command_service.py app/services/lesson_task_manager.py</automated>
  </verify>
  <done>Task recovery metadata and lesson detail reads now reinforce one canonical lesson artifact.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for canonical local/cloud lesson output parity</name>
  <files>
    - tests/contracts/test_lessons_contract.py
    - tests/integration/test_regression_api.py
  </files>
  <read_first>
    - tests/contracts/test_lessons_contract.py
    - tests/integration/test_regression_api.py
    - app/schemas/lesson.py
    - app/api/routers/lessons/router.py
  </read_first>
  <action>
    Extend `tests/contracts/test_lessons_contract.py` so lesson list/detail/catalog/task schemas validate the canonical learner-facing fields used by both local and cloud generation, including `subtitle_cache_seed` and task result metadata where the schema exposes them.

    In `tests/integration/test_regression_api.py`, add or update focused regressions proving these exact behaviors:
    - a local generated lesson response contains the same canonical lesson fields as the cloud lesson detail path
    - task polling returns canonical result metadata (`completion_kind`, `result_kind`, `result_message`, `partial_failure_*`) without source-specific omissions
    - `subtitle_cache_seed` survives into learner-facing lesson detail payloads

    Use the existing local-generation and lesson-task helpers already present in the file instead of creating a parallel testing harness.
  </action>
  <acceptance_criteria>
    - `tests/contracts/test_lessons_contract.py` references `LessonTaskResponse` and `LessonDetailResponse`
    - `tests/integration/test_regression_api.py` contains assertions for `completion_kind`, `result_kind`, and `subtitle_cache_seed`
    - `tests/integration/test_regression_api.py` contains at least one parity-oriented assertion that local generated lessons and canonical lesson detail expose the same learner-facing fields
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/contracts/test_lessons_contract.py tests/integration/test_regression_api.py -k "lesson or subtitle_cache_seed or result_kind" -q</automated>
  </verify>
  <done>Automated tests now lock canonical local/cloud learner-facing lesson output parity.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pytest tests/contracts/test_lessons_contract.py -q` exits 0
2. `pytest tests/integration/test_regression_api.py -k "lesson or subtitle_cache_seed or result_kind" -q` exits 0
3. Local generated lesson responses and cloud task/detail responses expose the same canonical learner-facing result fields
4. No duplicate `LessonTaskResponse` schema remains in `app/schemas/lesson.py`
</verification>

<success_criteria>
- Local and cloud generation produce the same canonical learner-facing lesson/task contracts
- Degraded-success metadata remains preserved in the shared task/lesson model
- Canonical lesson detail serialization stays centralized instead of drifting across entry points
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/03-lesson-output-consistency/03-01-SUMMARY.md`
</output>
