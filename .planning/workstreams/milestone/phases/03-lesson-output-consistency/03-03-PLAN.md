---
phase: 03-lesson-output-consistency
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - frontend/src/features/upload/UploadPanel.jsx
  - frontend/src/features/immersive/ImmersiveLessonPage.jsx
  - tests/contracts/test_lessons_contract.py
  - tests/integration/test_lesson_task_recovery.py
  - tests/e2e/test_e2e_key_flows.py
autonomous: true
requirements:
  - LESS-03
  - LEARN-01
  - LEARN-02
must_haves:
  truths:
    - "Shared generation-state feedback remains visible across pause, resume, failure, full success, and degraded success"
    - "ASR-only degraded outcomes remain usable learner handoffs instead of dead ends"
    - "Immersive learning continues to consume persisted lesson sentences and progress without source-specific branching"
  artifacts:
    - path: frontend/src/features/upload/UploadPanel.jsx
      provides: Shared generation-state, success, degraded-success, and recovery UI
      contains: 'result_kind'
    - path: frontend/src/features/immersive/ImmersiveLessonPage.jsx
      provides: Practice handoff on canonical lesson sentences/progress
      contains: 'currentSentence'
    - path: tests/integration/test_lesson_task_recovery.py
      provides: Pause/resume/task-state regression coverage
      contains: 'resume'
---

<objective>
Align the shared generation-state UX, degraded-success handling, and practice handoff so users can see consistent progress and continue learning from canonical lesson artifacts regardless of generation source.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@frontend/src/features/upload/UploadPanel.jsx
@frontend/src/features/immersive/ImmersiveLessonPage.jsx
@frontend/src/store/slices/lessonSlice.ts
@app/api/routers/lessons/router.py
@app/services/lesson_task_manager.py
@tests/contracts/test_lessons_contract.py
@tests/integration/test_lesson_task_recovery.py
@tests/e2e/test_e2e_key_flows.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize shared generation-state and degraded-success UI on the upload surface</name>
  <files>
    - frontend/src/features/upload/UploadPanel.jsx
  </files>
  <read_first>
    - frontend/src/features/upload/UploadPanel.jsx
    - app/api/routers/lessons/router.py
    - app/services/lesson_task_manager.py
  </read_first>
  <action>
    In `frontend/src/features/upload/UploadPanel.jsx`, keep one shared state model for:
    - pending/running progress
    - pause and terminate requests
    - full success
    - degraded success where `result_kind === "asr_only"`
    - retry/resume after failure

    Make the upload surface read and render these exact fields consistently from task snapshots or completed local responses:
    - `completion_kind`
    - `result_kind`
    - `result_label`
    - `result_message`
    - `partial_failure_stage`
    - `partial_failure_code`
    - `partial_failure_message`
    - `lesson`

    Keep degraded-success UI actionable: when `result_kind === "asr_only"`, show success-with-warning copy and keep the "go learn" or equivalent lesson-navigation affordance available instead of collapsing to a terminal error-only state.
  </action>
  <acceptance_criteria>
    - `frontend/src/features/upload/UploadPanel.jsx` contains `completion_kind`, `result_kind`, `result_message`, and `partial_failure_stage` handling
    - `frontend/src/features/upload/UploadPanel.jsx` contains a success branch for partial/degraded success rather than only total failure
    - `frontend/src/features/upload/UploadPanel.jsx` keeps lesson navigation available from the success/degraded-success surface
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'completion_kind|result_kind|result_message|partial_failure_stage|partial_failure_code|partial_failure_message|onNavigateToLesson|去学习' frontend/src/features/upload/UploadPanel.jsx</automated>
  </verify>
  <done>The upload surface now treats degraded success as a usable learner handoff instead of a dead end.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve practice handoff on persisted lesson sentences and progress only</name>
  <files>
    - frontend/src/features/immersive/ImmersiveLessonPage.jsx
  </files>
  <read_first>
    - frontend/src/features/immersive/ImmersiveLessonPage.jsx
    - frontend/src/store/slices/lessonSlice.ts
    - app/models/lesson.py
  </read_first>
  <action>
    In `frontend/src/features/immersive/ImmersiveLessonPage.jsx`, keep immersive learning and practice handoff driven by the canonical lesson object:
    - sentence list from persisted lesson detail
    - progress synchronization through lesson progress APIs
    - no branch that requires users to know or choose local vs cloud runtime once the lesson is open

    If source-specific helper logic exists, restrict it to media/recovery behavior and do not let it alter the sentence/progress learning contract.
  </action>
  <acceptance_criteria>
    - `frontend/src/features/immersive/ImmersiveLessonPage.jsx` still derives `currentSentence` from the lesson sentence list
    - `frontend/src/features/immersive/ImmersiveLessonPage.jsx` contains no source-specific branch that selects different sentence/progress contracts for local vs cloud lessons
    - progress sync remains wired through learner-facing lesson/progress APIs rather than task-only state
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'currentSentence|lesson\\.sentences|progress|local|cloud|runtime_kind|asr_model' frontend/src/features/immersive/ImmersiveLessonPage.jsx</automated>
  </verify>
  <done>Practice handoff remains tied to persisted lesson sentences and progress, not runtime source.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for progress-state continuity and learner handoff</name>
  <files>
    - tests/contracts/test_lessons_contract.py
    - tests/integration/test_lesson_task_recovery.py
    - tests/e2e/test_e2e_key_flows.py
  </files>
  <read_first>
    - tests/contracts/test_lessons_contract.py
    - tests/integration/test_lesson_task_recovery.py
    - tests/e2e/test_e2e_key_flows.py
    - frontend/src/features/upload/UploadPanel.jsx
  </read_first>
  <action>
    Extend `tests/contracts/test_lessons_contract.py` so learner-facing task responses validate the fields needed by shared generation-state UI:
    - `completion_kind`
    - `result_kind`
    - `result_message`
    - `partial_failure_stage`
    - `partial_failure_code`
    - `partial_failure_message`

    Extend `tests/integration/test_lesson_task_recovery.py` so pause/resume/terminate and degraded-success paths assert canonical task-state continuity instead of source-specific behavior.

    Extend `tests/e2e/test_e2e_key_flows.py` so the learner flow proves a generated lesson can be opened and progressed after generation, including the persisted progress update path that Phase 03 depends on.
  </action>
  <acceptance_criteria>
    - `tests/contracts/test_lessons_contract.py` contains task-response assertions for `completion_kind`, `result_kind`, and `partial_failure_*`
    - `tests/integration/test_lesson_task_recovery.py` contains assertions for pause/resume or degraded-success continuity
    - `tests/e2e/test_e2e_key_flows.py` still proves create lesson -> open lesson -> progress update continuity
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/contracts/test_lessons_contract.py tests/integration/test_lesson_task_recovery.py tests/e2e/test_e2e_key_flows.py -k "lesson or progress or resume or result_kind" -q</automated>
  </verify>
  <done>Automated coverage now locks generation-state continuity and learner handoff on the canonical lesson pipeline.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pytest tests/contracts/test_lessons_contract.py -q` exits 0
2. `pytest tests/integration/test_lesson_task_recovery.py -k "resume or pause or terminate or result_kind" -q` exits 0
3. `pytest tests/e2e/test_e2e_key_flows.py -k "login_create_lesson_practice_progress" -q` exits 0
4. Partial-success outcomes still allow users to enter learning from the resulting lesson artifact
</verification>

<success_criteria>
- Shared generation-state UX remains consistent across success, degraded success, failure, pause, and resume
- Practice handoff stays source-agnostic once a lesson exists
- Automated tests lock learner handoff and task-state continuity
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/03-lesson-output-consistency/03-03-SUMMARY.md`
</output>
