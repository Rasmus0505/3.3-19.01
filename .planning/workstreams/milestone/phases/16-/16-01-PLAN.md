---
phase: 16
wave: 1
plan_slug: announcement-backend
depends_on: []
requirements_addressed: [ANNC-01, ANNC-02, ANNC-03, ANNC-04, ANNC-05]
autonomous: true
files_modified:
  - app/models/__init__.py
  - app/schemas/__init__.py
  - app/repositories/__init__.py
  - app/repositories/announcement.py
  - app/api/routers/announcement.py
  - app/api/routers/admin.py
  - app/main.py
---

# Phase 16 Wave 1: 后端公告数据层与 API

## Objective

建立公告系统的后端基础设施：数据模型、Pydantic Schema、Repository 层、Admin API 路由。

## Scope

- ANNC-01: Admin 创建公告（标题+内容）
- ANNC-02: Admin 标记公告类型（changelog/banner/modal）
- ANNC-03: Admin 置顶和排序（当前阶段：仅置顶/取消置顶）
- ANNC-04: Admin 删除公告
- ANNC-05: 用户只看到当前有效且匹配端能力的公告

## Tasks

### 16-01-TASK-01: 创建 Announcement 数据模型

<read_first>
`app/models/__init__.py` — 现有模型导出方式
`app/models/billing.py` — 现有模型写法参考（含 created_at, updated_at）
`app/db/base.py` — declarative base 引用
</read_first>

<action>
在 `app/models/` 目录下新建 `announcement.py` 文件，内容如下：

```python
from __future__ import annotations

from datetime import datetime

from sqlalchemy import Boolean, DateTime, Enum, String, Text
from sqlalchemy.orm import Mapped, mapped_column

from app.db.base import Base


class Announcement(Base):
    __tablename__ = "announcements"

    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    title: Mapped[str] = mapped_column(String(200), nullable=False)
    content: Mapped[str] = mapped_column(Text, nullable=False)
    type: Mapped[str] = mapped_column(
        String(20), nullable=False, default="banner"
    )  # changelog | banner | modal
    is_active: Mapped[bool] = mapped_column(Boolean, default=True, nullable=False)
    is_pinned: Mapped[bool] = mapped_column(Boolean, default=False, nullable=False)
    created_at: Mapped[datetime] = mapped_column(
        DateTime, default=datetime.utcnow, nullable=False
    )
    updated_at: Mapped[datetime] = mapped_column(
        DateTime, default=datetime.utcnow, onupdate=datetime.utcnow, nullable=False
    )
```

同时：
1. 在 `app/models/__init__.py` 中添加导出：
   ```python
   from app.models.announcement import Announcement
   ```
2. 在 `app/models/__init__.py` 的 `__all__` 列表中添加 `"Announcement"`

**验证：** grep 确认 `app/models/__init__.py` 包含 `Announcement` 导出。
</action>

<acceptance_criteria>
- `app/models/announcement.py` 存在并包含 Announcement 类定义
- `Announcement` 表名 `announcements`，字段：id, title, content, type, is_active, is_pinned, created_at, updated_at
- `app/models/__init__.py` 导出 Announcement
- type 字段值限定：changelog / banner / modal
</acceptance_criteria>

---

### 16-01-TASK-02: 创建公告 Pydantic Schema

<read_first>
`app/schemas/admin.py` — 现有 admin Schema 写法参考
`app/schemas/common.py` — PaginationMeta 等通用 schema
</read_first>

<action>
在 `app/schemas/` 目录下新建 `announcement.py` 文件：

```python
from __future__ import annotations

from datetime import datetime
from enum import Enum

from pydantic import BaseModel, Field


class AnnouncementType(str, Enum):
    CHANGELOG = "changelog"
    BANNER = "banner"
    MODAL = "modal"


class AnnouncementBase(BaseModel):
    title: str = Field(..., max_length=200, description="公告标题")
    content: str = Field(..., description="公告内容，支持 Markdown")
    type: AnnouncementType = Field(default=AnnouncementType.BANNER)
    is_active: bool = Field(default=True)
    is_pinned: bool = Field(default=False)


class AnnouncementCreate(AnnouncementBase):
    pass


class AnnouncementUpdate(BaseModel):
    title: str | None = Field(default=None, max_length=200)
    content: str | None = None
    type: AnnouncementType | None = None
    is_active: bool | None = None
    is_pinned: bool | None = None


class AnnouncementItem(BaseModel):
    id: int
    title: str
    content: str
    type: AnnouncementType
    is_active: bool
    is_pinned: bool
    created_at: datetime
    updated_at: datetime

    model_config = {"from_attributes": True}


class AnnouncementListResponse(BaseModel):
    ok: bool = True
    items: list[AnnouncementItem]
    total: int
```

同时：
1. 在 `app/schemas/__init__.py` 中添加：
   ```python
   from app.schemas.announcement import (
       Announcement,
       AnnouncementCreate,
       AnnouncementUpdate,
       AnnouncementItem,
       AnnouncementListResponse,
       AnnouncementType,
   )
   ```
2. 在 `app/schemas/__init__.py` 的 `__all__` 列表中添加相应的导出名

**验证：** `python -c "from app.schemas.announcement import Announcement, AnnouncementCreate, AnnouncementItem, AnnouncementListResponse, AnnouncementType"` 执行无错误。
</action>

<acceptance_criteria>
- `app/schemas/announcement.py` 存在
- AnnouncementItem, AnnouncementCreate, AnnouncementUpdate, AnnouncementListResponse, AnnouncementType 均已定义
- `app/schemas/__init__.py` 导出上述所有类型
</acceptance_criteria>

---

### 16-01-TASK-03: 创建公告 Repository 层

<read_first>
`app/repositories/admin.py` — 现有 repository 写法参考
`app/repositories/base.py` — base repository 模式
</read_first>

<action>
在 `app/repositories/` 目录下新建 `announcement.py`：

```python
from __future__ import annotations

from sqlalchemy import desc, func, select
from sqlalchemy.orm import Session

from app.models.announcement import Announcement


def list_announcements(db: Session, skip: int = 0, limit: int = 20) -> dict:
    """返回公告列表（按置顶+创建时间倒序）"""
    total = db.execute(select(func.count(Announcement.id))).scalar_one()
    rows = (
        db.execute(
            select(Announcement)
            .order_by(desc(Announcement.is_pinned), desc(Announcement.created_at))
            .offset(skip)
            .limit(limit)
        )
    ).scalars().all()
    return {"items": list(rows), "total": total}


def get_announcement(db: Session, announcement_id: int) -> Announcement | None:
    """按 ID 获取单条公告"""
    return db.execute(select(Announcement).where(Announcement.id == announcement_id)).scalar_one_or_none()


def create_announcement(db: Session, title: str, content: str, type: str, is_active: bool, is_pinned: bool) -> Announcement:
    """创建公告"""
    ann = Announcement(title=title, content=content, type=type, is_active=is_active, is_pinned=is_pinned)
    db.add(ann)
    db.commit()
    db.refresh(ann)
    return ann


def update_announcement(db: Session, announcement_id: int, **fields) -> Announcement | None:
    """更新公告字段"""
    ann = get_announcement(db, announcement_id)
    if not ann:
        return None
    for key, value in fields.items():
        if value is not None and hasattr(ann, key):
            setattr(ann, key, value)
    db.commit()
    db.refresh(ann)
    return ann


def delete_announcement(db: Session, announcement_id: int) -> bool:
    """删除公告"""
    ann = get_announcement(db, announcement_id)
    if not ann:
        return False
    db.delete(ann)
    db.commit()
    return True


def list_active_announcements(db: Session) -> list[Announcement]:
    """获取所有已启用公告（按置顶+创建时间倒序）"""
    return (
        db.execute(
            select(Announcement)
            .where(Announcement.is_active == True)
            .order_by(desc(Announcement.is_pinned), desc(Announcement.created_at))
        )
    ).scalars().all()
```

同时在 `app/repositories/__init__.py` 添加导出：

```python
from app.repositories.announcement import (
    create_announcement,
    delete_announcement,
    get_announcement,
    list_active_announcements,
    list_announcements,
    update_announcement,
)
```

**验证：** `python -c "from app.repositories.announcement import list_announcements, create_announcement, update_announcement, delete_announcement, list_active_announcements"` 执行无错误。
</action>

<acceptance_criteria>
- `app/repositories/announcement.py` 存在并包含全部 CRUD 函数
- `app/repositories/__init__.py` 导出全部函数
- list_announcements 按置顶 + 创建时间倒序排列
- list_active_announcements 仅返回 is_active=True 的公告
</acceptance_criteria>

---

### 16-01-TASK-04: 创建 Admin 公告 API 路由

<read_first>
`app/api/routers/admin/router.py` — 现有 admin 路由注册方式
`app/api/routers/admin.py` — admin API 端点写法参考
`app/api/deps/auth.py` — get_admin_user 依赖
</read_first>

<action>
在 `app/api/routers/` 目录下新建 `announcement.py`：

```python
from __future__ import annotations

from fastapi import APIRouter, Depends
from sqlalchemy.orm import Session

from app.api.deps.auth import get_admin_user
from app.core.errors import error_response
from app.db import get_db
from app.models.user import User
from app.repositories.announcement import (
    create_announcement,
    delete_announcement,
    get_announcement,
    list_announcements,
    update_announcement,
)
from app.schemas.announcement import (
    AnnouncementCreate,
    AnnouncementItem,
    AnnouncementListResponse,
    AnnouncementUpdate,
)
from app.schemas.common import ErrorResponse, SuccessResponse

router = APIRouter(prefix="/api/admin/announcements", tags=["admin"])


@router.get(
    "",
    response_model=AnnouncementListResponse,
    responses={401: {"model": ErrorResponse}, 403: {"model": ErrorResponse}},
)
def list_admin_announcements(
    page: int = 1,
    page_size: int = 20,
    db: Session = Depends(get_db),
    _: User = Depends(get_admin_user),
):
    skip = (max(page, 1) - 1) * max(page_size, 1)
    limit = max(1, min(page_size, 100))
    result = list_announcements(db, skip=skip, limit=limit)
    return AnnouncementListResponse(
        ok=True,
        items=[AnnouncementItem.model_validate(r) for r in result["items"]],
        total=result["total"],
    )


@router.post(
    "",
    response_model=AnnouncementItem,
    status_code=201,
    responses={400: {"model": ErrorResponse}, 401: {"model": ErrorResponse}, 403: {"model": ErrorResponse}},
)
def create_admin_announcement(
    payload: AnnouncementCreate,
    db: Session = Depends(get_db),
    _: User = Depends(get_admin_user),
):
    ann = create_announcement(
        db,
        title=payload.title,
        content=payload.content,
        type=payload.type.value,
        is_active=payload.is_active,
        is_pinned=payload.is_pinned,
    )
    return AnnouncementItem.model_validate(ann)


@router.get("/{announcement_id}", response_model=AnnouncementItem)
def get_admin_announcement(
    announcement_id: int,
    db: Session = Depends(get_db),
    _: User = Depends(get_admin_user),
):
    ann = get_announcement(db, announcement_id)
    if not ann:
        return error_response(404, "NOT_FOUND", "公告不存在")
    return AnnouncementItem.model_validate(ann)


@router.put("/{announcement_id}", response_model=AnnouncementItem)
def update_admin_announcement(
    announcement_id: int,
    payload: AnnouncementUpdate,
    db: Session = Depends(get_db),
    _: User = Depends(get_admin_user),
):
    fields = payload.model_dump(exclude_unset=True)
    if "type" in fields and fields["type"] is not None:
        fields["type"] = fields["type"].value
    ann = update_announcement(db, announcement_id, **fields)
    if not ann:
        return error_response(404, "NOT_FOUND", "公告不存在")
    return AnnouncementItem.model_validate(ann)


@router.delete("/{announcement_id}", response_model=SuccessResponse)
def delete_admin_announcement(
    announcement_id: int,
    db: Session = Depends(get_db),
    _: User = Depends(get_admin_user),
):
    ok = delete_announcement(db, announcement_id)
    if not ok:
        return error_response(404, "NOT_FOUND", "公告不存在")
    return SuccessResponse(ok=True)
```

同时在 `app/main.py` 中找到 admin router 注册位置，添加公告路由：

```python
from app.api.routers.announcement import router as announcement_router
```

并在 admin 路由注册处添加：

```python
admin_router.include_router(announcement_router)
```

**验证：**
1. `python -c "from app.api.routers.announcement import router"` 执行无错误
2. `grep -n "announcement" app/main.py` 确认路由已注册
</action>

<acceptance_criteria>
- `app/api/routers/announcement.py` 存在，包含 list/create/get/update/delete 端点
- `app/main.py` 包含 announcement router 注册
- 所有端点需要 get_admin_user 认证
- POST /api/admin/announcements 创建公告
- GET /api/admin/announcements 获取公告列表（分页）
- GET /api/admin/announcements/{id} 获取单条
- PUT /api/admin/announcements/{id} 更新公告
- DELETE /api/admin/announcements/{id} 删除公告
</acceptance_criteria>

---

### 16-01-TASK-05: 创建用户端公告 API（公开端点）

<read_first>
`app/api/routers/auth.py` — 现有公开 API 路由写法参考
</read_first>

<action>
在 `app/api/routers/` 目录下新建 `announcement_public.py`：

```python
from __future__ import annotations

from fastapi import APIRouter, Depends
from sqlalchemy.orm import Session

from app.db import get_db
from app.repositories.announcement import list_active_announcements
from app.schemas.announcement import AnnouncementItem

router = APIRouter(prefix="/api/announcements", tags=["public"])


@router.get("/active", response_model=list[AnnouncementItem])
def get_active_announcements(db: Session = Depends(get_db)):
    """获取当前有效公告列表（登录用户专属）"""
    announcements = list_active_announcements(db)
    return [AnnouncementItem.model_validate(a) for a in announcements]
```

同时在 `app/main.py` 中注册此公开路由（与 auth router 同级）：

```python
from app.api.routers.announcement_public import router as announcement_public_router
```

并在 app 实例创建处添加：

```python
app.include_router(announcement_public_router)
```

**验证：**
1. `python -c "from app.api.routers.announcement_public import router"` 执行无错误
2. `grep -n "announcement_public" app/main.py` 确认路由已注册
</action>

<acceptance_criteria>
- `app/api/routers/announcement_public.py` 存在
- GET /api/announcements/active 返回所有 is_active=True 的公告
- 此端点无需认证（用户端根据登录态决定是否调用）
- `app/main.py` 包含 announcement_public router 注册
</acceptance_criteria>

---

## Verification

**后端 API 验证（Wave 1 完成标准）：**

- [ ] `Announcement` 模型在 `app/models/` 中定义并导出
- [ ] `AnnouncementItem` / `AnnouncementCreate` / `AnnouncementUpdate` Schema 在 `app/schemas/` 中定义并导出
- [ ] `list_announcements` / `create_announcement` / `update_announcement` / `delete_announcement` Repository 函数存在
- [ ] Admin API 路由 `/api/admin/announcements` CRUD 端点全部就绪
- [ ] 公开端点 `/api/announcements/active` 就绪
- [ ] `python -c "from app.main import app; print('app ok')"` 执行无错误

## must_haves

- Admin 可通过 API 创建/编辑/删除/置顶公告
- 用户端可获取当前有效公告列表
- 公告按置顶 + 创建时间倒序排列

---
