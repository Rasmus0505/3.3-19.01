---
phase: 04.1-desktop-local-course-fidelity
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - app/services/lesson_service.py
  - app/api/routers/lessons.py
  - frontend/src/features/upload/UploadPanel.jsx
  - tests/contracts/test_desktop_runtime_contract.py
  - tests/integration/test_regression_api.py
autonomous: true
requirements:
  - DESK-04
must_haves:
  truths:
    - "Bottle 1.0 desktop-local generation defaults to the canonical translated lesson contract when translation succeeds"
    - "If local generation returns subtitle-only output, the product labels it as partial success instead of full success"
    - "User-visible success/error copy distinguishes full success, partial success, and generation failure"
    - "Packaged helper/runtime validation includes a real desktop-local transcription smoke check, not only static resource assertions"
  artifacts:
    - path: app/services/lesson_service.py
      provides: Canonical local-generation result contract
      contains: 'build_local_generation_result'
    - path: app/api/routers/lessons.py
      provides: Local-generation completion response contract
      contains: '/local-asr/complete'
    - path: tests/integration/test_regression_api.py
      provides: API assertions for local-generation result payloads
      contains: 'local-asr'
---

<objective>
Finish the Bottle 1.0 desktop-local result contract so translation/full-success semantics are explicit, user-visible copy matches reality, and packaged desktop-local runtime validation covers a real transcription path.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@app/services/lesson_service.py
@app/api/routers/lessons.py
@frontend/src/features/upload/UploadPanel.jsx
@tests/contracts/test_desktop_runtime_contract.py
@tests/integration/test_regression_api.py
@.planning/workstreams/milestone/phases/04.1-desktop-local-course-fidelity/04.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the canonical Bottle 1.0 local result contract</name>
  <files>
    - app/services/lesson_service.py
    - app/api/routers/lessons.py
  </files>
  <read_first>
    - app/services/lesson_service.py
    - app/api/routers/lessons.py
    - scripts/run_desktop_backend.py
    - app/api/routers/desktop_asr.py
  </read_first>
  <action>
    Clarify and enforce the backend contract for Bottle 1.0 desktop-local lesson completion:
    - when translation succeeds, desktop-local lessons land as canonical translated lessons
    - when translation fails or is explicitly unavailable, return an explicit partial/degraded contract
    - ensure `lesson_status`, `translation_pending`, `translation_debug`, and partial-failure fields tell the truth about the resulting lesson
    - avoid silently returning untranslated subtitles under a success contract that looks equivalent to full success
  </action>
  <acceptance_criteria>
    - backend local-generation result distinguishes full success vs partial translation outcomes explicitly
    - translated desktop-local lessons expose non-empty learner-facing translation fields when translation succeeded
    - partial desktop-local output exposes explicit partial/degraded metadata instead of ambiguous success semantics
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'partial_ready|translation_pending|translation_debug|result_kind|full_success|asr_only' app/services/lesson_service.py app/api/routers/lessons.py</automated>
  </verify>
  <done>The Bottle 1.0 desktop-local backend result contract now truthfully distinguishes translated lessons from partial subtitle-only outputs.</done>
</task>

<task type="auto">
  <name>Task 2: Align user-visible success and failure copy with the real local result contract</name>
  <files>
    - frontend/src/features/upload/UploadPanel.jsx
  </files>
  <read_first>
    - frontend/src/features/upload/UploadPanel.jsx
    - app/api/routers/lessons.py
  </read_first>
  <action>
    Update upload-surface success/failure messaging so Bottle 1.0 local generation communicates the actual result clearly:
    - full success: translated lesson ready and playable
    - partial success: subtitles available but translation incomplete or deferred
    - failure: generation failed and needs retry / recovery
    Do not reuse cloud failure copy for a Bottle 1.0 desktop-local user-selected path.
  </action>
  <acceptance_criteria>
    - full-success copy is distinct from partial-success copy
    - partial-success copy references translation incompleteness explicitly
    - Bottle 1.0 desktop-local path does not reuse cloud failure copy when the failure source is local-specific
  </acceptance_criteria>
  <verify>
    <automated>rg -n '翻译待补全|课程已生成|云端识别当前不可用|partial' frontend/src/features/upload/UploadPanel.jsx</automated>
  </verify>
  <done>User-visible Bottle 1.0 desktop-local messaging now matches the actual result contract instead of mixing local and cloud semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Add real runtime regression coverage for packaged local generation</name>
  <files>
    - tests/contracts/test_desktop_runtime_contract.py
    - tests/integration/test_regression_api.py
  </files>
  <read_first>
    - tests/contracts/test_desktop_runtime_contract.py
    - tests/integration/test_regression_api.py
    - desktop-client/scripts/build-helper-runtime.mjs
  </read_first>
  <action>
    Extend automated coverage beyond static packaging assertions:
    - keep contract checks for packaged helper/runtime assets
    - add integration/regression assertions for Bottle 1.0 local-generation result payloads
    - include a smoke path that validates packaged helper transcription can run with required runtime assets present
  </action>
  <acceptance_criteria>
    - packaged-helper regression coverage asserts runtime assets required by local transcription remain included
    - integration coverage asserts local-generation completion payload shape and translated-vs-partial semantics
    - regression checks would fail if packaged local transcription lost required runtime assets again
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/contracts/test_desktop_runtime_contract.py tests/integration/test_regression_api.py -k "desktop or local_asr or translation" -q</automated>
  </verify>
  <done>Automated coverage now protects the packaged Bottle 1.0 local runtime and its translated-vs-partial result semantics.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pytest tests/contracts/test_desktop_runtime_contract.py -q` exits 0
2. `pytest tests/integration/test_regression_api.py -k "desktop or local_asr or translation" -q` exits 0
3. Bottle 1.0 desktop-local full-success path returns translated learner sentences
4. Partial local output, if any, is explicitly labeled as partial rather than masquerading as full success
</verification>

<success_criteria>
- Bottle 1.0 desktop-local result semantics are explicit and user-visible
- Full-success local lessons contain translated learner content
- Packaged helper/runtime coverage guards against future silent runtime regressions
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/04.1-desktop-local-course-fidelity/04.1-03-SUMMARY.md`
</output>
