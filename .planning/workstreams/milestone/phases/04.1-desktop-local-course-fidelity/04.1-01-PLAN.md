---
phase: 04.1-desktop-local-course-fidelity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/upload/UploadPanel.jsx
  - frontend/src/app/LearningShellLocalSubtitles.jsx
  - tests/contracts/test_desktop_runtime_contract.py
autonomous: true
requirements:
  - DESK-04
must_haves:
  truths:
    - "Desktop upload exposes one shared `开始生成` CTA for Bottle 1.0 and Bottle 2.0"
    - "Selected model card, not a separate local-only button, decides the generation path"
    - "Desktop file selection survives into the shared submit path with the correct file/path context"
    - "Bottle 1.0 desktop-local path does not auto-fallback into cloud routing before the selected local path runs"
  artifacts:
    - path: frontend/src/features/upload/UploadPanel.jsx
      provides: Shared CTA routing and desktop file/path handoff
      contains: '开始生成'
    - path: frontend/src/app/LearningShellLocalSubtitles.jsx
      provides: Desktop shell callback wiring for generated-lesson navigation
      contains: 'onNavigateToLesson'
    - path: tests/contracts/test_desktop_runtime_contract.py
      provides: Contract coverage for shared CTA routing
      contains: 'desktop local'
---

<objective>
Unify Bottle 1.0 and Bottle 2.0 behind one shared desktop generate CTA and make sure desktop file selection still carries the source file/path into the chosen generation route.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@frontend/src/features/upload/UploadPanel.jsx
@frontend/src/app/LearningShellLocalSubtitles.jsx
@tests/contracts/test_desktop_runtime_contract.py
@.planning/workstreams/milestone/phases/04.1-desktop-local-course-fidelity/04.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse desktop generation entry to one shared CTA</name>
  <files>
    - frontend/src/features/upload/UploadPanel.jsx
  </files>
  <read_first>
    - frontend/src/features/upload/UploadPanel.jsx
    - .planning/workstreams/milestone/phases/04.1-desktop-local-course-fidelity/04.1-CONTEXT.md
  </read_first>
  <action>
    Remove the dedicated Bottle 1.0-only generation CTA and keep `开始生成` as the single primary desktop action. The selected model card must determine whether the user enters the Bottle 1.0 desktop-local route or the Bottle 2.0 cloud route.
  </action>
  <acceptance_criteria>
    - the upload action row contains `开始生成` but no separate `本地生成`
    - Bottle 1.0 desktop-local routing is still reachable from the shared CTA
    - Bottle 2.0 still uses the cloud path
  </acceptance_criteria>
  <verify>
    <automated>rg -n '本地生成|开始生成|submitDesktopLocalFast|submitCloudDirectUpload' frontend/src/features/upload/UploadPanel.jsx</automated>
  </verify>
  <done>One shared CTA now dispatches by selected model instead of forcing users to understand runtime-specific buttons.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve desktop file/path state through shared submit</name>
  <files>
    - frontend/src/features/upload/UploadPanel.jsx
  </files>
  <read_first>
    - frontend/src/features/upload/UploadPanel.jsx
  </read_first>
  <action>
    Make sure desktop file selection and the normalized desktop source path survive into the shared submit path. If the custom desktop file-selection bridge is unreliable, prefer the path that preserves a visible selected file, desktop source path, and stable submit behavior.
  </action>
  <acceptance_criteria>
    - selected desktop files remain visible in preview state before generation
    - shared submit path can resolve the desktop source file/path without `Desktop source path is required.`
    - shared desktop generation does not silently switch the user into a cloud error path before the selected local route runs
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'resolveDesktopSelectedSourcePath|desktop_source_path|fileInputRef.current.click|submit\\(' frontend/src/features/upload/UploadPanel.jsx</automated>
  </verify>
  <done>Desktop file selection survives into the shared submit path with the required file/path context intact.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the shared CTA routing contract</name>
  <files>
    - tests/contracts/test_desktop_runtime_contract.py
  </files>
  <read_first>
    - tests/contracts/test_desktop_runtime_contract.py
    - frontend/src/features/upload/UploadPanel.jsx
  </read_first>
  <action>
    Extend contract coverage so the shared CTA contract cannot regress:
    - one shared `开始生成` CTA remains
    - no dedicated Bottle 1.0 local button remains
    - desktop-local routing and desktop file/path normalization remain represented in contract assertions
  </action>
  <acceptance_criteria>
    - contract tests assert one shared CTA expectation
    - contract tests assert no dedicated `本地生成` button remains
    - contract tests assert desktop-local routing and path normalization hooks remain present
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/contracts/test_desktop_runtime_contract.py -q</automated>
  </verify>
  <done>Regression coverage now locks the shared CTA routing contract for Bottle 1.0 desktop-local generation.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pytest tests/contracts/test_desktop_runtime_contract.py -q` exits 0
2. Desktop upload UI shows only one primary `开始生成` CTA
3. Selecting a desktop file keeps the preview visible and the shared CTA can start Bottle 1.0 local generation
</verification>

<success_criteria>
- Model selection, not a second CTA, determines generation routing
- Desktop file selection survives into the shared submit path
- Bottle 1.0 desktop-local path no longer misroutes into cloud entry behavior
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/04.1-desktop-local-course-fidelity/04.1-01-SUMMARY.md`
</output>
