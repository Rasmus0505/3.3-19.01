---
phase: 11-conversion-rollout-and-regression-closeout
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/upload/UploadPanel.jsx
  - tests/contracts/test_phase11_surface_contract.py
autonomous: true
requirements:
  - GROW-01
must_haves:
  truths:
    - "模型卡只按用户给定文案和布局实现，不额外增加副标题或说明段落"
    - "余额不足、Bottle 1.0 网页不可执行、链接导入、复杂视频四类场景都要使用 Phase 11 context 锁定的精确按钮文案"
    - "用户侧仍然只显示 `Bottle 1.0 / Bottle 2.0` 主命名，不抬回技术名"
  artifacts:
    - path: frontend/src/features/upload/UploadPanel.jsx
      provides: User-facing model cards, blocked-state CTA, desktop guidance dialog
      contains: "Bottle 1.0"
    - path: tests/contracts/test_phase11_surface_contract.py
      provides: Contract guard for exact phase-11 upload wording
      contains: "继续生成素材"
  key_links:
    - "Phase 11 context already freezes the exact card copy and explicitly forbids adding extra subtitles the user did not ask for"
    - "Phase 7 CTA rules remain in force: Bottle 1.0 stays desktop-only and insufficient balance still recovers through recharge"
---

<objective>
Finalize the user-facing upload conversion surface so the model cards and blocked-state actions exactly match the Phase 11 context wording without adding extra copy.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/REQUIREMENTS.md
@.planning/workstreams/milestone/phases/07-competitive-research-and-product-specs/07-WEB-CTA-SPEC.md
@.planning/workstreams/milestone/phases/07-competitive-research-and-product-specs/07-COPY-DECK.md
@.planning/workstreams/milestone/phases/11-conversion-rollout-and-regression-closeout/11-CONTEXT.md
@frontend/src/features/upload/UploadPanel.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 按 context 收口两张模型卡的文案和布局</name>
  <files>
    - frontend/src/features/upload/UploadPanel.jsx
  </files>
  <read_first>
    - frontend/src/features/upload/UploadPanel.jsx
    - .planning/workstreams/milestone/phases/11-conversion-rollout-and-regression-closeout/11-CONTEXT.md
    - .planning/workstreams/milestone/phases/07-competitive-research-and-product-specs/07-COPY-DECK.md
  </read_first>
  <action>
    Update the upload model-card section so the visible text and row structure match the Phase 11 context exactly.

    Implement these exact visible outcomes:
    - the section heading becomes `选择学习素材质量`
    - the `Bottle 1.0` card shows exactly:
      - first row: `Bottle 1.0` and the backend-configured price label
      - second row: `客户端专属` and `通用素材生成`
    - the `Bottle 2.0` card shows exactly:
      - first row: `Bottle 2.0` and the backend-configured price label
      - second row: `网站/客户端`
      - third row: `更强大的AI模型` and `适合复杂视频`

    Do not add any extra subtitle, helper paragraph, or recommendation tag outside the exact rows above.

    Keep the existing selection, readiness badge, and prepare/download actions working, but rearrange the visible copy so it respects the locked card layout rather than the older "title + subtitle + note" wording.
  </action>
  <verify>
    <automated>python -m pytest tests/contracts/test_phase11_surface_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/features/upload/UploadPanel.jsx` contains `选择学习素材质量`
    - `frontend/src/features/upload/UploadPanel.jsx` contains `客户端专属`
    - `frontend/src/features/upload/UploadPanel.jsx` contains `通用素材生成`
    - `frontend/src/features/upload/UploadPanel.jsx` contains `网站/客户端`
    - `frontend/src/features/upload/UploadPanel.jsx` contains `更强大的AI模型`
    - `frontend/src/features/upload/UploadPanel.jsx` contains `适合复杂视频`
    - `python -m pytest tests/contracts/test_phase11_surface_contract.py -q` exits 0
  </acceptance_criteria>
  <done>上传页两张模型卡已经完全改成 Phase 11 context 锁定的文案和布局。</done>
</task>

<task type="auto">
  <name>Task 2: 把受阻场景按钮和提示语收口为 Phase 11 最终文案</name>
  <files>
    - frontend/src/features/upload/UploadPanel.jsx
    - tests/contracts/test_phase11_surface_contract.py
  </files>
  <read_first>
    - frontend/src/features/upload/UploadPanel.jsx
    - .planning/workstreams/milestone/phases/11-conversion-rollout-and-regression-closeout/11-CONTEXT.md
    - .planning/workstreams/milestone/phases/07-competitive-research-and-product-specs/07-WEB-CTA-SPEC.md
  </read_first>
  <action>
    Update the upload blocked-state and desktop-guidance wording so the user-visible copy exactly matches the Phase 11 context.

    Apply these exact texts:
    - insufficient balance main message: `余额不足，充值后即可继续生成当前内容`
    - insufficient balance primary button: `充值后生成`
    - insufficient balance secondary button: `稍后再试`
    - Bottle 1.0 web-only message: `Bottle 1.0 仅支持在客户端使用，请下载桌面端继续`
    - Bottle 1.0 desktop-only secondary action: `我知道了`
    - link-import message: `链接导入仅支持在客户端使用，请下载桌面端继续`
    - link-import secondary action: `继续上传本地文件`
    - large/long/complex media message: `当前素材推荐使用客户端生成，效果和稳定性更好`
    - large/long/complex media secondary action: `继续生成素材`

    Keep the existing CTA routing rules intact:
    - recharge still routes to `/redeem`
    - desktop-only cases still route to the desktop guidance/download path
    - Bottle 1.0 never becomes a browser-executable path
  </action>
  <verify>
    <automated>python -m pytest tests/contracts/test_phase11_surface_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/features/upload/UploadPanel.jsx` contains `余额不足，充值后即可继续生成当前内容`
    - `frontend/src/features/upload/UploadPanel.jsx` contains `充值后生成`
    - `frontend/src/features/upload/UploadPanel.jsx` contains `稍后再试`
    - `frontend/src/features/upload/UploadPanel.jsx` contains `Bottle 1.0 仅支持在客户端使用，请下载桌面端继续`
    - `frontend/src/features/upload/UploadPanel.jsx` contains `我知道了`
    - `frontend/src/features/upload/UploadPanel.jsx` contains `链接导入仅支持在客户端使用，请下载桌面端继续`
    - `frontend/src/features/upload/UploadPanel.jsx` contains `继续上传本地文件`
    - `frontend/src/features/upload/UploadPanel.jsx` contains `当前素材推荐使用客户端生成，效果和稳定性更好`
    - `frontend/src/features/upload/UploadPanel.jsx` contains `继续生成素材`
    - `python -m pytest tests/contracts/test_phase11_surface_contract.py -q` exits 0
  </acceptance_criteria>
  <done>受阻场景 CTA 和提示语已经全部按 Phase 11 context 收口。</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `python -m pytest tests/contracts/test_phase11_surface_contract.py -q` exits 0
2. UploadPanel visibly uses only the locked Phase 11 card copy and blocked-state wording
</verification>

<success_criteria>
- GROW-01 is satisfied on the user-facing upload conversion surface
- No extra subtitle or explanation text is added beyond the locked context copy
- Bottle 1.0 remains desktop-only while the CTA wording becomes final
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/11-conversion-rollout-and-regression-closeout/11-01-SUMMARY.md`
</output>
