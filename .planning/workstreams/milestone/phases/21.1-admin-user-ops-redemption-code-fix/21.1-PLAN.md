# Phase 21.1 Plan: 后台管理用户运营优化

## Phase Overview

**Goal:** 用户列表显示用户名；兑换码操作从"启用/停用/废弃"精简为"删除/废弃"；兑换码增加复制按钮；兑换码状态改为中文且只保留一个状态。

**Decisions (locked):**
- D-01: 用户列表增加"用户名"列（`User.username` 字段）
- D-02: 用户列表增加"注册时间"列（`User.created_at`，北京时间格式化）
- D-04: 兑换码"删除"为硬删除——数据库记录彻底移除
- D-06: 废弃接口执行时自动扣款，生成负向钱包流水
- D-08: 废弃扣款逻辑需加事务保护
- D-09: 复制按钮位于每行操作列最右侧
- D-10: 点击复制按钮直接复制完整明文兑换码
- D-11: 复制成功后显示 toast 提示"兑换码已复制"
- D-12: 删除"有效状态"列，改为单一中文状态
- D-13: 状态映射：`unredeemed`+`enabled`→**正常**, `unredeemed`+`disabled`→**停用**, `redeemed`→**已兑换**, `expired`→**失效**, `abandoned`→**废弃**

---

## Plan 21.1-01: 用户列表增强（后端 + 前端）

**Phase:** 21.1-admin-user-ops-redemption-code-fix
**Plan:** 01
**Type:** execute
**Wave:** 1
**Depends on:** []
**Files modified:**
- `app/repositories/admin_console.py`
- `app/schemas/admin_console.py`
- `app/api/routers/admin/console.py`
- `frontend/src/features/admin-users/AdminUsersTab.jsx`
**Autonomous:** true

### Objective

在用户活跃列表的后端和前端增加"用户名"和"注册时间"两列（D-01, D-02）。

### Must-haves

**Truths:**
- 用户列表每行显示用户名
- 用户列表每行显示注册时间（北京格式化）

**Artifacts:**
- `AdminUserActivityItem` schema 增加 `username` 和 `created_at` 字段
- `list_admin_user_activity()` 查询包含 `User.username` 和 `User.created_at`
- `AdminUsersTab.jsx` 表格有"用户名"和"注册时间"两列

### Tasks

<task type="auto">
<name>Task 1: 后端 Schema 增加 username 和 created_at 字段</name>
<files>`app/schemas/admin_console.py`</files>
<read_first>`app/schemas/admin_console.py` (查看 AdminUserActivityItem 定义)</read_first>
<action>在 `AdminUserActivityItem` 类中增加两个字段：

```python
class AdminUserActivityItem(BaseModel):
    id: int
    email: str
    username: str = ""  # 新增字段 per D-01
    created_at: datetime  # 新增字段 per D-02
    # ... 其余字段保持不变 ...
```

注意：`created_at` 在原始 Schema 中已存在，只需确保字段顺序和类型正确。
</action>
<acceptance_criteria>
- `AdminUserActivityItem` 类包含 `username: str = ""` 字段
- `AdminUserActivityItem` 类包含 `created_at: datetime` 字段
</acceptance_criteria>
<done>AdminUserActivityItem schema 包含 username 和 created_at 字段</done>
</task>

<task type="auto">
<name>Task 2: Repository 查询增加 username 字段</name>
<files>`app/repositories/admin_console.py`</files>
<read_first>`app/repositories/admin_console.py` (查看 list_admin_user_activity 函数第 654-666 行的 select 语句)</read_first>
<action>在 `list_admin_user_activity()` 函数的 base select 语句中增加 `User.username` 字段：

在第 656-665 行的 select 语句中，在 `.email` 后增加 `.username`：

```python
base = (
    select(
        User.id,
        User.email,
        User.username,  # 新增 per D-01
        User.created_at,  # 已有但需确保在返回字段中
        User.last_login_at,
        func.coalesce(WalletAccount.balance_amount_cents, 0).label("balance_points"),
        # ... 其余字段 ...
    )
)
```

确保 `User.username` 被 select 出来供后续使用。
</action>
<acceptance_criteria>
- `list_admin_user_activity()` 函数的 select 语句包含 `User.username`
- rows 变量返回的每行包含 username 值
</acceptance_criteria>
<done>Repository 查询返回 username 字段</done>
</task>

<task type="auto">
<name>Task 3: API 路由序列化增加 username</name>
<files>`app/api/routers/admin/console.py`</files>
<read_first>`app/api/routers/admin/console.py` (查看 _to_user_activity_item 函数第 158-170 行)</read_first>
<action>在 `_to_user_activity_item()` 函数中增加 `username` 字段映射：

```python
def _to_user_activity_item(row) -> AdminUserActivityItem:
    return AdminUserActivityItem(
        id=int(row.id),
        email=str(row.email or ""),
        username=str(row.username or ""),  # 新增 per D-01
        created_at=to_shanghai_aware(row.created_at),  # 确保格式化 per D-02
        last_login_at=to_shanghai_aware(row.last_login_at) if row.last_login_at else None,
        balance_points=int(row.balance_points or 0),
        login_days=int(row.login_days or 0),
        login_events=int(row.login_events or 0),
        lessons_created=int(row.lessons_created or 0),
        consumed_points=int(row.consumed_points or 0),
        redeemed_points=int(row.redeemed_points or 0),
    )
```
</action>
<acceptance_criteria>
- `_to_user_activity_item()` 返回的对象包含 `username` 字段
- `created_at` 使用 `to_shanghai_aware()` 格式化
</acceptance_criteria>
<done>_to_user_activity_item 序列化包含 username 字段</done>
</task>

<task type="auto">
<name>Task 4: 前端表格增加用户名和注册时间列</name>
<files>`frontend/src/features/admin-users/AdminUsersTab.jsx`</files>
<read_first>`frontend/src/features/admin-users/AdminUsersTab.jsx` (查看 columns 定义约第 413-508 行)</read_first>
<action>在 `columns` 数组中，在"邮箱"列后增加"用户名"列和"注册时间"列：

```javascript
const columns = useMemo(
  () => [
    {
      key: "email",
      header: (
        <button type="button" className="inline-flex items-center gap-1" onClick={() => toggleSort("email")}>
          邮箱
          <ArrowUpDown className="size-3.5" />
        </button>
      ),
      mobileLabel: "邮箱",
      render: (item) => item.email,
    },
    // 新增 per D-01: 用户名列
    {
      key: "username",
      header: "用户名",
      mobileLabel: "用户名",
      render: (item) => item.username || "-",
    },
    // 新增 per D-02: 注册时间列
    {
      key: "created_at",
      header: (
        <button type="button" className="inline-flex items-center gap-1" onClick={() => toggleSort("created_at")}>
          注册时间
          <ArrowUpDown className="size-3.5" />
        </button>
      ),
      mobileLabel: "注册时间",
      render: (item) => formatDateTimeBeijing(item.created_at),
    },
    // ... 其余列保持不变 ...
```

注意：保留现有的"邮箱"列，在其后依次增加"用户名"和"注册时间"两列。
</action>
<acceptance_criteria>
- 表格第一页加载后，浏览器控制台无 JS 错误
- 每行数据显示 `item.username` 的值
- 每行数据显示 `formatDateTimeBeijing(item.created_at)` 格式化的注册时间
</acceptance_criteria>
<done>用户列表显示用户名和注册时间两列</done>
</task>

### Verification

1. 启动后端服务，访问 `/api/admin/user-activity?page=1&page_size=10`
2. 验证返回 JSON 中每个 item 包含 `username` 和 `created_at` 字段
3. 访问管理后台用户列表页面，验证表格显示用户名和注册时间列

### Success Criteria

- [ ] 后端 API `/api/admin/user-activity` 返回的 items 包含 `username` 字段
- [ ] 后端 API `/api/admin/user-activity` 返回的 items 包含 `created_at` 字段（北京时间格式）
- [ ] 前端表格显示"用户名"列（位于邮箱列后）
- [ ] 前端表格显示"注册时间"列（位于用户名列后）

---

## Plan 21.1-02: 兑换码后端增强（复制字段 + 硬删除 + 废弃扣款）

**Phase:** 21.1-admin-user-ops-redemption-code-fix
**Plan:** 02
**Type:** execute
**Wave:** 1
**Depends on:** []
**Files modified:**
- `app/repositories/admin.py`
- `app/schemas/admin.py`
- `app/api/routers/admin.py`
- `app/services/billing.py`
**Autonomous:** true

### Objective

实现兑换码后端变更：增加明文字段（复制功能）、硬删除接口、废弃扣款逻辑（D-06, D-08, D-10）。

### Must-haves

**Truths:**
- 兑换码列表 API 返回 `code_plain` 明文字段
- 删除兑换码 API 执行硬删除（数据库记录移除）
- 废弃已兑换兑换码时自动扣除用户钱包余额

**Artifacts:**
- `AdminRedeemCodeItem` schema 包含 `code_plain` 字段
- `DELETE /api/admin/redeem-codes/{id}` 执行硬删除
- `POST /api/admin/redeem-codes/{id}/abandon` 包含钱包扣款逻辑

### Tasks

<task type="auto">
<name>Task 1: Schema 增加 code_plain 字段</name>
<files>`app/schemas/admin.py`</files>
<read_first>`app/schemas/admin.py` (查看 AdminRedeemCodeItem 类约第 357-368 行)</read_first>
<action>在 `AdminRedeemCodeItem` 类中，在 `code_mask` 后增加 `code_plain` 字段：

```python
class AdminRedeemCodeItem(BaseModel):
    id: int
    batch_id: int
    batch_name: str
    code_mask: str
    code_plain: str = ""  # 新增 per D-10，用于复制功能
    status: str
    effective_status: str
    face_value_points: int
    redeemed_user_email: str | None
    redeemed_at: datetime | None
    created_by_user_id: int | None
    created_at: datetime
```
</action>
<acceptance_criteria>
- `AdminRedeemCodeItem` 类包含 `code_plain: str = ""` 字段
- 字段位于 `code_mask` 之后
</acceptance_criteria>
<done>AdminRedeemCodeItem schema 包含 code_plain 字段</done>
</task>

<task type="auto">
<name>Task 2: Repository 查询返回 code_plain</name>
<files>`app/repositories/admin.py`</files>
<read_first>`app/repositories/admin.py` (查看 list_redeem_codes 函数第 456-601 行)</read_first>
<action>在 `list_redeem_codes()` 函数的 select 语句中，确保查询 `code_plain` 字段：

在第 498-516 行的 select 中，增加 `code_plain` 字段：

```python
redeemed_user = User.__table__.alias("redeemed_user")

base = (
    select(
        code_table.c.id.label("code_id"),
        code_table.c.batch_id.label("code_batch_id"),
        code_table.c.masked_code.label("code_masked_code"),
        code_table.c.code_plain.label("code_plain"),  # 新增 per D-10
        code_table.c.status.label("code_status"),
        # ... 其余字段 ...
    )
)
```

同时在 SimpleNamespace 构建时（第 582-600 行）确保包含 `code_plain`：

```python
return total, [
    (
        SimpleNamespace(
            id=int(row.code_id),
            batch_id=int(row.code_batch_id),
            masked_code=str(row.code_masked_code or ""),
            code_plain=str(row.code_plain or ""),  # 新增
            status=str(row.code_status or ""),
            created_by_user_id=row.code_created_by_user_id,
            redeemed_at=row.code_redeemed_at,
            created_at=row.code_created_at,
        ),
        # ... 其余 ...
    )
    for row in rows
]
```
</action>
<acceptance_criteria>
- `list_redeem_codes()` 返回的 code 对象包含 `code_plain` 属性
- 每行返回的 SimpleNamespace 包含 `code_plain` 字段
</acceptance_criteria>
<done>list_redeem_codes 返回 code_plain 字段</done>
</task>

<task type="auto">
<name>Task 3: API 序列化返回 code_plain</name>
<files>`app/api/routers/admin.py`</files>
<read_first>`app/api/routers/admin.py` (查看 admin_list_redeem_codes 函数约第 1163-1235 行)</read_first>
<action>在 `admin_list_redeem_codes()` 函数中，为每个 `AdminRedeemCodeItem` 填充 `code_plain` 字段：

在第 1203-1223 行构建 `items` 时，增加 `code_plain` 映射：

```python
items = [
    AdminRedeemCodeItem(
        id=code.id,
        batch_id=batch.id,
        batch_name=batch.batch_name,
        code_mask=code.masked_code,
        code_plain=code.code_plain,  # 新增 per D-10
        status=code.status,
        effective_status=_effective_code_status(
            code_status=code.status,
            batch_status=batch.status,
            expire_at=batch.expire_at,
            now=now,
        ),
        face_value_points=batch.face_value_points,
        redeemed_user_email=redeemed_user_email_item,
        redeemed_at=to_shanghai_aware(code.redeemed_at),
        created_by_user_id=code.created_by_user_id,
        created_at=to_shanghai_aware(code.created_at),
    )
    for code, batch, redeemed_user_email_item in rows
]
```
</action>
<acceptance_criteria>
- API 返回的每个兑换码 item 包含 `code_plain` 字段
- `code_plain` 值与数据库中 `code_plain` 字段一致
</acceptance_criteria>
<done>API 序列化包含 code_plain 字段</done>
</task>

<task type="auto">
<name>Task 4: 实现废弃扣款服务函数</name>
<files>`app/services/billing.py`</files>
<read_first>`app/services/billing.py` (查看 _append_ledger 和 manual_adjust 函数了解钱包操作模式)</read_first>
<action>在 `app/services/billing.py` 中，在 `bulk_disable_redeem_codes` 函数后（约第 1856 行后）添加废弃扣款函数：

```python
def abandon_redeem_code_with_refund(
    db: Session,
    *,
    code_id: int,
    operator_user_id: int,
) -> dict[str, object]:
    """
    废弃兑换码并扣除已兑换用户钱包余额 per D-06, D-08
    - 如果兑换码未兑换，直接标记为 abandoned
    - 如果兑换码已兑换，遍历其兑换记录，生成负向钱包流水
    - 所有操作在同一事务中完成
    """
    code = db.scalar(select(RedeemCode).where(RedeemCode.id == code_id).with_for_update())
    if not code:
        raise BillingError("REDEEM_CODE_NOT_FOUND", "兑换码不存在", str(code_id))

    batch = db.get(RedeemCodeBatch, code.batch_id)
    if not batch:
        raise BillingError("REDEEM_BATCH_NOT_FOUND", "批次不存在", str(code.batch_id))

    # 未兑换：直接废弃
    if code.status != REDEEM_CODE_STATUS_REDEEMED:
        before = {"status": code.status}
        code.status = REDEEM_CODE_STATUS_ABANDONED
        db.add(code)
        db.flush()
        append_admin_operation_log(
            db,
            operator_user_id=operator_user_id,
            action_type="redeem_code_abandon",
            target_type="redeem_code",
            target_id=str(code.id),
            before_value=before,
            after_value={"status": code.status, "refund": False},
            note="abandon_no_redeem",
        )
        return {"status": code.status, "refunded": False, "refund_amount": 0}

    # 已兑换：扣除用户钱包余额 per D-06
    redeemed_user_id = code.redeemed_by_user_id
    if redeemed_user_id is None:
        raise BillingError("REDEEM_CODE_NO_REDEEMER", "已兑换兑换码无兑换用户")

    refund_amount = batch.face_value_points  # 扣除面额
    account = get_or_create_wallet_account(db, redeemed_user_id, for_update=True)
    
    # 扣除余额
    account.balance_points -= refund_amount
    if account.balance_points < 0:
        # 允许负余额 per D-13 discretion
        pass
    db.add(account)

    # 生成负向流水 per D-06
    _append_ledger(
        db,
        user_id=redeemed_user_id,
        operator_user_id=operator_user_id,
        event_type="abandon_refund",  # 新事件类型
        delta_points=-refund_amount,
        balance_after=account.balance_points,
        redeem_batch_id=batch.id,
        redeem_code_id=code.id,
        redeem_code_mask=code.masked_code,
        note=f"废弃扣回:{code.masked_code}",
    )

    # 更新兑换码状态
    before = {"status": code.status}
    code.status = REDEEM_CODE_STATUS_ABANDONED
    db.add(code)
    db.flush()

    append_admin_operation_log(
        db,
        operator_user_id=operator_user_id,
        action_type="redeem_code_abandon",
        target_type="redeem_code",
        target_id=str(code.id),
        before_value=before,
        after_value={"status": code.status, "refund": True, "refund_amount": refund_amount},
        note="abandon_with_refund",
    )

    return {
        "status": code.status,
        "refunded": True,
        "refund_amount": refund_amount,
        "user_id": redeemed_user_id,
        "balance_after": account.balance_points,
    }
```

同时在文件顶部的事件类型常量区（约第 38-53 行）增加新事件类型：

```python
EVENT_ABANDON_REFUND = "abandon_refund"  # 新增 per D-06
```

并在 `billing_service.py` 的 `__all__` 中导出新函数（如果需要）。
</action>
<acceptance_criteria>
- `abandon_redeem_code_with_refund()` 函数存在且可被导入
- 函数签名：`(db, code_id, operator_user_id) -> dict`
- 函数返回包含 `status`, `refunded`, `refund_amount` 字段
</acceptance_criteria>
<done>废弃扣款服务函数已实现</done>
</task>

<task type="auto">
<name>Task 5: 实现硬删除兑换码 API</name>
<files>`app/api/routers/admin.py`</files>
<read_first>`app/api/routers/admin.py` (查看 admin_abandon_redeem_code 函数约第 1302-1331 行作为参考)</read_first>
<action>在 `app/api/routers/admin.py` 中，在 `admin_abandon_redeem_code` 函数后（约第 1331 行后）添加硬删除兑换码端点：

```python
@router.delete(
    "/redeem-codes/{code_id}",
    responses={400: {"model": ErrorResponse}, 401: {"model": ErrorResponse}, 403: {"model": ErrorResponse}},
)
def admin_delete_redeem_code(
    code_id: int,
    db: Session = Depends(get_db),
    current_admin: User = Depends(get_admin_user),
):
    """
    硬删除兑换码 per D-04
    数据库记录彻底移除，兑换码立即失效，不可恢复。
    """
    code = db.get(RedeemCode, code_id)
    if not code:
        return error_response(404, "REDEEM_CODE_NOT_FOUND", "兑换码不存在")

    # 记录操作日志
    append_admin_operation_log(
        db,
        operator_user_id=current_admin.id,
        action_type="redeem_code_hard_delete",
        target_type="redeem_code",
        target_id=str(code.id),
        before_value={
            "code_id": code.id,
            "batch_id": code.batch_id,
            "status": code.status,
            "masked_code": code.masked_code,
        },
        after_value={"deleted": True},
        note="hard_delete",
    )

    # 执行硬删除
    db.delete(code)
    db.commit()

    return {"ok": True, "code_id": code_id, "deleted": True}
```

注意：硬删除前确保 FK 约束可以处理（如有外键约束需先处理关联记录）。
</action>
<acceptance_criteria>
- `DELETE /api/admin/redeem-codes/{code_id}` 端点存在
- 删除后数据库中该兑换码记录不存在
- API 返回 `{"ok": true, "code_id": xxx, "deleted": true}`
</acceptance_criteria>
<done>硬删除兑换码 API 已实现</done>
</task>

<task type="auto">
<name>Task 6: 更新废弃接口调用扣款逻辑</name>
<files>`app/api/routers/admin.py`</files>
<read_first>`app/api/routers/admin.py` (查看 admin_abandon_redeem_code 函数约第 1302-1331 行)</read_first>
<action>更新 `admin_abandon_redeem_code()` 函数，调用新的 `abandon_redeem_code_with_refund()` 函数：

```python
@router.post(
    "/redeem-codes/{code_id}/abandon",
    response_model=AdminRedeemCodeStatusActionResponse,
    responses={400: {"model": ErrorResponse}, 401: {"model": ErrorResponse}, 403: {"model": ErrorResponse}},
)
def admin_abandon_redeem_code(
    code_id: int,
    db: Session = Depends(get_db),
    current_admin: User = Depends(get_admin_user),
):
    """
    废弃兑换码 per D-06, D-08
    - 已兑换：扣除用户钱包余额（事务保护）
    - 未兑换：直接标记为废弃
    """
    try:
        # 调用新的废弃扣款函数
        result = abandon_redeem_code_with_refund(
            db,
            code_id=code_id,
            operator_user_id=current_admin.id,
        )
        db.commit()

        return AdminRedeemCodeStatusActionResponse(
            ok=True,
            code_id=code_id,
            status=result["status"],
            effective_status=result["status"],
        )
    except BillingError as exc:
        db.rollback()
        return map_billing_error(exc)
    except Exception as exc:
        db.rollback()
        return error_response(500, "INTERNAL_ERROR", "废弃兑换码失败", str(exc)[:1200])
```

需要确保在文件顶部导入了 `abandon_redeem_code_with_refund` 函数（从 billing_service）。
</action>
<acceptance_criteria>
- POST `/api/admin/redeem-codes/{code_id}/abandon` 对已兑换码执行扣款
- 扣款在同一事务中完成（db.commit 在扣款成功后调用）
- API 返回包含废弃后的状态
</acceptance_criteria>
<done>废弃接口已更新为使用扣款逻辑</done>
</task>

### Verification

1. GET `/api/admin/redeem-codes?page=1&page_size=10` 返回的 items 包含 `code_plain` 字段
2. DELETE `/api/admin/redeem-codes/1` 后，数据库中该记录不存在
3. POST `/api/admin/redeem-codes/1/abandon`（已兑换码）后，用户钱包余额减少

### Success Criteria

- [ ] 兑换码列表 API 返回 `code_plain` 明文字段
- [ ] 删除兑换码 API 执行硬删除（数据库记录移除）
- [ ] 废弃已兑换兑换码时，用户钱包余额减少
- [ ] 废弃操作在同一事务中完成（原子性）

---

## Plan 21.1-03: 兑换码前端增强（状态中文 + 操作按钮 + 复制功能）

**Phase:** 21.1-admin-user-ops-redemption-code-fix
**Plan:** 03
**Type:** execute
**Wave:** 2
**Depends on:** [21.1-02]
**Files modified:**
- `frontend/src/features/admin-redeem/AdminRedeemCodesTab.jsx`
**Autonomous:** false

### Objective

实现兑换码前端变更：状态改为中文（单一状态）、操作按钮精简为"删除"+"废弃"、增加复制按钮（D-09, D-11, D-12, D-13）。

### Must-haves

**Truths:**
- 兑换码状态显示为中文（非英文/双语）
- 操作列只有"删除"和"废弃"两个按钮
- 每行右侧有"复制"按钮，点击后 toast 提示"兑换码已复制"
- "有效状态"列已删除

**Artifacts:**
- `AdminRedeemCodesTab.jsx` 表格状态列显示中文
- 操作列包含删除、废弃、复制三个按钮

### Tasks

<task type="auto">
<name>Task 1: 状态列改为中文单一状态</name>
<files>`frontend/src/features/admin-redeem/AdminRedeemCodesTab.jsx`</files>
<read_first>`frontend/src/features/admin-redeem/AdminRedeemCodesTab.jsx` (查看表格列定义约第 376-432 行)</read_first>
<action>修改表格列定义，删除"有效状态"列，将状态列改为中文：

1. 删除 TableHeader 中的 `<TableHead>有效状态</TableHead>`
2. 修改状态列的 Badge 显示逻辑，改为中文映射 per D-13：

```javascript
// 在文件顶部或组件内添加状态映射函数
function getRedeemCodeStatusLabel(status, effectiveStatus) {
  // per D-13 状态映射规则
  if (effectiveStatus === "redeemed") return "已兑换";
  if (effectiveStatus === "abandoned") return "废弃";
  if (effectiveStatus === "expired") return "失效";
  if (effectiveStatus === "disabled") return "停用";
  if (status === "active") return "正常";
  return "正常";
}

// 在表格列中使用
{
  key: "status",
  header: "状态",
  render: (item) => {
    const label = getRedeemCodeStatusLabel(item.status, item.effective_status);
    // 颜色映射：正常=默认, 停用=secondary, 已兑换=success, 失效=warning, 废弃=destructive
    const variant = item.effective_status === "abandoned" ? "destructive" 
                 : item.effective_status === "expired" ? "warning"
                 : item.effective_status === "redeemed" ? "default"
                 : "secondary";
    return <Badge variant={variant}>{label}</Badge>;
  },
},
```

同时删除 TableBody 中的 `<TableCell><Badge>{item.effective_status}</Badge></TableCell>` 整行。
</action>
<acceptance_criteria>
- 表格不再有"有效状态"列
- 状态列显示中文：正常/停用/已兑换/失效/废弃
- Badge 颜色与状态对应（废弃=destructive）
</acceptance_criteria>
<done>状态列改为中文单一状态，"有效状态"列已删除</done>
</task>

<task type="auto">
<name>Task 2: 操作按钮改为删除 + 废弃</name>
<files>`frontend/src/features/admin-redeem/AdminRedeemCodesTab.jsx`</files>
<read_first>`frontend/src/features/admin-redeem/AdminRedeemCodesTab.jsx` (查看操作列定义约第 415-421 行)</read_first>
<action>修改操作列，将"启用"、"停用"按钮替换为"删除"按钮：

```javascript
<TableCell>
  <div className="flex items-center gap-2">
    {/* per D-04: 删除按钮 - 硬删除 */}
    <Button 
      size="sm" 
      variant="destructive"
      onClick={() => setActionDialog({ 
        codeId: item.id, 
        actionPath: "delete",  // 使用 DELETE 方法
        actionLabel: "删除", 
        codeMask: item.code_mask,
        codePlain: item.code_plain  // 传递明文供复制
      })}
    >
      删除
    </Button>
    {/* 废弃按钮保持不变 */}
    <Button 
      size="sm" 
      onClick={() => setActionDialog({ 
        codeId: item.id, 
        actionPath: "abandon", 
        actionLabel: "废弃", 
        codeMask: item.code_mask,
        codePlain: item.code_plain
      })}
    >
      废弃
    </Button>
  </div>
</TableCell>
```

同时更新 `applyCodeAction` 函数以支持 DELETE 方法的删除操作：

```javascript
async function applyCodeAction(codeId, actionPath, actionLabel, method = "POST") {
  // ...
  try {
    const resp = await apiCall(`/api/admin/redeem-codes/${codeId}/${actionPath}`, { 
      method: method,  // 支持 DELETE 方法
      // ... 其余保持不变
    });
    // ...
  } catch (error) {
    // ...
  }
}
```

在调用删除时传入 `method: "DELETE"`。
</action>
<acceptance_criteria>
- 操作列只有"删除"和"废弃"两个按钮
- 删除按钮使用 `variant="destructive"`
- 点击删除按钮触发确认对话框
</acceptance_criteria>
<done>操作按钮精简为删除 + 废弃</done>
</task>

<task type="auto">
<name>Task 3: 增加复制按钮</name>
<files>`frontend/src/features/admin-redeem/AdminRedeemCodesTab.jsx`</files>
<read_first>`frontend/src/features/admin-redeem/AdminRedeemCodesTab.jsx` (查看 import 语句和操作列位置)</read_first>
<action>在操作列按钮组最右侧添加复制按钮 per D-09, D-11：

```javascript
import { Copy } from "lucide-react";  // 如果尚未导入

<TableCell>
  <div className="flex items-center gap-2">
    <Button 
      size="sm" 
      variant="destructive"
      onClick={() => setActionDialog({ 
        codeId: item.id, 
        actionPath: "delete",
        actionLabel: "删除", 
        codeMask: item.code_mask,
        codePlain: item.code_plain
      })}
    >
      删除
    </Button>
    <Button 
      size="sm" 
      onClick={() => setActionDialog({ 
        codeId: item.id, 
        actionPath: "abandon", 
        actionLabel: "废弃", 
        codeMask: item.code_mask,
        codePlain: item.code_plain
      })}
    >
      废弃
    </Button>
    {/* 新增 per D-09: 复制按钮 - 位于最右侧 */}
    <Button 
      size="sm" 
      variant="outline"
      onClick={async () => {
        try {
          await navigator.clipboard.writeText(item.code_plain || item.code_mask);
          toast.success("兑换码已复制");  // per D-11
        } catch (err) {
          toast.error("复制失败");
        }
      }}
    >
      <Copy className="size-4" />
    </Button>
  </div>
</TableCell>
```

确保在文件顶部导入了 `Copy` 图标：
```javascript
import { Download, RefreshCcw, ShieldBan, Ticket, Copy } from "lucide-react";
```
</action>
<acceptance_criteria>
- 每行操作列最右侧有复制按钮（Copy 图标）
- 点击复制按钮后显示 toast "兑换码已复制"
- 复制内容为 `code_plain` 明文字段
</acceptance_criteria>
<done>复制按钮已添加，toast 提示"兑换码已复制"</done>
</task>

<task type="checkpoint:human-verify">
<name>Task 4: 人工验证兑换码前端功能</name>
<what-built>兑换码列表完整改造：状态中文化、删除+废弃按钮、复制按钮</what-built>
<how-to-verify>
1. 访问管理后台 → 兑换码列表
2. 验证表格列：应该只有状态列（中文）、无"有效状态"列
3. 验证状态显示：正常/停用/已兑换/失效/废弃
4. 验证操作列：只有"删除"和"废弃"两个按钮 + 复制图标按钮
5. 点击复制按钮，验证 toast 提示"兑换码已复制"
6. 点击删除按钮，验证确认对话框
7. 点击废弃按钮（针对已兑换码），验证扣款逻辑执行
</how-to-verify>
<resume-signal>输入 "approved" 或描述发现的问题</resume-signal>
</task>

### Verification

1. 表格状态列显示中文
2. 表格无"有效状态"列
3. 操作列有删除、废弃、复制三个按钮
4. 复制按钮功能正常，toast 提示正确

### Success Criteria

- [ ] 兑换码状态显示为中文（正常/停用/已兑换/失效/废弃）
- [ ] "有效状态"列已删除
- [ ] 操作列有"删除"、"废弃"、"复制"三个按钮
- [ ] 复制按钮位于操作列最右侧
- [ ] 点击复制按钮 toast 显示"兑换码已复制"
- [ ] 删除操作执行硬删除
- [ ] 废弃操作扣除已兑换用户钱包余额

---

## Output

完成所有计划后，创建以下文件：

`.planning/workstreams/milestone/phases/21.1-admin-user-ops-redemption-code-fix/21.1-01-SUMMARY.md`
`.planning/workstreams/milestone/phases/21.1-admin-user-ops-redemption-code-fix/21.1-02-SUMMARY.md`
`.planning/workstreams/milestone/phases/21.1-admin-user-ops-redemption-code-fix/21.1-03-SUMMARY.md`

---
