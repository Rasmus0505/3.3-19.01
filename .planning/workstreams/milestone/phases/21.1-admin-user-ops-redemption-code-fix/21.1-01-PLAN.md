---
phase: 21.1-admin-user-ops-redemption-code-fix
plan: "21.1-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/repositories/admin_console.py
  - app/schemas/admin_console.py
  - app/api/routers/admin/console.py
  - frontend/src/features/admin-users/AdminUsersTab.jsx
autonomous: true
requirements:
  - ADMIN-USER-01
  - ADMIN-USER-02
must_haves:
  truths:
    - 用户列表每行显示用户名
    - 用户列表每行显示注册时间（北京格式化）
  artifacts:
    - path: app/schemas/admin_console.py
      provides: AdminUserActivityItem schema 包含 username 和 created_at 字段
    - path: app/repositories/admin_console.py
      provides: list_admin_user_activity() 查询包含 User.username 和 User.created_at
    - path: frontend/src/features/admin-users/AdminUsersTab.jsx
      provides: 表格显示用户名和注册时间两列
---

<objective>

在用户活跃列表的后端和前端增加"用户名"和"注册时间"两列（D-01, D-02）。

**用户决策：**
- D-01: 用户列表增加"用户名"列（`User.username` 字段）
- D-02: 用户列表增加"注册时间"列（`User.created_at`，北京时间格式化）

</objective>

<context>

@app/models/user.py — User 模型定义
@app/schemas/admin_console.py — AdminUserActivityItem schema
@app/repositories/admin_console.py — list_admin_user_activity() 函数
@app/api/routers/admin/console.py — _to_user_activity_item() 函数
@frontend/src/features/admin-users/AdminUsersTab.jsx — 用户列表前端组件

</context>

<tasks>

<task type="auto">
<name>Task 1: 后端 Schema 增加 username 和 created_at 字段</name>
<files>app/schemas/admin_console.py</files>
<read_first>app/schemas/admin_console.py (查看 AdminUserActivityItem 定义)</read_first>
<action>

在 `AdminUserActivityItem` 类中增加两个字段：

```python
class AdminUserActivityItem(BaseModel):
    id: int
    email: str
    username: str = ""  # 新增字段 per D-01
    created_at: datetime  # 新增字段 per D-02
    # ... 其余字段保持不变 ...
```

注意：`created_at` 在原始 Schema 中已存在，只需确保字段顺序和类型正确。

</action>
<acceptance_criteria>
- `AdminUserActivityItem` 类包含 `username: str = ""` 字段
- `AdminUserActivityItem` 类包含 `created_at: datetime` 字段
</acceptance_criteria>
<done>AdminUserActivityItem schema 包含 username 和 created_at 字段</done>
</task>

<task type="auto">
<name>Task 2: Repository 查询增加 username 字段</name>
<files>app/repositories/admin_console.py</files>
<read_first>app/repositories/admin_console.py (查看 list_admin_user_activity 函数第 654-666 行的 select 语句)</read_first>
<action>

在 `list_admin_user_activity()` 函数的 base select 语句中增加 `User.username` 字段：

在第 656-665 行的 select 语句中，在 `.email` 后增加 `.username`：

```python
base = (
    select(
        User.id,
        User.email,
        User.username,  # 新增 per D-01
        User.created_at,  # 已有但需确保在返回字段中
        User.last_login_at,
        func.coalesce(WalletAccount.balance_amount_cents, 0).label("balance_points"),
        # ... 其余字段 ...
    )
)
```

确保 `User.username` 被 select 出来供后续使用。

</action>
<acceptance_criteria>
- `list_admin_user_activity()` 函数的 select 语句包含 `User.username`
- rows 变量返回的每行包含 username 值
</acceptance_criteria>
<done>Repository 查询返回 username 字段</done>
</task>

<task type="auto">
<name>Task 3: API 路由序列化增加 username</name>
<files>app/api/routers/admin/console.py</files>
<read_first>app/api/routers/admin/console.py (查看 _to_user_activity_item 函数第 158-170 行)</read_first>
<action>

在 `_to_user_activity_item()` 函数中增加 `username` 字段映射：

```python
def _to_user_activity_item(row) -> AdminUserActivityItem:
    return AdminUserActivityItem(
        id=int(row.id),
        email=str(row.email or ""),
        username=str(row.username or ""),  # 新增 per D-01
        created_at=to_shanghai_aware(row.created_at),  # 确保格式化 per D-02
        last_login_at=to_shanghai_aware(row.last_login_at) if row.last_login_at else None,
        balance_points=int(row.balance_points or 0),
        login_days=int(row.login_days or 0),
        login_events=int(row.login_events or 0),
        lessons_created=int(row.lessons_created or 0),
        consumed_points=int(row.consumed_points or 0),
        redeemed_points=int(row.redeemed_points or 0),
    )
```

</action>
<acceptance_criteria>
- `_to_user_activity_item()` 返回的对象包含 `username` 字段
- `created_at` 使用 `to_shanghai_aware()` 格式化
</acceptance_criteria>
<done>_to_user_activity_item 序列化包含 username 字段</done>
</task>

<task type="auto">
<name>Task 4: 前端表格增加用户名和注册时间列</name>
<files>frontend/src/features/admin-users/AdminUsersTab.jsx</files>
<read_first>frontend/src/features/admin-users/AdminUsersTab.jsx (查看 columns 定义约第 413-508 行)</read_first>
<action>

在 `columns` 数组中，在"邮箱"列后增加"用户名"列和"注册时间"列：

```javascript
const columns = useMemo(
  () => [
    {
      key: "email",
      header: (
        <button type="button" className="inline-flex items-center gap-1" onClick={() => toggleSort("email")}>
          邮箱
          <ArrowUpDown className="size-3.5" />
        </button>
      ),
      mobileLabel: "邮箱",
      render: (item) => item.email,
    },
    // 新增 per D-01: 用户名列
    {
      key: "username",
      header: "用户名",
      mobileLabel: "用户名",
      render: (item) => item.username || "-",
    },
    // 新增 per D-02: 注册时间列
    {
      key: "created_at",
      header: (
        <button type="button" className="inline-flex items-center gap-1" onClick={() => toggleSort("created_at")}>
          注册时间
          <ArrowUpDown className="size-3.5" />
        </button>
      ),
      mobileLabel: "注册时间",
      render: (item) => formatDateTimeBeijing(item.created_at),
    },
    // ... 其余列保持不变 ...
```

注意：保留现有的"邮箱"列，在其后依次增加"用户名"和"注册时间"两列。

</action>
<acceptance_criteria>
- 表格第一页加载后，浏览器控制台无 JS 错误
- 每行数据显示 `item.username` 的值
- 每行数据显示 `formatDateTimeBeijing(item.created_at)` 格式化的注册时间
</acceptance_criteria>
<done>用户列表显示用户名和注册时间两列</done>
</task>

</tasks>

<verification>

1. 启动后端服务，访问 `/api/admin/user-activity?page=1&page_size=10`
2. 验证返回 JSON 中每个 item 包含 `username` 和 `created_at` 字段
3. 访问管理后台用户列表页面，验证表格显示用户名和注册时间列

</verification>

<success_criteria>

- [ ] 后端 API `/api/admin/user-activity` 返回的 items 包含 `username` 字段
- [ ] 后端 API `/api/admin/user-activity` 返回的 items 包含 `created_at` 字段（北京时间格式）
- [ ] 前端表格显示"用户名"列（位于邮箱列后）
- [ ] 前端表格显示"注册时间"列（位于用户名列后）

</success_criteria>

<output>

After completion, create `.planning/workstreams/milestone/phases/21.1-admin-user-ops-redemption-code-fix/21.1-01-SUMMARY.md`

</output>
