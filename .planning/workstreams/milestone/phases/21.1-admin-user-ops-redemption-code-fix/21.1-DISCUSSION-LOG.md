# Phase 21.1: 后台管理用户运营优化 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-03
**Phase:** 21.1-admin-user-ops-redemption-code-fix
**Areas discussed:** 用户名显示范围, 删除行为, 废弃行为, 复制按钮, 状态栏合并

---

## 用户名显示范围

| Option | Description | Selected |
|--------|-------------|----------|
| A | 仅在邮箱列后增加"用户名"列（最小改动） | |
| B | 增加用户名列，同时增加"注册时间"列（运营更方便） | ✓ |

**User's choice:** B
**Notes:** 用户列表需要同时增加"用户名"和"注册时间"两列，方便运营人员识别用户

---

## 删除行为（软/硬删除）

| Option | Description | Selected |
|--------|-------------|----------|
| A | 软删除——数据库保留记录，状态改为 deleted，但保留审计痕迹 | |
| B | 硬删除——从数据库彻底移除，兑换码立即失效，无法恢复 | ✓ |

**User's choice:** B
**Notes:** 兑换码删除为硬删除，立即失效，不可恢复

---

## 废弃行为的具体逻辑

| Option | Description | Selected |
|--------|-------------|----------|
| A | 后端废弃接口自动遍历兑换记录，将对应金额从用户钱包扣除（生成负向流水） | ✓ |
| B | 废弃只改变状态，扣款由运营人员手动调账 | |

**User's choice:** A
**Notes:** 后端废弃接口自动扣除用户钱包余额，生成负向流水。废弃仅针对已兑换兑换码，未兑换兑换码废弃后直接删除，不产生扣款。需事务保护

---

## 复制按钮位置和内容

| Option | Description | Selected |
|--------|-------------|----------|
| A | 每行操作列中增加"复制"按钮，复制完整明文兑换码（不脱敏） | ✓ |
| B | 单独一列放置复制图标按钮（更紧凑） | |

**User's choice:** A
**Notes:** 复制按钮位于操作列最右侧，复制明文兑换码，复制成功后 toast 提示"兑换码已复制"

---

## 状态栏合并规则

| Option | Description | Selected |
|--------|-------------|----------|
| 合并方案 | unredeemed+enabled→正常，unredeemed+disabled→停用，redeemed→已兑换，expired→失效，abandoned→废弃 | ✓ |
| 保留原样 | 不合并 | |

**User's choice:** 合并方案认可
**Notes:** 认可合并规则，只保留一个中文状态列，无冗余维度

---

## Deferred Ideas

None — discussion stayed within phase scope.
