---
phase: 13-desktop-release-pipeline-and-signed-installer
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - desktop-client/package.json
  - desktop-client/scripts/package-win.mjs
  - desktop-client/scripts/write-runtime-defaults.mjs
  - desktop-client/scripts/release-win.mjs
  - tests/contracts/test_desktop_installer_contract.py
autonomous: true
requirements:
  - SECU-01
must_haves:
  truths:
    - "正式桌面发布必须有一个重复可执行的命令，而不是手工拼装"
    - "正式发布必须区分 stable 与 preview，并把 channel 写入产物/元数据"
    - "正式 stable 发布必须把签名步骤纳入流程，而不是事后补签"
  artifacts:
    - path: desktop-client/scripts/release-win.mjs
      provides: 正式 Windows release orchestrator
      contains: "stable"
    - path: desktop-client/scripts/write-runtime-defaults.mjs
      provides: channel-aware packaged runtime defaults
      contains: "metadataUrl"
---

<objective>
把 Bottle 桌面端从“可以本地打包”升级为“可以按 channel 产出正式 Windows 发布物和 release record 的重复流程”，并把签名作为 stable 发布的内建步骤。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/workstreams/milestone/phases/13-/13-CONTEXT.md
@.planning/workstreams/milestone/phases/13-/13-RESEARCH.md
@desktop-client/package.json
@desktop-client/scripts/package-win.mjs
@desktop-client/scripts/write-runtime-defaults.mjs
@tests/contracts/test_desktop_installer_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增正式 release 脚本并清理 package-win 中的 preview 假定</name>
  <files>
    - desktop-client/scripts/package-win.mjs
    - desktop-client/scripts/write-runtime-defaults.mjs
    - desktop-client/scripts/release-win.mjs
    - desktop-client/package.json
  </files>
  <read_first>
    - desktop-client/scripts/package-win.mjs
    - desktop-client/scripts/write-runtime-defaults.mjs
    - desktop-client/package.json
    - .planning/workstreams/milestone/phases/13-/13-CONTEXT.md
    - .planning/workstreams/milestone/phases/13-/13-RESEARCH.md
  </read_first>
  <action>
    为 Windows 正式发布建立独立脚本 `desktop-client/scripts/release-win.mjs`，并把当前 `package-win.mjs` 中写死 preview URL 的行为收口。

    具体目标状态：
    - `package-win.mjs` 只负责基础构建/打包，不再把 preview URL 当成正式默认值写死。
    - 新增 `release-win.mjs` 作为正式发布 orchestrator，要求显式接收：
      - `channel` (`stable` / `preview`)
      - `version`
      - `entryUrl`
      - `metadataBaseUrl` 或等价 release base
      - `releaseNotes`
    - `write-runtime-defaults.mjs` 写入 packaged defaults 时包含明确 channel-aware metadata URL 和 entry URL。
    - `desktop-client/package.json` 新增正式 release script，例如 `release:win` 或等价命令，供 operator 重复执行。

    生成的 release record 至少要包含：
    - `channel`
    - `version`
    - `releaseName`
    - `publishedAt`
    - `entryUrl`
    - `notes`
    - `artifacts`

    不要引入真正程序内 auto-update 执行逻辑；保持 Phase 13 边界。
  </action>
  <acceptance_criteria>
    - `desktop-client/scripts/release-win.mjs` 文件存在
    - `desktop-client/scripts/release-win.mjs` 包含 `stable`
    - `desktop-client/scripts/release-win.mjs` 包含 `preview`
    - `desktop-client/scripts/package-win.mjs` 不再包含写死的 `https://351636.preview.aliyun-zeabur.cn`
    - `desktop-client/package.json` 包含 `release:win` 或等价正式 release script
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/contracts/test_desktop_installer_contract.py -q</automated>
  </verify>
  <done>桌面端有了明确的正式 release 脚本和 channel-aware runtime defaults，不再依赖写死 preview 环境。</done>
</task>

<task type="auto">
  <name>Task 2: 把 stable 签名要求纳入 release contract</name>
  <files>
    - desktop-client/scripts/release-win.mjs
    - tests/contracts/test_desktop_installer_contract.py
  </files>
  <read_first>
    - desktop-client/scripts/release-win.mjs
    - tests/contracts/test_desktop_installer_contract.py
    - .planning/workstreams/milestone/phases/13-/13-RESEARCH.md
  </read_first>
  <action>
    在 `release-win.mjs` 中把 stable 签名要求纳入正式发布 contract。

    具体实现要求：
    - preview 允许无签名本地验证输出。
    - stable 发布必须检查显式签名输入，例如证书路径/密钥密码或脚本自定义的稳定命名环境变量。
    - 当 stable 缺少签名输入时，脚本要直接失败并输出明确错误，而不是静默生成 unsigned official release。
    - release record 中写出 `signatureRequired: true` 和签名状态字段。

    同时扩展 `tests/contracts/test_desktop_installer_contract.py`，新增断言：
    - `release-win.mjs` 对 stable / preview 行为不同
    - stable 需要签名输入
    - release record 含 `signatureRequired`
  </action>
  <acceptance_criteria>
    - `desktop-client/scripts/release-win.mjs` 包含 `signatureRequired`
    - `desktop-client/scripts/release-win.mjs` 包含 `stable`
    - `desktop-client/scripts/release-win.mjs` 包含 `preview`
    - `tests/contracts/test_desktop_installer_contract.py` 包含 `signatureRequired`
    - `python -m pytest tests/contracts/test_desktop_installer_contract.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/contracts/test_desktop_installer_contract.py -q</automated>
  </verify>
  <done>stable 发布不再可能以“看起来正式、实际上未进入签名流程”的方式产出。</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `python -m pytest tests/contracts/test_desktop_installer_contract.py -q` exits 0
2. `desktop-client/scripts/release-win.mjs` exists and distinguishes `stable` vs `preview`
3. Stable release path fails closed when signing inputs are absent
</verification>

<success_criteria>
- SECU-01 有可重复执行的脚本化发布路径
- 正式 stable 与 preview 分离
- stable 将签名纳入正式 contract
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/13-/13-02-SUMMARY.md`
</output>
