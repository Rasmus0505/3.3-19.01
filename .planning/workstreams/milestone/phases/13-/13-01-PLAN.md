---
phase: 13-desktop-release-pipeline-and-signed-installer
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/main.py
  - tests/contracts/test_desktop_release_surface_contract.py
  - frontend/src/features/upload/UploadPanel.jsx
autonomous: true
requirements:
  - DESK-01
must_haves:
  truths:
    - "官网必须有一个统一的正式桌面下载入口，而不是散落的下载链接"
    - "网站下载页、机器可读 release metadata 与桌面客户端必须共享同一版本事实源"
    - "默认用户路径只能落到 stable，不得把 preview 暴露成普通用户默认入口"
  artifacts:
    - path: app/main.py
      provides: 官网下载页与 release metadata 路由
      contains: "/download/desktop"
    - path: tests/contracts/test_desktop_release_surface_contract.py
      provides: 官网 release surface 契约
      contains: "stable"
---

<objective>
把 Bottle 现有网站中的桌面下载入口从占位 fallback 升级为正式 release surface，并让网站页面、release metadata、桌面客户端诊断读取同一版本真相。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/workstreams/milestone/phases/13-/13-CONTEXT.md
@.planning/workstreams/milestone/phases/13-/13-RESEARCH.md
@app/main.py
@frontend/src/features/upload/UploadPanel.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 收口官网 release metadata 为结构化 stable/preview 版本真相</name>
  <files>
    - app/main.py
    - tests/contracts/test_desktop_release_surface_contract.py
  </files>
  <read_first>
    - app/main.py
    - .planning/workstreams/milestone/phases/13-/13-CONTEXT.md
    - .planning/workstreams/milestone/phases/13-/13-RESEARCH.md
    - tests/contracts/test_desktop_installer_contract.py
  </read_first>
  <action>
    在 `app/main.py` 中把当前基于零散环境变量拼装的桌面 release payload 收口为结构化 release surface。

    具体目标状态：
    - 保留 `GET /desktop/client/latest.json` 作为兼容旧客户端的 stable metadata 路由。
    - 新增 channel-aware metadata 路由：
      - `GET /desktop/client/channels/stable.json`
      - `GET /desktop/client/channels/preview.json`
    - payload 至少包含这些具体字段：
      - `channel`
      - `version`
      - `releaseName`
      - `publishedAt`
      - `entryUrl`
      - `notes`
      - `signatureRequired`
      - `artifacts`
    - 当 `stable` 没有配置正式发布记录时，返回明确的“未配置正式桌面发布信息”说明。
    - `latest.json` 的内容必须严格映射 stable channel，而不是 preview。

    同时新增 `tests/contracts/test_desktop_release_surface_contract.py`，断言：
    - `app/main.py` 包含 `/desktop/client/channels/stable.json`
    - `app/main.py` 包含 `/desktop/client/channels/preview.json`
    - `app/main.py` 中 legacy `latest.json` 映射 stable
    - payload 中存在 `channel`、`entryUrl`、`releaseName`
  </action>
  <acceptance_criteria>
    - `app/main.py` 包含 `/desktop/client/channels/stable.json`
    - `app/main.py` 包含 `/desktop/client/channels/preview.json`
    - `app/main.py` 包含 `/desktop/client/latest.json`
    - `tests/contracts/test_desktop_release_surface_contract.py` 包含 `channel`
    - `tests/contracts/test_desktop_release_surface_contract.py` 包含 `stable`
    - `python -m pytest tests/contracts/test_desktop_release_surface_contract.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/contracts/test_desktop_release_surface_contract.py -q</automated>
  </verify>
  <done>官网和客户端都可以读取同一套 channel-aware release metadata，且 stable 成为唯一默认版本真相。</done>
</task>

<task type="auto">
  <name>Task 2: 把 `/download/desktop` 升级为正式官网下载页并统一网页 CTA 目标</name>
  <files>
    - app/main.py
    - frontend/src/features/upload/UploadPanel.jsx
    - tests/contracts/test_desktop_release_surface_contract.py
  </files>
  <read_first>
    - app/main.py
    - frontend/src/features/upload/UploadPanel.jsx
    - .planning/workstreams/milestone/phases/13-/13-CONTEXT.md
    - .planning/workstreams/milestone/phases/07.1-memo/07.1-CONTEXT.md
  </read_first>
  <action>
    把 `GET /download/desktop` 从占位 redirect/fallback 页升级为正式官网下载页。

    具体可见结果：
    - 页面标题明确是 Bottle 官方桌面下载页。
    - 页面展示 stable 版本号、发布时间、更新说明摘要、主下载按钮。
    - 页面允许展示 preview 的内部测试提示，但 preview 不成为默认主按钮。
    - 当 stable 未配置时，页面用明确文案说明“尚未配置正式桌面发布”，而不是返回泛化 HTML。

    同时收口网页端桌面 CTA 目标：
    - `frontend/src/features/upload/UploadPanel.jsx` 中任何桌面下载/获取桌面端 CTA 最终都使用统一的官网下载页 URL，而不是散落地址。
    - 不把 `download desktop` 新增为学习 shell 的默认高频 panel；仅保持统一 CTA 目标。

    在 `tests/contracts/test_desktop_release_surface_contract.py` 中新增断言：
    - `app/main.py` 包含 `Bottle Desktop` 或等价正式下载页标题
    - `frontend/src/features/upload/UploadPanel.jsx` 仍包含桌面下载 CTA
    - CTA 使用统一 `VITE_DESKTOP_CLIENT_ENTRY_URL` / 官方下载页逻辑
  </action>
  <acceptance_criteria>
    - `app/main.py` 包含 `/download/desktop`
    - `app/main.py` 包含 `Bottle Desktop`
    - `frontend/src/features/upload/UploadPanel.jsx` 包含 `下载桌面端` 或 `获取桌面端`
    - `tests/contracts/test_desktop_release_surface_contract.py` 包含 `/download/desktop`
    - `python -m pytest tests/contracts/test_desktop_release_surface_contract.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/contracts/test_desktop_release_surface_contract.py -q</automated>
  </verify>
  <done>官网有了正式桌面下载页，网页端所有桌面引导都能收敛到同一官方入口。</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `python -m pytest tests/contracts/test_desktop_release_surface_contract.py -q` exits 0
2. `app/main.py` exposes stable/preview metadata + official download page
3. `frontend/src/features/upload/UploadPanel.jsx` still routes desktop CTA to one official page
</verification>

<success_criteria>
- DESK-01 在官网入口层面有正式实现
- stable 成为默认用户下载/读取路径
- 网站下载页与 metadata 使用同一版本事实源
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/13-/13-01-SUMMARY.md`
</output>
