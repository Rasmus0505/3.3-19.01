---
phase: 11-conversion-rollout-and-regression-closeout
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - app/static/index.html
  - app/static/assets/*
  - .planning/workstreams/milestone/phases/11-conversion-rollout-and-regression-closeout/11-VERIFICATION.md
autonomous: true
requirements:
  - GROW-01
  - GROW-02
must_haves:
  truths:
    - "网页端前端改动完成标准必须包含 `app/static` 同步和验证"
    - "Phase 11 的最小回归范围固定为 8 条，不需要额外扩展成独立经营建议文档"
    - "旧深链不应因为本阶段改动失效"
  artifacts:
    - path: frontend/package.json
      provides: Canonical `build:app-static` script
      contains: "build:app-static"
    - path: app/static/index.html
      provides: Synced web static entry
      contains: "/static/assets/"
  key_links:
    - "Phase 11 context explicitly treats static sync and regression closure as the final completion gate"
    - "This plan must verify the exact user-facing copy and routing behavior after the built assets are regenerated"
---

<objective>
Sync the finalized Phase 11 frontend into `app/static` and close the phase with the exact regression checklist the context locked.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/phases/11-conversion-rollout-and-regression-closeout/11-CONTEXT.md
@frontend/package.json
@app/static/index.html
@tests/contracts/test_phase11_surface_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 构建并同步 app/static</name>
  <files>
    - app/static/index.html
    - app/static/assets/*
  </files>
  <read_first>
    - frontend/package.json
    - .planning/PROJECT.md
    - .planning/workstreams/milestone/phases/11-conversion-rollout-and-regression-closeout/11-CONTEXT.md
  </read_first>
  <action>
    Run the canonical web delivery command `npm --prefix frontend run build:app-static` so the finalized Phase 11 upload surface is propagated from `frontend/src` into `app/static`.

    Do not substitute a plain Vite build. The exact command must remain `build:app-static` so `app/static/index.html` and the generated `app/static/assets/*` files stay in sync with the deploy contract.
  </action>
  <verify>
    <automated>npm --prefix frontend run build:app-static</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` contains `build:app-static`
    - `npm --prefix frontend run build:app-static` exits 0
    - `app/static/index.html` contains `/static/assets/`
  </acceptance_criteria>
  <done>网页静态产物已经按项目交付契约同步到 app/static。</done>
</task>

<task type="auto">
  <name>Task 2: 按 Phase 11 锁定的 8 条范围完成回归记录</name>
  <files>
    - tests/contracts/test_phase11_surface_contract.py
    - .planning/workstreams/milestone/phases/11-conversion-rollout-and-regression-closeout/11-VERIFICATION.md
  </files>
  <read_first>
    - tests/contracts/test_phase11_surface_contract.py
    - .planning/workstreams/milestone/phases/11-conversion-rollout-and-regression-closeout/11-CONTEXT.md
    - app/static/index.html
  </read_first>
  <action>
    Verify and record the exact minimum regression scope locked in the Phase 11 context:
    - upload page model-card copy/layout is correct
    - insufficient balance primary CTA is `充值后生成`
    - Bottle 1.0 web-only case primary CTA is `下载桌面端`
    - large/long/complex-media prompt and secondary CTA use the locked wording
    - link import still guides to desktop only
    - user-facing surface still only shows `Bottle 1.0 / Bottle 2.0`
    - `app/static` is synced
    - old deep links remain intact because this phase did not rewrite routing

    Use `tests/contracts/test_phase11_surface_contract.py` plus the static-build result as the automated guard, then write a concise `11-VERIFICATION.md` documenting the checklist outcome and any residual risk.
  </action>
  <verify>
    <automated>python -m pytest tests/contracts/test_phase11_surface_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/contracts/test_phase11_surface_contract.py` contains `充值后生成`
    - `tests/contracts/test_phase11_surface_contract.py` contains `下载桌面端`
    - `tests/contracts/test_phase11_surface_contract.py` contains `继续生成素材`
    - `python -m pytest tests/contracts/test_phase11_surface_contract.py -q` exits 0
    - `.planning/workstreams/milestone/phases/11-conversion-rollout-and-regression-closeout/11-VERIFICATION.md` exists
    - `.planning/workstreams/milestone/phases/11-conversion-rollout-and-regression-closeout/11-VERIFICATION.md` contains `status: passed`
  </acceptance_criteria>
  <done>Phase 11 最小回归范围已经验证并记录完成。</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm --prefix frontend run build:app-static` exits 0
2. `python -m pytest tests/contracts/test_phase11_surface_contract.py -q` exits 0
3. `11-VERIFICATION.md` records the fixed regression scope as passed
</verification>

<success_criteria>
- Phase 11 is deployable under the project’s static-sync contract
- The exact context-locked regression scope is recorded, not inferred later
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/11-conversion-rollout-and-regression-closeout/11-03-SUMMARY.md`
</output>
