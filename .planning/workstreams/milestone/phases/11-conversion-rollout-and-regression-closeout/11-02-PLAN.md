---
phase: 11-conversion-rollout-and-regression-closeout
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/input.jsx
  - frontend/src/features/admin-rates/AdminRatesTab.jsx
  - frontend/src/features/admin-redeem/AdminRedeemBatchesTab.jsx
  - frontend/src/features/admin-users/AdminUsersTab.jsx
  - tests/contracts/test_phase11_surface_contract.py
autonomous: true
requirements:
  - GROW-01
must_haves:
  truths:
    - "默认值为 `0` 的数字输入框必须能先清空再输入新值，不能在用户删空时立刻跳回 0"
    - "修复不能破坏现有数值校验和提交时的金额/数量规范化"
    - "优先覆盖价格、充值和其他会影响转化或关键操作流畅度的数字输入场景"
  artifacts:
    - path: frontend/src/components/ui/input.jsx
      provides: Shared numeric input behavior used across the frontend
      contains: "type === \"number\""
    - path: frontend/src/features/admin-rates/AdminRatesTab.jsx
      provides: Price-edit inputs that currently coerce values during editing
      contains: "type=\"number\""
    - path: frontend/src/features/admin-redeem/AdminRedeemBatchesTab.jsx
      provides: Recharge/batch amount and quantity inputs
      contains: "type=\"number\""
  key_links:
    - "Phase 11 folded the numeric-input todo into scope, but the fix still needs to stay compatible with the existing validation and formatting rules"
    - "This should be solved at the shared input behavior level where possible, rather than one-off hacks per field"
---

<objective>
Fix the frontend’s numeric-editing behavior so zero-valued inputs can be cleared and retyped naturally across the key pricing and recharge flows without breaking existing validation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/milestone/phases/11-conversion-rollout-and-regression-closeout/11-CONTEXT.md
@frontend/src/components/ui/input.jsx
@frontend/src/features/admin-rates/AdminRatesTab.jsx
@frontend/src/features/admin-redeem/AdminRedeemBatchesTab.jsx
@frontend/src/features/admin-users/AdminUsersTab.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在共享输入组件层允许 number 输入临时为空</name>
  <files>
    - frontend/src/components/ui/input.jsx
  </files>
  <read_first>
    - frontend/src/components/ui/input.jsx
    - .planning/workstreams/milestone/phases/11-conversion-rollout-and-regression-closeout/11-CONTEXT.md
  </read_first>
  <action>
    Update the shared `Input` component so `type="number"` fields can temporarily hold an empty editing state while the user is focused in the field, instead of immediately snapping back to `0` because the parent stored a numeric value.

    Keep these concrete behavior rules:
    - while a number input is focused, deleting `0` must leave the field visually empty
    - typing a new value after clearing must show the new digits immediately
    - on blur or external updates, the field must still resync with the controlled value
    - non-number inputs must keep their existing behavior
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/ui/input.jsx` contains `type === "number"`
    - `frontend/src/components/ui/input.jsx` contains `useState`
    - `frontend/src/components/ui/input.jsx` contains `onFocus`
    - `frontend/src/components/ui/input.jsx` contains `onBlur`
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>共享 number 输入组件已经允许先清空再重输。</done>
</task>

<task type="auto">
  <name>Task 2: 收口关键金额和数量输入场景，避免编辑时强制回跳</name>
  <files>
    - frontend/src/features/admin-rates/AdminRatesTab.jsx
    - frontend/src/features/admin-redeem/AdminRedeemBatchesTab.jsx
    - frontend/src/features/admin-users/AdminUsersTab.jsx
    - tests/contracts/test_phase11_surface_contract.py
  </files>
  <read_first>
    - frontend/src/features/admin-rates/AdminRatesTab.jsx
    - frontend/src/features/admin-redeem/AdminRedeemBatchesTab.jsx
    - frontend/src/features/admin-users/AdminUsersTab.jsx
    - frontend/src/components/ui/input.jsx
    - .planning/workstreams/milestone/phases/11-conversion-rollout-and-regression-closeout/11-CONTEXT.md
  </read_first>
  <action>
    Adjust the highest-impact number-editing screens so they cooperate with the shared input behavior and do not force a `Number(event.target.value || 0)` rewrite during active editing when that would destroy the temporary empty state.

    Cover at least these scenarios:
    - admin pricing fields in `AdminRatesTab.jsx`
    - redeem batch amount or quantity fields in `AdminRedeemBatchesTab.jsx`
    - user balance-adjust amount editing in `AdminUsersTab.jsx`

    Preserve the existing submit-time numeric normalization:
    - prices still submit as yuan decimals
    - batch quantity still submits as integers
    - wallet adjust still submits converted cents/points where required

    Add contract assertions in `tests/contracts/test_phase11_surface_contract.py` that guard the removal of the direct `Number(event.target.value || 0)` coercion in the key pricing field path.
  </action>
  <verify>
    <automated>python -m pytest tests/contracts/test_phase11_surface_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/features/admin-rates/AdminRatesTab.jsx` contains no `Number(event.target.value || 0)`
    - `frontend/src/features/admin-redeem/AdminRedeemBatchesTab.jsx` contains `type="number"`
    - `frontend/src/features/admin-users/AdminUsersTab.jsx` contains `type="number"`
    - `tests/contracts/test_phase11_surface_contract.py` contains `test_number_inputs_do_not_force_zero_while_editing_contract`
    - `python -m pytest tests/contracts/test_phase11_surface_contract.py -q` exits 0
  </acceptance_criteria>
  <done>关键价格和数量输入场景已经能顺手清空重输，不再在编辑途中强制跳回 0。</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm --prefix frontend run build` exits 0
2. `python -m pytest tests/contracts/test_phase11_surface_contract.py -q` exits 0
3. Zero-valued number fields in the covered pricing/recharge/admin flows can be cleared and retyped naturally
</verification>

<success_criteria>
- The folded numeric-input todo is actually resolved on shared and high-impact surfaces
- Validation/submit semantics remain intact after the editing fix
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/11-conversion-rollout-and-regression-closeout/11-02-SUMMARY.md`
</output>
