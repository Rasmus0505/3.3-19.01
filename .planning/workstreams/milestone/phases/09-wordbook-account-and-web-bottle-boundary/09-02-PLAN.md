---
phase: 09-wordbook-account-and-web-bottle-boundary
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - frontend/src/features/auth/shared/SharedAuthPanel.tsx
  - frontend/src/features/auth/components/AuthPanel.jsx
  - frontend/src/store/slices/authSlice.ts
  - frontend/src/app/authStorage.ts
  - frontend/src/app/learning-shell/LearningShellSidebar.jsx
  - frontend/src/app/learning-shell/LearningShellPanelContent.jsx
  - frontend/src/features/account/AccountPanel.jsx
  - frontend/src/features/wallet/components/RedeemCodePanel.jsx
autonomous: true
requirements:
  - ACC-01
  - ACC-02
  - ACC-03
  - ACC-04
must_haves:
  truths:
    - "登录/注册继续共用同一张认证卡片，但通过显式页签区分两种模式"
    - "注册页签比登录页签多一个用户名字段；登录页签仍只用邮箱和密码"
    - "个人中心取代独立兑换码充值面板，并成为学习侧边栏最上方入口"
    - "前端认证态会缓存 username，并能通过 current-user payload 保持改名后的状态一致"
  artifacts:
    - path: frontend/src/features/auth/shared/SharedAuthPanel.tsx
      provides: Tabbed login/register surface with username-on-register
      contains: "注册"
    - path: frontend/src/features/account/AccountPanel.jsx
      provides: Canonical personal center panel
      contains: "个人中心"
    - path: frontend/src/app/learning-shell/LearningShellSidebar.jsx
      provides: Sidebar order with account at the top and no standalone redeem panel
      contains: "account"
  key_links:
    - "`LearningShellPanelContent.jsx` already centralizes panel rendering; AccountPanel should be mounted there instead of inventing a separate shell"
    - "`authSlice.ts` must stop treating `currentUser` as `id / email / is_admin` only"
---

<objective>
Refactor the frontend auth surface into tabbed login/register flows, then add a dedicated personal-center panel that absorbs redeem-code recharge and becomes the top learning-shell entry.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/REQUIREMENTS.md
@.planning/workstreams/milestone/phases/09-wordbook-account-and-web-bottle-boundary/09-CONTEXT.md
@.planning/workstreams/milestone/phases/09-wordbook-account-and-web-bottle-boundary/09-RESEARCH.md
@frontend/src/features/auth/shared/SharedAuthPanel.tsx
@frontend/src/features/auth/components/AuthPanel.jsx
@frontend/src/store/slices/authSlice.ts
@frontend/src/app/learning-shell/LearningShellSidebar.jsx
@frontend/src/app/learning-shell/LearningShellPanelContent.jsx
@frontend/src/features/wallet/components/RedeemCodePanel.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert the auth card into explicit login/register tabs with username-aware auth state</name>
  <files>
    - frontend/src/features/auth/shared/SharedAuthPanel.tsx
    - frontend/src/features/auth/components/AuthPanel.jsx
    - frontend/src/store/slices/authSlice.ts
    - frontend/src/app/authStorage.ts
  </files>
  <read_first>
    - frontend/src/features/auth/shared/SharedAuthPanel.tsx
    - frontend/src/features/auth/components/AuthPanel.jsx
    - frontend/src/store/slices/authSlice.ts
    - frontend/src/app/authStorage.ts
    - app/schemas/auth.py
    - .planning/workstreams/milestone/phases/09-wordbook-account-and-web-bottle-boundary/09-CONTEXT.md
  </read_first>
  <action>
    Refactor the shared auth surface from “one card with two action buttons” into an explicit tabbed flow.

    Implement these concrete frontend behaviors:
    - add visible `登录` and `注册` tabs
    - keep login fields exactly `邮箱` + `密码`
    - add a `用户名` field on the register tab only
    - send `username` only on register requests
    - keep all visible copy Chinese-first and Bottle-branded

    Expand auth storage and `authSlice.ts` so `currentUser` includes `username`, and hydrate it from backend auth responses plus `/api/auth/me`.

    Preserve email/password login semantics; do not add username login UI or fallback login attempts by username.
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/features/auth/shared/SharedAuthPanel.tsx` contains `用户名`
    - `frontend/src/features/auth/shared/SharedAuthPanel.tsx` contains tab labels `登录` and `注册`
    - `frontend/src/features/auth/components/AuthPanel.jsx` contains `/api/auth/register`
    - `frontend/src/features/auth/components/AuthPanel.jsx` contains `/api/auth/login`
    - `frontend/src/store/slices/authSlice.ts` contains `username`
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>The auth card now presents explicit login/register tabs and stores username-aware user state.</done>
</task>

<task type="auto">
  <name>Task 2: Add a personal-center panel and absorb redeem-code recharge into it</name>
  <files>
    - frontend/src/features/account/AccountPanel.jsx
    - frontend/src/app/learning-shell/LearningShellSidebar.jsx
    - frontend/src/app/learning-shell/LearningShellPanelContent.jsx
    - frontend/src/features/wallet/components/RedeemCodePanel.jsx
  </files>
  <read_first>
    - frontend/src/app/learning-shell/LearningShellSidebar.jsx
    - frontend/src/app/learning-shell/LearningShellPanelContent.jsx
    - frontend/src/features/wallet/components/RedeemCodePanel.jsx
    - .planning/workstreams/milestone/phases/09-wordbook-account-and-web-bottle-boundary/09-CONTEXT.md
  </read_first>
  <action>
    Create `frontend/src/features/account/AccountPanel.jsx` as the canonical personal-center panel.

    Implement these concrete changes:
    - add a new sidebar panel item with key `account`, title `个人中心`, and place it before `history`
    - remove the standalone `redeem` panel item from `LearningShellSidebar.jsx`
    - remove the standalone `activePanel === "redeem"` branch from `LearningShellPanelContent.jsx`
    - render `RedeemCodePanel` inside `AccountPanel.jsx` as a subsection
    - show current username and a lightweight rename form inside AccountPanel

    Preserve the existing `RedeemCodePanel` implementation as the reusable recharge widget; do not fork a second redeem implementation.
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/features/account/AccountPanel.jsx` exists
    - `frontend/src/features/account/AccountPanel.jsx` contains `个人中心`
    - `frontend/src/app/learning-shell/LearningShellSidebar.jsx` contains `key: "account"`
    - `frontend/src/app/learning-shell/LearningShellSidebar.jsx` contains no `key: "redeem"`
    - `frontend/src/app/learning-shell/LearningShellPanelContent.jsx` contains `AccountPanel`
    - `frontend/src/app/learning-shell/LearningShellPanelContent.jsx` contains no `activePanel === "redeem"`
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>The learning shell now exposes a top-level personal center and no longer uses a standalone redeem panel.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm --prefix frontend run build` exits 0
2. The auth surface exposes explicit login/register tabs with username on register only
3. The learning shell sidebar shows `个人中心` as the top entry and no standalone redeem panel
</verification>

<success_criteria>
- ACC-04 is met through a tabbed, Chinese-first auth surface
- Personal center exists as a first-class panel
- Redeem-code recharge is absorbed into personal center instead of remaining a standalone navigation destination
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/09-wordbook-account-and-web-bottle-boundary/09-02-SUMMARY.md`
</output>
