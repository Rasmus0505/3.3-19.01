---
phase: 09-wordbook-account-and-web-bottle-boundary
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/upload/UploadPanel.jsx
  - frontend/src/shared/lib/asrModels.js
  - app/services/asr_model_registry.py
  - tests/contracts/test_phase09_surface_contract.py
  - frontend/package.json
autonomous: true
requirements:
  - WEB-01
  - WEB-02
  - WEB-03
must_haves:
  truths:
    - "用户可见命名只保留 `Bottle 1.0 / Bottle 2.0`"
    - "旧词 `本机识别 / 云端识别` 不再出现在用户可见主标题、副文案、说明文案或 fallback metadata 中"
    - "Bottle 1.0 仍然只承担网页说明和桌面引导，不变成网页可执行流程"
    - "网页端交付包含 `app/static` 同步验证"
  artifacts:
    - path: frontend/src/features/upload/UploadPanel.jsx
      provides: User-facing Bottle model cards and desktop guidance copy
      contains: "Bottle 1.0"
    - path: frontend/src/shared/lib/asrModels.js
      provides: Frontend fallback model metadata aligned to Bottle naming
      contains: "Bottle 2.0"
    - path: tests/contracts/test_phase09_surface_contract.py
      provides: Contract check for old-name removal
      contains: "本机识别"
  key_links:
    - "Phase 07 already locked the CTA boundary; Phase 09 only removes old wording and keeps the same Bottle 1.0 desktop-only rule"
    - "`frontend/src` changes are not complete until `app/static` is rebuilt and verified"
---

<objective>
Finish the user-visible Bottle boundary cleanup by removing old model names from upload surfaces and shared metadata while preserving the Phase 7 desktop-only contract for Bottle 1.0.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/phases/07-competitive-research-and-product-specs/07-CONTEXT.md
@.planning/workstreams/milestone/phases/07-competitive-research-and-product-specs/07-WEB-CTA-SPEC.md
@.planning/workstreams/milestone/phases/07-competitive-research-and-product-specs/07-PRODUCT-POSITIONING-SPEC.md
@.planning/workstreams/milestone/phases/09-wordbook-account-and-web-bottle-boundary/09-CONTEXT.md
@frontend/src/features/upload/UploadPanel.jsx
@frontend/src/shared/lib/asrModels.js
@app/services/asr_model_registry.py
@frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy model names from user-facing upload and metadata surfaces</name>
  <files>
    - frontend/src/features/upload/UploadPanel.jsx
    - frontend/src/shared/lib/asrModels.js
    - app/services/asr_model_registry.py
    - tests/contracts/test_phase09_surface_contract.py
  </files>
  <read_first>
    - frontend/src/features/upload/UploadPanel.jsx
    - frontend/src/shared/lib/asrModels.js
    - app/services/asr_model_registry.py
    - .planning/workstreams/milestone/phases/07-competitive-research-and-product-specs/07-WEB-CTA-SPEC.md
    - .planning/workstreams/milestone/phases/09-wordbook-account-and-web-bottle-boundary/09-CONTEXT.md
  </read_first>
  <action>
    Align all user-facing naming to the stricter Phase 09 contract.

    Implement these concrete rules:
    - replace UploadPanel user-visible titles and descriptions so they use only `Bottle 1.0` / `Bottle 2.0`
    - remove `本机识别` and `云端识别` from UploadPanel user-facing subtitles, notes, and desktop-guidance copy
    - update `frontend/src/shared/lib/asrModels.js` fallback metadata so `subtitle` and `note` no longer expose the old terms
    - update `app/services/asr_model_registry.py` returned `subtitle` and `note` fields to keep Bottle naming while preserving the desktop/cloud capability semantics

    Add `tests/contracts/test_phase09_surface_contract.py` assertions that:
    - `frontend/src/features/upload/UploadPanel.jsx` contains `Bottle 1.0` and `Bottle 2.0`
    - `frontend/src/features/upload/UploadPanel.jsx` contains no `本机识别`
    - `frontend/src/features/upload/UploadPanel.jsx` contains no `云端识别`
  </action>
  <verify>
    <automated>pytest tests/contracts/test_phase09_surface_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/features/upload/UploadPanel.jsx` contains `Bottle 1.0`
    - `frontend/src/features/upload/UploadPanel.jsx` contains `Bottle 2.0`
    - `frontend/src/features/upload/UploadPanel.jsx` contains no `本机识别`
    - `frontend/src/features/upload/UploadPanel.jsx` contains no `云端识别`
    - `frontend/src/shared/lib/asrModels.js` contains no `Cloud transcription via DashScope API.`
    - `pytest tests/contracts/test_phase09_surface_contract.py -q` exits 0
  </acceptance_criteria>
  <done>Legacy model names are removed from the user-facing upload and metadata surface.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild app/static and verify the Bottle 1.0 desktop-only contract still holds</name>
  <files>
    - frontend/src/features/upload/UploadPanel.jsx
    - frontend/package.json
  </files>
  <read_first>
    - frontend/package.json
    - frontend/src/features/upload/UploadPanel.jsx
    - .planning/PROJECT.md
    - .planning/workstreams/milestone/phases/07-competitive-research-and-product-specs/07-PRODUCT-POSITIONING-SPEC.md
  </read_first>
  <action>
    Complete the web-delivery side of the Bottle naming cleanup.

    Run the existing `build:app-static` path so `frontend/dist` is synced to `app/static`, and verify that the post-cleanup upload surface still obeys the Phase 7 Bottle boundary:
    - Bottle 2.0 remains the default web-executable path
    - Bottle 1.0 still routes users to desktop guidance instead of creating a browser execution path

    Keep the command path exactly `npm --prefix frontend run build:app-static`; do not replace it with a plain Vite build.
  </action>
  <verify>
    <automated>npm --prefix frontend run build:app-static</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` contains `build:app-static`
    - `npm --prefix frontend run build:app-static` exits 0
    - `frontend/src/features/upload/UploadPanel.jsx` contains `下载桌面端`
    - `frontend/src/features/upload/UploadPanel.jsx` contains no `网页端直接开始 Bottle 1.0`
  </acceptance_criteria>
  <done>The Bottle naming cleanup is fully propagated to the deployed web static assets without breaking the desktop-only boundary.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pytest tests/contracts/test_phase09_surface_contract.py -q` exits 0
2. `npm --prefix frontend run build:app-static` exits 0
3. UploadPanel no longer exposes the old model names to users
</verification>

<success_criteria>
- Users only see Bottle 1.0 / Bottle 2.0 naming
- Bottle 1.0 remains a web explanation + desktop CTA path, not a browser execution path
- Web delivery includes `app/static` sync and verification
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/09-wordbook-account-and-web-bottle-boundary/09-04-SUMMARY.md`
</output>
