---
phase: 09-wordbook-account-and-web-bottle-boundary
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/user.py
  - app/schemas/auth.py
  - app/api/routers/auth.py
  - app/api/serializers.py
  - app/repositories/user.py
  - migrations/versions/20260328_0030_usernames_and_profile_api.py
  - tests/contracts/test_auth_contract.py
  - tests/fixtures/auth.py
autonomous: true
requirements:
  - ACC-01
  - ACC-02
  - ACC-03
must_haves:
  truths:
    - "用户注册必须写入唯一用户名，但登录继续只接受邮箱和密码"
    - "后端存在 current-user API，前端可以在登录后和恢复会话时读取完整用户资料"
    - "用户名改名是轻量 profile 更新，不会扩散成用户名登录或新的认证方式"
  artifacts:
    - path: app/models/user.py
      provides: Canonical user identity fields
      contains: "username_normalized"
    - path: app/api/routers/auth.py
      provides: Current-user and profile update APIs
      contains: "@router.get(\"/me\")"
    - path: app/schemas/auth.py
      provides: Auth and profile response schema with username
      contains: "username"
  key_links:
    - "`tests/contracts/test_auth_contract.py` already probes `/api/auth/me`; Phase 09 should turn that expectation into a real contract"
    - "Current auth storage and frontend hydration should consume a single `UserResponse` shape rather than inventing a parallel profile payload"
---

<objective>
Add backend username identity support, current-user retrieval, and rename capability without changing the project’s email-password login contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/REQUIREMENTS.md
@.planning/workstreams/milestone/STATE.md
@.planning/workstreams/milestone/phases/09-wordbook-account-and-web-bottle-boundary/09-CONTEXT.md
@.planning/workstreams/milestone/phases/09-wordbook-account-and-web-bottle-boundary/09-RESEARCH.md
@app/models/user.py
@app/schemas/auth.py
@app/api/routers/auth.py
@app/api/serializers.py
@app/repositories/user.py
@tests/contracts/test_auth_contract.py
@tests/fixtures/auth.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add username storage and normalization to the user model</name>
  <files>
    - app/models/user.py
    - app/repositories/user.py
    - app/schemas/auth.py
    - migrations/versions/20260328_0030_usernames_and_profile_api.py
    - tests/fixtures/auth.py
  </files>
  <read_first>
    - app/models/user.py
    - app/repositories/user.py
    - app/schemas/auth.py
    - migrations/versions/20260322_0027_wordbook_storage.py
    - .planning/workstreams/milestone/phases/09-wordbook-account-and-web-bottle-boundary/09-CONTEXT.md
    - .planning/workstreams/milestone/phases/09-wordbook-account-and-web-bottle-boundary/09-RESEARCH.md
  </read_first>
  <action>
    Extend the canonical user identity model with username support.

    Implement these concrete changes:
    - add `username` and `username_normalized` columns to `app/models/user.py`
    - make `username_normalized` unique and indexed
    - create a migration file `migrations/versions/20260328_0030_usernames_and_profile_api.py` that adds both columns to `users`, backfills existing rows to deterministic placeholder usernames such as `user-{id}`, and creates the unique index
    - add schema fields so register/profile responses can carry `username`
    - add repository helpers for lookup/update by normalized username
    - update `tests/fixtures/auth.py` test users so they populate username data

    Use a single normalization helper for uniqueness comparisons that:
    - applies Unicode NFKC normalization
    - trims outer whitespace
    - collapses repeated inner whitespace to single spaces
    - casefolds the normalized value

    Do not add username to login credentials; keep login email/password-only.
  </action>
  <verify>
    <automated>pytest tests/contracts/test_auth_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/models/user.py` contains `username`
    - `app/models/user.py` contains `username_normalized`
    - `migrations/versions/20260328_0030_usernames_and_profile_api.py` exists
    - `migrations/versions/20260328_0030_usernames_and_profile_api.py` contains `username_normalized`
    - `app/schemas/auth.py` contains `username`
    - `pytest tests/contracts/test_auth_contract.py -q` exits 0
  </acceptance_criteria>
  <done>The backend user model can now store unique usernames with a canonical normalization path.</done>
</task>

<task type="auto">
  <name>Task 2: Expose current-user and rename APIs without changing login semantics</name>
  <files>
    - app/api/routers/auth.py
    - app/api/serializers.py
    - app/schemas/auth.py
    - app/repositories/user.py
    - tests/contracts/test_auth_contract.py
  </files>
  <read_first>
    - app/api/routers/auth.py
    - app/api/serializers.py
    - app/schemas/auth.py
    - app/api/deps/auth.py
    - tests/contracts/test_auth_contract.py
    - .planning/workstreams/milestone/phases/09-wordbook-account-and-web-bottle-boundary/09-CONTEXT.md
  </read_first>
  <action>
    Add the canonical profile APIs under the existing auth router.

    Implement these endpoints and response contracts:
    - `POST /api/auth/register` accepts `email`, `password`, and `username`
    - `POST /api/auth/login` still accepts only `email` and `password`
    - `GET /api/auth/me` returns the full `UserResponse` with `id`, `email`, `username`, `is_admin`, and `created_at`
    - `PATCH /api/auth/profile` accepts `username` and updates only the current user’s username

    Update serializers and schemas so register, login, refresh, and `/me` all share the same expanded `UserResponse`.

    Extend `tests/contracts/test_auth_contract.py` so it verifies:
    - `/api/auth/me` returns 200 for an authenticated client
    - `UserResponse.model_validate(...)` succeeds with `username`
    - login still rejects bad email/password credentials rather than introducing username login
  </action>
  <verify>
    <automated>pytest tests/contracts/test_auth_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/routers/auth.py` contains `@router.get("/me")`
    - `app/api/routers/auth.py` contains `@router.patch("/profile")`
    - `app/api/routers/auth.py` contains `payload.username`
    - `app/api/serializers.py` contains `username=user.username`
    - `tests/contracts/test_auth_contract.py` contains `/api/auth/me`
    - `pytest tests/contracts/test_auth_contract.py -q` exits 0
  </acceptance_criteria>
  <done>Current-user retrieval and username rename are now supported without changing the email-password login contract.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pytest tests/contracts/test_auth_contract.py -q` exits 0
2. `app/api/routers/auth.py` exposes `GET /api/auth/me` and `PATCH /api/auth/profile`
3. `app/models/user.py` stores canonical username fields
</verification>

<success_criteria>
- Username is required at registration and stored uniquely
- Login remains email/password-only
- Backend exposes a canonical current-user payload and rename route for Phase 09 frontend work
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/09-wordbook-account-and-web-bottle-boundary/09-01-SUMMARY.md`
</output>
