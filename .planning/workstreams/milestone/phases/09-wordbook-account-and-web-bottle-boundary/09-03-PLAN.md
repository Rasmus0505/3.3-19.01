---
phase: 09-wordbook-account-and-web-bottle-boundary
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/lesson.py
  - app/schemas/wordbook.py
  - app/api/routers/wordbook.py
  - app/services/wordbook_service.py
  - app/services/wordbook_review_scheduler.py
  - app/repositories/wordbook.py
  - migrations/versions/20260328_0031_wordbook_review_fields.py
  - frontend/src/features/wordbook/WordbookPanel.jsx
  - tests/integration/api/test_wordbook_api.py
  - tests/contracts/test_phase09_surface_contract.py
autonomous: true
requirements:
  - WBK-01
  - WBK-02
  - WBK-03
  - WBK-04
must_haves:
  truths:
    - "现有收词链路继续保留最新语境，但词条同时拥有 review metadata、due queue 和 review actions"
    - "生词本顶部有独立的开始复习入口，不要求用户手动筛整表才能进入复习"
    - "列表主信息显示最新中英语境、下次复习时间、复习次数和记忆率百分比"
    - "复习反馈按钮固定为 `重来 / 很吃力 / 想起来了 / 很轻松`，并驱动动态 next-review 计算"
  artifacts:
    - path: app/services/wordbook_review_scheduler.py
      provides: Encapsulated review scheduling logic
      contains: "REVIEW_GRADES"
    - path: app/schemas/wordbook.py
      provides: Wordbook API payload with review metadata
      contains: "memory_score"
    - path: frontend/src/features/wordbook/WordbookPanel.jsx
      provides: List + due-review entry + review mode UI
      contains: "开始复习"
  key_links:
    - "`WordbookEntry` remains the canonical per-user vocabulary record; review state should extend it instead of duplicating identity into another table"
    - "Current `status` may remain for filtering, but mastered must be driven by `memory_score >= 0.85` rather than manual primary UI"
---

<objective>
Upgrade the existing wordbook from a passive collection list into a review-capable entry point with dynamic due scheduling, four-grade feedback, and memory-score-based mastery.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/milestone/ROADMAP.md
@.planning/workstreams/milestone/REQUIREMENTS.md
@.planning/workstreams/milestone/phases/09-wordbook-account-and-web-bottle-boundary/09-CONTEXT.md
@.planning/workstreams/milestone/phases/09-wordbook-account-and-web-bottle-boundary/09-RESEARCH.md
@app/models/lesson.py
@app/schemas/wordbook.py
@app/api/routers/wordbook.py
@app/services/wordbook_service.py
@app/repositories/wordbook.py
@frontend/src/features/wordbook/WordbookPanel.jsx
@tests/integration/api/test_wordbook_api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add review metadata, due-queue query, and grade-driven scheduler logic</name>
  <files>
    - app/models/lesson.py
    - app/schemas/wordbook.py
    - app/api/routers/wordbook.py
    - app/services/wordbook_service.py
    - app/services/wordbook_review_scheduler.py
    - app/repositories/wordbook.py
    - migrations/versions/20260328_0031_wordbook_review_fields.py
    - tests/integration/api/test_wordbook_api.py
  </files>
  <read_first>
    - app/models/lesson.py
    - app/schemas/wordbook.py
    - app/api/routers/wordbook.py
    - app/services/wordbook_service.py
    - app/repositories/wordbook.py
    - migrations/versions/20260322_0027_wordbook_storage.py
    - .planning/workstreams/milestone/phases/09-wordbook-account-and-web-bottle-boundary/09-CONTEXT.md
    - .planning/workstreams/milestone/phases/09-wordbook-account-and-web-bottle-boundary/09-RESEARCH.md
  </read_first>
  <action>
    Extend the existing wordbook backend to support review state and due queue retrieval.

    Implement these concrete schema fields on `WordbookEntry`:
    - `next_review_at`
    - `last_reviewed_at`
    - `review_count`
    - `wrong_count`
    - `memory_score`

    Create `app/services/wordbook_review_scheduler.py` as the only place that computes review outcomes. It must:
    - define the four review grades `again`, `hard`, `good`, `easy`
    - update `memory_score`, `review_count`, `wrong_count`, and `next_review_at`
    - treat `0.85` as the mastered threshold and map `status` to `mastered` only when `memory_score >= 0.85`

    Add backend API support for:
    - fetching due items via a dedicated review-queue response or equivalent `due_only` query path
    - submitting a review grade per entry

    Extend `tests/integration/api/test_wordbook_api.py` to cover:
    - due queue retrieval
    - grade submission
    - `memory_score` and `next_review_at` in responses
  </action>
  <verify>
    <automated>pytest tests/integration/api/test_wordbook_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/models/lesson.py` contains `next_review_at`
    - `app/models/lesson.py` contains `memory_score`
    - `app/services/wordbook_review_scheduler.py` exists
    - `app/services/wordbook_review_scheduler.py` contains `again`, `hard`, `good`, and `easy`
    - `app/api/routers/wordbook.py` contains `review`
    - `app/schemas/wordbook.py` contains `next_review_at`
    - `app/schemas/wordbook.py` contains `memory_score`
    - `pytest tests/integration/api/test_wordbook_api.py -q` exits 0
  </acceptance_criteria>
  <done>The wordbook backend can now calculate due reviews and update review state through an encapsulated scheduler.</done>
</task>

<task type="auto">
  <name>Task 2: Turn the Wordbook panel into a list-plus-review entry point</name>
  <files>
    - frontend/src/features/wordbook/WordbookPanel.jsx
    - tests/contracts/test_phase09_surface_contract.py
  </files>
  <read_first>
    - frontend/src/features/wordbook/WordbookPanel.jsx
    - .planning/workstreams/milestone/phases/09-wordbook-account-and-web-bottle-boundary/09-CONTEXT.md
    - .planning/workstreams/milestone/phases/09-wordbook-account-and-web-bottle-boundary/09-RESEARCH.md
    - app/schemas/wordbook.py
  </read_first>
  <action>
    Refactor `frontend/src/features/wordbook/WordbookPanel.jsx` so the panel has two product modes inside the same learning-shell surface:
    - default list mode
    - dedicated review mode entered from a top-of-panel `开始复习` entry

    In list mode, make the default primary information exactly:
    - latest English context
    - latest Chinese context
    - next review time
    - review count
    - memory score percentage

    Do not show the old `单词 / 短语` badge as default primary information.

    In review mode, render four buttons with exact labels:
    - `重来`
    - `很吃力`
    - `想起来了`
    - `很轻松`

    Keep `source_count` and `wrong_count` only as secondary metadata. Add a contract test file `tests/contracts/test_phase09_surface_contract.py` that asserts the presence of `开始复习`, the four review buttons, and the absence of `短语` / `单词` label text as default visible metadata in the Wordbook panel source.
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/features/wordbook/WordbookPanel.jsx` contains `开始复习`
    - `frontend/src/features/wordbook/WordbookPanel.jsx` contains `重来`
    - `frontend/src/features/wordbook/WordbookPanel.jsx` contains `很吃力`
    - `frontend/src/features/wordbook/WordbookPanel.jsx` contains `想起来了`
    - `frontend/src/features/wordbook/WordbookPanel.jsx` contains `很轻松`
    - `frontend/src/features/wordbook/WordbookPanel.jsx` contains `next_review_at`
    - `tests/contracts/test_phase09_surface_contract.py` exists
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>The Wordbook panel now works as a due-review entry point instead of a passive collection list.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pytest tests/integration/api/test_wordbook_api.py -q` exits 0
2. `npm --prefix frontend run build` exits 0
3. Wordbook panel exposes `开始复习`, review-mode buttons, and due-review metadata
</verification>

<success_criteria>
- Wordbook still preserves latest context and source lineage
- Users can enter a due-review flow without filtering the full list manually
- Review actions update next review time and mastery through dynamic scheduling rather than a fixed public ladder
</success_criteria>

<output>
After completion, create `.planning/workstreams/milestone/phases/09-wordbook-account-and-web-bottle-boundary/09-03-SUMMARY.md`
</output>
