文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：为上传页生成流程补齐服务端任务控制与恢复体验：在不破坏 `/api/transcribe/file`、现有上传创建/查询/恢复接口主契约的前提下，为课程生成任务增加“暂停并保留进度 / 终止并保留素材”能力；上传页主按钮区在运行中原位切换为双按钮；刷新后自动接管进行中的服务端任务；暂停后支持免上传继续生成，终止后回到可编辑态但保留素材与上次终止信息。
修改的文件清单（精确路径）：
- Docx/AI分工/上传页停止恢复控制_03月19日_10时28分.md
- app/api/routers/lessons.py
- app/schemas/__init__.py
- app/services/lesson_command_service.py
- app/services/lesson_task_manager.py
- app/schemas/lesson.py
- app/static/index.html
- app/static/assets/*
- frontend/src/features/upload/UploadPanel.jsx
- tests/test_regression_api.py
关联衔接：无
风险：
- 课程生成目前跑在线程中，本次暂停只能做“安全暂停”，不能保证任意外部请求中的毫秒级硬中断。
- 若控制元信息直接新增数据库列，会对现有库产生迁移要求；本次优先复用 `artifacts_json` 持久化控制状态，避免现网库因缺列不可用。
- `UploadPanel.jsx` 近期连续多轮修改，本次要避免回退已有上传恢复、本地识别停止和成功态快照逻辑。
验证：
- `python -m py_compile app\api\routers\lessons.py app\services\lesson_command_service.py app\services\lesson_task_manager.py app\schemas\lesson.py app\schemas\__init__.py tests\test_regression_api.py` 通过
- `pytest tests\test_regression_api.py -q -k "pause_and_resume_from_safe_point or terminate_marks_task_as_terminated or lesson_task_resume_reuses_failed_task_artifacts or lesson_task_resume_marks_missing_artifacts_as_non_resumable or lesson_task_resume_restarts_failed_task_when_resume_unavailable or create_lesson_task_and_poll_success"` 通过，6 项通过
- `frontend\npm.cmd run build:app-static` 通过，前端已重新构建并同步到 `app/static`
清理记录：
- 未删除历史任务/衔接文档：本次未获得文件删除确认，按规则保留现有文档。
结束时间：2026-03-19 11:02:01
