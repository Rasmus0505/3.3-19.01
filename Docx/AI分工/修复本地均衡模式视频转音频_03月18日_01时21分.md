文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：修复浏览器本地均衡模式中视频素材未经过稳定音轨提取就直接走 `AudioContext.decodeAudioData`，导致本地模型下载成功但 MP4 素材无法生成课程的问题；改为本地 ASR 仍在浏览器执行，但对视频文件先调用后端 ffmpeg 提取音频，再继续本地识别，不破坏 `/api/lessons/tasks/local-asr` 与 `/api/transcribe/file` 契约。
修改的文件清单（精确路径）：
- Docx/AI分工/修复本地均衡模式视频转音频_03月18日_01时21分.md
- app/api/routers/lessons.py
- frontend/src/features/upload/UploadPanel.jsx
- tests/test_regression_api.py
关联衔接：无
风险：本地均衡模式处理视频时会新增一次上传到后端做音轨提取，因此首轮等待时间上升；但真正 ASR 识别仍在浏览器本地完成，服务器只承担 ffmpeg 转音频压力。
验证：
- 运行 `python -m py_compile app/api/routers/lessons.py tests/test_regression_api.py` 通过
- 运行 `pytest tests/test_regression_api.py -q -k "local_asr or local_asr_asset_route or extract_local_asr_audio_route"`，4 项通过
- 运行 `npm run build`，前端构建通过
清理记录：
- 删除 `Docx/AI分工/修复本地SenseVoice缺少git自愈安装_03月18日_01时21分.md`：创建者为 Codex，状态为“已完成”，且无关联“待衔接”文档。
结束时间：2026-03-18 11:26:05
