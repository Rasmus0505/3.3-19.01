文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
- 在不与其他 AI 进行中任务文件重叠的前提下，为管理台新增 SQL 控台面板，支持管理员执行只允许单条语句的受控 SQL 查询与 DML 写操作。
- 保持现有 API 契约不变（含 `/api/transcribe/file`），新增后台接口用于 SQL 预检与执行，加入双重确认、全量审计、结果限流和完整错误回传。
- 将 SQL 控台接入现有 `/admin/monitoring` 的 operations 分区，并补齐兼容路由 `/admin/sql-console`。
修改的文件清单（精确路径）：
- Docx/AI分工/管理台新增SQL控台_03月11日_17时01分.md
- app/main.py
- app/api/routers/__init__.py
- app/api/routers/admin_sql_console.py
- app/schemas/admin_sql_console.py
- app/services/admin_sql_console.py
- frontend/src/AdminApp.jsx
- frontend/src/features/admin-workspaces/AdminMonitoringWorkspace.jsx
- frontend/src/features/admin-sql-console/AdminSqlConsoleTab.jsx
- tests/test_admin_sql_console_api.py
关联衔接：无
风险：
- 当前存在进行中任务 `管理员素材生成原始日志可视化_03月11日_16时40分.md`，占用 `app/api/routers/admin_console.py`、`app/repositories/admin_console.py`、`app/schemas/admin_console.py`、`tests/test_admin_console_api.py` 等文件；本次实现必须避开这些文件。
- SQL 控台直接面向线上数据库，若护栏实现不完整，可能带来误写库风险；必须限制语句类型、拒绝多语句、强制二次确认并写入审计日志。
验证：
- 已执行 `pytest -q tests/test_admin_sql_console_api.py`，结果 `5 passed`。
- 已执行 `.\node_modules\.bin\vite.cmd build -c vite.admin.config.js --outDir dist-admin-sql-console-check`，构建通过；为避免覆盖仓库中现有 `frontend/dist-admin/` 产物，使用了临时输出目录完成验证。
- 已执行 `pytest -q tests/test_regression_api.py -k "health_endpoint or transcribe_file_endpoint_with_stubbed_service"`，结果 `2 passed`。
清理记录：
- 已删除 `Docx/AI分工/修复翻译日志500缺少case导入_03月11日_16时16分.md`：该文档由我创建，状态已完成，且无关联待衔接文档，满足删除条件。
- 已删除 `frontend/dist-admin-sql-console-check/`：本轮后台构建验证生成的临时目录，未纳入版本管理，可安全删除。
结束时间：2026-03-11 17:27:30
