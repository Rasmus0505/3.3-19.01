文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
- 在不改变 `/api/transcribe/file` 契约前提下，实现 Qwen ASR 实耗结算：预扣后按 `usage.seconds` 最终多退少补。
- 当无法获取 `usage` 时回退到现有时长估算；并允许补扣导致负余额（欠费记录）。
修改的文件清单（精确路径）：
- Docx/AI分工/Qwen实耗结算改造_03月06日_19时18分.md
- app/infra/asr_dashscope.py
- app/services/billing.py
- app/services/billing_service.py
- app/services/lesson_service.py
- app/models/billing.py
- migrations/versions/20260306_0006_allow_negative_wallet_balance.py
- tests/test_regression_api.py
关联衔接：无
风险：
- 结算与余额规则变化会影响钱包流水与管理端对账，需保持管理端筛选与账本解释口径一致。
- 历史环境若未执行迁移，数据库仍可能保留旧约束，需在部署时执行 Alembic 升级。
验证：
- 已执行 `pytest -q tests/test_regression_api.py`：22 passed。
- 已执行 `pytest -q`：32 passed, 1 skipped。
- 回归覆盖 `GET /health`、`POST /api/transcribe/file`（位于回归集内）与结算差额路径（usage 命中/缺失/失败退款）。
清理记录：
- 本次未删除文档文件（未进入下一项任务，且无本人已消解衔接文档可删）。
结束时间：2026-03-06 19:23:40
