文档类型：任务
创建者：Codex
状态：进行中
完整任务目标：在不破坏上传页现有用户交互和 `/api/lessons/tasks/local-asr` 契约的前提下，为上传页均衡模式的浏览器本地 SenseVoice 识别增加按电脑性能自动并行的前端基础能力；严格避开当前进行中的 `frontend/src/features/upload/UploadPanel.jsx` 占用，优先完成可复用的并行切段、worker 池调度、时间对齐回拼与结果合并模块，并视冲突情况决定是否通过衔接文档交接最后接入步骤。
修改的文件清单（精确路径）：
- Docx/AI分工/上传页均衡模式本地自动并行提速_03月19日_23时18分.md
- frontend/src/features/upload/localAsrParallelRuntime.js
关联衔接：Docx/AI分工/衔接_上传页均衡模式本地自动并行提速_03月19日_23时27分.md
风险：
- 当前存在进行中的上传页任务占用 `frontend/src/features/upload/UploadPanel.jsx`，若最终接入必须修改该文件，则需改为衔接方案，避免文件冲突。
- 浏览器本地并行会放大 CPU/内存占用，必须固定安全上限并保证 worker 失败时可整体中断。
- 自动切段如果回拼去重策略不稳，可能出现句子首尾漂移或重复句。
验证：
- 已运行 `frontend\npm.cmd run build`，通过。
- 已静态检查 `localAsrParallelRuntime.js` 的并行度计算、切段、keep range 过滤与全局时间回拼逻辑，结果按时间戳单调递增构造。
清理记录：
- 未删除历史任务/衔接文档：本次未获得文件删除确认，按规则保留现有文档。
状态：已完成
结束时间：2026-03-19 23:27:00
