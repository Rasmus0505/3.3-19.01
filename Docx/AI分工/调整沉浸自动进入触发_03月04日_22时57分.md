文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
- 调整沉浸式自动进入策略：禁止登录后默认进入，仅在用户主动选择历史记录或生成素材后自动进入沉浸。
修改的文件清单（精确路径）：
- frontend/src/app/LearningShell.jsx
关联衔接：无
风险：
- 当前环境缺少 Node/Git 可执行文件，无法执行构建与提交验证。
验证：
- 已完成逻辑自检：登录后首次自动加载第一条课程时，不再自动进入沉浸模式（`autoEnterImmersive: false`）。
- 已完成触发点自检：用户通过历史记录选择课程时仍自动进入沉浸（`loadLessonDetail` 默认 `autoEnterImmersive: true`）。
- 已完成触发点自检：素材生成成功后仍自动进入沉浸（`handleLessonCreated` 显式 `autoEnterImmersive: true`）。
- 已完成兼容自检：删除当前课程后的回退课程仅在当前已处于沉浸态时保持沉浸，避免非用户主动场景强制进入沉浸。
- 当前环境缺少 Node/Git 可执行文件，无法执行 `npm run build` 与 git 提交验证。
清理记录：
- 已删除：`AI分工/沉浸全屏与句子控制增强_03月04日_21时47分.md`。
- 可删原因：该文档由我创建，且 `状态=已完成`、`关联衔接=无`，满足“任务 md 可删除”判定。
结束时间：2026-03-04 23:08
