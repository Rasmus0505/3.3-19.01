文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：将学习页快捷键默认配置调整为 Shift+A / Shift+S / Shift+Q / Shift+W / Shift+R / Space，并在录入新快捷键时改为“新快捷键覆盖旧动作，旧动作清空为未设置”，不再提示被占用。
修改的文件清单（精确路径）：
- frontend/src/features/immersive/learningSettings.js
- frontend/src/features/lessons/LessonList.jsx
- Docx/AI分工/学习页快捷键默认值与冲突覆盖调整_03月11日_15时25分.md
关联衔接：无
风险：需要保证显式清空的快捷键不会被设置读取逻辑自动补回默认值，同时保留首屏默认配置和旧配置兼容。
验证：
- 已执行函数级校验：默认快捷键为 `Shift+A / Shift+S / Shift+Q / Shift+W / Shift+R / Space`。
- 已执行函数级校验：发生冲突覆盖时，旧动作会保留为 `未设置`，不会被自动补回默认值。
- 已执行 `npm run build`，通过。
- 已执行 `py -m pytest -q tests/test_regression_api.py::test_health_endpoint`，结果 1 passed。
清理记录：未删除历史任务/衔接文档；文件删除仍需用户确认，本轮仅补留痕。
结束时间：2026-03-11 15:27:47
