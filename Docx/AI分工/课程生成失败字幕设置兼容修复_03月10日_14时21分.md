文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
- 修复旧版 `subtitle_settings` 表缺列时导致课程生成失败、`/health/ready` 返回 503、字幕设置相关接口返回 500 的兼容问题。
- 通过“迁移补齐 + 运行时自愈 + 回归测试”三层兜底，保持现有 API 契约不变。
修改的文件清单（精确路径）：
- app/services/billing.py
- app/main.py
- migrations/versions/20260310_0011_backfill_subtitle_settings_columns.py
- tests/test_regression_api.py
- Docx/AI分工/课程生成失败字幕设置兼容修复_03月10日_14时21分.md
关联衔接：无
风险：
- 当前工作区存在其他未提交改动，需只叠加本次兼容修复，避免覆盖既有修改。
- 旧库兼容逻辑需要同时兼容 SQLite 与 PostgreSQL，若 SQL 分支判断不完整，可能导致自愈失败。
验证：
- 已执行：`python -m py_compile app/services/billing.py app/main.py migrations/versions/20260310_0011_backfill_subtitle_settings_columns.py tests/test_regression_api.py`
- 已执行：`python -m pytest -q tests/test_regression_api.py -k "subtitle_settings_migration_idempotent_when_table_exists or subtitle_settings_endpoints_self_heal_missing_columns or generate_lesson_self_heals_legacy_subtitle_settings_columns or public_billing_rates_self_heals_missing_subtitle_settings_table or admin_subtitle_settings_update_self_heals_missing_table or health_ready_endpoint"`，结果 `6 passed`
- 已补充覆盖：旧版 `subtitle_settings` 缺列自愈、`/health/ready` 恢复 200、`/api/billing/rates` 与 `/api/admin/subtitle-settings/history` 恢复 200、课程生成主链路不再在字幕设置处失败、Alembic 迁移重复执行幂等。
清理记录：
- 暂无。按“文件删除需确认”规则，本轮不删除既有任务/衔接文档。
结束时间：2026-03-10 14:30:44
