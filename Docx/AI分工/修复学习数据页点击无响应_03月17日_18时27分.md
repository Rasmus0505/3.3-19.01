文档类型：任务
创建者：Codex(GPT-5)
状态：已完成
完整任务目标：
- 修复点击“学习数据”后页面无响应的问题。
- 保持现有学习壳层与路由契约不变，仅在共享图表渲染层处理隐藏容器首帧尺寸为 0 导致的图表挂载失败。
修改的文件清单（精确路径）：
- Docx/AI分工/修复学习数据页点击无响应_03月17日_18时27分.md
- frontend/src/components/ui/MetricChart.jsx
关联衔接：无
风险：
- 共享图表组件被多个页面复用，尺寸监听逻辑需要避免引入额外闪烁或重复渲染。
- 本轮不改学习壳层的面板保活方式，问题修复依赖图表组件在容器可见后能正确重测尺寸。
验证：
- 已执行：`npm --prefix frontend run build`，通过。
- 已执行：`GET http://127.0.0.1:8080/health`，返回 `200 {"ok":true,"service":"zeabur3.3-min-asr","ready":false}`。
- 已执行：本地临时静态代理 `http://127.0.0.1:4173/` 可访问，返回 `200`。
- 已完成静态确认：图表改为先监听容器尺寸，只有拿到正的宽高后才挂载 `ResponsiveContainer`，可避免隐藏面板首帧 `width(0)/height(0)` 报错。
- 已尝试：使用 Playwright 做真实点击验证；受当前机器 Chrome 已占用同一 profile 影响，浏览器进程启动即退出，未能完成自动化点击回归。
清理记录：
- 已停止本次验证拉起的本地 FastAPI 进程（PID `24344`）与临时静态代理进程（PID `32256`）。
- 已删除本次验证生成的系统临时脚本 `%TEMP%\\codex_metric_chart_proxy.py`。
结束时间：2026-03-17 18:33:26
