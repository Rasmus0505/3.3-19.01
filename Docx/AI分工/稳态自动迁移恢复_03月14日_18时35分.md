文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
- 在不破坏 `/health`、`/health/ready`、`/api/transcribe/file` 契约的前提下，恢复更稳妥的 Alembic 自动迁移能力。
- 启动前自动执行 `python -m alembic -c alembic.ini upgrade head`，并为 PostgreSQL 增加锁保护，避免多实例重复迁移。
- 保留完整迁移报错；当自动迁移失败时默认继续启动应用，使 `/health` 可用且 `/health/ready` 继续严格返回 `503`。
- 同步更新启动脚本回归测试与部署文档，减少 Zeabur 发布时的人工迁移步骤。
修改的文件清单（精确路径）：
- Docx/AI分工/稳态自动迁移恢复_03月14日_18时35分.md
- scripts/start.sh
- app/db/migration_bootstrap.py
- tests/test_start_script_smoke.py
- README.md
- ops/zeabur-minimal-deploy.md
- .env.example
关联衔接：无
风险：
- 启动链路重新接入自动迁移后，若历史脏库仍存在未覆盖的非幂等迁移，仍可能触发失败；本次会尽量以“失败不阻塞启动”的方式降低影响。
- PostgreSQL 锁逻辑需要兼容当前 Zeabur 单实例与未来多实例部署，避免死锁或无期限等待。
验证：
- 已执行 `python -m py_compile app\db\migration_bootstrap.py app\db\init.py app\main.py tests\test_start_script_smoke.py`，通过。
- 已执行 `pytest -q tests\test_start_script_smoke.py`，结果 `3 skipped`（当前环境未提供 `START_SCRIPT_SMOKE_DATABASE_URL` 对应 PostgreSQL，未能跑通真实启动烟测）。
- 已完成静态自检：`scripts/start.sh`、`.env.example`、`README.md`、`ops/zeabur-minimal-deploy.md` 已统一到自动迁移默认开启口径，并保留 `AUTO_MIGRATE_ON_START=0` 手动回退开关。
清理记录：
- 暂无
结束时间：2026-03-14 18:46
