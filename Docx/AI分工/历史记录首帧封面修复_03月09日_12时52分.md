文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
- 修复历史记录视频封面偶发黑屏的问题，统一改为稳定提取视频第一帧作为封面。
- 复用上传预览阶段已生成的封面，避免上传预览与历史记录缓存封面不一致。
- 兼容修复本地 IndexedDB 中已存下的旧黑屏封面，不修改后端接口契约。
修改的文件清单（精确路径）：
- Docx/AI分工/历史记录首帧封面修复_03月09日_12时52分.md
- frontend/src/shared/media/localMediaStore.js
- frontend/src/features/upload/UploadPanel.jsx
关联衔接：无
风险：
- 浏览器对视频 `currentTime=0` 的首帧解码行为存在差异，需要保留小幅回退策略兜底。
- 本地媒体记录将新增封面版本字段，需保证旧记录可平滑读取并自动回填。
验证：
- 已执行：`git diff --check -- frontend/src/shared/media/localMediaStore.js frontend/src/features/upload/UploadPanel.jsx Docx/AI分工/历史记录首帧封面修复_03月09日_12时52分.md`
- 已执行：`python -m pytest tests/test_regression_api.py -k 'health or create_lesson' -q`，结果 `7 passed`
- 未执行：前端 `vite build`；当前环境仍缺少 `node` / `npm`，需在 Zeabur 或具备 Node 的环境补跑。
清理记录：
- 本轮未删除任何任务 / 衔接文档；暂无满足“可删除判定”且由我创建的文档。
结束时间：2026-03-09 12:56:58
