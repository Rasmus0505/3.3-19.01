文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
- 修复新生成素材课程未自动绑定本地视频的问题，避免用户进入学习前还要去历史记录手动恢复视频。
- 修复上传后与历史记录中的视频封面黑屏问题，改为优先提取最早可见画面并复用到本地媒体缓存。
- 保持现有后端 API 契约、Zeabur 部署方式与环境变量不变，仅修复前端本地媒体持久化链路。
修改的文件清单（精确路径）：
- Docx/AI分工/修复新素材自动绑定与黑屏封面_03月09日_15时42分.md
- frontend/src/shared/media/localMediaStore.js
- frontend/src/features/upload/UploadPanel.jsx
- frontend/src/app/LearningShell.jsx
关联衔接：无
风险：
- IndexedDB 持久化与课程详情加载存在前后时序，需避免生成成功后列表短暂回退为“待恢复视频”。
- 浏览器视频首帧解码存在差异，需要在不过度增加计算量的前提下识别纯黑/近黑帧并向后取样。
验证：
- 已执行：`python -m pytest tests/test_regression_api.py -k 'health or create_lesson or create_lesson_task_and_poll_success' -q`，结果 `7 passed`
- 已执行：`npm run build`（目录：`frontend`），构建通过；保留 Vite 默认 chunk size 警告，未影响产物生成
- 未执行：浏览器手测上传黑场视频 / 拒绝持久化存储场景，需在真实浏览器中补点选验证
清理记录：
- 本次开始前未删除任何任务/衔接文档；未获得文件删除确认，按规则保留既有文档。
- 回归后确认工作区存在一批非本次任务产生的既有后端改动：`app/api/routers/lessons.py`、`app/core/config.py`、`app/infra/asr_dashscope.py`、`app/infra/translation_qwen_mt.py`、`app/schemas/__init__.py`、`app/schemas/lesson.py`、`app/services/lesson_service.py`，本次已按规则忽略，未触碰。
结束时间：2026-03-09 15:53:04
