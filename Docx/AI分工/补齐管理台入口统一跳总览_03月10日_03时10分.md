文档类型：任务
创建者：Codex（GPT-5）
状态：已完成
完整任务目标：
- 补齐主学习页与本地字幕页中进入管理台的入口一致性，将所有“进入后台”按钮从 `/admin/users` 统一切到 `/admin/overview`。
- 重新构建前端并同步到 `app/static`，确保内嵌 `/admin` 与入口跳转行为一致。
修改的文件清单（精确路径）：
- Docx/AI分工/补齐管理台入口统一跳总览_03月10日_03时10分.md
- frontend/src/app/LearningShell.jsx
- frontend/src/app/LearningShellLocalSubtitles.jsx
- app/static/index.html
- app/static/assets/*
关联衔接：无
风险：
- 需要重新构建前端并同步 `app/static`，否则源码与线上嵌入产物会出现跳转不一致。
验证：
- 已执行前端构建：`npm --prefix frontend run build` 通过。
- 已完成静态产物同步：`frontend/dist -> app/static`，`app/static/index.html` 已指向最新 hash（`index-fwutdz-y.js` / `index-m2EpcVhl.css`）。
- 已执行核心回归：`pytest -q tests/test_regression_api.py -k "health_endpoint or transcribe_file_endpoint_with_stubbed_service"`，结果 `2 passed`。
清理记录：
- 已删除本轮临时构建目录：`frontend/dist/`；该目录为本轮构建产物，未纳入版本管理。
结束时间：2026-03-10 03:12:14
