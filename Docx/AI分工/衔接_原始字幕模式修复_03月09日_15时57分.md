文档类型：衔接
创建者：Codex
状态：已完成
关联任务：原始字幕模式修复_03月09日_15时46分.md
1) 冲突原因（与哪个任务/文件冲突）
- 与 `历史记录菜单悬浮与字幕流式反馈_03月09日_15时43分.md` 冲突：其占用 `app/api/routers/lessons.py`、`app/schemas/__init__.py`、`app/schemas/lesson.py`、`app/services/lesson_service.py`、`frontend/src/app/LearningShell.jsx`、`frontend/src/features/lessons/LessonList.jsx`、`tests/test_regression_api.py`。
- 与 `修复新素材自动绑定与黑屏封面_03月09日_15时42分.md` 冲突：其占用 `frontend/src/app/LearningShell.jsx`、`frontend/src/features/upload/UploadPanel.jsx`、`frontend/src/shared/media/localMediaStore.js`。
2) 本次已完成部分
- 在不重叠文件中落地前端兼容方案：`frontend/src/shared/media/localSubtitleStore.js` 将旧 plain 缓存判定为失效，不再继续复用。
- 在 `frontend/src/app/LearningShellLocalSubtitles.jsx` 中把“非语义模式”改为发送仅含 ASR 原句的 payload，让现有字幕接口回到 ASR 原始句子并重新翻译中文字幕。
- 新上传课程若初始关闭语义分句，会自动补一次“原始字幕”切换，避免继续展示旧 plain 规则分句。
- 在 `frontend/src/features/lessons/LessonListLocalSubtitles.jsx` 中将“普通分句”文案改为“原始字幕”，并将入口解释改为“回到 ASR 原始分句结果”。
- 在 `frontend/src/App.jsx` 与 `frontend/src/pages/LearningPage.jsx` 暂时切到本地字幕壳层，保证上述修复可实际生效。
3) 暂未完成部分
- 后端正式链路仍未改为 `semantic_split_enabled=false => extract_sentences(asr_payload)`；当前数据库落库语义仍取决于占用中的 `app/services/lesson_service.py`。
- 正式壳层 `frontend/src/app/LearningShell.jsx` 与正式历史列表 `frontend/src/features/lessons/LessonList.jsx` 尚未并入本次“原始字幕”语义与缓存失效逻辑。
- `tests/test_regression_api.py` 尚未补充“非语义=ASR 原始结果”的正式后端回归断言。
4) 待冲突解除后的补充步骤（可直接执行）
- 在 `app/services/lesson_service.py` 的 `build_subtitle_variant` 中将 `semantic_split_enabled=false` 直接切到 `extract_sentences(asr_payload)`，并把 `split_mode` 固定为 `asr_sentences`。
- 复用当前本地字幕方案中的缓存失效判定与文案语义，合并到 `frontend/src/app/LearningShell.jsx` 和 `frontend/src/features/lessons/LessonList.jsx`。
- 补充 `tests/test_regression_api.py`：上传关闭语义分句时落库句子等于 ASR 原句；字幕变体接口在关闭语义时返回 `asr_sentences`。
- 合并完成后，将 `frontend/src/App.jsx` / `frontend/src/pages/LearningPage.jsx` 重新指回正式 `LearningShell.jsx`，并评估是否保留或删除 `LearningShellLocalSubtitles.jsx` / `LessonListLocalSubtitles.jsx`（删除仍需人工确认）。
5) 建议修改文件清单（冲突解除后）
- app/services/lesson_service.py
- frontend/src/app/LearningShell.jsx
- frontend/src/features/lessons/LessonList.jsx
- tests/test_regression_api.py
消解任务：原始字幕模式修复_03月09日_15时46分.md
消解时间：2026-03-09 17:23:33
