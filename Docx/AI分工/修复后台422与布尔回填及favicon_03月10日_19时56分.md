文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
- 修复管理台日志接口在空 `lesson_id` 参数下触发 `422 Unprocessable Entity` 的问题，确保前后端都具备容错。
- 修复 PostgreSQL 下 `subtitle_settings` 布尔回填使用 `0/1` 导致的 `DatatypeMismatch` 报错。
- 消除浏览器默认请求 `/favicon.ico` 时的无意义 404 噪音。
修改的文件清单（精确路径）：
- Docx/AI分工/修复后台422与布尔回填及favicon_03月10日_19时56分.md
- app/services/billing.py
- app/api/routers/admin_console.py
- app/api/routers/admin.py
- app/main.py
- frontend/src/features/admin-logs/AdminLessonTaskLogsTab.jsx
- frontend/src/features/admin-logs/AdminTranslationLogsTab.jsx
- tests/test_admin_console_api.py
- tests/test_regression_api.py
关联衔接：无
风险：
- 管理台查询参数修复若处理不当，可能影响既有筛选逻辑（尤其 URL 回填与分页）。
- `subtitle_settings` 回填 SQL 需同时兼容 PostgreSQL 与 SQLite，避免引入新方言问题。
- `/favicon.ico` 路由需避免影响现有 SPA 路由与静态资源挂载。
验证：
- 已执行：`python -m py_compile app/services/billing.py app/api/routers/admin.py app/api/routers/admin_console.py app/main.py tests/test_regression_api.py tests/test_admin_console_api.py`
- 已执行：`python -m pytest -q tests/test_regression_api.py`，结果：`60 passed`
- 已执行：`python -m pytest -q tests/test_admin_console_api.py`，结果：`5 passed`
- 已执行：`npm --prefix frontend run build`，结果：`built`
清理记录：
- 本轮未删除文件。未创建衔接文档，暂无满足“可删除判定”且由本任务创建的历史文档。
结束时间：2026-03-10 20:06:37
