文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
- 在不与进行中的管理台四页重构、上传页中断恢复任务冲突的前提下，为学习侧边栏新增“学习数据”面板。
- 后端通过 `practice` 路由新增当前用户学习汇总接口，并记录轻量学习日统计，输出教练反馈型数据。
- 前端在学习壳层内新增独立学习数据面板，复用现有 `MetricCard`、`MetricChart`，支持 7 天 / 30 天切换与继续学习 CTA。
修改的文件清单（精确路径）：
- Docx/AI分工/侧边栏学习数据面板落地_03月17日_01时30分.md
- app/models/learning_stats.py
- app/repositories/learning_stats.py
- app/services/learning_stats_service.py
- app/api/routers/practice.py
- app/schemas/practice.py
- frontend/src/app/learning-shell/LearningShellSidebar.jsx
- frontend/src/app/learning-shell/LearningShellPanelContent.jsx
- frontend/src/features/learning-stats/LearningStatsPanel.jsx
- migrations/versions/20260317_0020_user_learning_daily_stats.py
- tests/test_learning_summary_api.py
关联衔接：无
风险：
- `frontend/src/app/learning-shell/LearningShellPanelContent.jsx` 存在存量未提交修改，需在不回退既有逻辑的前提下插入新面板。
- 学习日统计为本轮新增能力，历史学习行为无法完整回补，首版近 7 天 / 30 天趋势以功能上线后的新增统计为准。
- `practice` 路由既服务拼写练习又服务沉浸页进度同步，统计逻辑必须区分“新增完成句数”和“普通进度刷新”，避免重复累计。
验证：
- 已执行 `pytest -q tests/test_learning_summary_api.py`，结果 `4 passed`。
- 已执行 `pytest -q tests/test_regression_api.py -k "health_endpoint or transcribe_file_endpoint_with_stubbed_service"`，结果 `2 passed`。
- 已执行 `pytest -q tests/test_e2e_key_flows.py::test_e2e_login_create_lesson_practice_progress`，结果 `1 passed`。
- 已执行 `npm --prefix frontend run build`，通过。
- 已执行 `create_app(enable_lifespan=False)` 导入检查，成功。
- 已执行 `TestClient(create_app(enable_lifespan=False)).get("/health")`，结果 `200 {"ok": true, "service": "zeabur3.3-min-asr", "ready": false}`。
清理记录：
- 未删除任何其他 AI 文档。
- 尝试删除本轮 `frontend/dist` 构建产物，但删除命令被当前策略阻止；该目录未纳入版本管理，可忽略，不影响提交与 Zeabur 部署。
结束时间：2026-03-17 01:43:33
