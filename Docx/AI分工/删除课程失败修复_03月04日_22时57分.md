文档类型：任务
创建者：Codex(ai-3)
状态：已完成
完整任务目标：
- 定位并修复前端出现 `INTERNAL_ERROR: 删除课程失败` 的后端根因，保证课程删除在有账本流水关联时可成功。
修改的文件清单（精确路径）：
- app/api/routers/lessons.py
- tests/test_regression_api.py
关联衔接：无
风险：
- 当前环境缺少 Python/Git 可执行文件，无法本地执行 pytest 与提交验证。
验证：
- 删除接口增加保护：删除课程前先将 `wallet_ledger.lesson_id` 置空，再执行 `db.delete(lesson)`。
- 新增回归测试 `test_delete_lesson_clears_wallet_ledger_reference`，覆盖“课程被账本流水引用时仍可删除”的场景。
- 静态检查通过：新增代码引用与测试函数均可检索到，未引入额外 API 路径变更。
清理记录：
- 本次未删除文档文件（无“已消解衔接 md”与满足可删除条件的历史任务 md）。
结束时间：2026-03-04 22:58
