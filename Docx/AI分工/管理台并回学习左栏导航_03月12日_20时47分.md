文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
- 将主前端内嵌管理台并回学习页左侧边栏，取消管理台独立左侧导航与内容区重复导航。
- 保持 `/admin/*` 深链、现有后台 API 契约、`/health` 与 `/api/transcribe/file` 不变，只调整前端壳层和导航交互。
- 左侧新增可展开的“管理台”父项，展开后提供“系统健康 / 任务监控 / 操作审计 / 用户计费 / 活动管理”5 个直达入口。
修改的文件清单（精确路径）：
- Docx/AI分工/管理台并回学习左栏导航_03月12日_20时47分.md
- frontend/src/shared/lib/adminSearchParams.js
- frontend/src/app/learning-shell/LearningShellSidebar.jsx
- frontend/src/app/learning-shell/LearningShellHeader.jsx
- frontend/src/app/learning-shell/LearningShellContainer.jsx
- frontend/src/AdminApp.jsx
- frontend/src/features/admin-workspaces/AdminMonitoringWorkspace.jsx
- frontend/src/features/admin-workspaces/AdminBusinessWorkspace.jsx
- frontend/src/pages/AdminPage.jsx
- frontend/dist-admin/index.admin.html
- frontend/dist-admin/assets/*
关联衔接：无
风险：
- 需要同时兼顾主前端内嵌管理台与独立 admin 构建复用的 `AdminApp`，避免只修一边导致另一边构建失败。
- 旧 `/admin/*` 深链需要继续正确映射到新的 5 个左栏入口，否则会出现高亮与内容不一致。
- 左侧“管理台”父项在桌面折叠态、移动端抽屉态都要保持可理解的展开行为，避免新增迷惑交互。
验证：
- 已执行 `npm --prefix frontend run build`，通过。
- 已执行 `npm --prefix frontend run build:admin`，通过。
- 已执行 `pytest -q tests/test_regression_api.py -k "health_endpoint or transcribe_file_endpoint_with_stubbed_service"`，结果 `2 passed`。
清理记录：
- 未删除 `frontend/dist-admin/`：该目录包含仓库已跟踪发布文件，本轮因共享 `AdminApp` 变更而同步更新，按规则保留并纳入修改清单。
- 未删除 `frontend/dist/`：该目录为本轮构建产物，但当前命令策略阻止直接删除；其未纳入版本管理，不影响提交与部署。
结束时间：2026-03-12 21:02:36
