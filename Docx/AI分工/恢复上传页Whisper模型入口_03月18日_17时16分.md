文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：恢复上传素材页里 Whisper Base 与 Whisper Small 模型卡片的可见性；在当前 worker 仅支持 SenseVoice 的前提下，明确标注 Whisper 在上传页均衡模式暂未开放，避免用户看不到模型或误点后报错，同时保持 SenseVoice 仍为默认可用模型。
修改的文件清单（精确路径）：
- Docx/AI分工/恢复上传页Whisper模型入口_03月18日_17时16分.md
- frontend/src/features/upload/UploadPanel.jsx
关联衔接：无
风险：若直接恢复 Whisper 选择能力而不限制，会导致当前上传页 worker 因未接入 Whisper 内核而失败；需要把“可见”与“可运行”明确区分，避免生成流程被带坏。
验证：
- 已尝试 `npm.cmd run build`，但当前终端环境被 `spawn EPERM` 拦截，卡在 Vite/esbuild 配置装载阶段，非本次业务代码报错
- 已尝试 `npm.cmd run build -- --configLoader runner`，同样被当前环境的 `spawn EPERM` 拦截
清理记录：
- 未删除历史任务/衔接文档：本次未获得文件删除确认，按规则保留现有文档
结束时间：2026-03-18 17:19:00
