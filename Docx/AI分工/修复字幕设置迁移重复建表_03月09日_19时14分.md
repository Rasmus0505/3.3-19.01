文档类型：任务
创建者：Codex（GPT-5）
状态：已完成
完整任务目标：
- 修复 `20260307_0007_subtitle_settings.py` 在表已存在场景下重复建表导致的迁移失败，消除启动阶段 `DuplicateTable` 报错。
- 保持现有 API 契约不变（含 `/api/transcribe/file`），仅做迁移幂等与回归测试补强。
修改的文件清单（精确路径）：
- migrations/versions/20260307_0007_subtitle_settings.py
- tests/test_regression_api.py
- Docx/AI分工/修复字幕设置迁移重复建表_03月09日_19时14分.md
关联衔接：无
风险：
- 迁移幂等逻辑需同时兼容 SQLite 与 PostgreSQL，若检查条件不完整可能导致默认行漏写。
验证：
- 已执行 `git fetch origin`，结果失败（`Recv failure: Connection was reset`），按约定不阻塞本地实施。
- 已执行迁移复现链路：
  - `python -m alembic -c alembic.ini upgrade 20260306_0006`
  - 预建 `subtitle_settings` 表
  - `python -m alembic -c alembic.ini upgrade head`
  - 结果通过，日志出现 `[DEBUG] migration.20260307_0007 table_exists=true skip_create=true`，未再出现重复建表错误。
- 已执行链路结果校验：`alembic_version=20260309_0008`，`subtitle_settings(id=1)` 存在。
- 已执行新增回归：`pytest -q tests/test_regression_api.py -k subtitle_settings_migration_idempotent`，结果 `1 passed`。
- 已执行核心回归：`pytest -q tests/test_regression_api.py -k "health_endpoint or transcribe_file_endpoint_with_stubbed_service"`，结果 `2 passed`。
清理记录：
- 未删除任何分工文档：本任务未创建衔接 md，且当前不存在由本任务创建且满足可删除判定的文档。
- 已删除本地临时复现产物 `tmp_migration_idempotent_verify_1916.db`（验证用临时文件，非交付文件）。
结束时间：2026-03-09 19:19:37
