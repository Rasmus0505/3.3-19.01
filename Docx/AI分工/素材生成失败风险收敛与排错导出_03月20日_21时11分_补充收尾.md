文档类型：任务收尾补充
创建者：Codex
关联任务：素材生成失败风险收敛与排错导出
时间：2026-03-20 21:11:16

本轮补充完成：
- 修复 `app/services/learning_stats_service.py` 中缺失的 `build_learning_progress_summary`
- 对齐 `record_progress_activity` 与 `app/api/routers/practice.py` 的调用参数
- 补齐学习成长面板所需返回结构：
  `summary / hero / today_tasks / risk_cards / milestones / focus_cards / charts / continue_lesson / stalled_lesson / primary_recommendation`
- 保持前一轮已完成的素材生成链路改造不回退：
  上传页阶段可视化、部分成功（字幕成功/翻译失败）、排错信息导出、前端复制排错信息按钮

本轮验证：
- `python -m py_compile app/services/learning_stats_service.py`
- `pytest tests/test_learning_summary_api.py -q`
- `pytest tests/test_regression_api.py -q -k "create_lesson_task_and_poll_success or lesson_task_partial_success_and_debug_report or lesson_task_reports_translation_parse_failure_with_explicit_error"`

验证结果：
- `tests/test_learning_summary_api.py`：`5 passed`
- `tests/test_regression_api.py` 目标用例：`3 passed`

结论：
- “大视频一直转圈无结果”“后台失败前端只显示笼统错误”“字幕成功但翻译失败”对应的任务链路收敛已完成
- 学习成长面板此前被 `learning_stats_service` 缺失实现阻塞，现在已恢复可导入、可汇总、可测试
- 本次任务范围内无剩余未完成项
