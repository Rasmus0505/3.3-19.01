文档类型：任务
创建者：Codex（GPT-5）
状态：已完成
完整任务目标：
- 计费配置强制“翻译仅保留 qwen-mt-flash”，清理所有非 flash 的 qwen-mt-* 模型脏数据。
- 在管理端与公共计费接口增加自愈，确保页面返回中翻译模型仅剩 qwen-mt-flash。
- 禁止通过管理端接口继续维护非 flash 的 qwen-mt-* 模型。
- 补强 20260309_0009 迁移与回归测试，防止历史数据回流。
修改的文件清单（精确路径）：
- app/services/billing.py
- app/services/billing_service.py
- app/api/routers/admin.py
- app/api/routers/billing.py
- migrations/versions/20260309_0009_translation_batch_chars_flash_only.py
- tests/test_regression_api.py
- Docx/AI分工/计费翻译仅保留Flash自愈_03月09日_20时08分.md
关联衔接：无
风险：
- 接口级自愈会在 GET 请求时触发数据库写入，已通过“仅检测到脏数据时 commit”降低影响。
- 遗留库表缺列场景若强行自愈会报错，已增加 partial schema 自动跳过逻辑。
验证：
- 已执行 `python -m py_compile app/services/billing.py app/services/billing_service.py app/api/routers/admin.py app/api/routers/billing.py migrations/versions/20260309_0009_translation_batch_chars_flash_only.py tests/test_regression_api.py`，通过。
- 已执行 `pytest -q tests/test_regression_api.py -k "billing_rates or subtitle_settings or health_endpoint or transcribe_file_endpoint_with_stubbed_service"`，结果 `9 passed, 43 deselected`。
- 已新增并通过以下覆盖：
  - `ensure_default_billing_rates` 清理 `qwen-mt-custom`。
  - 管理端/公共计费接口仅返回 `qwen-mt-flash` 翻译模型。
  - 管理端拒绝更新非 flash 的 `qwen-mt-*` 模型。
  - 迁移到 head 后 `qwen-mt-*` 集合仅保留 `qwen-mt-flash`。
清理记录：
- 未执行文档删除：本轮未新增衔接文档，且未收到“允许删除文件”的明确确认。
结束时间：2026-03-09 20:25:45

