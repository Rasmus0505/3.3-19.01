文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
1. 为“重播本句”增加快捷键 Shift+R。
2. 重播本句时不清空用户已输入的拼写内容。
3. 鼠标悬浮在有快捷键的按钮上时显示快捷键提示。
修改的文件清单（精确路径）：
- frontend/src/features/immersive/ImmersiveLessonPage.jsx
关联衔接：无
风险：
- 当前环境缺少 Node/Git 可执行文件，无法执行前端构建与提交验证。
验证：
- 已完成快捷键接入：`Shift+R` 触发“重播本句”，并兼容全局键盘监听与输入焦点场景。
- 已完成重播行为校验：重播调用改为 `preserveTyping=true`，不会清空当前句已输入内容。
- 已完成悬浮提示校验：所有带快捷键按钮均显示 tooltip（`esc`、`shift+r`、`space`、`enter`）。
- 当前环境缺少 Node/Git 可执行文件，无法执行 `npm run build` 与 git 提交验证。
清理记录：
- 本次未删除文档文件（无“已消解衔接 md”；其余已完成任务文档创建者标识不完全一致，按规则保留）。
结束时间：2026-03-05 00:09
