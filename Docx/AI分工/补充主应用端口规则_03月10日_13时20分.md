文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
- 在不修改启动脚本、Dockerfile、README 的前提下，仅更新仓库级协作规则，明确主应用对外端口统一按 8080 说明和交付。
- 约束后续 Zeabur 部署指引、环境变量建议、验收说明统一使用 `PORT=8080`，避免继续把 8000 作为推荐端口输出。
修改的文件清单（精确路径）：
- AGENTS.md
- Docx/AI分工/补充主应用端口规则_03月10日_13时20分.md
关联衔接：无
风险：
- 本次只改规则，不改实际启动默认值；仓库仍保留 `scripts/start.sh` 默认回退 `8000` 的历史不一致。
- 当前工作区存在其他任务留下的未提交改动，本次忽略并避免触碰。
验证：
- 已静态检查 `AGENTS.md`，新增规则明确包含 `8080`、`Zeabur`、`PORT=8080` 三个要点。
- 已复核 `scripts/start.sh` 与 `zeabur-template.yaml`，确认本次没有误改运行逻辑；当前仍保持 `scripts/start.sh` 默认回退 `8000`、模板默认 `8080`。
清理记录：
- 本次未删除任何文档；暂无满足“可删除判定”且由我创建的旧任务或衔接文档。
结束时间：2026-03-10 13:21:14
