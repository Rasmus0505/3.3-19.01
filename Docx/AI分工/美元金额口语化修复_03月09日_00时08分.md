文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
- 修复课程句子中的美元金额显示与校验问题，让 `$40?` 这类内容以学习口语形式输出为 `forty dollars?`。
- 保持现有 `/api` 契约不变，同时兼容新课程入库与旧课程返回/练习校验。
修改的文件清单（精确路径）：
- Docx/AI分工/美元金额口语化修复_03月09日_00时08分.md
- app/services/lesson_builder.py
- app/services/lesson_service.py
- app/api/serializers.py
- app/services/practice_service.py
- tests/test_regression_api.py
关联衔接：无
风险：
- 金额口语化会影响课程展示文本与答案 token，需确保仅匹配美元金额，不误伤普通数字或非金额符号文本。
- 旧课程不做数据迁移，需依赖出参兼容与校验兼容覆盖历史数据。
- `git fetch origin` 已尝试，但因网络重置失败；本轮基于当前本地代码继续开发。
验证：
- 已完成：`python -m pytest -q tests/test_regression_api.py -k "health_endpoint or normalize_learning_english_text_spells_usd_amounts or legacy_lesson_detail_and_check_spell_usd_amounts or generate_lesson_stores_spoken_usd_amounts"`，4 通过。
- 已完成：`python -m pytest -q tests/test_regression_api.py -k "test_generate_lesson_applies_semantic_split_when_enabled or test_generate_lesson_semantic_split_failure_falls_back_to_rule_split"`，2 通过。
- 已完成：`python -m pytest -q tests/test_regression_api.py -k "test_lessons_progress_and_check"`，1 通过。
- 已完成：`git diff --check`，无补丁格式错误，仅有 LF/CRLF 警告。
清理记录：
- 本次未删除任何文档文件；按“文件删除需人工确认”约束，保留全部留痕文档。
结束时间：2026-03-09 00:16:04
