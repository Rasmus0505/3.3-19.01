文档类型：任务
创建者：Codex(GPT-5)
状态：已完成
完整任务目标：
- 修复点击“学习数据”后没有进入对应页面的问题。
- 在不触碰进行中的管理台文件前提下，补齐学习壳层前端路由注册，保持现有侧栏点击契约不变。
修改的文件清单（精确路径）：
- Docx/AI分工/修复学习数据路由缺失_03月17日_18时43分.md
- frontend/src/app/bootstrap.jsx
关联衔接：无
风险：
- 当前主应用通过 React Router 挂学习页与管理页，若路由补齐位置不对，可能影响现有 `* -> /` 回退行为。
- 本轮只补前端路由，不修改侧栏、学习数据面板或后端接口。
验证：
- 已执行：`npm --prefix frontend run build`，通过。
- 已完成静态确认：`frontend/src/main.jsx` 实际加载 `BootstrapApp`，`frontend/src/app/bootstrap.jsx` 已补齐 `Route path="/stats" element={<LearningPage />}`。
- 已完成链路确认：侧栏 `LearningShellSidebar -> handlePanelChange -> navigate("/stats")` 现在有匹配路由，不再落入 `* -> /` 通配重定向。
清理记录：
- 本轮未删除任何文档；按“文件删除需确认”规则保留历史留痕。
结束时间：2026-03-17 18:45:00
