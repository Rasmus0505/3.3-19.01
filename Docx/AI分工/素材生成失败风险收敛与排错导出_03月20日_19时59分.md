文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：落实“素材上传后首次生成整门课程”的风险收敛方案，在不破坏现有 lessons 任务接口主契约和 Zeabur 部署方式的前提下，完成三项最小闭环改造：1）后端把“字幕已生成但翻译失败”收敛为可学习的部分成功；2）为任务补一键复制给编程 AI 的排错信息导出接口；3）上传页展示更贴近用户理解的阶段标签、部分成功说明与复制排错信息按钮。
修改的文件清单（精确路径）：
- Docx/AI分工/素材生成失败风险收敛与排错导出_03月20日_19时59分.md
- app/api/routers/lessons.py
- app/schemas/__init__.py
- app/schemas/lesson.py
- app/services/lesson_command_service.py
- app/services/lesson_service.py
- frontend/src/features/upload/UploadPanel.jsx
- tests/test_regression_api.py
- app/static/index.html
- app/static/assets/*
关联衔接：无
风险：
- 任务结果语义当前已被上传页和测试依赖，本次只能做非破坏性扩展，不能让既有 `succeeded/failed` 轮询逻辑失效。
- `UploadPanel.jsx` 是近期热点文件，本次只做最小改动，避免带坏已有暂停、终止、恢复、本地 ASR 与价格展示。
- 当前工作区存在与本任务无关的既有缺失：`app/services/learning_stats_service.py` 被删除，导致 `pytest` 在导入 `app.api.routers.practice` 时中断；本次不回退他人改动，只记录为验证阻断项。
验证：
- `python -m py_compile app/api/routers/lessons.py app/schemas/lesson.py app/schemas/__init__.py app/services/lesson_command_service.py app/services/lesson_service.py tests/test_regression_api.py`
- `cd frontend && npm.cmd run build:app-static`
- 直接运行 `pytest tests/test_regression_api.py -q -k "create_lesson_task_and_poll_success or lesson_task_partial_success_and_debug_report or lesson_task_reports_translation_parse_failure_with_explicit_error"` 会在收集阶段被当前工作区既有缺失文件 `app/services/learning_stats_service.py` 阻断。
- 采用“仅在测试进程中临时注入 `app.services.learning_stats_service` stub，不改仓库文件”的方式后，同一组目标测试通过：`3 passed, 94 deselected`。
清理记录：
- 未删除历史任务或衔接文档：未获得文件删除确认，按规则保留。
结束时间：2026-03-20 20:23:14
