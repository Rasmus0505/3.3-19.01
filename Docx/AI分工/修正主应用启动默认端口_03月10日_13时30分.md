文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
- 按当前端口规则，将主应用启动脚本默认端口从 `8000` 修正为 `8080`。
- 保持启动脚本继续优先读取 Zeabur 注入的 `PORT` 环境变量，不改动其他启动逻辑。
修改的文件清单（精确路径）：
- scripts/start.sh
- Docx/AI分工/修正主应用启动默认端口_03月10日_13时30分.md
关联衔接：无
风险：
- 本次仅修正启动脚本默认端口，`Dockerfile` 的 `EXPOSE 8000` 仍保留，仓库里仍存在次要文档不一致。
- 根据文件删除确认规则，本轮不删除我上一个已完成任务文档，只在清理记录中说明原因。
验证：
- 已检查 `scripts/start.sh` 末行，当前为 `exec uvicorn app.main:app --host 0.0.0.0 --port "${PORT:-8080}"`。
- 已尝试执行 `sh -n scripts/start.sh` 做 shell 语法校验，但当前 Windows 环境不存在 `sh` 命令，无法完成该项；本次以单行 diff 静态复核代替。
清理记录：
- 未删除 [补充主应用端口规则_03月10日_13时20分.md](/d:/3.3-19.01/Docx/AI分工/补充主应用端口规则_03月10日_13时20分.md)，原因：虽满足任务文档可删除判定，但“文件删除需经用户确认”未满足。
结束时间：2026-03-10 13:30:53
