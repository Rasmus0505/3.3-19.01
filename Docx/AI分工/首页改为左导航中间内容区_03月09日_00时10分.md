文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
- 将学习首页改为“左侧导航卡 + 中间内容区”结构：左侧固定展示历史记录、上传素材、兑换码充值，中间按所选卡片切换主内容，删除现有右侧栏。
- 删除左侧状态卡和中间非沉浸态沉浸学习卡；历史记录改为带视频封面的大卡片列表，保留原有重命名、删除、恢复视频能力，并支持直接开始/继续学习。
- 上传素材面板移入中间区，优化为“顶部实时反馈 + 总进度条 + 四阶段小卡片（转换/识别/翻译/生成）”；保持现有上传接口契约不变。
- 兑换码充值也改为中间独立面板，沉浸学习页本身逻辑保持不变，仅调整首页进入路径。
修改的文件清单（精确路径）：
- frontend/src/app/LearningShell.jsx
- frontend/src/app/LearningShell.js
- frontend/src/features/lessons/LessonList.jsx
- frontend/src/features/upload/UploadPanel.jsx
- frontend/src/features/wallet/RedeemCodePanel.jsx
- frontend/src/shared/media/localMediaStore.js
- Docx/AI分工/首页改为左导航中间内容区_03月09日_00时10分.md
关联衔接：无
风险：
- 首页骨架重排会同时影响桌面与移动端导航，需要确保沉浸态切换、课程加载、钱包刷新等现有链路不回归。
- 历史记录封面依赖本地媒体 Blob；旧课未绑定本地视频、音频素材或提取失败时需要稳定回退到默认封面。
- 当前环境可能仍缺少 Node/npm，前端构建验证可能需依赖 Zeabur 构建日志补充确认。
验证：
- 已完成静态检查：`frontend/src/app/LearningShell.jsx` 已恢复为左侧“历史记录 / 上传素材 / 兑换码充值”三卡导航 + 中间单内容区，右侧旧副区不再渲染。
- 已完成交互接线：历史记录大卡片支持直接“开始学习 / 继续学习”，上传成功后刷新课程并直接进入沉浸学习，退出沉浸学习后回到历史记录。
- 已完成封面与进度收口：历史记录优先显示本地视频封面，缺失时统一显示渐变默认封面；上传面板保留“顶部实时反馈 + 总进度条 + 四阶段卡片”。
- 已完成本地入口兜底：新增 `frontend/src/app/LearningShell.js` 转发到 `frontend/src/app/LearningShell.jsx`，避免本地模块解析优先命中旧壳层。
- 已完成补丁校验：`git diff --check -- frontend/src/app/LearningShell.jsx frontend/src/app/LearningShell.js frontend/src/features/lessons/LessonList.jsx frontend/src/features/upload/UploadPanel.jsx frontend/src/features/wallet/RedeemCodePanel.jsx frontend/src/shared/media/localMediaStore.js` 通过。
- 未完成构建：当前环境缺少 `node` / `npm`，无法本机执行前端构建；需在 Zeabur 或有 Node 的环境补跑前端构建与手测。
- 未完成远端同步：按流程尝试 `git fetch origin`，但当前环境无法连通 GitHub 443，未能完成拉取更新。
清理记录：
- 本次未删除任何任务/衔接文档；现有已完成任务文档因未获删除确认，暂不清理。
结束时间：2026-03-09 01:02:15
