文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：收敛上传页在 Zeabur 重部署后的旧任务恢复体验，避免页面把已中断的旧任务误显示为仍在运行；在不修改 `/api/transcribe/file`、不触碰当前进行中的 `lessons.py`/`schemas`/`tests/test_regression_api.py` 占用文件前提下，实现前端“先验真再展示”的恢复流程、任务状态卡与重新开始入口，并在服务层补齐重部署后孤儿活任务的收口能力，使旧任务自动转为可恢复态，继续生成优先从安全断点续跑。
修改的文件清单（精确路径）：
- Docx/AI分工/上传页重部署任务恢复收敛_03月19日_11时42分.md
- frontend/src/features/upload/UploadPanel.jsx
- app/services/lesson_command_service.py
- app/services/lesson_task_manager.py
- tests/test_lesson_task_recovery.py
- app/static/index.html
- app/static/assets/*
关联衔接：无
风险：
- `app/services/lesson_command_service.py` 当前工作区已存在既有改动，本次只能在现有基础上叠加，不能回退他人改动。
- 上传页源码与 `app/static` 构建产物需要同步，若只改源码不重建，部署与本地静态页可能表现不一致。
- 孤儿任务收口需要避免误伤当前进程内仍在执行的真实活任务，因此判定必须同时依赖进程启动时间与活动任务注册表。
验证：
- `python -m py_compile app\services\lesson_command_service.py app\services\lesson_task_manager.py tests\test_lesson_task_recovery.py` 通过
- `pytest tests\test_lesson_task_recovery.py -q` 通过，4 项通过
- `frontend\npm.cmd run build:app-static` 通过，前端已重新构建并同步到 `app/static`
清理记录：
- 未删除历史任务/衔接文档：本次未获得文件删除确认，按规则保留现有文档。
结束时间：2026-03-19 13:23:39
