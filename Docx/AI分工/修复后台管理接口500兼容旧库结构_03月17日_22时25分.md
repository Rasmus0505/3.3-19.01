文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：修复后台管理页 `/api/admin/overview`、`/api/admin/redeem-batches`、`/api/admin/redeem-codes` 在 Zeabur 旧数据库结构或迁移未补齐时直接 500 的问题；要求不破坏现有 API 契约，在数据库缺少兑换/翻译相关表或列时返回可读且可用的后台数据，并补充回归测试。
修改的文件清单（精确路径）：
- Docx/AI分工/修复后台管理接口500兼容旧库结构_03月17日_22时25分.md
- app/repositories/admin.py
- app/repositories/admin_console.py
- tests/test_admin_console_api.py
关联衔接：无
风险：如果线上数据库缺失的是未预期的其他业务表或字段，本次保护不会覆盖全部后台模块；优先处理当前报错涉及的 overview 与 redeem 查询链路。
验证：
- 运行 `pytest tests/test_admin_console_api.py -q -k "overview or redeem or missing_tables or legacy_batch"`，4 项通过
- 运行 `python -m py_compile app/repositories/admin.py app/repositories/admin_console.py tests/test_admin_console_api.py` 通过
- 说明：`pytest tests/test_admin_console_api.py -q` 仍有一个既有失败 `test_admin_subtitle_settings_history_and_rollback`（返回 422），与本次后台 500 修复无关，未在本任务内扩散处理
清理记录：本次未删除文件；当前未创建衔接文档，也未满足“开始下一项任务前”的自清理触发条件。
结束时间：2026-03-17 22:43:24
