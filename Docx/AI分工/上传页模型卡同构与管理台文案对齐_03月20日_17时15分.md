文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：按已确认方案收敛上传页生成方式卡片与管理台模型说明：保留上传页 3 张模型卡，但让 `SenseVoice Small` 与 `Faster Whisper Medium` 在前端呈现上共用同一套极简卡片骨架，做到除名字外信息区结构、状态区位置、进度区位置、按钮区位置一致；`Qwen ASR Flash` 继续并列保留；同时把管理台模型页与参数页中对 `SenseVoice` / `Faster Whisper` 的可见说明文案调整为与上传页同口径，不破坏现有上传、准备模型、任务控制与 `/api/transcribe/file` 契约。
修改的文件清单（精确路径）：
- Docx/AI分工/上传页模型卡同构与管理台文案对齐_03月20日_17时15分.md
- frontend/src/features/upload/UploadPanel.jsx
- frontend/src/shared/lib/asrModels.js
- frontend/src/features/admin-pages/AdminModelsPage.jsx
- frontend/src/features/admin-sensevoice-settings/AdminSenseVoiceSettingsTab.jsx
- frontend/src/features/admin-faster-whisper-settings/AdminFasterWhisperSettingsTab.jsx
- app/static/index.html
- app/static/assets/*
- frontend/dist-admin/index.admin.html
- frontend/dist-admin/assets/*
关联衔接：无
风险：
- 上传页模型卡当前在同一渲染循环里按模型类型分叉，若收敛不彻底，容易继续出现前两张卡片结构不一致的问题。
- `SenseVoice` 与 `Faster Whisper` 的准备链路不同，只能统一卡片骨架与信息槽位，不能误改实际准备/卸载逻辑。
- 管理台同时存在主站内嵌管理入口与独立 `dist-admin` 产物，构建时需要分别验证，避免仓库产物不一致。
验证：
- `cd frontend && npm.cmd run build:app-static`
- `cd frontend && npm.cmd run build:admin`
- 手动检查上传页三张模型卡，确认前两张结构一致且 `Qwen` 仍可正常展示
- 手动检查管理台模型页说明文案是否与上传页口径一致
清理记录：
- subagent 占位文档：仅完成只读探索与留痕，未执行代码改动；为避免后续冲突检查误判，收口为已完成并保留文件。
结束时间：2026-03-20 17:23:10
