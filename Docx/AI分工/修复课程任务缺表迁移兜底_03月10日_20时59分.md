文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
- 修复 `app.lesson_generation_tasks` 缺表导致 `DB_MIGRATION_REQUIRED` 持续报错的问题。
- 增加幂等迁移兜底，确保执行 `alembic upgrade head` 后可自动补齐该表与关键列。
- 补充就绪检查与回归测试，保证 `/health/ready` 与课程任务链路一致反映迁移状态。
修改的文件清单（精确路径）：
- Docx/AI分工/修复课程任务缺表迁移兜底_03月10日_20时59分.md
- migrations/versions/20260310_0014_repair_lesson_generation_tasks_table.py
- app/main.py
- tests/test_regression_api.py
关联衔接：无
风险：
- 迁移脚本需同时兼容 PostgreSQL 与 SQLite，避免出现方言差异导致失败。
- 新增列/索引判断要幂等，防止重复执行迁移时报错。
验证：
- 已执行 `python -m py_compile app/main.py migrations/versions/20260310_0014_repair_lesson_generation_tasks_table.py tests/test_regression_api.py`，无报错。
- 已执行 `pytest -q tests/test_regression_api.py -k "subtitle_settings_migration_idempotent_when_table_exists or lesson_generation_tasks_repair_migration_recreates_missing_table"`，结果 `2 passed, 59 deselected`。
- 已执行 `pytest -q tests/test_admin_console_api.py -k "task_table_missing"`，结果 `2 passed, 5 deselected`。
- 已执行 `pytest -q tests/test_regression_api.py -k "health_ready_endpoint"`，结果 `1 passed, 60 deselected`。
清理记录：
- 暂无
结束时间：2026-03-10 21:05:00

