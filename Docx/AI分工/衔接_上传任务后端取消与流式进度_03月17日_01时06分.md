文档类型：衔接
创建者：Codex
状态：待衔接
关联任务：Docx/AI分工/上传页中断恢复与静态产物同步_03月17日_01时06分.md

1) 冲突原因（与哪个任务/文件冲突）
- 与进行中的任务 `Docx/AI分工/管理台四页重构与登录活跃落地_03月16日_18时59分.md` 冲突。
- 该任务已占用本次后端方案必改文件：`app/api/routers/lessons.py`、`app/api/serializers.py`、`app/main.py`、`app/services/lesson_command_service.py`。

2) 本次已完成部分
- 上传页已新增浏览器侧 `upload_paused` 状态：上传中可点“取消上传”，刷新后若发现上次停在 `uploading` 且未创建 `task_id`，会自动恢复为“可继续上传当前素材”。
- 上传百分比已按节流写入 IndexedDB，本浏览器刷新后可恢复文件、封面、时长、进度与提示文案。
- 已新增 `npm run build:app-static`，并完成一次 `frontend/dist -> app/static` 同步；当前 `app/static/index.html` 已引用新 bundle，旧上传页语义分句开关不再来自陈旧静态产物。

3) 暂未完成部分
- `POST /api/lessons/tasks/{task_id}/cancel`
- `GET /api/lessons/tasks/{task_id}/stream`
- `LessonTaskResponse` 扩展 `canceled/cancel_requested/canceled_at`
- 生成线程协作式取消与从 `canceled` 状态 `/resume`

4) 待冲突解除后的补充步骤（可直接执行）
- 扩展任务模型、schema 与任务管理器，加入显式取消状态和取消标记。
- 在 `lessons` 路由新增 cancel 与 stream 接口，前端优先消费 SSE，失败时回退轮询。
- 在生成链路的转换后、分段 ASR 后、翻译批次后、写课前加入取消检查，并保留 artifacts 以支持恢复。
- 上传页处理阶段按钮切换为“停止生成”，接口成功后进入 `canceling/canceled`，并允许从 `canceled` 继续生成。

5) 建议修改文件清单（冲突解除后）
- app/api/routers/lessons.py
- app/api/serializers.py
- app/main.py
- app/models/lesson.py
- app/schemas/lesson.py
- app/services/lesson_command_service.py
- app/services/lesson_task_manager.py
- tests/test_regression_api.py
