文档类型：任务
创建者：Codex(ai-3)
状态：已完成
完整任务目标：
- 沉浸学习输入框常驻可输入：点击页面任意位置后无需重新点输入框。
- 播放中可提前输入：不再等待句子播放结束才允许输入。
- 保持“输入完成后等待本句播放结束再切下一句”，播放失败时不阻塞切句。
修改的文件清单（精确路径）：
- frontend/src/features/immersive/ImmersiveLessonPage.jsx
关联衔接：无
风险：
- 当前环境缺少 Node/Git 可执行文件，无法本地执行 `npm run build` 与提交验证。
验证：
- 已实现输入门控解耦：输入焦点与 `readOnly` 不再依赖 `phase===typing`，改为独立 `typingEnabled`。
- 已实现播放中输入：字符输入逻辑改为按 `typingEnabled` 放行，播放阶段可直接输入。
- 已实现双门控切句：引入 `sentenceTypingDone/sentencePlaybackDone/sentencePlaybackRequired` 与防重入锁，确保“输入完成后等待播放结束再切句”。
- 已实现失败兜底：播放失败/不可播放时置 `sentencePlaybackRequired=false`，输入完成即可前进。
- 已实现轻量提示：输入已完成但仍等待播放结束时显示提示文案。
- 已完成静态自检：无残留 `phase !== "typing"` 限制、无残留 `[DEBUG]` 日志、新增变量引用完整。
清理记录：
- 本次未删除文档文件（无“已消解衔接 md”与满足可删除条件的历史任务 md）。
结束时间：2026-03-05 00:29
