文档类型：衔接
创建者：Codex
状态：已消解
关联任务：Docx/AI分工/上传页均衡模式本地自动并行提速_03月19日_23时18分.md
1) 冲突原因（与哪个任务/文件冲突）
- 当前存在进行中的任务 `Docx/AI分工/修复Whisper翻译阶段非法JSON报错_03月19日_22时29分.md`。
- 冲突文件：
  - `frontend/src/features/upload/UploadPanel.jsx`
- 本次并行提速最终接入上传页必须修改上述文件，按协作规则禁止与进行中任务重叠修改。
2) 本次已完成部分
- 新增 `frontend/src/features/upload/localAsrParallelRuntime.js`，实现上传页本地 SenseVoice 的自动并行基础能力。
- 已实现能力包括：
  - 根据 `navigator.hardwareConcurrency` 和音频时长自动计算并行度。
  - 基于预处理后的 `Float32Array` 切段，不重复解码，不引入额外 ffmpeg。
  - 使用多个 `localAsrPreviewWorker.js` 实例组成 worker 池并发识别。
  - 固定 `800ms` overlap，并按 keep range 做全局时间偏移、过滤和回拼。
  - 回拼后统一重建全局句子，保证时间戳单调递增，避免句子首尾漂移。
  - 暴露 `runLocalAsrWithAutoParallelism(...)`、`computeLocalAsrConcurrency(...)`、`buildLocalAsrSegmentPlan(...)`。
3) 暂未完成部分
- 无。相关接入已在后续任务中完成：`UploadPanel.jsx` 已改为调用 `runLocalAsrWithAutoParallelism(...)`，本地识别阶段进度已切换为优先展示 `completedSegments/segmentCount`，停止生成继续通过 `AbortController` 中断并行运行时。
4) 待冲突解除后的补充步骤（可直接执行）
- 在 `submitBalanced(...)` 中，用 `runLocalAsrWithAutoParallelism(...)` 替换当前单 worker `createWorkerRequest("transcribe-audio", ...)` 调用。
- 将上传页本地识别阶段的可视进度从“按时长估算”改为优先展示并行运行时回调返回的 `completedSegments/segmentCount`。
- 停止生成时，确保将 `AbortController` 传入并行运行时，使 worker 池整体终止。
- 保持提交到 `/api/lessons/tasks/local-asr` 的 `asr_payload` 结构不变。
5) 建议修改文件清单（冲突解除后）
- frontend/src/features/upload/UploadPanel.jsx
消解任务：Docx/AI分工/完成未消解衔接任务_03月20日_18时02分.md
消解时间：2026-03-20 18:02:38
