文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
- 修复线上历史记录卡片右上角“三个点”点击无响应问题，确保菜单可稳定打开并执行重命名/恢复视频/删除操作。
- 保持后端 API 契约不变（含 `/api/transcribe/file`），仅修改前端交互和前端发布产物同步。
修改的文件清单（精确路径）：
- Docx/AI分工/修复历史记录三点菜单无响应_03月09日_18时43分.md
- frontend/src/components/ui/button.jsx
- frontend/src/features/lessons/LessonList.jsx
- app/static/index.html
- app/static/assets/index-BV6nYCY9.css
- app/static/assets/index-hFKMTx2x.js
关联衔接：无
风险：
- 若仅改 `frontend/src` 但未同步 `app/static`，Zeabur 线上仍可能继续加载旧包。
验证：
- 已执行前端构建：`cd frontend && npm run build` 通过。
- 已同步前端产物到服务静态目录：`frontend/dist -> app/static`，`app/static/index.html` 已指向最新 hash（`index-hFKMTx2x.js` / `index-BV6nYCY9.css`）。
- 已执行接口回归：`pytest -q tests/test_regression_api.py -k "health_endpoint or transcribe_file_endpoint_with_stubbed_service"`，结果 `2 passed`。
- 已执行补充回归：`pytest -q tests/test_regression_api.py -k "health_endpoint or create_lesson_endpoint_with_stubbed_service"`，结果 `2 passed`。
- 已完成并行冲突检查：`Docx/AI分工` 中 `状态=进行中` 任务仅占用 `frontend/src/features/immersive/*`，与本任务文件清单不重叠。
清理记录：
- 未删除任何文档：当前存在他人 `进行中/待衔接` 文档，不满足删除条件。
- 已按你确认忽略非本任务既有改动并继续实施（含沉浸页相关文件与文档改动）。
结束时间：2026-03-09 18:49:15
