文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
- 修复 admin-web 打开后仍显示学习产品页的问题，确保独立 admin 构建入口加载 `main-admin.jsx`。
- 仅调整前端构建入口，不改后端 API 契约。
修改的文件清单（精确路径）：
- Docx/AI分工/修复admin-web构建入口错误_03月06日_22时13分.md
- frontend/index.admin.html
- frontend/vite.admin.config.js
- admin-web/Dockerfile
关联衔接：无
风险：
- 若 Zeabur 未拉取最新代码或未重新构建 admin-web，修复不会生效。
验证：
- 已完成静态检查：admin 构建入口改为 `index.admin.html -> /src/main-admin.jsx`。
- 已完成静态检查：`vite.admin.config.js` 已指定 `rollupOptions.input=index.admin.html`。
- 已完成静态检查：Docker 构建后会将 `index.admin.html` 兼容改名为 `index.html`。
- 未完成构建验证：当前环境缺少 `npm`，无法本地执行 `npm run build:admin`。
清理记录：
- 本次未删除文档文件（当前仅新增本任务文档，且未进入下一项任务清理阶段）。
结束时间：2026-03-06 22:14:22
