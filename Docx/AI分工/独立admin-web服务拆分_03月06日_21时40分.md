文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
- 落地独立 `admin-web` 服务：管理员不再依赖学习站右上角按钮，直接访问独立域名使用后台。
- 仅拆前端服务，后端继续复用现有 `/api/auth/*` 与 `/api/admin/*`，保持管理员账号+`ADMIN_EMAILS` 机制不变。
- 支持 `VITE_API_BASE_URL` 配置后端地址，默认走 `/api` 并可通过反向代理转发到主 `web` 服务。
修改的文件清单（精确路径）：
- Docx/AI分工/独立admin-web服务拆分_03月06日_21时40分.md
- frontend/package.json
- frontend/vite.admin.config.js
- frontend/src/app/bootstrap-admin.jsx
- frontend/src/app/AdminShellStandalone.jsx
- frontend/src/main-admin.jsx
- frontend/src/shared/api/adminClient.js
- admin-web/Dockerfile
- admin-web/nginx.conf.template
- docs/ADMIN_WEB_DEPLOY.md
关联衔接：无
风险：
- 独立 admin 域名与主 web 域名跨域时，若未走 `/api` 反向代理或未配 CORS，会出现浏览器拦截。
- `VITE_API_BASE_URL` 为构建时变量，若部署后需改地址应重新构建或使用反向代理路径。
验证：
- 已完成静态检查：独立 admin 入口、独立构建脚本、Nginx `/api` 反向代理模板与部署文档已落地。
- 已执行 `rg -n` 关键字校验：`build:admin`、`BootstrapAdminApp`、`VITE_API_BASE_URL`、`UPSTREAM_API_BASE_URL` 均可检索。
- 未完成构建验证：当前环境缺少 `npm` 命令，无法本地执行 `npm run build:admin`。
清理记录：
- 本次未删除文档文件（当前仅新增本任务文档，且未进入下一项任务清理阶段）。
结束时间：2026-03-06 21:45:17
