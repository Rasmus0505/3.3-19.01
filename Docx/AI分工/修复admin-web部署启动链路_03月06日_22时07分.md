文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
- 修复 admin-web 持续部署失败问题，将 Nginx 模板渲染从隐式入口改为显式启动命令，确保 `PORT` 与 `UPSTREAM_API_BASE_URL` 一定在启动时注入生效。
- 保持独立 admin-web 架构不变，不改后端 API 契约。
修改的文件清单（精确路径）：
- Docx/AI分工/修复admin-web部署启动链路_03月06日_22时07分.md
- admin-web/Dockerfile
关联衔接：无
风险：
- 若 Zeabur 未使用仓库最新提交构建，修复不会生效。
- 若 `UPSTREAM_API_BASE_URL` 填写错误（带 /api 或尾斜杠），仍会代理失败。
验证：
- 已完成静态检查：Dockerfile 已包含 `ENV PORT`、`ENV UPSTREAM_API_BASE_URL`、`EXPOSE 8080`、显式 `envsubst` 与 `nginx` 启动命令。
清理记录：
- 本次未删除文档文件（当前仅新增本任务文档，且未进入下一项任务清理阶段）。
结束时间：2026-03-06 22:08:19
