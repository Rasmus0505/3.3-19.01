文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
- 将历史记录课程卡片右上角三点菜单改为贴近按钮显示的悬浮小卡片，不再在卡片下方展开操作区。
- 为历史记录中的“重新生成字幕”增加流式反馈，在切换普通分句/语义分句时展示阶段进度，并在异常时回退到现有非流式接口。
- 保持现有 `/api/transcribe/file` 与 `/api/lessons/{lesson_id}/subtitle-variants` 契约不变，仅新增流式配套能力与前端状态展示。
修改的文件清单（精确路径）：
- Docx/AI分工/历史记录菜单悬浮与字幕流式反馈_03月09日_15时43分.md
- app/api/routers/lessons.py
- app/schemas/__init__.py
- app/schemas/lesson.py
- app/services/lesson_service.py
- frontend/src/app/LearningShell.jsx
- frontend/src/features/lessons/LessonList.jsx
- tests/test_regression_api.py
关联衔接：无
风险：
- 历史记录菜单与字幕弹窗都在正式前端文件内，需避免回退现有课程卡片布局和本地字幕缓存逻辑。
- 流式接口需要保持出错可降级，避免因为 SSE 失败导致字幕重切不可用。
- 仅允许删除自己创建且满足条件的文档；本轮如需清理历史任务文档，仍受文件删除确认约束。
验证：
- 已执行：`python -m py_compile .\app\api\routers\lessons.py .\app\schemas\lesson.py .\app\schemas\__init__.py .\app\services\lesson_service.py .\tests\test_regression_api.py`
- 已执行：`pytest .\tests\test_regression_api.py -k "health or regenerate_lesson_subtitle_variant"`
- 已执行：`npm.cmd -C frontend run build`
清理记录：
- 未删除任何历史任务文档或衔接文档；按仓库规则这些删除动作仍受“文件删除需确认”约束，本轮只清理了运行验证产生的非跟踪构建噪音影响。
结束时间：2026-03-09 15:59:51
