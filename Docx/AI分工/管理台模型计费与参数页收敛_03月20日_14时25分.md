文档类型：任务
创建者：AI 助手
状态：已完成
完整任务目标：
在不破坏现有 API 契约的前提下，收敛管理台模型页的计费与运行参数配置入口：修复计费表横向不可滚动与保存路径不清晰的问题；下线“默认策略”独立编辑页，只保留后台固定默认策略能力；补充 SenseVoice / Faster Whisper 参数页的使用提示，并确保整体布局仍然以 tab 为主。
修改的文件清单（精确路径）：
- frontend/src/features/admin-rates/AdminRatesTab.jsx
- frontend/src/features/admin-pages/AdminModelsPage.jsx
- frontend/src/features/admin-workspaces/AdminPipelineWorkspace.jsx
- frontend/src/features/admin-workspaces/AdminMonitoringWorkspace.jsx
- frontend/src/AdminApp.jsx
关联衔接：无
风险：
- 计费表保存逻辑调整不当，可能导致管理员误以为已保存或重复提交。
- 删除“默认策略” tab 及相关入口时，如处理不好路由降级，旧链接可能出现空白或跳转不符合预期。
验证：
- 管理员登录后访问 /admin/models?tab=billing，确认表格可以横向滚动看到最右侧“操作”列，并验证逐行保存和“保存全部更改”都能正常工作且无报错。
- 访问 /admin/models，确认不再显示“默认策略” tab，旧路由 /admin/subtitle-settings 或 tab=strategy 能自动跳转到计费页。
- 访问 Admin 管控工作台相关入口（AdminPipelineWorkspace、AdminMonitoringWorkspace），确认不再有“字幕策略/策略配置”卡片或 tab 指向原来的 AdminSubtitleSettingsTab，其他功能不受影响。
- 本地在 frontend 目录执行 `npm run build`，确认前端能正常构建通过。
清理记录：
- 本次任务未新增额外调试日志；已在任务结束前完成前端构建，生成的静态资源由后续部署流程接管。
结束时间：2026-03-20 15:45

