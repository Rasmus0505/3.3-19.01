文档类型：衔接
创建者：Codex
状态：待衔接
关联任务：Docx/AI分工/试玩卡本地音频预处理提速_03月18日_15时56分.md
1) 冲突原因（与哪个任务/文件冲突）
- 与 `Docx/AI分工/上传页步骤进度与识别文案优化_03月18日_15时38分.md` 冲突。
- 冲突文件：
  - `frontend/src/features/upload/UploadPanel.jsx`
  - `frontend/src/features/upload/localAsrPreviewWorker.js`
- 该任务当前状态为“进行中”，按协作规则禁止重叠修改上述文件。
2) 本次已完成部分
- 新增 `frontend/src/features/upload/localAsrAudioPreprocess.js`，把本地试玩用的音频预处理封装为共享模块，优先走 `OfflineAudioContext` 原生异步重采样，缺失时回退到原 JS 方案。
- 更新 `frontend/src/features/upload/LocalAsrPreviewCard.jsx`，接入共享预处理模块，补充 `[DEBUG]` 级别的 `decode_ms`、`resample_ms`、`worker_decode_ms`、`postprocess_ms`、`total_local_asr_ms` 计时日志。
- 为试玩卡增加长音频非阻塞提示：超过 8 分钟时展示 “WASM 模式会明显较慢，更建议改用高速模式”。
3) 暂未完成部分
- 上传页均衡模式尚未接入 `localAsrAudioPreprocess.js`。
- 上传页均衡模式尚未补齐同口径的长音频提示与本地识别计时日志。
- `localAsrPreviewWorker.js` 尚未补内部 decode/postprocess 级别的更细日志。
4) 待冲突解除后的补充步骤（可直接执行）
- 在 `frontend/src/features/upload/UploadPanel.jsx` 中移除旧的 `mixAudioBufferToMono` / `resampleFloat32` / `decodeFileForLocalAsr` 预处理逻辑，改为调用 `preprocessLocalAsrFile(...)`。
- 在上传页均衡模式里沿用本次试玩卡的计时口径，输出 `[DEBUG]` 的 `audio_extract_ms`、`decode_ms`、`resample_ms`、`worker_decode_ms`、`postprocess_ms`、`total_local_asr_ms`。
- 在上传页均衡模式里增加超过 8 分钟的非阻塞性能提示，不改变现有按钮与接口契约。
- 如需更细分内部耗时，再在 `frontend/src/features/upload/localAsrPreviewWorker.js` 中补 worker 内部的 decode/postprocess 日志，但不要改返回字段结构。
5) 建议修改文件清单（冲突解除后）
- frontend/src/features/upload/UploadPanel.jsx
- frontend/src/features/upload/localAsrPreviewWorker.js
