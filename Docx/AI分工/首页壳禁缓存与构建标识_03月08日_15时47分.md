文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
- 修复主站首次打开偶发命中旧前端壳、刷新后恢复的问题。
- 仅对 `/`、`/admin`、`/admin/*` 返回的 HTML 壳增加禁缓存策略，并补充可追踪的构建标识响应头。
- 保持 `/static/assets/*` 等带 hash 静态资源缓存策略不变，不修改业务 API。
修改的文件清单（精确路径）：
- app/main.py
- tests/test_regression_api.py
- Docx/AI分工/首页壳禁缓存与构建标识_03月08日_15时47分.md
关联衔接：无
风险：
- 若根页缓存头设置过严，会让首页 HTML 每次都重新获取；但静态资源仍保留 hash 缓存，整体影响可控。
- 仓库中存在我上一个任务留下的已完成任务文档 `Docx/AI分工/上传极简色条进度恢复_03月08日_15时04分.md`，按“文件删除确认”要求，本次未删除。
验证：
- 已完成：回归 `python -m pytest -q tests/test_regression_api.py -k 'test_health_endpoint or test_health_ready_endpoint or test_spa_shell_pages_disable_html_cache_and_expose_build_marker or test_static_assets_keep_cache_behavior_unmodified or test_transcribe_file_endpoint_with_stubbed_service or test_create_lesson_task_and_poll_success'`，结果 6 passed。
- 已完成：`/`、`/admin`、`/admin/users` 均验证包含 `Cache-Control: no-store...`、`Pragma: no-cache`、`Expires: 0` 与 `X-Frontend-Build`。
- 已完成：`/static/assets/<hash>.js` 未附带 HTML 壳禁缓存头，静态资源缓存行为保持不变。
清理记录：
- 本次未删除任何文件；上一个已完成任务文档因未获删除确认，暂保留。
结束时间：2026-03-08 15:50
