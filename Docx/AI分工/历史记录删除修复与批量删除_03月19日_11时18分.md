文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：修复主产品历史记录页“单条删除后列表不刷新”的问题，并在不破坏现有单条删除接口契约、学习页主流程和 `/api/transcribe/file` 的前提下，为历史记录页增加常驻复选框、多选、全选全部历史和批量删除能力；若批量删除包含当前课程，删除后直接回空状态。
修改的文件清单（精确路径）：
- Docx/AI分工/历史记录删除修复与批量删除_03月19日_11时18分.md
- frontend/src/app/learning-shell/LearningShellContainer.jsx
- frontend/src/app/learning-shell/LearningShellPanelContent.jsx
- frontend/src/features/lessons/LessonList.jsx
- frontend/src/store/slices/lessonSlice.js
- app/api/routers/lessons.py
- app/schemas/__init__.py
- app/schemas/lesson.py
- app/services/lesson_command_service.py
- app/static/index.html
- app/static/assets/*
- tests/test_regression_api.py
关联衔接：无
风险：
- 历史记录删除链路分散在历史页 UI、store、容器层和本地媒体/字幕缓存清理四层，若状态更新不统一，容易再次出现“接口成功但 UI 不刷新”。
- “全选全部历史”需要后端补批量删除接口；若只在前端按已加载列表循环删除，会与已确认的产品语义不一致。
- 常驻复选框会提升管理感，需要避免影响既有“开始学习 / 修改标题 / 恢复本地视频”的点击区域和移动端布局。
验证：
- `python -m py_compile app\api\routers\lessons.py app\services\lesson_command_service.py app\schemas\lesson.py app\schemas\__init__.py tests\test_regression_api.py` 通过
- `pytest tests\test_regression_api.py -q -k "lesson_rename_and_delete_endpoints or delete_lesson_clears_wallet_ledger_reference or bulk_delete_lessons_by_ids_keeps_other_users_data or bulk_delete_all_lessons_only_removes_current_user_history"` 通过，4 项通过
- `frontend\npm.cmd run build:app-static` 通过，前端已重新构建并同步到 `app/static`
清理记录：
- 未删除历史任务/衔接文档：本次未获得文件删除确认，按规则保留现有文档。
结束时间：2026-03-19 11:38:25
