文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：
- 将学习端与后台管理台统一改造成接近 `shadcn/ui sidebar-09` 的左侧侧边栏壳层：桌面端可折叠、移动端左侧抽屉、内容区保留轻量头部。
- 学习端把“历史记录 / 上传素材 / 兑换码充值”从页内切换改为 `/`、`/upload`、`/redeem` 独立路由，但继续复用现有业务面板与 API 契约。
- 后台管理台将原顶部工作台与内容区内 Tabs 改为双层 sidebar 导航，同时保持 `/admin/*`、`tab` 查询参数与 legacy redirect 兼容。
- 避开进行中的沉浸页任务文件 `frontend/src/features/immersive/ImmersiveLessonPage.jsx`、`frontend/src/features/immersive/immersive.css`，仅在外层壳层处理沉浸态显隐。
修改的文件清单（精确路径）：
- Docx/AI分工/全站统一sidebar09壳层改造_03月10日_21时01分.md
- frontend/src/components/ui/sidebar.jsx
- frontend/src/shared/ui/index.js
- frontend/src/app/bootstrap.jsx
- frontend/src/app/LearningShell.jsx
- frontend/src/AdminApp.jsx
- frontend/src/features/admin-workspaces/AdminOpsWorkspace.jsx
- frontend/src/features/admin-workspaces/AdminPipelineWorkspace.jsx
- frontend/src/features/admin-workspaces/AdminUsersWorkspace.jsx
- frontend/src/features/admin-workspaces/AdminRedeemWorkspace.jsx
关联衔接：无
风险：
- `git fetch origin` 已按规则尝试，但当前环境访问 GitHub 时出现 `Recv failure: Connection was reset`，无法完成远端同步确认。
- 学习端壳层文件较大，若路由态与现有本地状态同步不完整，可能出现 sidebar 高亮或沉浸退出回落错误。
- 后台二级 sidebar 需要同时兼容旧 `tab` 深链，若映射不完整可能出现旧链接落错页。
验证：
- 已执行 `npm --prefix frontend run build`，通过。
- 已执行 `npm --prefix frontend run build:admin`，通过。
- 已执行 `pytest -q tests/test_regression_api.py -k "health_endpoint or transcribe_file_endpoint_with_stubbed_service"`，结果 `2 passed`。
清理记录：
- 已自动清理本轮未跟踪的 `frontend/dist/` 构建产物。
- 尝试清理 `frontend/dist-admin/` 时发现其为仓库受版本控制目录，已立即执行 `git checkout -- frontend/dist-admin` 恢复，未删除任何受版本控制文件。
结束时间：2026-03-10 21:29:18
