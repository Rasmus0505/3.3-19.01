文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：修复前端学习页在 Zeabur 部署环境下刷新 `/upload` 等前端子路由时直接落到 FastAPI 并返回 `{"detail":"Not Found"}` 的问题；补充 SPA 路由回退逻辑，同时保持 `/api/*`、`/health*`、静态资源请求与现有 API 契约不变。
修改的文件清单（精确路径）：
- Docx/AI分工/修复刷新上传页404_03月18日_14时53分.md
- app/main.py
- tests/test_spa_route_fallback.py
关联衔接：无
风险：若 SPA 回退范围判断过宽，可能误吞后端 404 或静态资源请求；需要用回归测试锁定 `/upload` 可刷新且 `/api/does-not-exist` 仍保持原始 404。
验证：
- 运行 `pytest tests/test_spa_route_fallback.py -q` 通过（3 项通过）
- 运行 `python -m py_compile app/main.py tests/test_spa_route_fallback.py` 通过
清理记录：
- 本次未删除文档：当前任务文档刚完成，按单目录协作流程保留，待后续任务开始前再按“可删除判定”统一清理。
结束时间：2026-03-18 14:56:31
