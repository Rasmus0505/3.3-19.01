文档类型：任务
创建者：Codex
状态：已完成
完整任务目标：修复 Zeabur 主应用启动链路对 `AUTO_MIGRATE_ON_START=false/no/off` 的布尔误判，避免误执行迁移 bootstrap 后以非零退出码阻断 uvicorn 启动并触发 502；同步补齐独立测试与部署说明，保持 `/health`、`/health/ready`、`/api/transcribe/file` 契约不变。
修改的文件清单（精确路径）：
- Docx/AI分工/修复启动链路布尔误判502_03月17日_18时23分.md
- scripts/start.sh
- app/db/migration_bootstrap.py
- tests/test_start_script_smoke.py
- README.md
- ops/zeabur-minimal-deploy.md
关联衔接：无
风险：
- 启动链路涉及 shell 与 Python 双层布尔解析，若语义不一致，可能仍会出现 Zeabur 环境变量取值和本地行为不一致。
- 本轮不能改动 `app/main.py` 与 `tests/test_regression_api.py`，需要通过独立 smoke 测试覆盖“关闭迁移仍能启动”的回归。
- 当前 Zeabur 线上服务仍需用户或平台侧把 `AUTO_MIGRATE_ON_START` 改为 `0` 并 Redeploy；本地代码修复无法直接改线上现有实例。
验证：
- 已执行 `python -m py_compile app/db/migration_bootstrap.py`，通过。
- 已执行 `python -m pytest -q tests/test_start_script_smoke.py`，结果 `7 passed, 7 skipped`；新增覆盖 `AUTO_MIGRATE_ON_START=0/false/no/off` 时启动不被阻断，以及迁移真失败时 `AUTO_MIGRATE_CONTINUE_ON_FAILURE` 的退出码行为。
- 已执行 `git diff --check -- scripts/start.sh app/db/migration_bootstrap.py tests/test_start_script_smoke.py README.md ops/zeabur-minimal-deploy.md Docx/AI分工/修复启动链路布尔误判502_03月17日_18时23分.md`，无 diff 语法错误，仅有既有 LF/CRLF 提示。
清理记录：
- 未删除任何文档：本轮没有新增“已消解衔接 md”，且按“文件删除需经过用户确认”规则保留任务留痕。
结束时间：2026-03-17 18:31:20
