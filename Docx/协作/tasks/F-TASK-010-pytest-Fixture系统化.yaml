schema_version: 1
task_id: TASK-010
title: pytest Fixture系统化
status: done
priority: medium
project_root: D:/3.3-19.01
base_ref: main
goal: >
  在 tests/fixtures/ 下建立系统的 pytest fixture：
  auth.py（认证 Fixture）、db.py（数据库 Fixture）、
  lessons.py（课程 Fixture）、billing.py（计费 Fixture）。
  分离测试数据的创建和清理逻辑。
scope:
  - pytest fixture
  - 测试数据管理
allowed_paths:
  - tests/fixtures/
  - tests/conftest.py
acceptance:
  - tests/fixtures/auth.py 存在 authenticated_client、test_user 等 fixture
  - tests/fixtures/db.py 存在 db_session、db_engine 等 fixture
  - tests/fixtures/lessons.py 存在 test_lesson、test_sentence 等 fixture
  - tests/fixtures/billing.py 存在 test_wallet、test_redeem_code 等 fixture
  - conftest.py 正确导入所有 fixtures
  - pytest 运行无新增错误
check_commands:
  - pytest --fixtures 2>&1 | Select-Object -First 30
depends_on:
  - TASK-009
dependency_reason: 测试目录结构重组（TASK-009）完成后才有 fixtures 目录基础
blocked_by: []
conflicts_with: []
notes_for_after: |
  fixture 使用 scope 控制生命周期（function/session/module）。
  数据库 fixture 使用 transaction rollback 模式，确保测试隔离。
  auth fixture 支持普通用户和管理员两种。
  billing fixture 不依赖真实支付 API。
result_summary: |
  pytest Fixture 系统化完成：
  - tests/fixtures/db.py: db_engine、db_session（含 transaction rollback 隔离）
  - tests/fixtures/auth.py: test_user、admin_user、authenticated_client（含延迟导入绕过循环依赖）
  - tests/fixtures/lessons.py: test_lesson、test_lesson_with_sentences、test_sentence
  - tests/fixtures/billing.py: test_wallet、test_billing_rate、test_redeem_batch、test_redeem_code
  - tests/fixtures/__init__.py: 统一导出
  - tests/conftest.py: 导入所有 fixtures
  - pytest --fixtures 验证通过，test_user、db_session、test_lesson、test_wallet 均可见
  - 同时修复了 dashscope.py 的顶层模型初始化循环导入问题
changed_files:
  - tests/fixtures/db.py (新增)
  - tests/fixtures/auth.py (新增)
  - tests/fixtures/lessons.py (新增)
  - tests/fixtures/billing.py (新增)
  - tests/fixtures/__init__.py (新增)
  - tests/conftest.py (新增)
  - app/infra/asr/dashscope.py (修复顶层循环导入)
tests_run:
  - pytest --fixtures 2>&1 | Select-String "test_user|db_session|test_lesson|test_wallet" → 通过
blocked_reason: ""
completed_at: 2026-03-24T21:00:00+08:00
fix_request: ""
fix_reason: ""
fix_round: 0
fix_result_summary: ""
fix_updated_at: ""
