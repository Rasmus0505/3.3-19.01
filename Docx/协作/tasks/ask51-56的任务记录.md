# TASK-051~056 任务行为记录

## TASK-051 — 清理废弃后端路由与服务

**状态:** F（已完成）

**改动文件:**

- `app/api/routers/desktop_asr.py` — 删除
- `app/api/routers/local_asr_assets.py` — 删除
- `app/api/routers/dashscope_upload.py` — 删除
- `app/services/media_url_import.py` — 删除
- `app/services/faster_whisper_asr.py` — 删除
- `app/infra/asr/faster_whisper.py` — 删除
- `app/services/asr_model_registry.py` — 移除 Faster-Whisper 模型常量，清理 BILLING_MODEL_ORDER
- `app/main.py` — 移除废弃 router 注册、lifespan 清理、FAST 等常量
- `app/infra/asr/__init__.py` — 移除 FasterWhisperASRProvider 导入
- `app/infra/__init__.py` — 移除 FasterWhisperASRProvider 导入
- `app/services/billing.py` — 移除 FASTER_WHISPER_ASR_MODEL 及相关计费配置
- `app/services/lesson_command_service.py` — 移除 Faster-Whisper 相关导入和逻辑
- `app/services/lesson_service.py` — 移除 _FASTER_WHISPER_* 常量和方法
- `app/api/routers/admin/router.py` — 移除 Faster-Whisper 设置 API（8个端点）
- `app/api/routers/admin.py` — 移除 Faster-Whisper 端点
- `app/api/serializers.py` — 移除 FasterWhisperSetting/to_faster_whisper_settings_item
- `app/core/config.py` — 移除 FASTER_WHISPER_* 配置常量

**关键逻辑:** 级联删除 Faster-Whisper 相关代码后，通过逐步运行 `python -c "from app.main import create_app"` 定位每个 ImportError，逐个修复。

**数据库迁移:** 无

**验证命令及结果:**

- `python -c "from app.main import create_app; print('OK')"` → OK

---

## TASK-052 — 清理废弃前端代码

**状态:** F（已完成）

**改动文件:**

- `frontend/src/features/upload/localAsrParallelRuntime.js` — 删除
- `frontend/src/features/upload/localAsrPreviewWorker.js` — 删除
- `frontend/src/features/upload/localAsrAudioPreprocess.js` — 删除
- `frontend/src/shared/media/localAsrAssetManager.js` — 删除
- `frontend/src/shared/media/localAsrPreviewStore.js` — 删除
- `frontend/src/features/upload/LocalAsrPreviewCard.jsx` — 删除
- `frontend/src/shared/lib/asrModels.js` — 替换为仅包含 qwen 模型配置
- `frontend/src/features/upload/UploadPanel.jsx` — 移除 browser_local 模式相关代码，保留 desktop_local 和 fast(STS) 模式

**关键逻辑:** UploadPanel.jsx 中移除所有 `browser_local` runtime 逻辑，清理 `localAsrAssetManager`、`localAsrAudioPreprocess`、`localAsrParallelRuntime` 的引用。

**数据库迁移:** 无

---

## TASK-053 — 新建 Electron 桌面端项目骨架

**状态:** F（已完成）

**改动文件:**

- `Desktop/package.json` — Electron + React + Vite + electron-builder 依赖，scripts(build/dev/package:win)
- `Desktop/electron-vite.config.ts` — electron-vite 配置（main/preload/renderer 三进程，outDir 分离）
- `Desktop/electron-builder.json` — electron-builder 打包配置（out 目录 + asarUnpack）
- `Desktop/tsconfig.json` — TypeScript 配置（ES2022, React JSX）
- `Desktop/tailwind.config.js` — Tailwind CSS 配置（content 路径）
- `Desktop/electron/main.ts` — Electron 主进程（窗口管理/IPC/菜单/自动更新）
- `Desktop/electron/preload.ts` — 预加载脚本（contextBridge 安全 API）
- `Desktop/src/App.tsx` — React 应用入口（带 header 和开发模式标识）
- `Desktop/src/main.tsx` — React DOM 入口
- `Desktop/src/index.css` — Tailwind CSS 基础样式
- `Desktop/index.html` — HTML 入口
- `Desktop/.env.example` — 环境变量模板（VITE_API_BASE_URL）
- `Desktop/.gitignore` — Node.js/Electron gitignore
- `Desktop/build/.gitkeep` — 打包资源目录占位

**关键逻辑:**

- 解决了 electron-vite 配置问题：必须有 `--config electron-vite.config.ts` 参数才能正确加载配置
- main/preload 分离输出到不同 outDir（out/main 和 out/preload），避免相互覆盖
- preload 输出为 `.mjs`（ESM），main.ts 中的 preload 路径需对应 `../preload/preload.mjs`
- NODE_PATH 指向 `app.asar.unpacked/node_modules` 以便打包后子进程能访问 sherpa-onnx-node

**数据库迁移:** 无

**验证命令及结果:**

- `npm run build` → 成功（electron-vite build）
- `npm run package:win:dir` → 成功（electron-builder --win --dir），`release/win-unpacked/Bottle Desktop.exe` 生成

---

## TASK-054 — 桌面端集成本地 ASR 推理

**状态:** F（已完成）

**改动文件:**

- `Desktop/addon/sherpa-onnx/asr_offline_process.js` — 独立 Node.js 子进程，Sherpa-ONNX SenseVoice 模型，JSON-RPC over stdin/stdout，支持 init/transcribe/healthcheck
- `Desktop/src/features/asr/asrProcess.ts` — Electron 主进程子进程管理器（spawn/JSON-RPC 通信/崩溃重启/最多3次/IPC handlers）
- `Desktop/src/features/asr/sherpaAsr.ts` — 高层封装（ffmpeg 音频格式转换/进度回调/transcribeAudio）
- `Desktop/src/features/asr/asrModels.ts` — ASR 模型配置（sense-voice-local）
- `Desktop/src/features/upload/DesktopUploadPanel.tsx` — 桌面端上传面板（文件选择/本地识别/发送到服务器）
- `Desktop/electron/main.ts` — 导入并注册 ASR IPC handlers
- `Desktop/electron/preload.ts` — 暴露 asrStart/asrTranscribe/asrHealthcheck/asrStop/onAsrProgress API
- `Desktop/electron-builder.json` — asarUnpack 配置（addon/models/sherpa-onnx-node）
- `app/services/asr_model_registry.py` — 添加 SENSE_VOICE_LOCAL_MODEL 及 get_asr_model_status/get_asr_display_meta
- `app/api/routers/auth.py` — 添加 /api/auth/desktop-token-login 端点
- `app/schemas/auth.py` — 添加 DesktopTokenLoginRequest/DesktopTokenLoginResponse/AuthResponse/LogoutResponse/RefreshRequest

**关键逻辑:**

- Sherpa-ONNX SenseVoice 模型配置：sampleRate=16000, featureDim=80, useInverseTextNormalization=1, numThreads=2, provider=cpu
- 子进程脚本路径（打包后）：`resources/app.asar.unpacked/addon/sherpa-onnx/asr_offline_process.js`
- NODE_PATH（打包后）：`resources/app.asar.unpacked/node_modules`（包含 sherpa-onnx-node）

**数据库迁移:** 无

**验证命令及结果:**

- `npm run build` → 成功
- `npm run package:win:dir` → 成功，resources/app.asar.unpacked/addon/ 和 sherpa-onnx-node/ 正确打包

---

## TASK-055 — 实现 Bottle 2.0 云端直传架构（修订版）

**状态:** F（已完成，2026-03-25 修订：移除 OSS/STS 依赖）

**改动文件:**

- `app/api/routers/lessons/cloud_transcribe.py` — **新增**，POST /api/lessons/tasks/cloud-transcribe 端点，接收文件 FormData → 转推 DashScope Files API → 提取文字 → 创建课程。临时文件上传后立即删除，服务器不持久化媒体。
- `app/main.py` — 将 `cloud_transcribe_router` 注册到 FastAPI app（替换原 STS router）
- `app/core/config.py` — 移除 ALIYUN_STS_* 系列环境变量配置
- `frontend/src/features/upload/CloudUploadPanel.tsx` — 简化为 FormData 直发 /api/lessons/tasks/cloud-transcribe，去除 OSS/STS/HMAC-SHA1 签名等全部复杂逻辑，phase 从 5 个减为 3 个（idle/sending+transcribing/done/error）

**删除文件:**

- `app/services/sts_service.py` — 删除（不再需要 STS 凭证生成）
- `app/api/routers/lessons/sts_credentials.py` — 删除
- `frontend/src/shared/api/stsCredentials.ts` — 删除

**关键逻辑:**

- Bottle 2.0 不再需要阿里云 OSS！客户端将文件 POST 到 Bottle 服务器（FormData），服务器立即将文件转发给 DashScope（Files.upload），DashScope 返回识别文字，服务器提取文字后创建课程
- 文件仅暂存于服务器临时目录，完成后立即删除，不落地存储
- 无需配置任何阿里云 RAM/STS/OSS 环境变量，仅需已有的 `DASHSCOPE_API_KEY`

**数据库迁移:** 无

**验证命令及结果:**

- `python -c "from app.main import create_app; print('OK')"` → OK

**Zeabur 环境变量需求:**

- `DASHSCOPE_API_KEY`（已有）
- 无需 ALIYUN_* 系列变量（已移除）

---

## TASK-056 — 更新产品文档与修改历史

**状态:** F（已完成）

**改动文件:**

- `Docx/协作/产品修改历史.md` — 追加 2026-03-25 记录（方向/原始目标/用户动机）
- `Docx/协作/tasks/TASK-055-实现Bottle2.0云端直传STS架构.yaml` — 更新 result_summary、changed_files、tests_run、completed_at
- `Docx/协作/tasks/TASK-056-更新产品文档与修改历史.yaml` — 更新 result_summary、changed_files、tests_run、completed_at

**关键逻辑:** 追加记录反映了从 5 个混合流程精简为 Bottle 1.0（本地推理）+ Bottle 2.0（云端直传）两个模式的第一性原理。

**数据库迁移:** 无
