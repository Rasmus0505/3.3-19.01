# 计划清单
# 仅保留当前未完成且可执行的任务
# 由 after / before 在任务池整理时更新
# 仅扫描当前协作目录（Docx/协作/tasks/）下的任务池

schema_version: 1
updated_at: "2026-03-25T17:00:00+08:00"
updated_by: after

# 当前可并行执行的任务（已满足 depends_on，且无未解决 conflicts_with）
executable: []

# 需等待的任务（存在 depends_on 或 conflicts_with 未满足）
waiting: []

# 推荐执行顺序
notes: |
  2026-03-25 本次规划：

  背景：用户希望将 Bottle 生成素材流程从 5 个精简为 2 个，同时重建桌面端。
  第一性原理：生成素材时，服务器不承受任何压力——文件在用户本地跑 ASR，或直传阿里云跑 ASR。

  新增六个任务：

  并行组A（可直接执行）：
    - TASK-051（high）：清理废弃后端代码（desktop_asr/local_asr_assets/dashscope_upload/faster_whisper/url_import）
    - TASK-052（high）：清理废弃前端代码（浏览器端本地 ASR 相关 6 个 JS 文件 + UploadPanel 改造）

  串行执行：
    - TASK-053（critical）：新建 Electron + React + Vite 桌面端骨架（D:/3.3-19.01/Desktop/）
    - TASK-054（critical）：桌面端集成本地 ASR（Sherpa-ONNX，模型已在 Desktop/models/sherpa-onnx/）
    - TASK-055（critical）：实现 Bottle 2.0 STS 凭证架构（服务器下发临时凭证，客户端直连阿里云）
    - TASK-056（medium）：更新产品文档和产品修改历史

  执行顺序建议：
    第一轮：TASK-051 + TASK-052（并行，无依赖）
    第二轮：TASK-053（桌面端骨架，独立）
    第三轮：TASK-054（桌面端 ASR，依赖 053）+ TASK-055（Bottle 2.0 STS，依赖 051）
    第四轮：TASK-056（文档，依赖全部）

  执行结果：
    ✅ TASK-051 — 已完成（F-TASK-051）
    ✅ TASK-052 — 已完成（F-TASK-052）
    ✅ TASK-053 — 已完成（F-TASK-053）
    ✅ TASK-054 — 已完成（F-TASK-054）
    ✅ TASK-055 — 已完成（F-TASK-055）
    ✅ TASK-056 — 已完成（F-TASK-056）

    全部任务已完成！executable 和 waiting 均为空。

  Zeabur 操作提醒：
    - TASK-051~052 清理废弃代码：直接部署即可
    - TASK-053~054 Desktop/ 目录仅本地使用，不影响 Zeabur 部署
    - TASK-055 Bottle 2.0 STS：部署时需在环境变量中配置 ALIYUN_* 系列变量
    - 以上任务均无需数据库迁移
