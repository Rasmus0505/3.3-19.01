# 计划清单
# 仅保留当前未完成且可执行的任务
# 由 after / before 在任务池整理时更新
# 仅扫描当前协作目录（Docx/协作/tasks/）下的任务池

schema_version: 1
updated_at: "2026-03-25T18:00:00+08:00"
updated_by: before

# 当前可并行执行的任务（已满足 depends_on，且无未解决 conflicts_with）
executable:
  - TASK-057  # 移除重播降速 + Shift+E 快捷键（纯前端，无依赖）
  - TASK-058  # UI精简：新手教程按钮+模型卡+生词本标签（纯前端，无依赖）

# 需等待的任务（存在 depends_on 或 conflicts_with 未满足）
waiting:
  - TASK-059  # 生词本Bug修复+回溯视频弹窗（纯前端，但依赖前端组件结构确认）
  - TASK-060  # 打卡连续天数系统（需数据库迁移）
  - TASK-061  # 热更新公告系统（需数据库迁移）

# 推荐执行顺序
notes: |
  2026-03-25 本次规划（第2批）：

  背景：产品经理提出8个功能需求，涵盖视频播放器、生词本、UI精简、打卡系统、热更新5大模块。

  并行组A（可直接执行）：
    - TASK-057（critical）：移除重播自动降速 + 新增 Shift+E 降倍速快捷键
    - TASK-058（high）：UI精简（新手教程登录按钮+模型卡说明+生词本标签）

  并行组B（第二轮执行）：
    - TASK-059（critical）：生词本Bug修复（最后一句无法加入）+ 回溯视频弹窗
    - TASK-060（critical）：打卡连续天数系统（后端数据模型+API+前端两处显示）
    - TASK-061（high）：热更新公告系统（后端+管理后台+用户端弹窗）

  执行顺序建议：
    第一轮：TASK-057 + TASK-058（纯前端并行，无依赖）
    第二轮：TASK-059 + TASK-060 + TASK-061（TASK-060/061 需数据库迁移）
    （可并行执行，但数据库迁移只需执行一次）

  执行结果：
    待执行

  数据库迁移提醒（TASK-060、TASK-061）：
    - TASK-060：alembic revision --autogenerate（DailyCheckin表、User/Streak字段、LessonProgress字段）
    - TASK-061：alembic revision --autogenerate（Announcement表）
    - 两个任务可合并一次迁移执行
