/* Data Visualization */
.stat-row { display: flex; gap: 32px; margin-bottom: 40px; }
.stat-box { flex: 1; background: var(--bg-glass); border: 1px solid var(--border); border-radius: 20px; padding: 32px; text-align: center; }
.s-num { font-size: 64px; font-weight: 900; font-family: 'Inter'; letter-spacing: -2px; background: linear-gradient(to right, #3b82f6, #06b6d4); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
.s-lbl { font-size: 20px; color: var(--text-muted); margin-top: 12px; font-weight: 600; }
.chart-container { flex: 1; position: relative; width: 100%; min-height: 0; }

/* Quotes & Formulas */
.academic-quote { border-left: 6px solid var(--cyan); padding: 32px 48px; background: linear-gradient(90deg, rgba(6,182,212,0.15), transparent); border-radius: 0 20px 20px 0; font-size: 28px; font-style: italic; color: #e2e8f0; line-height: 1.75; margin: 24px 0; }
.academic-src { font-size: 18px; color: var(--text-muted); font-style: normal; margin-top: 20px; font-weight: 700; letter-spacing: 0.5px; text-transform: uppercase; }
.formula { font-family: 'Cambria Math', 'Times New Roman', serif; font-size: 48px; text-align: center; padding: 48px; background: rgba(0,0,0,0.3); border-radius: 20px; border: 1px solid var(--border); margin: 32px 0; color: var(--cyan); letter-spacing: 2px; }

/* Micro-components */
.badge { display: inline-flex; align-items: center; padding: 8px 20px; border-radius: 10px; font-size: 18px; font-weight: 800; background: rgba(255,255,255,0.05); border: 1px solid var(--border); margin-right: 12px; margin-bottom: 12px; }
.badge.c { color: var(--cyan); border-color: rgba(6,182,212,0.3); }
.badge.p { color: var(--purple); border-color: rgba(139,92,246,0.3); }
.badge.e { color: var(--emerald); border-color: rgba(16,185,129,0.3); }

/* S1 Specific */
.hero-logo { font-size: 140px; font-weight: 900; letter-spacing: -6px; line-height: 1; margin-bottom: 24px; background: linear-gradient(135deg, #ffffff 0%, #3b82f6 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; filter: drop-shadow(0 0 50px rgba(59,130,246,0.5)); }
